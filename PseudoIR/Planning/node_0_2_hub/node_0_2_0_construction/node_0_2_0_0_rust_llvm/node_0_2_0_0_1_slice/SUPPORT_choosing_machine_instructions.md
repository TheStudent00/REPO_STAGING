---
id: pir.rust_llvm_slice.support.choosing_machine_instructions
status: projected
---

# SUPPORT — choosing machine instructions

projected 2026-07-30 from the previous plan, now archived at
`<WORKSPACE_DIR>/PseudoCoup_v6/.archive/Planning_superseded_2026-07-31/`.
the `source:` paths below are relative to that folder.

source: node_0_3_application_rust_llvm/node_0_3_1_isel_gap/CORE_0_3_1_isel_gap.md  (304 words)
verdict: clean
changed: nothing to the carried body. a plain-language statement of
what this is was added above it, because the carried text names the
thing only in llvm's own vocabulary.

---

## what this is, before the carried text

deciding that llvm's `add i64` becomes x86-64's `addq` instruction.
that is the whole of it. we call this IRMapping. llvm calls it
instruction selection and the previous plan called this node the
isel gap; both names are retired here.

the carried text below asserts the table is "NOT transpilable by the
1:1-function strategy". that is the previous plan's phrasing, not
ours, and it does not describe this project's transpiler, which
works a file at a time from tree-sitter output. the real obstruction
is that there is no seam to slice on, plus scale. see this node's
CORE.

it sits between two stages we CAN get: rust source to llvm ir, and
a machine instruction to bytes. the full three-stage picture is in
this node's CORE at
`<WORKSPACE_DIR>/PseudoIR/Planning/node_0_2_hub/node_0_2_0_construction/node_0_2_0_0_rust_llvm/node_0_2_0_0_1_slice/CORE_0_2_0_0_1_slice.md`.

vocabulary the carried text uses without defining:

- **ISD node** — llvm's name for one operation in the graph the
  selector reads. `add i64 %a, %b` is one.
- **`SelectCodeCommon`** — llvm's 1,145-line virtual machine that
  performs the selection.
- **`MatcherTable[]`** — the ~375,000-line generated byte array that
  virtual machine executes. the patterns, compiled to bytes.
- **`X86ISelDAGToDAG.cpp`** — 210 hand-written x86 special cases
  alongside the table.
- **`llc`** — llvm's standalone compiler, used here as ground truth:
  whatever instruction it picks is the right answer.
- **llvmlite** — python bindings to llvm. option (c) would call it
  at run time instead of extracting anything.

---

# CORE 0_3_1 — The Instruction-Selection Gap

Between the front half's output (an LLVM IR opcode / ISD node) and
the encoder's input (a machine instruction) sits LLVM's
instruction selector: a 1,145-line hand-written bytecode VM
(`SelectCodeCommon`) plus a ~375k-line generated `MatcherTable[]`
byte array, plus 210 hand-matched C++ cases in
`X86ISelDAGToDAG.cpp::Select()`. Measured in PCv5; the survey said
the generated matcher is NOT transpilable by the 1:1-function
strategy.

## The three options (priced in PCv5, DECIDED HERE only after the
front half lands — do not pre-commit)

| option | what it is | cost |
|---|---|---|
| a. hand-map | declare the few opcodes the hub actually emits (integer arithmetic is a handful) as a small table; stub the rest with asserted reachability | cheap, honest, cuttable — fits the imprecise-but-safe default |
| b. transpile the VM | slice `SelectCodeCommon` and carry `MatcherTable[]` as data | full solution, expensive; the table-as-data is the hard part |
| c. llvmlite oracle | use llvmlite as a reference oracle rather than extracting the selector | avoids extraction; introduces a runtime dependency |

## The deciding evidence (gathered by doing the front half first)

How many distinct ISD nodes does the hub actually exercise? The
front half's output set answers it. If it is a handful (expected
for integer arithmetic), option (a) is obviously right and the
expensive options wait for a real need. This is the churn-
resilience principle applied: build the cheap safe version, widen
only where an intention demands.

## Acceptance (option-independent)

The selected machine instruction for each hub-emitted ISD node
matches what `llc` produces for that node (host ground truth), by
whichever option is chosen.

## Open (the owner)

- The option choice itself, after the front half's ISD-node count
  is known.
