---
id: hq.research.lean_proof_path_resistant_to_churn.progress
status: living
---

# PROGRESS — lean_proof_path

- 2026-09-13: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_3_architectures/node_0_3_2_3_1_riscv64/CORE_0_3_2_3_1_riscv64.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-13: created from the conversation with the owner of this afternoon,
  banked as `PRIVATE/PseudoCoupHQ/DevComms/log_274_the_lean_proof_path_resistant_to_churn.md`;
  the tree built to leaf depth (10 sub-nodes, 27 leaves: one per class
  and method of log 274 §4) by the generator from a spec. The emit of
  the definitions is measured (log 274 §3; `emit_measured`). Status:
  planned; next is `the_run/handful` on the tower, then `everything`,
  then `churn_tests`. Awaiting the owner: the node's name, and the gate moving
  from z3 to Lean (assumed).

- 2026-09-13: task lp1 (the handful) ran on the tower, instance `lp1`.
  BLOCKED at its foundation and flagged for the coordinator. The cached
  emit at `Research/oracle/riscv/leanpath/cache/sail-riscv_3243f93_I_insts_M_insts_postlude_main/`
  does not build under the toolchain it pins (`leanprover/lean4:v4.29.0`,
  `lean-sail` v4): `LeanIM/Defs.lean` fails with 12 errors because sail
  0.20.2's Lean backend emitted Sail call syntax `is_sv32_mode(k_v)` and
  an unbound `k_v` in the virtual-memory type abbreviations, so 0 of 116
  modules build. The model pins sail 0.20.2 for C
  (`cmake/sail_required_version.txt`) but builds its Lean with sail
  `"latest"` (`compile-lean.yml`); the repair is a re-emit with a newer
  sail — a fetch/install outside lane 1, so the coordinator's call.
  `lean-sail` v4 itself builds fine (5.7 s). Report:
  `PRIVATE/PseudoCoupHQ/DevComms/log_275_lp1_the_cached_emit_does_not_build_under_the_pinned_toolchain.md`.
  Second, minor flag: the toolchain host `release.lean-lang.org` is not
  on the `lp1` allowlist (`TCP_DENIED/403`); the identical archive was
  taken from the allowlisted, brief-named github.com. Reached without a
  build: `SailModel.definitions` at the text level (29 execute clauses:
  I 20, M 6, cfi 1, postlude 2; 49 keys), guard PASS. The handful's ten
  rows, the `mulh` headline, pass A over 255, and the per-stage Lean
  speed are all downstream of the build and unreached.

- 2026-09-14: task lp1, SECOND launch (brief §4: build sail from source
  at `5745ea9e53`, re-emit, build, then the handful). Lane 6 built that
  sail in 85 s by the model's own CI recipe (`Sail 0.20.2 (HEAD @
  5745ea9e…)`); lane 7b re-emitted in 217 s / 552 MB (not 45 min / 17 GB)
  into `cache/sail-riscv_3243f93_sail_5745ea9e_I_insts_M_insts_postlude_main/`;
  lane 8 fetched the newly pinned `lean-sail` v5 and ran `lake build`:
  BLOCKED AGAIN at `LeanIM/Defs.lean`, 14 error lines, the same three
  `is_sv32_mode(k_v)` sites and the free `k_v` of `root_level` — the
  construct is a Sail TYPE-LEVEL function (`type is_sv32_mode('v : Int)
  -> Bool`, `model/core/vmem_types.sail` at 3243f93) that this sail
  commit's Lean backend prints as Sail call syntax, which Lean 4 does not
  parse. 0 of 118 modules build; the task stops at that flag (brief §4).
  Reached without a build: the module `Research/oracle/riscv/leanpath/leanpath/`
  in the plan's shape (8 classes, 27 leaves; logic only in
  `SailModel.definitions` and `key_of`, which read the keys from the
  model's own `assembly_forwards` and `instruction` inductive — no typed
  mnemonic list): 29 execute clauses (I 20, M 6, cfi 1, postlude 2), 74
  keys, guard PASS. Second flag: the `lp1-proxy` container serves the
  allowlist baked into the proxy image (2026-08-22, 39 lines), not the
  tower's `proxy/allowlist.txt` edited at 23:39; `opam.ocaml.org` and
  `ocaml.org` were `TCP_DENIED/403`; the sail build needed nothing from
  them (`opam install --deps-only`: "Nothing to do."). Report:
  `PRIVATE/PseudoCoupHQ/DevComms/log_276_lp1_the_re_emit_with_sail_5745ea9e_does_not_build_either.md`.
  Awaiting the coordinator: a sail commit whose Lean backend lowers
  type-level functions (after 2026-09-09, if one exists) or a model
  commit before `is_sv32_mode` entered; the proxy allowlist push
  (`allow.sh --instance lp1 sync`).

- 2026-09-14: the launching session took the implementation into the
  foreground (the owner: "make use of your intelligence for this task").
  The last green pair (model `6266b40c`, sail `8eb1fb6b`) builds; the
  pruned executable decoder (69 modules) evaluates. **The handful
  closes** on instance `lp3` (lane `lp3_l28`): strip 93 of 339 clauses
  certified; walk 11 of 11 walkable units certified (7 refused for
  stated reasons); equals 10 of 11 units find their definition, `mulh`
  and `mulhu` with swapped operands at the integer level by `grind` in
  2.3 s (log 274 §5.3's expectation, met). Every repair between the
  first run and the closing run replaced a spelling with a rule
  (aliases, register-index definitions, reachable emitted definitions,
  the library helpers they name). Pass A over the rendered corpus
  (1,960 sources, c/cpp/rust/go, both routes) walked: 846 certified,
  363 failed on one syntax error (repaired; re-run queued as
  `lp3_l29`), 751 refused; its equals stage and tallies pending.
  Report: `DevComms/log_278_the_lean_proof_path_runs_the_handful_closes.md`.
  Leaves reached: `sail_model.{emit,definitions,strip}`,
  `arch_unit.{carve,decode,compose,certify}`,
  `lean_expr.{equals,normalize_integer_level,decide_fixed_width}`,
  `language.compile` (four routes), `the_run.handful`;
  `the_run.everything` in progress; `churn_tests` and `dictionary`
  not yet.
- 2026-09-14, later: pass A over the corpus is in. Walk: 1,209 of 1,960
  certified (363 as the identity on the answering register), 751
  refused for stated reasons, 0 failed; 203 of 861 cells have a
  certified unit. Equals: 80 of the 846 walked units find their
  definition (76 by the same text, 4 at the integer level); 722 have
  every candidate refuted by evaluation; 44 survive evaluation and no
  prover closes them, because `bv_decide` still sees Sail's shift and
  extract helpers as atoms (the widening leaf, log 274; probe lane
  `l44`). Two rules to add, found by the run: a non-enumerable clause
  parameter (an immediate) as an unknown solved by evaluation (414
  one-register units have no candidate today); `let` sharing in
  compose (a 53-instruction adder is a 3,137-application term by
  substitution). the owner's branch-enumeration stage is next after those.
  Fourteen equals lanes (`l28`..`l42`) to make the stage affordable;
  the evaluate-before-prove filter is what did it (48 min for 846).
  Report: `DevComms/log_278_...` §7.1–7.4. Leaves: `the_run.everything`
  reached; `dictionary` partial; `churn_tests` not yet.
- 2026-09-14, close of day: the widening rule (lean-sail's definitions
  indexed by namespace and unfolded when a pure form names them, lanes
  `l43`–`l47`) closes 34 of the 44 survivors at the fixed width: the
  constructed adders equal `RTYPE ADD` by `bv_decide` (124 s each), the
  constructed shifts their `RTYPE`/`RTYPEW` clauses, two shifts by 48
  equal `ZBB_EXTOP ZEXTH`. Equals over the corpus stands at 114 of 846
  walked units with a definition (76 same text, 34 fixed width, 4
  integer level). The ten left are the constructed comparisons, whose
  `<_s`/`<_u` are `Int`-level in the emit; next rule. Artifacts synced
  back to `Research/oracle/riscv/leanpath/` (`tally_corpus_final.json`).
  Report: log 278 §7.2, §7.5.
- 2026-09-14: carried into `Planning/node_0_3_research/node_0_3_3_lean_proof_path_resistant_to_churn` (the owner: start over in a different research node; copy the node, delete the wrong parts, re-plan them). This node was one of the right parts. It supersedes `hq.research.arch_unit_oracle.architectures.riscv64.lean_proof_path`.
- 2026-09-14, late evening: verified at the owner's request against log 274 and
  log 280 (`DevComms/log_281_verification_of_the_re_planned_lean_proof_path_against_the_design.md`).
  The tree is a faithful copy-and-re-plan (carried nodes identical, chains
  and archive in place, checker clean). Nine gaps between the record and
  the state, and one unrecorded run (lane `lp3_l49`, on the tower, stopped
  with no verdict), listed there. Status: draft; still awaiting the owner on log
  280 §8.
