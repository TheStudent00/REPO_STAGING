# Task mn1 — the `mnem` field rename: regenerate the o2, o8, o9, o10 artifacts with the field the spelling guard already recognises as machine form

Law: `LAW.md` beside this file — read ALL of it first, including its last
section, "Compute runs on the TOWER now": every lane you submit runs on the
tower through `remote_lane.sh`, and the commands are given there. Node:
`PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_2_cross_construction/node_0_3_2_2_2_single_opcode_units/`
(read its CORE and PROGRESS). Instance: `mn1.conf` is already on the tower
and up; use it. Your lane scripts live in
`PRIVATE/PseudoCoupHQ/Research/oracle/arch_opcodes/lanes_mn1/`.

## What this is, in one sentence
The spelling guard, `Research/op_pipeline/check_no_spelling_keys.py`,
exempts a field named `mnem` as a machine form (its `PROSE_FIELDS`, with
the docstring reason "a byte/sem key is a machine form"); five generator
scripts on the oracle line wrote that field under the name `mnemonic`
instead, so the guard reports their artifacts (the mnemonics `and`, `or`,
`xor`, `not` collide with PHP's and Python's word operators), and this task
renames the field in the generators and REGENERATES the artifacts so they
conform. Ruled by the owner 2026-09-08, on the coordinator's reading that the
guard already holds the distinction and the artifacts merely mis-name the
field. It is a rename, not a change of anything measured.

## The five generators and their artifacts
| generator | artifacts it writes | `mnemonic` occurrences in the script |
|---|---|---|
| `Research/oracle/arch_opcodes/single_opcode_units.py` | `single_opcode_units.json`, `.md` | 4 |
| `Research/oracle/arch_opcodes/unique_opcodes.py` | `unique_opcodes.json`, `.md` | 17 |
| `Research/oracle/arch_opcodes/signatures/opcode_signatures.py` | `opcode_signatures.json`, `.md` | 79 |
| `Research/oracle/arch_opcodes/signatures/ledger_signatures.py` | `ledger_signatures.json`, `.md` | 58 |
| `Research/oracle/cross_construction/emulation/per_opcode/per_opcode.py` | `per_opcode_population.json`, `per_opcode_results.json`, `per_opcode_held.json`, `per_opcode_report.md` | 49 |

How each was originally run is recorded in its own lane scripts beside it
(`lane_logs/`, `lanes/`, `lanes_o10/`) and in its log: logs 208 (o2), 220
(o8), and the o9/o10 logs (`ls PRIVATE/PseudoCoupHQ/DevComms | grep
-i "o9\|o10"`). Re-run each generator the way its own lane ran it. The
scripts import each other's outputs in places (o9 reads o2's json; o8 reads
o2's): regenerate in the order o2, then o9 and o10, then o8, and sync-back
between them so each reads the renamed input.

## Deliverable
1. In each script: the JSON FIELD NAME `mnemonic` becomes `mnem`, in both
   what is written and what is read. Variable names, prose, column headers
   in the `.md` reports and docstrings may keep the word; only the json
   field name changes. Show the diff per script in the log (LITERAL).
2. Each artifact regenerated by its own generator in a lane on the tower.
   NOT edited by sed: regenerated. The log carries, per artifact, the
   generator command as run.
3. **The guard over every regenerated json**, output pasted LITERAL. The
   expected result is zero findings; if findings remain, list each by
   cause (protocol §5.3) and stop — do not add an exemption, do not touch
   the guard. `grep -c exempt` over files you add = 0.
4. **Counts unchanged**, as a table: for each artifact, the headline count
   before (read from the old artifact, which you keep as `<name>.before.json`
   until the log is written, then delete) and after. Expected: 162 unique
   mnemonics; 259 single-opcode rows (243 with a proved term); o9's 243
   valid / 16 skipped; o10's 31,078 units streamed; o8's 243 valid rows and
   its LANDED / byte-identical / proved totals. A count that differs is a
   FINDING: report it, do not reconcile it.
5. One note in the log for the record: commands in the earlier logs that
   read `.mnemonic` from these artifacts would now need `.mnem`; the old
   logs are not edited.
6. Log (next free number; check `ls PRIVATE/PseudoCoupHQ/DevComms |
   tail` right before writing); verifier lane on the tower; PROGRESS entry
   (append only, dated) on the node above; sync-back of the artifact
   folders and DevComms; instance down.

Memory: o10's census streams 332 shards; state the bound (the original ran
inside 12g), paste the peak RSS. Stop rules per LAW. Reply with the guard
outputs, the counts table, the tally, the two lists.
