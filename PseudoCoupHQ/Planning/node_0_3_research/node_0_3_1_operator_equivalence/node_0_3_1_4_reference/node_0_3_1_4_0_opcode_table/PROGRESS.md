---
id: hq.research.compiler_graph.reference.opcode_table.progress
status: living
---

# PROGRESS — opcode_table

- 2026-09-03: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_4_reference.md`.
- 2026-09-03: term builders for integer, condition, float and vector
  families — **done** (`layer4.py`, `vex_names.py`,
  `condition_table.py`). Evidence:
  [reference realization](../CORE_0_3_1_4_reference.md).
- 2026-09-03: opcodes with no builder reported as census rows —
  **done** (`name_census4.json`). Evidence:
  `PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §3.
- 2026-09-03: remainder corrected to `SRem` in the table the gate
  calls — **planned**; `canon12_behaviour_check.py` already has it.
  Evidence:
  `PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §5.
- 2026-09-03: unify the three files into one table shared with
  `term.transcribe` — **planned**. Evidence:
  [reference realization](../CORE_0_3_1_4_reference.md).
- 2026-09-03: remainder corrected to `SRem`/`URem` in the table the
  gate calls — **done** (`reference.build_division`). The answer term
  for `c/op_246` prints `bvsrem_i(...)` and gives `1` on `7, -3`.
  Evidence: `PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`
  part (a).
- 2026-09-03: the three files unified into ONE table —
  **done** (`reference.OpcodeTable`, folding in `vex_names.py`'s lane
  rule, `condition_table.py`'s `SUFFIX_TO_COND` / `cond_to_z3`,
  `layer4.py`'s producer table and `canon12`'s division). 162 arch
  opcodes the corpus's own bodies spell, computed off
  `canon38_wrapped_*.json` + `canon38_interp.json` +
  `canon38_regen_store/*.json` over 181,125 body lines; 162 entries;
  143 carry a term builder and 19 are census rows (`call`, `ud2`,
  `jmp`, twelve conditional transfers, `pcmpeqb`, `pcmpeqd`,
  `pmovmskb`). Sharing the table with `term.transcribe` is task 61's
  half and stays **planned**. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`
  part 0.
- 2026-09-03: float opcodes carried as REAL IEEE terms
  (`fpAdd`/`fpSub`/`fpMul`/`fpDiv` under `RNE`, lane 0 written and the
  upper lanes kept) rather than the uninterpreted functions
  `canon10_behaviour_check.py` used — **done**. Evidence: same file,
  and `PseudoCoupHQ/DevComms/log_159_task57_reference.md`.
- 2026-09-03 (task 63): the table extended to the ATTACHED ARCHIVE
  BODIES — **done**. An arch unit extracted from a toolchain's own
  builtins archive is an arch unit, so the opcodes its body spells are
  entries in this one table. Computed off `canon39_callee_units.json`
  — 76 attached bodies, 4,002 body lines, 73 distinct arch opcodes, of
  which the table lacked NINE: `endbr64` (64 lines), `bsr` (30), `inc`
  (14), `bt` (8), `cmova` (4), `pinsrw` (4), `cs` (2), `fld` (2),
  `xchg` (1). Each is now an entry with its reads, its writes and a
  term builder, and each is printed in the acceptance run on a real
  line from a real body with concrete values moving through it.
  Nothing else in the table moved: over a 3,000-unit sample of
  canon39's proved units, the answer this module returns is identical
  character-for-character to the answer the module returned before
  this task's edits — 0 changed, 0 regressions. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/acceptance63_printed.txt`,
  `canon39_callee_opcodes.json`, `regression63_printed.txt`.
- 2026-09-03 (task 63): a SEGMENT-RELATIVE memory operand (`%fs:0x28`,
  where the stack-protector cookie lives) is read as a memory cell —
  **done**. Eight attached bodies (`__modti3`, `__umodti3`,
  `__floattixf`, `__floatuntixf`, in clang's and clang++'s archives)
  stopped on it; reading it as an unconstrained cell is the model
  every other memory operand already gets, and it claims nothing about
  the value. After the fix, every one of the 76 attached bodies stops
  only where task 64's branch following will take over: 2 walk to the
  end, 74 stop at a transfer, 0 stop on an opcode the table lacks.
  Evidence: `acceptance63_printed.txt` part 2.
- 2026-09-03 (task 64): TWO settled rules written into this CORE
  before any code -- a conditional transfer `j<cc>` is an entry with
  the SAME condition builder `set<cc>` and `cmov<cc>` carry, not a
  census row (499 units, log_160 §1.7), and what the walk does with
  the condition is the reference's control-flow rule rather than the
  table's; 8- and 16-bit division and widening multiply are entries
  whose destination is a pair of bytes or words inside ONE register
  family (20 units). Evidence: this CORE's `## settled rules`.
- 2026-09-03 (task 64): `j<cc>` carries the SAME condition builder
  `set<cc>` and `cmov<cc>` carry — **done** (`build_branch_condition`
  through `predicate_of`). It was an entry with no builder and the
  cause "a transfer or trap, and this reference walks a body in text
  order"; 474 canon39 units stopped on it. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/acceptance64_printed.txt`
  part 1, `audit64_printed.txt`.
- 2026-09-03 (task 64): 8- and 16-bit division and widening multiply —
  **done** (`build_narrow_division`, `build_narrow_wide_multiply`,
  `place_bits`), each printed on a line the corpus itself spells with
  concrete values through it: `idiv %sil` over AX = 7 and SIL = -3
  leaves AL = 0xFE and AH = 1 and the upper 48 bits of `%rax`
  untouched; `mul %si` over AX = 65535 and SI = 2 leaves AX = 0xFFFE
  and DX = 1. THE POPULATION IS ZERO ON canon39 and this is said out
  loud: log_160's 20 units were canon38's, and canon39's own undecided
  list carries none. Five units DID stop just past the division, on
  `%ah`, and that is fixed with it. Evidence:
  `acceptance64_printed.txt` part 4, `audit64_printed.txt`.
- 2026-09-03 (task 64): the HIGH-BYTE registers `%ah`/`%bh`/`%ch`/`%dh`
  read and written as bits 15..8 of their family — **done**
  (`Operands.is_high_byte`, `HIGH_BYTE_OF`). `canon.FAMILY_OF` holds no
  entry for them because no canonical text names one; a body that
  divides at width 8 does, because the machine puts the remainder
  there. Found at first observation, in the shared layer: five units
  (`c/regen_15263`, `cpp/regen_14486`, `cpp/regen_14502`,
  `cpp/regen_15238`, `cpp/regen_15254`) moved from "a division at
  width 8 is not modeled" to a proof. Evidence:
  `regression64_printed.txt`, `audit64_printed.txt`.
- 2026-09-05 (task 91): the realization table's two **planned** rows
  closed against what is on disk — **done**. "ONE shared table" is
  `reference.py`'s `OpcodeTable`, and `term.py` line 362
  (`self.opcode_table = reference.opcode_table`) reads THE SAME OBJECT,
  which is what the "one table, shared with `term.transcribe`" rule
  asks for. "The wrong remainder" is gone: `build_division` and
  `build_narrow_division` use `z3.SRem` / `z3.URem`, and the z3 operator
  appears in neither. Put to the CORE's own counterexample rather than
  asserted: `SRem(7, -3) = 1` where the operator gives `-2`, and all
  **415** of log 153's withdrawn disproofs prove under it. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/t91_reference_evidence.json`
  section B1, `t91_audit_printed.txt`.
