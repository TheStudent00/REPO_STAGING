---
id: hq.research.compiler_graph.reference.machine_state.progress
status: living
---

# PROGRESS — machine_state

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_4_reference.md`.
- 2026-09-03: registers seeded `seed_<family>`, flags as a (setter,
  L, R) triple, memory as a z3 array with `ripconst_<n>` — **done**
  (`canon9_behaviour_check.py`, `canon10_behaviour_check.py`).
  Evidence: [reference realization](../CORE_0_3_1_4_reference.md).
- 2026-09-03: the machine stack modelled so push and pop round trip —
  **planned**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.3.
- 2026-09-03: the x87 stack modelled as eight positions and a top
  index — **planned**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.3.
- 2026-09-03: 5,602 units re-gated once the two blocks exist —
  **blocked** on those two models; the proved population is
  unaffected. Evidence:
  [gate realization](../../node_0_3_1_5_gate/CORE_0_3_1_5_gate.md).
- 2026-09-03: CORRECTION written into this CORE before any code
  (round 12 binding rule 2): an x87 position holds an
  `FPSort(15, 64)` term, not a bit vector, and a load is keyed
  `x87_<mangled memory operand>`. The reference CORE's own design
  block carried the same wrong wording and was corrected with it.
  Evidence: `~/Programming/PseudoCoupHQ/Research/op_pipeline/layer4c.py`
  section 1 (`X87_SORT = z3.FPSort(15, 64)`), and log_153 §4.3's
  printed term `x87_0x18_rsp_`
  (`~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`).
- 2026-09-03: the machine stack modelled so push and pop round trip —
  **done** (`MachineState.stack`, a pointer term plus cells keyed by
  the byte offset from the rsp the unit was entered with). Put to z3
  rather than asserted: `solver.add(popped != pushed)` answers
  `unsat`, and the pointer and offset both return to where they
  started. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`
  part (b), on `c/regen_11491`.
- 2026-09-03: the x87 stack modelled as eight positions and a top
  index, each an `FPSort(15, 64)` term — **done**
  (`MachineState.x87`). On `cpp/regen_36796` the two `fldt` loads read
  to `x87_0x8_rsp_` and `x87_0x18_rsp_`, the compare leaves the triple
  (`fucomip`, top, position 1), and the answer term reproduces
  log_153 §4.3's own printed GUARD-0 term character for character.
  Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`
  part (c).
- 2026-09-03: registers, flags and memory carried in ONE state object
  rather than four simulators — **done**; the flag triple now answers
  the carry and overflow suffixes by rebuilding them from (setter, L,
  R) instead of remembering separate bits. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/reference.py`
  (`carry_bit`, `overflow_bit`, `predicate_of`).
- 2026-09-03: the 5,602 units blocked on the two missing blocks —
  the blocks now exist and the reference reaches 4,935 units the
  superseded route refused; re-gating them is task 58's, so this line
  stays **blocked on the gate**, no longer on the models. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/coverage57_printed.txt`.
- 2026-09-03: CORRECTION to this CORE, with provenance (PROTOCOL §2):
  a settled rule now says WHICH two values the flag triple carries --
  a comparison-shaped setter leaves its own two sides, a setter whose
  zero and sign bits come from its RESULT leaves `(result, 0)`. The
  CORE was silent, and `reference.build_binary` recorded the operands
  for the logic family, so a condition after `or %rsi,%rdi` read
  `seed_rdi != seed_rsi` instead of `(seed_rdi | seed_rsi) != 0`.
  MEASURED: 1,338 units of 30,436 on which the gate's route one
  disproved a term route two proved; after the fix the consistency
  line is 0. Recorded honestly: the code fix was made before this
  rule was written, which is the reverse of round 12's binding rule
  2. Evidence: log_160 §3.
- 2026-09-03 (task 64): THREE settled rules written into this CORE
  before any code -- a state is mergeable and the merge REFUSES rather
  than guesses (the flag triple only merges when both setters carry
  one name; a stack-depth or x87-depth disagreement is refused); a
  rip-relative ADDRESS computation reads the same positional counter a
  rip-relative LOAD reads (32 units, log_160 §1.7); a sub-32-bit write
  into the accumulator pair PRESERVES the register's upper bits, which
  `full64` does not, so the 8/16-bit division and widening-multiply
  builders compose the family's value rather than going through the
  ordinary write. `full64` is untouched for every other opcode.
  Evidence: this CORE's `## settled rules`.
- 2026-09-03 (task 64): a state MERGES with another state — **done**
  (`MachineState.fork`, `merge_two`, `merge_flags`, `choose`).
  Registers, memory cells, machine-stack cells and x87 positions merge
  as `If`; the flag triple merges only when both sides' setter carries
  one name, and a differing setter -- or one name at two widths --
  leaves `("merged", ...)`, on which a later condition read refuses by
  name. A stack-depth or x87-depth disagreement refuses by name.
  Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance64_printed.txt`
  part 1 (the graph and the guard row), `audit64_printed.txt`.
- 2026-09-03 (task 64): a rip-relative ADDRESS computation reads the
  same positional counter a rip-relative LOAD reads — **done**.
  `lea 0x2e57(%rip),%rax` then `lea 0x1000(%rip),%rdx` leave
  `ripconst_0` and `ripconst_1`. All 32 units prove. Evidence:
  `acceptance64_printed.txt` part 4.
- 2026-09-03 (task 64): a sub-32-bit write into the accumulator pair
  PRESERVES the register's upper bits — **done** (`place_bits`), used
  by the narrow division and widening multiply and by nothing else;
  `full64` is untouched. Evidence: `acceptance64_printed.txt` part 4,
  where `%rax`'s upper 48 bits survive every one of the five lines.
- 2026-09-05 (task 91): the `stack` and `x87` rows of the realization
  table closed — **done**, and what their absence cost re-measured.
  `MachineState.stack` is `{"pointer": seed_rsp, "offset": 0,
  "cells": {}}` with `push_value` / `pop_value`; `MachineState.x87` is
  `{"slots": [None] * 8, "top": 0, "depth": 0}` at `FPSort(15, 64)`
  with `x87_push` / `x87_pop` / `x87_at` / `x87_set`. Of the **5,602**
  units this node's own rule blamed on their absence, **2,356 now
  prove**, 2,969 disprove and 277 stay undecided — and the 2,969 are
  the LEDGER's gap, not this node's: canon38 holds no row for a
  transfer into the compiler's runtime, so the term asserts the
  transfer changed nothing while the reference walks the callee's body.
  Evidence: `~/Programming/PseudoCoupHQ/Research/op_pipeline/t91_audit_printed.txt`,
  `t91_reference_evidence.json` section B2 and B4.
