# log_212 — the research node restructured into projects, and its master plan written

Line-wide (all three research projects). Written 2026-09-06 by the
coordinator (Fable), doing the work himself on the owner's instruction
("not delegated ... i want your intellectual capacity in full use").

## 1. What was asked, verbatim

> "`Planning/node_0_3_research/` contains a scattering of realized
> sub_nodes where some belong to one research project and others
> belong to other research projects. instead of each research project
> being an actually realized sub_node that contains its respective
> sub_nodes. which gives the research node a different responsibility
> but opens the door for some master planning with great focus on the
> PCHQ objectives."

Names ruled by the owner: `intentions`, `operator_equivalence`,
`arch_unit_oracle`. Scope ruled: "take PlanPlan as far as you can. as
much depth as is possible. the enrichment of the PlanPlan will also
please include the master plan of research."

## 2. What moved

| before (address under `node_0_3_research/`) | after |
|---|---|
| `node_0_3_0_kind_signature_clustering` | `node_0_3_0_intentions/node_0_3_0_0_kind_signature_clustering` |
| `node_0_3_1_dominant_intentions` | `node_0_3_0_intentions/node_0_3_0_1_dominant_intentions` |
| `node_0_3_2_kind_fuzz_clustering` | `node_0_3_0_intentions/node_0_3_0_2_kind_fuzz_clustering` |
| `node_0_3_3_type_vocabulary` | `node_0_3_0_intentions/node_0_3_0_3_type_vocabulary` |
| `node_0_3_4_data_representation` | `node_0_3_0_intentions/node_0_3_0_4_data_representation` |
| `node_0_3_5_compiler_graph` (+ 11 sub-nodes, 2 levels below) | `node_0_3_1_operator_equivalence` (+ `node_0_3_1_<i>_*`, same i) |
| `node_0_3_6_interp_feeder` (+ 8) | `node_0_3_1_operator_equivalence/node_0_3_1_11_interp_feeder` |
| `node_0_3_7_remaining_languages` (+ 6) | `node_0_3_1_operator_equivalence/node_0_3_1_12_remaining_languages` |
| `node_0_3_8_arch_unit_oracle` (+ 3) | `node_0_3_2_arch_unit_oracle` |

- 68 directories, 276 files moved; 204 folder and file names
  rewritten (every chain changed).
- Ids unchanged, except `hq.research.compiler_graph` → superseded by
  `hq.research.operator_equivalence` (the framework's rename rule:
  the folder's name segment must equal the id's last segment). The
  SUPPORT file under it re-homed to the new id.
- Every relative link under `Planning/` was resolved at the file's
  old location, mapped, and re-relativized at its new location
  (301 markdown files read; the script is
  `restructure_research.py`, kept in the session scratchpad, not the
  repo — it is a one-shot). Path strings in `AgentMemory.md`,
  `CLAUDE.md` and the coordinator's memory were rewritten by token.
  DevComms logs were NOT rewritten: they are records; the address map
  in the research CORE §8 resolves them.
- `## metadata`, `## super_node`, `## sub_nodes` projections and all
  121 dashboards regenerated by the framework's own tools.
- Fourteen sub-node COREs under interp_feeder and remaining_languages
  had a malformed register (`sub_nodes:` followed by `[]` on its own
  line, no `supersedes`) that the generator had been skipping; fixed.

## 3. What was written

- `CORE_0_3_research.md` — the master plan: the objective in the owner's
  words with the Hub, dominant operator and dominant type each
  defined in one sentence; the purpose shift and why the plan has
  this shape; the three projects and their contributions; the state
  table (every number with its population); the dependency table
  (what the Hub needs, who supplies it, its state); the order of
  work agreed 2026-09-06; the open rulings; the shared rulings; where
  things are recorded; the address map.
- `CORE_0_3_0_intentions.md` — new project node: the owner's definition of
  an intention, the three layers, the five sub-nodes in relation,
  the deliverables table, the rules this project adds, its open
  items.
- `CORE_0_3_1_operator_equivalence.md` — rewritten around the
  project: the rename record; the founding definition kept under a
  dated heading as the graph's; the pipeline as a table of thirteen
  sub-nodes with the state of each; the standing rules extended with
  the four rulings of 2026-08-31 to 2026-09-05; the ratified pipeline
  kept verbatim with a map from its eight steps to the sub-nodes
  that now hold them; what it delivers to the Hub; the open items.
- `CORE_0_3_1_11_interp_feeder.md` — from one sentence to: why the
  interpreted languages enter at a different step, the feeder
  mechanically with its eight sub-nodes named to its functions, the
  state table of the eleven units, open items.
- `CORE_0_3_1_12_remaining_languages.md` — from one sentence to: the
  two shapes (interpreter, JIT), the per-language state table with
  toolchain and evidence route, the designed JIT route, open items.
- Fourteen sub-node definitions under those two, from one line each
  to a paragraph anchored in the file, function, binary or log it
  describes; designations set (code (class) / code (function) / work
  / rule / grouping).
- `CORE_0_3_2_arch_unit_oracle.md` — the state table of tasks o1–o4,
  the freeze of cross_construction, the order now following the
  master plan.
- Research `PROGRESS.md` and `CHECK_0_3_research.md`.

## 4. One correction made while writing

The remaining_languages CORE first said .NET was absent from Airlock.
log_062 §1 says the opposite: absent from the IMAGE, present in the
persist VOLUME at SDK 10.0.400. Corrected before commit; recorded here
because it is the kind of claim that survives if unchecked.

## 5. Checks

`bash ~/Programming/PlanPlan/plan.sh check ~/Programming/PseudoCoupHQ/Planning`
after the work: the errors under `node_0_3_research` are six
dangling paths that predate this session (elided log names,
`~/Programming/SandboxDesign/allow.sh`, a shell glob in a PROGRESS
line). The 60 "`## sub_nodes` not first section" warnings are
tree-wide and older than this node. The framework's `heal_tree.py`
was read and not used: it re-indexes and renames but does not
re-home `node.path`, `super_node`, `level` or body links, which is
most of a move.

## 6. Awaiting the owner (unchanged from the master plan §5)

- The typing route for the Hub's front end.
- Whether the key carries a mode axis.
- type_vocabulary: archive or stay.
