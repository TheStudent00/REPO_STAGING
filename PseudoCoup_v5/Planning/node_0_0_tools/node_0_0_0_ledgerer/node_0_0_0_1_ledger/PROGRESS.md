---
id: pcv5.tools.ledgerer.ledger.progress
status: living
---

# PROGRESS — ledger

- 2026-08-06 **done and exercised**: `ledger.py` written at
  `<WORKSPACE_DIR>/PseudoCoup_v5/Tools/ledgerer/ledger.py`, two passes
  the same day (shape, then logic at the owner's go-ahead). one `Table`
  dataclass + six module-level functions; imports `ur` and the
  standard library only.
  - exercised: admission of a tree (walk covers sub-nodes); all four
    refusals raised BY NAME (None slot, undefined phase slot, repeat
    id, unknown phase) with `unresolvable` accepted where None is
    not; indexes derived, with a name held by two ids keeping both
    in admission order (no last-writer-wins); dump/load round trip
    byte-identical twice over, sets serialized sorted, tuples
    restored, origin kinds restored, unknown format version refused;
    merge re-addressing frames while leaving batch/ordinal untouched.
  - **finding for the owner, raised not patched**: the re-run triage needs
    a SOURCE FILE NAME on a row to raise its flag, and no `ur` form
    carries one — `TsOrigin` has kind/named/fields/span/language,
    `Tree` has bytes/language/grammar_version/root. so `merge`'s
    same-source discovery (and with it the true-fork edge) is
    unreachable until a field exists. no field was invented.

- 2026-08-06 **done** (the owner: "yes proceed", after "hard to disagree
  with that design"): **the node deepened — a dumb table, three
  doctrine-bearing operations.** register: `table`, `index_by_id`,
  `index_by_fqdn`, `load` as `realize: false` in-file entries
  (designed in this CORE); `admit`, `dump`, `merge` realized as
  folders, each CORE carrying its rules (refusal doctrine;
  serialization format + the reconstruction oracle; frame
  reconciliation with the true-fork edge flagged open).
  - the shape follows the 2026-08-06 brainstorm (log_016): every
    entry is a `ur.node`, connectors ride on their nodes, machinery
    is module-level functions taking the table (protocol §2b),
    module/class stutter dissolved by the owner's `table` naming.
  - realization criterion, from the owner's challenge: rules of its own →
    a CORE; one-sentence rule → in-file.

- 2026-08-02: node folder generated by
  `<WORKSPACE_DIR>/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `<WORKSPACE_DIR>/PseudoCoup_v5/Planning/node_0_0_tools/node_0_0_0_ledgerer/CORE_0_0_0_ledgerer.md`. Skeleton only — definition,
  designation, and content pending.
