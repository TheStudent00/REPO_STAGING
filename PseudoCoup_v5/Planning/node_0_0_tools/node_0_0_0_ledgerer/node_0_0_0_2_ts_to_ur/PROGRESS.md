---
id: pcv5.tools.ledgerer.ts_to_ur.progress
status: living
---

# PROGRESS — ts_to_ur

- 2026-08-02: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/Programming/PseudoCoup_v5/Planning/node_0_0_tools/node_0_0_0_ledgerer/CORE_0_0_0_ledgerer.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-08-02: moved under `builder` when the funnel was first drawn,
  then back to `ledgerer` level the same day, when the owner settled the
  definition/instance distinction: "the definition of the module
  doesnt make sense to be within a class." the builder now holds an
  INSTANCE of this module's class as an attribute; the definition
  lives here.
- 2026-08-04: renamed `ts_to_ur_mapper` → `ts_to_ur` (the owner: the
  `x_to_y` form is descriptive without a `<mapping_type>` suffix).
  folder renumbering unchanged — same address, new title.
- 2026-08-11: design section written from the brainstorm walk —
  `Mapper` + `LanguagePack` shapes ruled (the owner: "yeah those look
  good"), token tables ruled grammar-authored (corpus census
  demoted to frequency/oracle material), variant-by-role verified
  against the live parse. open edges recorded by log_017 number
  (q3 macros, q4 error files, q5 ratification, q6 pack file
  layout). next: shape-pass stubs in
  `~/Programming/PseudoCoup_v5/Tools/ledgerer/ts_to_ur.py`, the owner's
  review, logic pass, verification tests (the owner: skeptical until
  verified — the tests are the settling instrument).
- 2026-08-12: research detour, ruled worth it (logs 018/019): the
  compiler as second authority — differential alphabet check found
  the `safe` pin gap corpus-free; empirical stencil recovery from
  rustc's pretty-printer works (verified on 1.96.1 via the
  SandboxDesign lane). rulings: mirror the pin; pin bump before
  compiler fallback; `check_pin` + staleness check both in the pack.
- 2026-08-12: logic pass done
  (`~/Programming/PseudoCoup_v5/Tools/ledgerer/ts_to_ur.py`) and
  verification suite green — 17/17
  (`~/Programming/PseudoCoup_v5/Tools/ledgerer/test_ts_to_ur.py`):
  node conservation, id/walk-order agreement, content-leaf text,
  variant-by-role, grammar roles, no source retention, refusals
  (unknown kind; ERROR naming the pin), opacity, pin + staleness
  checks, and admit→dump→load fixed point. two seams found and
  fixed during the run: py-tree-sitter wrapper identity (roles now
  keyed by span+kind) and `TsOrigin.fields` serialization (roles
  dump as ids, re-linked on load). still open before this node
  settles: q1 ratified kind map (tests use an explicit test map),
  q3 macro cluster, q6 pack file layout.
- 2026-08-13: q1 instrument GENERATED — the PCHQ pack generator
  (research P-a) produced the machine-proposed rust ts_kind →
  ur_kind map with evidence per row: 106/163 proposed (22 strong /
  54 moderate / 30 weak), 57-row residue for review. report + full
  table:
  `~/Programming/PseudoCoup_v5/DevComms/log_021_rust_kind_map_proposal.md`;
  data:
  `~/Programming/PseudoCoupHQ/Research/kind_signature_clustering/proposed_kind_map_rust.json`.
  review pending — q1 ratification now means walking log_021 §3
  (the proposals), ruling §5 (the residue), and settling the §4
  disagreements with log_008's hand draft.
