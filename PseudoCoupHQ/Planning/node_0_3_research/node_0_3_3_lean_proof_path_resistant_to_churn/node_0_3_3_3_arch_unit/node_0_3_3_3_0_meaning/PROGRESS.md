---
id: hq.research.lean_proof_path_resistant_to_churn.arch_unit.meaning.progress
status: living
---

# PROGRESS — meaning

- 2026-09-13: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_3_architectures/node_0_3_2_3_1_riscv64/.archive/lean_proof_path_superseded_2026-09-14/node_0_3_2_3_1_3_3_arch_unit/CORE_0_3_2_3_1_3_3_arch_unit.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-14: carried into `Planning/node_0_3_research/node_0_3_3_lean_proof_path_resistant_to_churn` (the owner: start over in a different research node; copy the node, delete the wrong parts, re-plan them). This node was one of the right parts. It supersedes `hq.research.arch_unit_oracle.architectures.riscv64.lean_proof_path.arch_unit.meaning`.
- 2026-09-15, evening: the walk extended for floats in
  `Research/oracle/riscv/leanpath/leanpath/walk.py`: `compose` takes a
  step whose clause the effects rule read (`compose_effects`, new): each
  read comes from the integer or the float register file by the clause
  parameter's own type, the float argument registers fa0..fa3 (f10..f13)
  are unknowns, a selection (the rounding mode) is an unknown of its type
  stated as a hypothesis on the state, and the write goes to the file of
  its parameter's type. The answer is the ABI answer register the body
  writes LAST, a0 (x10) or fa0 (f10): machine form, because the corpus's
  sources state the result type as the compiler's `__typeof__`.
  `walk_file` takes the float hypotheses and the answer register as
  optional arguments; `cmd_walk` takes the effects rule's proposal where
  the one rule refuses (status: done).
- 2026-09-15, evening: regression, no drift for the integer units: the
  622 rows of the c corpus walk and the width-rule gate re-composed from
  their own decoded words give identical `compose` output and identical
  `walk_file` text from the old and the new `walk.py` (0 differ). Not yet
  exercised on a float unit (status: done).
- 2026-09-15, evening: the gate over a dozen c float arch-units against
  the working copy, submitted as lane
  `lanes_lp1/lp3_l89_gate_c_float_arch_units.sh`, after l88 (status:
  in-progress).
