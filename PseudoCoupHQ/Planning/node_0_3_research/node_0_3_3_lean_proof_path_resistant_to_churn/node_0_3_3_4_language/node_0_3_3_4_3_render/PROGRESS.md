---
id: hq.research.lean_proof_path_resistant_to_churn.language.render.progress
status: living
---

# PROGRESS — render

- 2026-09-14: planned (status: planned). Written fresh at the owner's order of 2026-09-14 ("start over. in a different research node ... the parts that are fucked up are deleted before you start them over again"); nothing of the superseded node's `render` was copied.
- 2026-09-14: coded as `leanpath/render.py` (`python3 -m leanpath render`): keys of operator_for as patterns, largest first; the unit's probe source copied verbatim; c plumbing only. Smoke-tested locally with a STAND-IN table (four keys pointed at the real int64 probes): RTYPE ADD/XOR/SUB rendered; ADD compiles to `add a0, a0, a1`. The real table comes from lane l53 (status: coded, awaiting the table).
- 2026-09-15: first real run (gate l61): 3 emulations from a 3-key table; the ABI width of a unit's parameter shows in the U and L columns and in the unproved row; a preference rule among units proving the same key is the owner's to decide (status: in-progress).
- 2026-09-15: go plumbing: a unit whose function returns `bool` (read off the probe's own `func op_N(...) bool {`) is called through `b2u`; the go compiler folds it back to one instruction (`sltu`) (status: coded, tested locally).
- 2026-09-15: the width rule in `render` is now a comparison against the key's OWN operand widths, not against 64. `Renderer.__init__` reads each key's `operand_widths` from the table (falling back to reading them off the key's text) and takes a unit whose holder widths equal them; the refusal names both ("uses its reads at widths [32, 32]; the units that prove it are held at [1, 1]"), and every rendered row records the widths beside the subterm and the unit it was built from. The eight W-form keys log 284 §10 recorded as proved-but-not-rendered are the case this answers. Measured locally, rendering from the tables of record: c 18 -> 25 emulations (new: RTYPEW ADDW, SUBW, SLLW, SRAW, MULW, DIVW, REMW), cpp 18 -> 25, rust 18 -> 23, go 4 -> 5, nothing lost; the one W key still refused is the 32-bit logical right shift, whose only c and cpp units are held at 1-bit holders (status: coded; lane l82 is its gate).
- 2026-09-15: the gate (lane l82, 313 s, exit 0): from a 12-key table over a dozen c units, 11 emulations written, 11 lowered and certified, 11 proved by the same text; the seven W forms (RTYPEW ADDW, SUBW, SLLW, SRAW, MULW, DIVW signed, REMW signed) rendered from arch-units held at (32, 32), each lowered to one instruction and the return; RTYPEW SRLW refused with its widths and its units' holders named. Lanes l84 (the c render from the l83 table) and l86 (the cpp render from the l85 table) submitted behind their tables; the l79 and l81 renders they replace had 18 emulations each for c and cpp (status: in-progress).
- 2026-09-15: the c render under the width rule by use, done (lane l84, 273 s, exit 0), from the l83 table. Keys usable at render **22 of 23** (was 15); **25 emulations written, 68 refused of 93 arms; 25 lowered, read back and certified; 25 proved equal to their definition by the same text** (was 18 / 18 / 18). The seven newly proved: RTYPEW ADDW, SUBW, SLLW, SRAW, MULW, DIVW (signed), REMW (signed), each rendered from c's arch-unit held at (32, 32) (c__op_102, 138, 678, 714, 174, 210, 246) and each lowered to one instruction and the return (`C_ADDW`, `C_SUBW`, `RTYPEW`, `RTYPEW`, `MULW`, `DIVW`, `REMW`). All eighteen of l79 still prove. RTYPEW SRLW refused at render: "uses its reads at widths [32, 32]; the units that prove it are held at [1, 1], [1, 32], [1, 64]". The unsigned 32-bit divide and remainder have no key in the table (no arch-unit proved equal to them) and refuse for want of a key. Spelling guard PASS on render.json, walk.json, equals.json. `runs/handful_c/pass_b/eye_table.md` (status: done for c).
- 2026-09-15: the cpp render under the width rule by use, done (lane l86, 270 s, exit 0), from the l85 table. Keys usable at render **22 of 23** (was 15); **25 emulations written, 68 refused of 93 arms; 25 lowered, read back and certified; 25 proved equal to their definition by the same text** (was 18 / 18 / 18). The seven newly proved are the same seven as c's — RTYPEW ADDW, SUBW, SLLW, SRAW, MULW, DIVW (signed), REMW (signed) — each rendered from cpp's arch-unit held at (32, 32) (cpp__op_102, 138, 678, 714, 174, 210, 246) and lowered to one instruction and the return (`C_ADDW`, `C_SUBW`, `RTYPEW`, `RTYPEW`, `MULW`, `DIVW`, `REMW`). All eighteen of the l81 cpp render still prove. RTYPEW SRLW refused at render with its widths and its units' holders ([1, 1], [1, 32], [1, 64]) named. `runs/corpus_cpp/pass_b/eye_table.md` (status: done for cpp; the width rule's wide run for the two languages with W-form keys is complete).
