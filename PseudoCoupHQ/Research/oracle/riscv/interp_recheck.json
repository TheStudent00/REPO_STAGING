{
 "meta": {
  "agreements": 800,
  "certificates": 6,
  "disagreements": 0,
  "memory_abort": "ABORT_MEMORY_RV2",
  "memory_bound_kb": 6291456,
  "peak_kb": 257368,
  "point_seed": "rv2",
  "points": 800,
  "points_per_certificate": 200,
  "target": "cpython",
  "task": "rv2",
  "what": "a handful of cpython emulations run again at a sample of points, against the term they are certified for -- a check at points, not a proof"
 },
 "rows": [
  {
   "agreements": 200,
   "cell": {
    "key_width": 32,
    "mnem": "add",
    "shape": "gpr_gpr"
   },
   "compared_on_bits": 64,
   "comparison_note": "a vector place is the whole 128-bit register and the operation writes one LANE of it, so a vector place is compared on the cell's own key_width -- the projection the pipeline's own gate makes (`handful.projected_lane`); a general-register place is compared whole",
   "disagreements": 0,
   "outcome": "AGREES_AT_EVERY_POINT",
   "place": "reg_rdi",
   "points": 200,
   "runner": "python3 PseudoCoupHQ/Research/oracle/cross_construction/emulation/interp/src_ex1/add_gpr_gpr_32__reg_rdi__cpython.py",
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "d259dfd93db7b908cc61a54a8ae19de873c3da592d0a2b8771409f88a3025856",
    "path": "interp/src_ex1/add_gpr_gpr_32__reg_rdi__cpython.py",
    "sha256": "d259dfd93db7b908cc61a54a8ae19de873c3da592d0a2b8771409f88a3025856"
   },
   "target": "cpython",
   "term_rebuilt": "Concat(0, Extract(31, 0, v0) + Extract(31, 0, v1))",
   "term_text": "Concat(0, Extract(31, 0, v0) + Extract(31, 0, v1))",
   "unevaluated_note": "a point where z3's own simplifier does not put the term at a single numeral -- a NaN has no one IEEE bit pattern in z3's float theory -- is UNEVALUATED and is dropped from both the numerator and the denominator, which is the fuzz census's own rule for a declined key"
  },
  {
   "agreements": 200,
   "cell": {
    "key_width": 32,
    "mnem": "imul",
    "shape": "gpr_gpr"
   },
   "compared_on_bits": 64,
   "comparison_note": "a vector place is the whole 128-bit register and the operation writes one LANE of it, so a vector place is compared on the cell's own key_width -- the projection the pipeline's own gate makes (`handful.projected_lane`); a general-register place is compared whole",
   "disagreements": 0,
   "outcome": "AGREES_AT_EVERY_POINT",
   "place": "reg_rdi",
   "points": 200,
   "runner": "python3 PseudoCoupHQ/Research/oracle/cross_construction/emulation/interp/src_ex1/imul_gpr_gpr_32__reg_rdi__cpython.py",
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "0d023985e5acd44f6d93cd0afb1e9d53080c9aee42c70ccd786ce898d0c1293d",
    "path": "interp/src_ex1/imul_gpr_gpr_32__reg_rdi__cpython.py",
    "sha256": "0d023985e5acd44f6d93cd0afb1e9d53080c9aee42c70ccd786ce898d0c1293d"
   },
   "target": "cpython",
   "term_rebuilt": "Concat(0, Extract(31, 0, v0)*Extract(31, 0, v1))",
   "term_text": "Concat(0, Extract(31, 0, v0)*Extract(31, 0, v1))",
   "unevaluated_note": "a point where z3's own simplifier does not put the term at a single numeral -- a NaN has no one IEEE bit pattern in z3's float theory -- is UNEVALUATED and is dropped from both the numerator and the denominator, which is the fuzz census's own rule for a declined key"
  },
  {
   "agreements": 200,
   "cell": {
    "key_width": 32,
    "mnem": "sar",
    "shape": "cl_gpr"
   },
   "compared_on_bits": 64,
   "comparison_note": "a vector place is the whole 128-bit register and the operation writes one LANE of it, so a vector place is compared on the cell's own key_width -- the projection the pipeline's own gate makes (`handful.projected_lane`); a general-register place is compared whole",
   "disagreements": 0,
   "outcome": "AGREES_AT_EVERY_POINT",
   "place": "reg_rdi",
   "points": 200,
   "runner": "python3 PseudoCoupHQ/Research/oracle/cross_construction/emulation/interp/src_ex1/sar_cl_gpr_32__reg_rdi__cpython.py",
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "2f5db53c10c3104473b8dfbbdb70e7738039a63e29424b9ba7b4be9e530219db",
    "path": "interp/src_ex1/sar_cl_gpr_32__reg_rdi__cpython.py",
    "sha256": "2f5db53c10c3104473b8dfbbdb70e7738039a63e29424b9ba7b4be9e530219db"
   },
   "target": "cpython",
   "term_rebuilt": "Concat(0, Extract(31, 0, v0) >> Concat(0, Extract(4, 0, v1)))",
   "term_text": "Concat(0, Extract(31, 0, v0) >> Concat(0, Extract(4, 0, v1)))",
   "unevaluated_note": "a point where z3's own simplifier does not put the term at a single numeral -- a NaN has no one IEEE bit pattern in z3's float theory -- is UNEVALUATED and is dropped from both the numerator and the denominator, which is the fuzz census's own rule for a declined key"
  },
  {
   "agreements": 200,
   "cell": {
    "key_width": 64,
    "mnem": "shr",
    "shape": "cl_gpr"
   },
   "compared_on_bits": 64,
   "comparison_note": "a vector place is the whole 128-bit register and the operation writes one LANE of it, so a vector place is compared on the cell's own key_width -- the projection the pipeline's own gate makes (`handful.projected_lane`); a general-register place is compared whole",
   "disagreements": 0,
   "outcome": "AGREES_AT_EVERY_POINT",
   "place": "reg_rdi",
   "points": 200,
   "runner": "python3 PseudoCoupHQ/Research/oracle/cross_construction/emulation/interp/src_ex1/shr_cl_gpr_64__reg_rdi__cpython.py",
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "da02d88a46ac065aff4a20db01bdbc610e7aee7c97b394f30a6689c1736f7d6c",
    "path": "interp/src_ex1/shr_cl_gpr_64__reg_rdi__cpython.py",
    "sha256": "da02d88a46ac065aff4a20db01bdbc610e7aee7c97b394f30a6689c1736f7d6c"
   },
   "target": "cpython",
   "term_rebuilt": "LShR(v0, Concat(0, Extract(5, 0, v1)))",
   "term_text": "LShR(v0, Concat(0, Extract(5, 0, v1)))",
   "unevaluated_note": "a point where z3's own simplifier does not put the term at a single numeral -- a NaN has no one IEEE bit pattern in z3's float theory -- is UNEVALUATED and is dropped from both the numerator and the denominator, which is the fuzz census's own rule for a declined key"
  },
  {
   "agreements": 0,
   "cell": {
    "key_width": 32,
    "mnem": "addss",
    "shape": "xmm_xmm"
   },
   "disagreements": 0,
   "outcome": "THE_CERTIFICATE_IS_ABOUT_A_RE_POSED_TERM",
   "place": "reg_xmm0",
   "points": 0,
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "e06aca694625cc10cccb7b48ee6747b9c3837eb2436a63f533838c888caf828d",
    "path": "interp/src_ex1/addss_xmm_xmm_32__reg_xmm0__cpython.py",
    "sha256": "e06aca694625cc10cccb7b48ee6747b9c3837eb2436a63f533838c888caf828d"
   },
   "target": "cpython",
   "term_rebuilt": "Concat(Extract(127, 32, v0), fp.to_ieee_bv(fpToFP(Extract(31, 0, v0)) + fpToFP(Extract(31, 0, v1))))",
   "term_text": "fp.to_ieee_bv(fpToFP(Extract(31, 0, v0)) + fpToFP(Extract(31, 0, v1)))",
   "why": "the certificate's own term is not this place's whole term: the pipeline's gate re-poses a vector place onto the LANE the operation writes (`handful.projected_lane`) and renders the emulation from that, so the source takes fewer arguments than the whole place reads. Re-posing a vector place is the driver's job and this file does not restate it; the row is named rather than counted as a disagreement."
  },
  {
   "agreements": 0,
   "cell": {
    "key_width": 64,
    "mnem": "cvtsi2sd",
    "shape": "gpr_xmm"
   },
   "disagreements": 0,
   "outcome": "THE_CERTIFICATE_IS_ABOUT_A_RE_POSED_TERM",
   "place": "reg_xmm0",
   "points": 0,
   "source": {
    "found_on_disk": true,
    "matches_disk": null,
    "on_disk_sha256": "fe97aa81da7615b8d3573006a825c3b68fff1d468c9dfec9eab9c513566491c4",
    "path": "interp/src_ex1/cvtsi2sd_gpr_xmm_64__reg_xmm0__cpython.py",
    "sha256": "fe97aa81da7615b8d3573006a825c3b68fff1d468c9dfec9eab9c513566491c4"
   },
   "target": "cpython",
   "term_rebuilt": "Concat(Extract(127, 64, v0), fp.to_ieee_bv(fpToFP(RNE(), Extract(7, 0, v1))))",
   "term_text": "fp.to_ieee_bv(fpToFP(RNE(), v0))",
   "why": "the certificate's own term is not this place's whole term: the pipeline's gate re-poses a vector place onto the LANE the operation writes (`handful.projected_lane`) and renders the emulation from that, so the source takes fewer arguments than the whole place reads. Re-posing a vector place is the driver's job and this file does not restate it; the row is named rather than counted as a disagreement."
  }
 ]
}
