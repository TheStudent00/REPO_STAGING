---
id: hq.research.compiler_graph.pool.progress
status: living
---

# PROGRESS — pool

- 2026-09-03: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: the_pool3 on three grounds: 2,247 entries / 30,436 members / 632 multi-language; families3 36 / 197; compare with computed causes — **done**. Evidence: log_154 (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: exception_families rebuilt over the current pool — **planned**. Evidence: exception_families2.json is round-5 (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: pool rebuilt after runtime_callee, sbb/adc, reference fixes land — **planned**. Evidence: this tree (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: `pool.py` written — class `Pool` with `merge`,
  `compare`, `representative`, `families`, `exception_families`;
  `dom_ops.py` imported unchanged — **done**. Evidence: `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: `the_pool4.json` over the 30,432 units canon39 proved:
  **1,961 entries / 30,432 members / 573 multi-language / 3
  compiled-and-interpreted**, against pool3's 2,247 / 30,436 / 632 / 3
  over canon38. Brief-strict count recorded as 5,668 and used for
  nothing — **done**. Evidence: `pool61_run.log`, `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: the delta against pool3, joined on member sets: 7
  splits, 107 merges, 4 units gone (the swift regen units canon39
  refused), 0 units new, every cause computed — **done**. Evidence:
  `pool3_pool4_delta.json`, `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: exception families rebuilt over the current pool —
  **done** (`exception_families4.json`, not `3`: see this node's
  exception_families sub-node for the name correction). Evidence:
  `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: `the_pool5.json` over the 30,432 units canon39 proved,
  on the round-13 gate: **1,831 entries / 30,432 members / 490
  multi-language / 3 compiled-and-interpreted**, against pool4's
  1,961 / 30,432 / 573 / 3. Brief-strict count recorded as **5,095**
  and used for nothing — **done**. Evidence: `pool65_run.log`, `PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03: the delta against pool4, joined on member sets: **35
  splits, 89 merges**, 0 units gone, 0 units new, every cause
  computed. THE FINDING: **233** pool4 members were layer-5 eligible
  and have since been DISPROVED, and **26 of the 35 splits** contain
  at least one of them (155 such members inside those splits) — those
  entries were resting on a withdrawn key — **done**. Evidence:
  `pool4_pool5_delta.json`, `pool_delta65_printed.txt`, `PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-04: the canon40 rebuild was NOT produced. It reads a complete
  `term66_store`, and the canon40 transcription does not fit the
  round's stated 6 GB memory cap: 3,927 of the 30,324 units canon40
  proves carry a runtime-callee row (49,362 rows against canon39's
  608), and over ceilings of 512/1024/2048/4096/6144 MB a hard shard's
  records never converge, with the bound firing into 2 records of 2,701
  as a written `MemoryError` reason. Nothing was built on the partial
  store — **blocked on the transcribe node, flagged for the owner**.
  Evidence:
  `PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §3, §4, §7.2.
- 2026-09-05: the pool over canon40 is BLOCKED by its own refusal, and
  the refusal was not worked around. `pool66_run.py` reads 30,324 units
  into the pool and 30,280 layer-4 records, and refuses:
  `REFUSED OWN OUTPUT: 44 proved units have no layer-4 record`. The 44
  are the units whose layer-5 normalization does not converge at any
  ceiling this line can reach. No second pool driver was written to
  walk around the guard, no member was dropped to make the counts
  agree, and no record was composed for the 44 — **blocked, reported**.
  So `the_pool6.json`, `the_pool6_bytes.json`, `the_families6.json`,
  `exception_families6.json`, `pool5_pool6_delta.json`,
  `pool6_prediction_check.json` and E00029's successor are NOT on disk
  and nothing was estimated in their place. The pool5 line they would
  have been read against is confirmed on disk: **1,831 entries / 30,432
  members / 490 spanning more than one language / 3 spanning compiled
  and interpreted / 34 families**. Evidence:
  `<runs>/t97/agent/logs/20260905T095120Z__t97_l10_downstream_partial.sh.log`;
  `PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §6, §9.1.
- 2026-09-06: task 100, the MISSING merge ground (a proved edge
  between two entries whose layer-5 texts differ but whose terms are
  equal for every input), run over pool5's 1,831 entries: 43,410
  candidate pairs across 88 `type_key` groups planned before any
  solver ran; the pair lane (three windows, an earlier session) ended
  on its own stated 4-hour-per-window budget with **21,502 of 43,410
  pairs answered (49.5%), 5 of 88 groups complete** (4 run in full —
  `rdi,rsi|32`, `rdi,rsi|8`, `rdi,rsi,rcx|8`, `rcx,xmm0,xmm1|32` —
  plus one trivial 0-pair group; 61 of the remaining 83 groups never
  started). This session ran the closing step only (no new pair
  solved): **12 edges applied, pool5's 1,831 entries close to 1,819**
  in `pool100_pool6_candidate.json` — a CANDIDATE for the owner to ratify,
  NOT written as `the_pool6.json`. States over the 21,502 answered:
  PROVED 226 (214 of those proved-but-not-applied — one side's term
  not proved against its own ship body), DISPROVED 19,640 (2,279 of
  those assign a constant-pool symbol), UNDECIDED 1,099 (all a 120 s
  runner limit, none a 3,000 ms solver timeout — 0 would be retried
  at 120,000 ms), UNBUILDABLE 537 (all `NO_TERM` on the
  `__floatuntidf`/`__floatuntisf` runtime callees). The 44
  non-converging units' 18 pool5 entries took part like any other; 0
  of them sit in a fully-answered group — **partially done, budget
  exhausted, flagged for a further pair-lane window**. Evidence:
  `pool100_report.md`, `pool100_edges.json`,
  `pool100_pool6_candidate.json`;
  `PseudoCoupHQ/DevComms/log_224_task_t100_pool_entry_equivalence_closure.md`.
