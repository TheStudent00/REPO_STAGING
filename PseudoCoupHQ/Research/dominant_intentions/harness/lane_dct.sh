#!/bin/sh
# dict census verification run -- generated by
# Research/dominant_intentions/harness/generate_run.py . Do not hand-edit;
# regenerate. Every line printed to /out/dct_<lang>.txt is
# FACT.PROBE|RESULT.
set -u
ROOT=/work/dct
rm -rf "$ROOT"; mkdir -p "$ROOT"
export HOME=/work
export GOFLAGS=-mod=mod
export GOCACHE=/work/.gocache
export GOPATH=/work/.gopath
export PATH=/persist/dart-sdk/bin:$PATH

# swift's binaries want an ncurses name the image does not ship under; the fix
# is per-container, so every script that may touch swift re-applies it.
ncurses_fix() {
  if [ -f /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 ]; then
    ln -sf /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 \
       /usr/lib/x86_64-linux-gnu/libncurses.so.6 2>/dev/null
    ldconfig 2>/dev/null
  fi
}
ncurses_fix

echo "=== dict census verification run ==="
date -u +%Y-%m-%dT%H:%M:%SZ


# ---------------------------------------------------------------- python
echo ""; echo "--- python ---"
OUT=/out/dct_python.txt
: > "$OUT"
if command -v python3 >/dev/null 2>&1; then
  D="$ROOT/python/main"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# dict probe bodies - python (insertion-ordered, any hashable key, KeyError).
def mkd(): return {"k": "v1"}
def f_dictU1_a(): return fmts(mkd()["k"])
def f_dictU2_a():
    d = {"k": "v1"}; d["k"] = "v2"; return fmts(d["k"])
def f_dictU3_a():
    d = mkd(); del d["k"]; return fmtb("k" in d)
def f_dictU4_a():
    d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(len(d))
def f_Fd1_a():
    d = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1])
def f_Fd2_a():
    d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return ",".join(d.keys())
def f_Fd3_a(): return fmts(mkd()["nope"])
def f_Fd4_a():
    d = {"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
    for k in d:
        if k == "c": del d[k]
    return "ok"
def f_Fd5_a():
    d = {}; d[(1, 2)] = "x"; d[(1, 2)] = "y"; return fmts(len(d))
def f_Fd5_b():
    d = {}; d[float("nan")] = "x"; return fmts(d[float("nan")])
def f_Fl9d_a():
    a = mkd(); b = a; b["k"] = "99"; return fmts(a["k"])


try:
    print("dict-U1.a|" + fmts(f_dictU1_a()))
except BaseException as _e:
    print("dict-U1.a|<raise:" + type(_e).__name__ + ">")
try:
    print("dict-U2.a|" + fmts(f_dictU2_a()))
except BaseException as _e:
    print("dict-U2.a|<raise:" + type(_e).__name__ + ">")
try:
    print("dict-U3.a|" + fmts(f_dictU3_a()))
except BaseException as _e:
    print("dict-U3.a|<raise:" + type(_e).__name__ + ">")
try:
    print("dict-U4.a|" + fmts(f_dictU4_a()))
except BaseException as _e:
    print("dict-U4.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-d1.a|" + fmts(f_Fd1_a()))
except BaseException as _e:
    print("F-d1.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-d2.a|" + fmts(f_Fd2_a()))
except BaseException as _e:
    print("F-d2.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-d5.a|" + fmts(f_Fd5_a()))
except BaseException as _e:
    print("F-d5.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-d5.b|" + fmts(f_Fd5_b()))
except BaseException as _e:
    print("F-d5.b|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.a|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d5.a|$VERDICT" >> "$OUT"
    echo "F-d5.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/missing"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# dict probe bodies - python (insertion-ordered, any hashable key, KeyError).
def mkd(): return {"k": "v1"}
def f_dictU1_a(): return fmts(mkd()["k"])
def f_dictU2_a():
    d = {"k": "v1"}; d["k"] = "v2"; return fmts(d["k"])
def f_dictU3_a():
    d = mkd(); del d["k"]; return fmtb("k" in d)
def f_dictU4_a():
    d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(len(d))
def f_Fd1_a():
    d = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1])
def f_Fd2_a():
    d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return ",".join(d.keys())
def f_Fd3_a(): return fmts(mkd()["nope"])
def f_Fd4_a():
    d = {"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
    for k in d:
        if k == "c": del d[k]
    return "ok"
def f_Fd5_a():
    d = {}; d[(1, 2)] = "x"; d[(1, 2)] = "y"; return fmts(len(d))
def f_Fd5_b():
    d = {}; d[float("nan")] = "x"; return fmts(d[float("nan")])
def f_Fl9d_a():
    a = mkd(); b = a; b["k"] = "99"; return fmts(a["k"])


try:
    print("F-d3.a|" + fmts(f_Fd3_a()))
except BaseException as _e:
    print("F-d3.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# dict probe bodies - python (insertion-ordered, any hashable key, KeyError).
def mkd(): return {"k": "v1"}
def f_dictU1_a(): return fmts(mkd()["k"])
def f_dictU2_a():
    d = {"k": "v1"}; d["k"] = "v2"; return fmts(d["k"])
def f_dictU3_a():
    d = mkd(); del d["k"]; return fmtb("k" in d)
def f_dictU4_a():
    d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(len(d))
def f_Fd1_a():
    d = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1])
def f_Fd2_a():
    d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return ",".join(d.keys())
def f_Fd3_a(): return fmts(mkd()["nope"])
def f_Fd4_a():
    d = {"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
    for k in d:
        if k == "c": del d[k]
    return "ok"
def f_Fd5_a():
    d = {}; d[(1, 2)] = "x"; d[(1, 2)] = "y"; return fmts(len(d))
def f_Fd5_b():
    d = {}; d[float("nan")] = "x"; return fmts(d[float("nan")])
def f_Fl9d_a():
    a = mkd(); b = a; b["k"] = "99"; return fmts(a["k"])


try:
    print("F-d4.a|" + fmts(f_Fd4_a()))
except BaseException as _e:
    print("F-d4.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# dict probe bodies - python (insertion-ordered, any hashable key, KeyError).
def mkd(): return {"k": "v1"}
def f_dictU1_a(): return fmts(mkd()["k"])
def f_dictU2_a():
    d = {"k": "v1"}; d["k"] = "v2"; return fmts(d["k"])
def f_dictU3_a():
    d = mkd(); del d["k"]; return fmtb("k" in d)
def f_dictU4_a():
    d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(len(d))
def f_Fd1_a():
    d = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1])
def f_Fd2_a():
    d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return ",".join(d.keys())
def f_Fd3_a(): return fmts(mkd()["nope"])
def f_Fd4_a():
    d = {"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
    for k in d:
        if k == "c": del d[k]
    return "ok"
def f_Fd5_a():
    d = {}; d[(1, 2)] = "x"; d[(1, 2)] = "y"; return fmts(len(d))
def f_Fd5_b():
    d = {}; d[float("nan")] = "x"; return fmts(d[float("nan")])
def f_Fl9d_a():
    a = mkd(); b = a; b["k"] = "99"; return fmts(a["k"])


try:
    print("F-l9-dict.a|" + fmts(f_Fl9d_a()))
except BaseException as _e:
    print("F-l9-dict.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "python: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- ruby
echo ""; echo "--- ruby ---"
OUT=/out/dct_ruby.txt
: > "$OUT"
if command -v ruby >/dev/null 2>&1; then
  D="$ROOT/ruby/main"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# dict probe bodies - ruby (insertion-ordered, nil on a missing key).
def mkd() = {"k" => "v1"}
def f_dictU1_a() = fmts(mkd()["k"])
def f_dictU2_a()
  d = {"k" => "v1"}; d["k"] = "v2"; fmts(d["k"])
end
def f_dictU3_a()
  d = mkd(); d.delete("k"); fmtb(d.key?("k"))
end
def f_dictU4_a()
  d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; fmts(d.length)
end
def f_Fd1_a()
  d = {}; d[1] = "int"; d["1"] = "str"; fmts(d[1])
end
def f_Fd2_a()
  d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; d.keys.join(",")
end
def f_Fd3_a() = fmts(mkd()["nope"].inspect)
def f_Fd4_a()
  d = {"a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5}
  d.each_key { |k| d.delete(k) if k == "c" }
  "ok"
end
def f_Fd5_a()
  d = {}; d[[1, 2]] = "x"; d[[1, 2]] = "y"; fmts(d.length)
end
def f_Fl9d_a()
  a = mkd(); b = a; b["k"] = "99"; fmts(a["k"])
end


begin; puts("dict-U1.a|" + fmts(f_dictU1_a())); rescue Exception => _e; puts("dict-U1.a|<raise:" + _e.class.name + ">"); end
begin; puts("dict-U2.a|" + fmts(f_dictU2_a())); rescue Exception => _e; puts("dict-U2.a|<raise:" + _e.class.name + ">"); end
begin; puts("dict-U3.a|" + fmts(f_dictU3_a())); rescue Exception => _e; puts("dict-U3.a|<raise:" + _e.class.name + ">"); end
begin; puts("dict-U4.a|" + fmts(f_dictU4_a())); rescue Exception => _e; puts("dict-U4.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-d1.a|" + fmts(f_Fd1_a())); rescue Exception => _e; puts("F-d1.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-d2.a|" + fmts(f_Fd2_a())); rescue Exception => _e; puts("F-d2.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-d5.a|" + fmts(f_Fd5_a())); rescue Exception => _e; puts("F-d5.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.a|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/missing"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# dict probe bodies - ruby (insertion-ordered, nil on a missing key).
def mkd() = {"k" => "v1"}
def f_dictU1_a() = fmts(mkd()["k"])
def f_dictU2_a()
  d = {"k" => "v1"}; d["k"] = "v2"; fmts(d["k"])
end
def f_dictU3_a()
  d = mkd(); d.delete("k"); fmtb(d.key?("k"))
end
def f_dictU4_a()
  d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; fmts(d.length)
end
def f_Fd1_a()
  d = {}; d[1] = "int"; d["1"] = "str"; fmts(d[1])
end
def f_Fd2_a()
  d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; d.keys.join(",")
end
def f_Fd3_a() = fmts(mkd()["nope"].inspect)
def f_Fd4_a()
  d = {"a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5}
  d.each_key { |k| d.delete(k) if k == "c" }
  "ok"
end
def f_Fd5_a()
  d = {}; d[[1, 2]] = "x"; d[[1, 2]] = "y"; fmts(d.length)
end
def f_Fl9d_a()
  a = mkd(); b = a; b["k"] = "99"; fmts(a["k"])
end


begin; puts("F-d3.a|" + fmts(f_Fd3_a())); rescue Exception => _e; puts("F-d3.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# dict probe bodies - ruby (insertion-ordered, nil on a missing key).
def mkd() = {"k" => "v1"}
def f_dictU1_a() = fmts(mkd()["k"])
def f_dictU2_a()
  d = {"k" => "v1"}; d["k"] = "v2"; fmts(d["k"])
end
def f_dictU3_a()
  d = mkd(); d.delete("k"); fmtb(d.key?("k"))
end
def f_dictU4_a()
  d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; fmts(d.length)
end
def f_Fd1_a()
  d = {}; d[1] = "int"; d["1"] = "str"; fmts(d[1])
end
def f_Fd2_a()
  d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; d.keys.join(",")
end
def f_Fd3_a() = fmts(mkd()["nope"].inspect)
def f_Fd4_a()
  d = {"a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5}
  d.each_key { |k| d.delete(k) if k == "c" }
  "ok"
end
def f_Fd5_a()
  d = {}; d[[1, 2]] = "x"; d[[1, 2]] = "y"; fmts(d.length)
end
def f_Fl9d_a()
  a = mkd(); b = a; b["k"] = "99"; fmts(a["k"])
end


begin; puts("F-d4.a|" + fmts(f_Fd4_a())); rescue Exception => _e; puts("F-d4.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# dict probe bodies - ruby (insertion-ordered, nil on a missing key).
def mkd() = {"k" => "v1"}
def f_dictU1_a() = fmts(mkd()["k"])
def f_dictU2_a()
  d = {"k" => "v1"}; d["k"] = "v2"; fmts(d["k"])
end
def f_dictU3_a()
  d = mkd(); d.delete("k"); fmtb(d.key?("k"))
end
def f_dictU4_a()
  d = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; fmts(d.length)
end
def f_Fd1_a()
  d = {}; d[1] = "int"; d["1"] = "str"; fmts(d[1])
end
def f_Fd2_a()
  d = {}; d["z"] = 1; d["a"] = 1; d["m"] = 1; d.keys.join(",")
end
def f_Fd3_a() = fmts(mkd()["nope"].inspect)
def f_Fd4_a()
  d = {"a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5}
  d.each_key { |k| d.delete(k) if k == "c" }
  "ok"
end
def f_Fd5_a()
  d = {}; d[[1, 2]] = "x"; d[[1, 2]] = "y"; fmts(d.length)
end
def f_Fl9d_a()
  a = mkd(); b = a; b["k"] = "99"; fmts(a["k"])
end


begin; puts("F-l9-dict.a|" + fmts(f_Fl9d_a())); rescue Exception => _e; puts("F-l9-dict.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "ruby: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- php
echo ""; echo "--- php ---"
OUT=/out/dct_php.txt
: > "$OUT"
if command -v php >/dev/null 2>&1; then
  D="$ROOT/php/main"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// dict probe bodies - php. php's array is ONE structure serving list and dict:
// an ordered hash whose string keys that look like integers BECOME integers.
function mkd() { return ["k" => "v1"]; }
function f_dictU1_a() { $d = mkd(); return fmts($d["k"]); }
function f_dictU2_a() { $d = ["k" => "v1"]; $d["k"] = "v2"; return fmts($d["k"]); }
function f_dictU3_a() { $d = mkd(); unset($d["k"]); return fmtb(array_key_exists("k", $d)); }
function f_dictU4_a() { $d = []; $d["a"] = 1; $d["b"] = 2; $d["c"] = 3; return fmts(count($d)); }
function f_Fd1_a() { $d = []; $d[1] = "int"; $d["1"] = "str"; return fmts($d[1]); }
function f_Fd2_a() { $d = []; $d["z"] = 1; $d["a"] = 1; $d["m"] = 1; return implode(",", array_keys($d)); }
function f_Fd3_a() { $d = mkd(); $v = @$d["nope"]; return $v === null ? "null" : fmts($v); }
function f_Fd4_a() {
  $d = ["a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5];
  foreach ($d as $k => $v) { if ($k === "c") { unset($d[$k]); } }
  return "ok";
}
function f_Fl9d_a() { $a = mkd(); $b = $a; $b["k"] = "99"; return fmts($a["k"]); }


try { echo "dict-U1.a|" . fmts(f_dictU1_a()) . "\n"; } catch (Throwable $_e) { echo "dict-U1.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "dict-U2.a|" . fmts(f_dictU2_a()) . "\n"; } catch (Throwable $_e) { echo "dict-U2.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "dict-U3.a|" . fmts(f_dictU3_a()) . "\n"; } catch (Throwable $_e) { echo "dict-U3.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "dict-U4.a|" . fmts(f_dictU4_a()) . "\n"; } catch (Throwable $_e) { echo "dict-U4.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-d1.a|" . fmts(f_Fd1_a()) . "\n"; } catch (Throwable $_e) { echo "F-d1.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-d2.a|" . fmts(f_Fd2_a()) . "\n"; } catch (Throwable $_e) { echo "F-d2.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.a|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/missing"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// dict probe bodies - php. php's array is ONE structure serving list and dict:
// an ordered hash whose string keys that look like integers BECOME integers.
function mkd() { return ["k" => "v1"]; }
function f_dictU1_a() { $d = mkd(); return fmts($d["k"]); }
function f_dictU2_a() { $d = ["k" => "v1"]; $d["k"] = "v2"; return fmts($d["k"]); }
function f_dictU3_a() { $d = mkd(); unset($d["k"]); return fmtb(array_key_exists("k", $d)); }
function f_dictU4_a() { $d = []; $d["a"] = 1; $d["b"] = 2; $d["c"] = 3; return fmts(count($d)); }
function f_Fd1_a() { $d = []; $d[1] = "int"; $d["1"] = "str"; return fmts($d[1]); }
function f_Fd2_a() { $d = []; $d["z"] = 1; $d["a"] = 1; $d["m"] = 1; return implode(",", array_keys($d)); }
function f_Fd3_a() { $d = mkd(); $v = @$d["nope"]; return $v === null ? "null" : fmts($v); }
function f_Fd4_a() {
  $d = ["a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5];
  foreach ($d as $k => $v) { if ($k === "c") { unset($d[$k]); } }
  return "ok";
}
function f_Fl9d_a() { $a = mkd(); $b = $a; $b["k"] = "99"; return fmts($a["k"]); }


try { echo "F-d3.a|" . fmts(f_Fd3_a()) . "\n"; } catch (Throwable $_e) { echo "F-d3.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// dict probe bodies - php. php's array is ONE structure serving list and dict:
// an ordered hash whose string keys that look like integers BECOME integers.
function mkd() { return ["k" => "v1"]; }
function f_dictU1_a() { $d = mkd(); return fmts($d["k"]); }
function f_dictU2_a() { $d = ["k" => "v1"]; $d["k"] = "v2"; return fmts($d["k"]); }
function f_dictU3_a() { $d = mkd(); unset($d["k"]); return fmtb(array_key_exists("k", $d)); }
function f_dictU4_a() { $d = []; $d["a"] = 1; $d["b"] = 2; $d["c"] = 3; return fmts(count($d)); }
function f_Fd1_a() { $d = []; $d[1] = "int"; $d["1"] = "str"; return fmts($d[1]); }
function f_Fd2_a() { $d = []; $d["z"] = 1; $d["a"] = 1; $d["m"] = 1; return implode(",", array_keys($d)); }
function f_Fd3_a() { $d = mkd(); $v = @$d["nope"]; return $v === null ? "null" : fmts($v); }
function f_Fd4_a() {
  $d = ["a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5];
  foreach ($d as $k => $v) { if ($k === "c") { unset($d[$k]); } }
  return "ok";
}
function f_Fl9d_a() { $a = mkd(); $b = $a; $b["k"] = "99"; return fmts($a["k"]); }


try { echo "F-d4.a|" . fmts(f_Fd4_a()) . "\n"; } catch (Throwable $_e) { echo "F-d4.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// dict probe bodies - php. php's array is ONE structure serving list and dict:
// an ordered hash whose string keys that look like integers BECOME integers.
function mkd() { return ["k" => "v1"]; }
function f_dictU1_a() { $d = mkd(); return fmts($d["k"]); }
function f_dictU2_a() { $d = ["k" => "v1"]; $d["k"] = "v2"; return fmts($d["k"]); }
function f_dictU3_a() { $d = mkd(); unset($d["k"]); return fmtb(array_key_exists("k", $d)); }
function f_dictU4_a() { $d = []; $d["a"] = 1; $d["b"] = 2; $d["c"] = 3; return fmts(count($d)); }
function f_Fd1_a() { $d = []; $d[1] = "int"; $d["1"] = "str"; return fmts($d[1]); }
function f_Fd2_a() { $d = []; $d["z"] = 1; $d["a"] = 1; $d["m"] = 1; return implode(",", array_keys($d)); }
function f_Fd3_a() { $d = mkd(); $v = @$d["nope"]; return $v === null ? "null" : fmts($v); }
function f_Fd4_a() {
  $d = ["a" => 1, "b" => 2, "c" => 3, "d" => 4, "e" => 5];
  foreach ($d as $k => $v) { if ($k === "c") { unset($d[$k]); } }
  return "ok";
}
function f_Fl9d_a() { $a = mkd(); $b = $a; $b["k"] = "99"; return fmts($a["k"]); }


try { echo "F-l9-dict.a|" . fmts(f_Fl9d_a()) . "\n"; } catch (Throwable $_e) { echo "F-l9-dict.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "php: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- typescript
echo ""; echo "--- typescript ---"
OUT=/out/dct_typescript.txt
: > "$OUT"
if command -v tsc >/dev/null 2>&1; then
  D="$ROOT/typescript/main"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// dict probe bodies - typescript. The SPLIT PERSONALITY the census flags is
// recorded as separate probes, not separate columns: the plain object (string
// keys only, silently coerced) and Map (any key, matched by identity).
function mkd(): any { const d: any = {}; d["k"] = "v1"; return d; }
function f_dictU1_a(): string { return fmts(mkd()["k"]); }
function f_dictU2_a(): string { const d: any = {}; d["k"] = "v1"; d["k"] = "v2"; return fmts(d["k"]); }
function f_dictU3_a(): string { const d = mkd(); delete d["k"]; return fmtb("k" in d); }
function f_dictU4_a(): string { const d: any = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(Object.keys(d).length); }
function f_Fd1_a(): string { const d: any = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1]); }
function f_Fd1_d(): string { const m = new Map<any, string>(); m.set(1, "int"); m.set("1", "str"); return fmts(m.get(1)); }
function f_Fd2_a(): string { const m = new Map<string, number>(); m.set("z", 1); m.set("a", 1); m.set("m", 1); return Array.from(m.keys()).join(","); }
function f_Fd2_c(): string { const d: any = {}; d["b"] = 1; d["2"] = 1; d["a"] = 1; d["1"] = 1; return Object.keys(d).join(","); }
function f_Fd3_a(): string { return fmts(mkd()["nope"]); }
function f_Fd4_a(): string {
  const m = new Map<string, number>([["a", 1], ["b", 2], ["c", 3], ["d", 4], ["e", 5]]);
  for (const k of m.keys()) { if (k === "c") { m.delete(k); } }
  return "ok";
}
function f_Fd5_a(): string { const m = new Map<any, string>(); m.set({ x: 1 }, "p"); m.set({ x: 1 }, "q"); return fmts(m.size); }
function f_Fd5_b(): string { const m = new Map<any, string>(); m.set(NaN, "x"); return m.get(NaN) === "x" ? "yes" : "no"; }
function f_Fl9d_a(): string { const a = mkd(); const b = a; b["k"] = "99"; return fmts(a["k"]); }


try { console.log("dict-U1.a|" + fmts(f_dictU1_a())); } catch (e: any) { console.log("dict-U1.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("dict-U2.a|" + fmts(f_dictU2_a())); } catch (e: any) { console.log("dict-U2.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("dict-U3.a|" + fmts(f_dictU3_a())); } catch (e: any) { console.log("dict-U3.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("dict-U4.a|" + fmts(f_dictU4_a())); } catch (e: any) { console.log("dict-U4.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d1.a|" + fmts(f_Fd1_a())); } catch (e: any) { console.log("F-d1.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d1.d|" + fmts(f_Fd1_d())); } catch (e: any) { console.log("F-d1.d|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d2.a|" + fmts(f_Fd2_a())); } catch (e: any) { console.log("F-d2.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d2.c|" + fmts(f_Fd2_c())); } catch (e: any) { console.log("F-d2.c|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d5.a|" + fmts(f_Fd5_a())); } catch (e: any) { console.log("F-d5.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-d5.b|" + fmts(f_Fd5_b())); } catch (e: any) { console.log("F-d5.b|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.a|$VERDICT" >> "$OUT"
    echo "F-d1.d|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d2.c|$VERDICT" >> "$OUT"
    echo "F-d5.a|$VERDICT" >> "$OUT"
    echo "F-d5.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/missing"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// dict probe bodies - typescript. The SPLIT PERSONALITY the census flags is
// recorded as separate probes, not separate columns: the plain object (string
// keys only, silently coerced) and Map (any key, matched by identity).
function mkd(): any { const d: any = {}; d["k"] = "v1"; return d; }
function f_dictU1_a(): string { return fmts(mkd()["k"]); }
function f_dictU2_a(): string { const d: any = {}; d["k"] = "v1"; d["k"] = "v2"; return fmts(d["k"]); }
function f_dictU3_a(): string { const d = mkd(); delete d["k"]; return fmtb("k" in d); }
function f_dictU4_a(): string { const d: any = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(Object.keys(d).length); }
function f_Fd1_a(): string { const d: any = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1]); }
function f_Fd1_d(): string { const m = new Map<any, string>(); m.set(1, "int"); m.set("1", "str"); return fmts(m.get(1)); }
function f_Fd2_a(): string { const m = new Map<string, number>(); m.set("z", 1); m.set("a", 1); m.set("m", 1); return Array.from(m.keys()).join(","); }
function f_Fd2_c(): string { const d: any = {}; d["b"] = 1; d["2"] = 1; d["a"] = 1; d["1"] = 1; return Object.keys(d).join(","); }
function f_Fd3_a(): string { return fmts(mkd()["nope"]); }
function f_Fd4_a(): string {
  const m = new Map<string, number>([["a", 1], ["b", 2], ["c", 3], ["d", 4], ["e", 5]]);
  for (const k of m.keys()) { if (k === "c") { m.delete(k); } }
  return "ok";
}
function f_Fd5_a(): string { const m = new Map<any, string>(); m.set({ x: 1 }, "p"); m.set({ x: 1 }, "q"); return fmts(m.size); }
function f_Fd5_b(): string { const m = new Map<any, string>(); m.set(NaN, "x"); return m.get(NaN) === "x" ? "yes" : "no"; }
function f_Fl9d_a(): string { const a = mkd(); const b = a; b["k"] = "99"; return fmts(a["k"]); }


try { console.log("F-d3.a|" + fmts(f_Fd3_a())); } catch (e: any) { console.log("F-d3.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// dict probe bodies - typescript. The SPLIT PERSONALITY the census flags is
// recorded as separate probes, not separate columns: the plain object (string
// keys only, silently coerced) and Map (any key, matched by identity).
function mkd(): any { const d: any = {}; d["k"] = "v1"; return d; }
function f_dictU1_a(): string { return fmts(mkd()["k"]); }
function f_dictU2_a(): string { const d: any = {}; d["k"] = "v1"; d["k"] = "v2"; return fmts(d["k"]); }
function f_dictU3_a(): string { const d = mkd(); delete d["k"]; return fmtb("k" in d); }
function f_dictU4_a(): string { const d: any = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(Object.keys(d).length); }
function f_Fd1_a(): string { const d: any = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1]); }
function f_Fd1_d(): string { const m = new Map<any, string>(); m.set(1, "int"); m.set("1", "str"); return fmts(m.get(1)); }
function f_Fd2_a(): string { const m = new Map<string, number>(); m.set("z", 1); m.set("a", 1); m.set("m", 1); return Array.from(m.keys()).join(","); }
function f_Fd2_c(): string { const d: any = {}; d["b"] = 1; d["2"] = 1; d["a"] = 1; d["1"] = 1; return Object.keys(d).join(","); }
function f_Fd3_a(): string { return fmts(mkd()["nope"]); }
function f_Fd4_a(): string {
  const m = new Map<string, number>([["a", 1], ["b", 2], ["c", 3], ["d", 4], ["e", 5]]);
  for (const k of m.keys()) { if (k === "c") { m.delete(k); } }
  return "ok";
}
function f_Fd5_a(): string { const m = new Map<any, string>(); m.set({ x: 1 }, "p"); m.set({ x: 1 }, "q"); return fmts(m.size); }
function f_Fd5_b(): string { const m = new Map<any, string>(); m.set(NaN, "x"); return m.get(NaN) === "x" ? "yes" : "no"; }
function f_Fl9d_a(): string { const a = mkd(); const b = a; b["k"] = "99"; return fmts(a["k"]); }


try { console.log("F-d4.a|" + fmts(f_Fd4_a())); } catch (e: any) { console.log("F-d4.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// dict probe bodies - typescript. The SPLIT PERSONALITY the census flags is
// recorded as separate probes, not separate columns: the plain object (string
// keys only, silently coerced) and Map (any key, matched by identity).
function mkd(): any { const d: any = {}; d["k"] = "v1"; return d; }
function f_dictU1_a(): string { return fmts(mkd()["k"]); }
function f_dictU2_a(): string { const d: any = {}; d["k"] = "v1"; d["k"] = "v2"; return fmts(d["k"]); }
function f_dictU3_a(): string { const d = mkd(); delete d["k"]; return fmtb("k" in d); }
function f_dictU4_a(): string { const d: any = {}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return fmts(Object.keys(d).length); }
function f_Fd1_a(): string { const d: any = {}; d[1] = "int"; d["1"] = "str"; return fmts(d[1]); }
function f_Fd1_d(): string { const m = new Map<any, string>(); m.set(1, "int"); m.set("1", "str"); return fmts(m.get(1)); }
function f_Fd2_a(): string { const m = new Map<string, number>(); m.set("z", 1); m.set("a", 1); m.set("m", 1); return Array.from(m.keys()).join(","); }
function f_Fd2_c(): string { const d: any = {}; d["b"] = 1; d["2"] = 1; d["a"] = 1; d["1"] = 1; return Object.keys(d).join(","); }
function f_Fd3_a(): string { return fmts(mkd()["nope"]); }
function f_Fd4_a(): string {
  const m = new Map<string, number>([["a", 1], ["b", 2], ["c", 3], ["d", 4], ["e", 5]]);
  for (const k of m.keys()) { if (k === "c") { m.delete(k); } }
  return "ok";
}
function f_Fd5_a(): string { const m = new Map<any, string>(); m.set({ x: 1 }, "p"); m.set({ x: 1 }, "q"); return fmts(m.size); }
function f_Fd5_b(): string { const m = new Map<any, string>(); m.set(NaN, "x"); return m.get(NaN) === "x" ? "yes" : "no"; }
function f_Fl9d_a(): string { const a = mkd(); const b = a; b["k"] = "99"; return fmts(a["k"]); }


try { console.log("F-l9-dict.a|" + fmts(f_Fl9d_a())); } catch (e: any) { console.log("F-l9-dict.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "typescript: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- go
echo ""; echo "--- go ---"
OUT=/out/dct_go.txt
: > "$OUT"
if command -v go >/dev/null 2>&1; then
  D="$ROOT/go/main"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// dict probe bodies - go. Iteration order is deliberately RANDOMISED, so the
// probe iterates three times inside ONE process and asserts that the orders
// DIFFER: an anti-guarantee can only be checked by showing instability.
func mkd() map[string]string { return map[string]string{"k": "v1"} }
func f_dictU1_a() string { return mkd()["k"] }
func f_dictU2_a() string { d := map[string]string{"k": "v1"}; d["k"] = "v2"; return d["k"] }
func f_dictU3_a() string { d := mkd(); delete(d, "k"); _, ok := d["k"]; return fmtb(ok) }
func f_dictU4_a() string { d := map[string]int{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return strconv.Itoa(len(d)) }
func f_Fd1_b() string    { d := map[int]string{}; d[1] = "v"; return d[1] }
func f_Fd2_b() string {
	d := map[string]int{}
	for i := 0; i < 10; i++ {
		d["k"+strconv.Itoa(i)] = i
	}
	seen := []string{}
	for pass := 0; pass < 3; pass++ {
		order := ""
		for k := range d {
			order += k + ","
		}
		seen = append(seen, order)
	}
	if seen[0] == seen[1] && seen[1] == seen[2] {
		return "stable"
	}
	return "unstable"
}
func f_Fd3_a() string { d := mkd(); v, ok := d["nope"]; return "\"" + v + "\"/" + fmtb(ok) }
func f_Fd4_a() string {
	d := map[string]int{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
	for k := range d {
		if k == "c" {
			delete(d, k)
		}
	}
	return "ok"
}
func f_Fl9d_a() string { a := mkd(); b := a; b["k"] = "99"; return a["k"] }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("dict-U1.a|<raise:%v>\n", r) } }(); fmt.Println("dict-U1.a|" + fmts(f_dictU1_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("dict-U2.a|<raise:%v>\n", r) } }(); fmt.Println("dict-U2.a|" + fmts(f_dictU2_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("dict-U3.a|<raise:%v>\n", r) } }(); fmt.Println("dict-U3.a|" + fmts(f_dictU3_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("dict-U4.a|<raise:%v>\n", r) } }(); fmt.Println("dict-U4.a|" + fmts(f_dictU4_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-d1.b|<raise:%v>\n", r) } }(); fmt.Println("F-d1.b|" + fmts(f_Fd1_b())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-d2.b|<raise:%v>\n", r) } }(); fmt.Println("F-d2.b|" + fmts(f_Fd2_b())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/missing"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// dict probe bodies - go. Iteration order is deliberately RANDOMISED, so the
// probe iterates three times inside ONE process and asserts that the orders
// DIFFER: an anti-guarantee can only be checked by showing instability.
func mkd() map[string]string { return map[string]string{"k": "v1"} }
func f_dictU1_a() string { return mkd()["k"] }
func f_dictU2_a() string { d := map[string]string{"k": "v1"}; d["k"] = "v2"; return d["k"] }
func f_dictU3_a() string { d := mkd(); delete(d, "k"); _, ok := d["k"]; return fmtb(ok) }
func f_dictU4_a() string { d := map[string]int{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return strconv.Itoa(len(d)) }
func f_Fd1_b() string    { d := map[int]string{}; d[1] = "v"; return d[1] }
func f_Fd2_b() string {
	d := map[string]int{}
	for i := 0; i < 10; i++ {
		d["k"+strconv.Itoa(i)] = i
	}
	seen := []string{}
	for pass := 0; pass < 3; pass++ {
		order := ""
		for k := range d {
			order += k + ","
		}
		seen = append(seen, order)
	}
	if seen[0] == seen[1] && seen[1] == seen[2] {
		return "stable"
	}
	return "unstable"
}
func f_Fd3_a() string { d := mkd(); v, ok := d["nope"]; return "\"" + v + "\"/" + fmtb(ok) }
func f_Fd4_a() string {
	d := map[string]int{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
	for k := range d {
		if k == "c" {
			delete(d, k)
		}
	}
	return "ok"
}
func f_Fl9d_a() string { a := mkd(); b := a; b["k"] = "99"; return a["k"] }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-d3.a|<raise:%v>\n", r) } }(); fmt.Println("F-d3.a|" + fmts(f_Fd3_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// dict probe bodies - go. Iteration order is deliberately RANDOMISED, so the
// probe iterates three times inside ONE process and asserts that the orders
// DIFFER: an anti-guarantee can only be checked by showing instability.
func mkd() map[string]string { return map[string]string{"k": "v1"} }
func f_dictU1_a() string { return mkd()["k"] }
func f_dictU2_a() string { d := map[string]string{"k": "v1"}; d["k"] = "v2"; return d["k"] }
func f_dictU3_a() string { d := mkd(); delete(d, "k"); _, ok := d["k"]; return fmtb(ok) }
func f_dictU4_a() string { d := map[string]int{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return strconv.Itoa(len(d)) }
func f_Fd1_b() string    { d := map[int]string{}; d[1] = "v"; return d[1] }
func f_Fd2_b() string {
	d := map[string]int{}
	for i := 0; i < 10; i++ {
		d["k"+strconv.Itoa(i)] = i
	}
	seen := []string{}
	for pass := 0; pass < 3; pass++ {
		order := ""
		for k := range d {
			order += k + ","
		}
		seen = append(seen, order)
	}
	if seen[0] == seen[1] && seen[1] == seen[2] {
		return "stable"
	}
	return "unstable"
}
func f_Fd3_a() string { d := mkd(); v, ok := d["nope"]; return "\"" + v + "\"/" + fmtb(ok) }
func f_Fd4_a() string {
	d := map[string]int{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
	for k := range d {
		if k == "c" {
			delete(d, k)
		}
	}
	return "ok"
}
func f_Fl9d_a() string { a := mkd(); b := a; b["k"] = "99"; return a["k"] }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-d4.a|<raise:%v>\n", r) } }(); fmt.Println("F-d4.a|" + fmts(f_Fd4_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// dict probe bodies - go. Iteration order is deliberately RANDOMISED, so the
// probe iterates three times inside ONE process and asserts that the orders
// DIFFER: an anti-guarantee can only be checked by showing instability.
func mkd() map[string]string { return map[string]string{"k": "v1"} }
func f_dictU1_a() string { return mkd()["k"] }
func f_dictU2_a() string { d := map[string]string{"k": "v1"}; d["k"] = "v2"; return d["k"] }
func f_dictU3_a() string { d := mkd(); delete(d, "k"); _, ok := d["k"]; return fmtb(ok) }
func f_dictU4_a() string { d := map[string]int{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return strconv.Itoa(len(d)) }
func f_Fd1_b() string    { d := map[int]string{}; d[1] = "v"; return d[1] }
func f_Fd2_b() string {
	d := map[string]int{}
	for i := 0; i < 10; i++ {
		d["k"+strconv.Itoa(i)] = i
	}
	seen := []string{}
	for pass := 0; pass < 3; pass++ {
		order := ""
		for k := range d {
			order += k + ","
		}
		seen = append(seen, order)
	}
	if seen[0] == seen[1] && seen[1] == seen[2] {
		return "stable"
	}
	return "unstable"
}
func f_Fd3_a() string { d := mkd(); v, ok := d["nope"]; return "\"" + v + "\"/" + fmtb(ok) }
func f_Fd4_a() string {
	d := map[string]int{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5}
	for k := range d {
		if k == "c" {
			delete(d, k)
		}
	}
	return "ok"
}
func f_Fl9d_a() string { a := mkd(); b := a; b["k"] = "99"; return a["k"] }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l9-dict.a|<raise:%v>\n", r) } }(); fmt.Println("F-l9-dict.a|" + fmts(f_Fl9d_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "go: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- cpp
echo ""; echo "--- cpp ---"
OUT=/out/dct_cpp.txt
: > "$OUT"
if command -v g++ >/dev/null 2>&1; then
  D="$ROOT/cpp/main"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// dict probe bodies - c++ (std::map / unordered_map). operator[] on a MISSING
// key INSERTS a default and answers it: probe F-d3.a reports the value AND the
// size afterwards, because the size is where the insertion shows.
#include <map>
#include <unordered_map>
static std::map<std::string, std::string> mkd() {
  std::map<std::string, std::string> d; d["k"] = "v1"; return d;
}
static std::string f_dictU1_a() { auto d = mkd(); return d["k"]; }
static std::string f_dictU2_a() { std::map<std::string, std::string> d; d["k"] = "v1"; d["k"] = "v2"; return d["k"]; }
static std::string f_dictU3_a() { auto d = mkd(); d.erase("k"); return fmtb(d.count("k") > 0); }
static std::string f_dictU4_a() { std::map<std::string, int> d; d["a"] = 1; d["b"] = 2; d["c"] = 3; return std::to_string(d.size()); }
static std::string f_Fd1_b() { std::map<int, std::string> d; d[1] = "v"; return d[1]; }
static std::string f_Fd2_a() {
  std::unordered_map<std::string, int> d; d["z"] = 1; d["a"] = 1; d["m"] = 1;
  std::string out; for (auto& kv : d) { if (!out.empty()) out += ","; out += kv.first; }
  return out;
}
static std::string f_Fd3_a() { auto d = mkd(); std::string v = d["nope"]; return "\"" + v + "\"/" + std::to_string(d.size()); }
static std::string f_Fd3_b() { auto d = mkd(); return d.at("nope"); }
static std::string f_Fd4_a() {
  std::map<std::string, int> d{{"a",1},{"b",2},{"c",3},{"d",4},{"e",5}};
  for (auto it = d.begin(); it != d.end(); ++it) { if (it->first == "c") { d.erase(it); } }
  return "ok";
}
static std::string f_Fl9d_a() { auto a = mkd(); auto b = a; b["k"] = "99"; return a["k"]; }


int main() {
  { try { std::string _v = fmts(f_dictU1_a()); std::cout << "dict-U1.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "dict-U1.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_dictU2_a()); std::cout << "dict-U2.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "dict-U2.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_dictU3_a()); std::cout << "dict-U3.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "dict-U3.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_dictU4_a()); std::cout << "dict-U4.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "dict-U4.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fd1_b()); std::cout << "F-d1.b|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-d1.b|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fd2_a()); std::cout << "F-d2.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-d2.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fd3_b()); std::cout << "F-d3.b|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-d3.b|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d3.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/missing"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// dict probe bodies - c++ (std::map / unordered_map). operator[] on a MISSING
// key INSERTS a default and answers it: probe F-d3.a reports the value AND the
// size afterwards, because the size is where the insertion shows.
#include <map>
#include <unordered_map>
static std::map<std::string, std::string> mkd() {
  std::map<std::string, std::string> d; d["k"] = "v1"; return d;
}
static std::string f_dictU1_a() { auto d = mkd(); return d["k"]; }
static std::string f_dictU2_a() { std::map<std::string, std::string> d; d["k"] = "v1"; d["k"] = "v2"; return d["k"]; }
static std::string f_dictU3_a() { auto d = mkd(); d.erase("k"); return fmtb(d.count("k") > 0); }
static std::string f_dictU4_a() { std::map<std::string, int> d; d["a"] = 1; d["b"] = 2; d["c"] = 3; return std::to_string(d.size()); }
static std::string f_Fd1_b() { std::map<int, std::string> d; d[1] = "v"; return d[1]; }
static std::string f_Fd2_a() {
  std::unordered_map<std::string, int> d; d["z"] = 1; d["a"] = 1; d["m"] = 1;
  std::string out; for (auto& kv : d) { if (!out.empty()) out += ","; out += kv.first; }
  return out;
}
static std::string f_Fd3_a() { auto d = mkd(); std::string v = d["nope"]; return "\"" + v + "\"/" + std::to_string(d.size()); }
static std::string f_Fd3_b() { auto d = mkd(); return d.at("nope"); }
static std::string f_Fd4_a() {
  std::map<std::string, int> d{{"a",1},{"b",2},{"c",3},{"d",4},{"e",5}};
  for (auto it = d.begin(); it != d.end(); ++it) { if (it->first == "c") { d.erase(it); } }
  return "ok";
}
static std::string f_Fl9d_a() { auto a = mkd(); auto b = a; b["k"] = "99"; return a["k"]; }


int main() {
  { try { std::string _v = fmts(f_Fd3_a()); std::cout << "F-d3.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-d3.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// dict probe bodies - c++ (std::map / unordered_map). operator[] on a MISSING
// key INSERTS a default and answers it: probe F-d3.a reports the value AND the
// size afterwards, because the size is where the insertion shows.
#include <map>
#include <unordered_map>
static std::map<std::string, std::string> mkd() {
  std::map<std::string, std::string> d; d["k"] = "v1"; return d;
}
static std::string f_dictU1_a() { auto d = mkd(); return d["k"]; }
static std::string f_dictU2_a() { std::map<std::string, std::string> d; d["k"] = "v1"; d["k"] = "v2"; return d["k"]; }
static std::string f_dictU3_a() { auto d = mkd(); d.erase("k"); return fmtb(d.count("k") > 0); }
static std::string f_dictU4_a() { std::map<std::string, int> d; d["a"] = 1; d["b"] = 2; d["c"] = 3; return std::to_string(d.size()); }
static std::string f_Fd1_b() { std::map<int, std::string> d; d[1] = "v"; return d[1]; }
static std::string f_Fd2_a() {
  std::unordered_map<std::string, int> d; d["z"] = 1; d["a"] = 1; d["m"] = 1;
  std::string out; for (auto& kv : d) { if (!out.empty()) out += ","; out += kv.first; }
  return out;
}
static std::string f_Fd3_a() { auto d = mkd(); std::string v = d["nope"]; return "\"" + v + "\"/" + std::to_string(d.size()); }
static std::string f_Fd3_b() { auto d = mkd(); return d.at("nope"); }
static std::string f_Fd4_a() {
  std::map<std::string, int> d{{"a",1},{"b",2},{"c",3},{"d",4},{"e",5}};
  for (auto it = d.begin(); it != d.end(); ++it) { if (it->first == "c") { d.erase(it); } }
  return "ok";
}
static std::string f_Fl9d_a() { auto a = mkd(); auto b = a; b["k"] = "99"; return a["k"]; }


int main() {
  { try { std::string _v = fmts(f_Fd4_a()); std::cout << "F-d4.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-d4.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// dict probe bodies - c++ (std::map / unordered_map). operator[] on a MISSING
// key INSERTS a default and answers it: probe F-d3.a reports the value AND the
// size afterwards, because the size is where the insertion shows.
#include <map>
#include <unordered_map>
static std::map<std::string, std::string> mkd() {
  std::map<std::string, std::string> d; d["k"] = "v1"; return d;
}
static std::string f_dictU1_a() { auto d = mkd(); return d["k"]; }
static std::string f_dictU2_a() { std::map<std::string, std::string> d; d["k"] = "v1"; d["k"] = "v2"; return d["k"]; }
static std::string f_dictU3_a() { auto d = mkd(); d.erase("k"); return fmtb(d.count("k") > 0); }
static std::string f_dictU4_a() { std::map<std::string, int> d; d["a"] = 1; d["b"] = 2; d["c"] = 3; return std::to_string(d.size()); }
static std::string f_Fd1_b() { std::map<int, std::string> d; d[1] = "v"; return d[1]; }
static std::string f_Fd2_a() {
  std::unordered_map<std::string, int> d; d["z"] = 1; d["a"] = 1; d["m"] = 1;
  std::string out; for (auto& kv : d) { if (!out.empty()) out += ","; out += kv.first; }
  return out;
}
static std::string f_Fd3_a() { auto d = mkd(); std::string v = d["nope"]; return "\"" + v + "\"/" + std::to_string(d.size()); }
static std::string f_Fd3_b() { auto d = mkd(); return d.at("nope"); }
static std::string f_Fd4_a() {
  std::map<std::string, int> d{{"a",1},{"b",2},{"c",3},{"d",4},{"e",5}};
  for (auto it = d.begin(); it != d.end(); ++it) { if (it->first == "c") { d.erase(it); } }
  return "ok";
}
static std::string f_Fl9d_a() { auto a = mkd(); auto b = a; b["k"] = "99"; return a["k"]; }


int main() {
  { try { std::string _v = fmts(f_Fl9d_a()); std::cout << "F-l9-dict.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l9-dict.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "cpp: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- java
echo ""; echo "--- java ---"
OUT=/out/dct_java.txt
: > "$OUT"
if command -v javac >/dev/null 2>&1; then
  D="$ROOT/java/main"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // dict probe bodies - java (HashMap: order NOT promised; null on a miss).
  static java.util.Map<String, String> mkd() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); return d;
  }
  static String f_dictU1_a() { return mkd().get("k"); }
  static String f_dictU2_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); d.put("k", "v2"); return d.get("k");
  }
  static String f_dictU3_a() { java.util.Map<String, String> d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
  static String f_dictU4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a", 1); d.put("b", 2); d.put("c", 3); return Integer.toString(d.size());
  }
  static String f_Fd1_b() {
    java.util.Map<Integer, String> d = new java.util.HashMap<Integer, String>();
    d.put(1, "v"); return d.get(1);
  }
  static String f_Fd2_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("z", 1); d.put("a", 1); d.put("m", 1); return String.join(",", d.keySet());
  }
  static String f_Fd3_a() { return fmts(String.valueOf(mkd().get("nope"))); }
  static String f_Fd4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a",1); d.put("b",2); d.put("c",3); d.put("d",4); d.put("e",5);
    for (String k : d.keySet()) { if (k.equals("c")) { d.remove(k); } }
    return "ok";
  }
  static String f_Fd5_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put(new String("k"), "p"); d.put(new String("k"), "q"); return Integer.toString(d.size());
  }
  static String f_Fl9d_a() {
    java.util.Map<String, String> a = mkd(); java.util.Map<String, String> b = a;
    b.put("k", "99"); return a.get("k");
  }


  public static void main(String[] args) {
    try { System.out.println("dict-U1.a|" + fmts(f_dictU1_a())); } catch (Throwable _e) { System.out.println("dict-U1.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("dict-U2.a|" + fmts(f_dictU2_a())); } catch (Throwable _e) { System.out.println("dict-U2.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("dict-U3.a|" + fmts(f_dictU3_a())); } catch (Throwable _e) { System.out.println("dict-U3.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("dict-U4.a|" + fmts(f_dictU4_a())); } catch (Throwable _e) { System.out.println("dict-U4.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-d1.b|" + fmts(f_Fd1_b())); } catch (Throwable _e) { System.out.println("F-d1.b|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-d2.a|" + fmts(f_Fd2_a())); } catch (Throwable _e) { System.out.println("F-d2.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-d5.a|" + fmts(f_Fd5_a())); } catch (Throwable _e) { System.out.println("F-d5.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/missing"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // dict probe bodies - java (HashMap: order NOT promised; null on a miss).
  static java.util.Map<String, String> mkd() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); return d;
  }
  static String f_dictU1_a() { return mkd().get("k"); }
  static String f_dictU2_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); d.put("k", "v2"); return d.get("k");
  }
  static String f_dictU3_a() { java.util.Map<String, String> d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
  static String f_dictU4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a", 1); d.put("b", 2); d.put("c", 3); return Integer.toString(d.size());
  }
  static String f_Fd1_b() {
    java.util.Map<Integer, String> d = new java.util.HashMap<Integer, String>();
    d.put(1, "v"); return d.get(1);
  }
  static String f_Fd2_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("z", 1); d.put("a", 1); d.put("m", 1); return String.join(",", d.keySet());
  }
  static String f_Fd3_a() { return fmts(String.valueOf(mkd().get("nope"))); }
  static String f_Fd4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a",1); d.put("b",2); d.put("c",3); d.put("d",4); d.put("e",5);
    for (String k : d.keySet()) { if (k.equals("c")) { d.remove(k); } }
    return "ok";
  }
  static String f_Fd5_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put(new String("k"), "p"); d.put(new String("k"), "q"); return Integer.toString(d.size());
  }
  static String f_Fl9d_a() {
    java.util.Map<String, String> a = mkd(); java.util.Map<String, String> b = a;
    b.put("k", "99"); return a.get("k");
  }


  public static void main(String[] args) {
    try { System.out.println("F-d3.a|" + fmts(f_Fd3_a())); } catch (Throwable _e) { System.out.println("F-d3.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/deliter"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // dict probe bodies - java (HashMap: order NOT promised; null on a miss).
  static java.util.Map<String, String> mkd() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); return d;
  }
  static String f_dictU1_a() { return mkd().get("k"); }
  static String f_dictU2_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); d.put("k", "v2"); return d.get("k");
  }
  static String f_dictU3_a() { java.util.Map<String, String> d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
  static String f_dictU4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a", 1); d.put("b", 2); d.put("c", 3); return Integer.toString(d.size());
  }
  static String f_Fd1_b() {
    java.util.Map<Integer, String> d = new java.util.HashMap<Integer, String>();
    d.put(1, "v"); return d.get(1);
  }
  static String f_Fd2_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("z", 1); d.put("a", 1); d.put("m", 1); return String.join(",", d.keySet());
  }
  static String f_Fd3_a() { return fmts(String.valueOf(mkd().get("nope"))); }
  static String f_Fd4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a",1); d.put("b",2); d.put("c",3); d.put("d",4); d.put("e",5);
    for (String k : d.keySet()) { if (k.equals("c")) { d.remove(k); } }
    return "ok";
  }
  static String f_Fd5_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put(new String("k"), "p"); d.put(new String("k"), "q"); return Integer.toString(d.size());
  }
  static String f_Fl9d_a() {
    java.util.Map<String, String> a = mkd(); java.util.Map<String, String> b = a;
    b.put("k", "99"); return a.get("k");
  }


  public static void main(String[] args) {
    try { System.out.println("F-d4.a|" + fmts(f_Fd4_a())); } catch (Throwable _e) { System.out.println("F-d4.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/dassign"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // dict probe bodies - java (HashMap: order NOT promised; null on a miss).
  static java.util.Map<String, String> mkd() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); return d;
  }
  static String f_dictU1_a() { return mkd().get("k"); }
  static String f_dictU2_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put("k", "v1"); d.put("k", "v2"); return d.get("k");
  }
  static String f_dictU3_a() { java.util.Map<String, String> d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
  static String f_dictU4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a", 1); d.put("b", 2); d.put("c", 3); return Integer.toString(d.size());
  }
  static String f_Fd1_b() {
    java.util.Map<Integer, String> d = new java.util.HashMap<Integer, String>();
    d.put(1, "v"); return d.get(1);
  }
  static String f_Fd2_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("z", 1); d.put("a", 1); d.put("m", 1); return String.join(",", d.keySet());
  }
  static String f_Fd3_a() { return fmts(String.valueOf(mkd().get("nope"))); }
  static String f_Fd4_a() {
    java.util.Map<String, Integer> d = new java.util.HashMap<String, Integer>();
    d.put("a",1); d.put("b",2); d.put("c",3); d.put("d",4); d.put("e",5);
    for (String k : d.keySet()) { if (k.equals("c")) { d.remove(k); } }
    return "ok";
  }
  static String f_Fd5_a() {
    java.util.Map<String, String> d = new java.util.HashMap<String, String>();
    d.put(new String("k"), "p"); d.put(new String("k"), "q"); return Integer.toString(d.size());
  }
  static String f_Fl9d_a() {
    java.util.Map<String, String> a = mkd(); java.util.Map<String, String> b = a;
    b.put("k", "99"); return a.get("k");
  }


  public static void main(String[] args) {
    try { System.out.println("F-l9-dict.a|" + fmts(f_Fl9d_a())); } catch (Throwable _e) { System.out.println("F-l9-dict.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "java: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- rust
echo ""; echo "--- rust ---"
OUT=/out/dct_rust.txt
: > "$OUT"
if command -v rustc >/dev/null 2>&1; then
  D="$ROOT/rust/main"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU1_a()) }); match _r { Ok(_v) => println!("dict-U1.a|{}", _v), Err(_) => println!("dict-U1.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU2_a()) }); match _r { Ok(_v) => println!("dict-U2.a|{}", _v), Err(_) => println!("dict-U2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU3_a()) }); match _r { Ok(_v) => println!("dict-U3.a|{}", _v), Err(_) => println!("dict-U3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU4_a()) }); match _r { Ok(_v) => println!("dict-U4.a|{}", _v), Err(_) => println!("dict-U4.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd1_b()) }); match _r { Ok(_v) => println!("F-d1.b|{}", _v), Err(_) => println!("F-d1.b|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd2_a()) }); match _r { Ok(_v) => println!("F-d2.a|{}", _v), Err(_) => println!("F-d2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd3_c()) }); match _r { Ok(_v) => println!("F-d3.c|{}", _v), Err(_) => println!("F-d3.c|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d3.c|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/missing"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd3_a()) }); match _r { Ok(_v) => println!("F-d3.a|{}", _v), Err(_) => println!("F-d3.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }
// The borrow checker REFUSES deletion during iteration, at build time. That is
// rust's whole answer to F-d4, and it costs nothing at run time.
fn f_Fd4_a() -> String {
    let mut d = HashMap::new();
    for (i, k) in ["a", "b", "c", "d", "e"].iter().enumerate() { d.insert(k.to_string(), i); }
    for k in d.keys() { if k == "c" { d.remove(k); } }
    "ok".to_string()
}


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd4_a()) }); match _r { Ok(_v) => println!("F-d4.a|{}", _v), Err(_) => println!("F-d4.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }
// `let b = a` MOVES the map; a is no longer usable. The refusal IS the
// guarantee -- F-l9's third camp, inherited by dict unchanged.
fn f_Fl9d_a() -> String { let a = mkd(); let mut b = a; b.insert("k".to_string(), "99".to_string()); a["k"].clone() }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl9d_a()) }); match _r { Ok(_v) => println!("F-l9-dict.a|{}", _v), Err(_) => println!("F-l9-dict.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "rust: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- rust-release
echo ""; echo "--- rust-release ---"
OUT=/out/dct_rust-release.txt
: > "$OUT"
if command -v rustc >/dev/null 2>&1; then
  D="$ROOT/rust-release/main"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU1_a()) }); match _r { Ok(_v) => println!("dict-U1.a|{}", _v), Err(_) => println!("dict-U1.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU2_a()) }); match _r { Ok(_v) => println!("dict-U2.a|{}", _v), Err(_) => println!("dict-U2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU3_a()) }); match _r { Ok(_v) => println!("dict-U3.a|{}", _v), Err(_) => println!("dict-U3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_dictU4_a()) }); match _r { Ok(_v) => println!("dict-U4.a|{}", _v), Err(_) => println!("dict-U4.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd1_b()) }); match _r { Ok(_v) => println!("F-d1.b|{}", _v), Err(_) => println!("F-d1.b|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd2_a()) }); match _r { Ok(_v) => println!("F-d2.a|{}", _v), Err(_) => println!("F-d2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd3_c()) }); match _r { Ok(_v) => println!("F-d3.c|{}", _v), Err(_) => println!("F-d3.c|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
    echo "F-d3.c|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/missing"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd3_a()) }); match _r { Ok(_v) => println!("F-d3.a|{}", _v), Err(_) => println!("F-d3.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }
// The borrow checker REFUSES deletion during iteration, at build time. That is
// rust's whole answer to F-d4, and it costs nothing at run time.
fn f_Fd4_a() -> String {
    let mut d = HashMap::new();
    for (i, k) in ["a", "b", "c", "d", "e"].iter().enumerate() { d.insert(k.to_string(), i); }
    for k in d.keys() { if k == "c" { d.remove(k); } }
    "ok".to_string()
}


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fd4_a()) }); match _r { Ok(_v) => println!("F-d4.a|{}", _v), Err(_) => println!("F-d4.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// dict probe bodies - rust (HashMap: order explicitly NOT promised; indexing
// panics on a missing key while .get answers None -- two spellings, two
// guarantees).
use std::collections::HashMap;
fn mkd() -> HashMap<String, String> {
    let mut d = HashMap::new(); d.insert("k".to_string(), "v1".to_string()); d
}
fn f_dictU1_a() -> String { mkd()["k"].clone() }
fn f_dictU2_a() -> String {
    let mut d = HashMap::new(); d.insert("k", "v1"); d.insert("k", "v2"); d["k"].to_string()
}
fn f_dictU3_a() -> String { let mut d = mkd(); d.remove("k"); fmtb(d.contains_key("k")) }
fn f_dictU4_a() -> String {
    let mut d = HashMap::new(); d.insert("a", 1); d.insert("b", 2); d.insert("c", 3);
    format!("{}", d.len())
}
fn f_Fd1_b() -> String { let mut d = HashMap::new(); d.insert(1, "v"); d[&1].to_string() }
fn f_Fd2_a() -> String {
    let mut d = HashMap::new(); d.insert("z", 1); d.insert("a", 1); d.insert("m", 1);
    d.keys().map(|k| k.to_string()).collect::<Vec<String>>().join(",")
}
fn f_Fd3_a() -> String { let d = mkd(); d[&"nope".to_string()].clone() }
fn f_Fd3_c() -> String { let d = mkd(); format!("{:?}", d.get(&"nope".to_string())) }
// `let b = a` MOVES the map; a is no longer usable. The refusal IS the
// guarantee -- F-l9's third camp, inherited by dict unchanged.
fn f_Fl9d_a() -> String { let a = mkd(); let mut b = a; b.insert("k".to_string(), "99".to_string()); a["k"].clone() }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl9d_a()) }); match _r { Ok(_v) => println!("F-l9-dict.a|{}", _v), Err(_) => println!("F-l9-dict.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "rust-release: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- kotlin
echo ""; echo "--- kotlin ---"
OUT=/out/dct_kotlin.txt
: > "$OUT"
if test -x /persist/kotlinc/bin/kotlinc >/dev/null 2>&1; then
  D="$ROOT/kotlin/main"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// dict probe bodies - kotlin (LinkedHashMap by default: insertion-ordered).
fun mkd(): MutableMap<String, String> = mutableMapOf("k" to "v1")
fun f_dictU1_a(): String = mkd()["k"].toString()
fun f_dictU2_a(): String { val d = mutableMapOf("k" to "v1"); d["k"] = "v2"; return d["k"].toString() }
fun f_dictU3_a(): String { val d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")) }
fun f_dictU4_a(): String { val d = mutableMapOf<String, Int>(); d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.size.toString() }
fun f_Fd1_b(): String { val d = mutableMapOf<Int, String>(); d[1] = "v"; return d[1].toString() }
fun f_Fd2_a(): String { val d = mutableMapOf<String, Int>(); d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.joinToString(",") }
fun f_Fd3_a(): String = mkd()["nope"].toString()
fun f_Fd4_a(): String {
    val d = mutableMapOf("a" to 1, "b" to 2, "c" to 3, "d" to 4, "e" to 5)
    for (k in d.keys) { if (k == "c") { d.remove(k) } }
    return "ok"
}
fun f_Fl9d_a(): String { val a = mkd(); val b = a; b["k"] = "99"; return a["k"].toString() }


fun main() {
    try { println("dict-U1.a|" + fmts(f_dictU1_a())) } catch (_e: Throwable) { println("dict-U1.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("dict-U2.a|" + fmts(f_dictU2_a())) } catch (_e: Throwable) { println("dict-U2.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("dict-U3.a|" + fmts(f_dictU3_a())) } catch (_e: Throwable) { println("dict-U3.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("dict-U4.a|" + fmts(f_dictU4_a())) } catch (_e: Throwable) { println("dict-U4.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("F-d1.b|" + fmts(f_Fd1_b())) } catch (_e: Throwable) { println("F-d1.b|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("F-d2.a|" + fmts(f_Fd2_a())) } catch (_e: Throwable) { println("F-d2.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/missing"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// dict probe bodies - kotlin (LinkedHashMap by default: insertion-ordered).
fun mkd(): MutableMap<String, String> = mutableMapOf("k" to "v1")
fun f_dictU1_a(): String = mkd()["k"].toString()
fun f_dictU2_a(): String { val d = mutableMapOf("k" to "v1"); d["k"] = "v2"; return d["k"].toString() }
fun f_dictU3_a(): String { val d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")) }
fun f_dictU4_a(): String { val d = mutableMapOf<String, Int>(); d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.size.toString() }
fun f_Fd1_b(): String { val d = mutableMapOf<Int, String>(); d[1] = "v"; return d[1].toString() }
fun f_Fd2_a(): String { val d = mutableMapOf<String, Int>(); d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.joinToString(",") }
fun f_Fd3_a(): String = mkd()["nope"].toString()
fun f_Fd4_a(): String {
    val d = mutableMapOf("a" to 1, "b" to 2, "c" to 3, "d" to 4, "e" to 5)
    for (k in d.keys) { if (k == "c") { d.remove(k) } }
    return "ok"
}
fun f_Fl9d_a(): String { val a = mkd(); val b = a; b["k"] = "99"; return a["k"].toString() }


fun main() {
    try { println("F-d3.a|" + fmts(f_Fd3_a())) } catch (_e: Throwable) { println("F-d3.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// dict probe bodies - kotlin (LinkedHashMap by default: insertion-ordered).
fun mkd(): MutableMap<String, String> = mutableMapOf("k" to "v1")
fun f_dictU1_a(): String = mkd()["k"].toString()
fun f_dictU2_a(): String { val d = mutableMapOf("k" to "v1"); d["k"] = "v2"; return d["k"].toString() }
fun f_dictU3_a(): String { val d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")) }
fun f_dictU4_a(): String { val d = mutableMapOf<String, Int>(); d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.size.toString() }
fun f_Fd1_b(): String { val d = mutableMapOf<Int, String>(); d[1] = "v"; return d[1].toString() }
fun f_Fd2_a(): String { val d = mutableMapOf<String, Int>(); d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.joinToString(",") }
fun f_Fd3_a(): String = mkd()["nope"].toString()
fun f_Fd4_a(): String {
    val d = mutableMapOf("a" to 1, "b" to 2, "c" to 3, "d" to 4, "e" to 5)
    for (k in d.keys) { if (k == "c") { d.remove(k) } }
    return "ok"
}
fun f_Fl9d_a(): String { val a = mkd(); val b = a; b["k"] = "99"; return a["k"].toString() }


fun main() {
    try { println("F-d4.a|" + fmts(f_Fd4_a())) } catch (_e: Throwable) { println("F-d4.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// dict probe bodies - kotlin (LinkedHashMap by default: insertion-ordered).
fun mkd(): MutableMap<String, String> = mutableMapOf("k" to "v1")
fun f_dictU1_a(): String = mkd()["k"].toString()
fun f_dictU2_a(): String { val d = mutableMapOf("k" to "v1"); d["k"] = "v2"; return d["k"].toString() }
fun f_dictU3_a(): String { val d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")) }
fun f_dictU4_a(): String { val d = mutableMapOf<String, Int>(); d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.size.toString() }
fun f_Fd1_b(): String { val d = mutableMapOf<Int, String>(); d[1] = "v"; return d[1].toString() }
fun f_Fd2_a(): String { val d = mutableMapOf<String, Int>(); d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.joinToString(",") }
fun f_Fd3_a(): String = mkd()["nope"].toString()
fun f_Fd4_a(): String {
    val d = mutableMapOf("a" to 1, "b" to 2, "c" to 3, "d" to 4, "e" to 5)
    for (k in d.keys) { if (k == "c") { d.remove(k) } }
    return "ok"
}
fun f_Fl9d_a(): String { val a = mkd(); val b = a; b["k"] = "99"; return a["k"].toString() }


fun main() {
    try { println("F-l9-dict.a|" + fmts(f_Fl9d_a())) } catch (_e: Throwable) { println("F-l9-dict.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "kotlin: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- dart
echo ""; echo "--- dart ---"
OUT=/out/dct_dart.txt
: > "$OUT"
if test -x /persist/dart-sdk/bin/dart >/dev/null 2>&1; then
  D="$ROOT/dart/main"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("dict-U1.a|" + fmts(f_dictU1_a())); } catch (_e) { print("dict-U1.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U2.a|" + fmts(f_dictU2_a())); } catch (_e) { print("dict-U2.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U3.a|" + fmts(f_dictU3_a())); } catch (_e) { print("dict-U3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U4.a|" + fmts(f_dictU4_a())); } catch (_e) { print("dict-U4.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-d1.b|" + fmts(f_Fd1_b())); } catch (_e) { print("F-d1.b|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-d2.a|" + fmts(f_Fd2_a())); } catch (_e) { print("F-d2.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/missing"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-d3.a|" + fmts(f_Fd3_a())); } catch (_e) { print("F-d3.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-d4.a|" + fmts(f_Fd4_a())); } catch (_e) { print("F-d4.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-l9-dict.a|" + fmts(f_Fl9d_a())); } catch (_e) { print("F-l9-dict.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "dart: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- dart-web
echo ""; echo "--- dart-web ---"
OUT=/out/dct_dart-web.txt
: > "$OUT"
if test -x /persist/dart-sdk/bin/dart >/dev/null 2>&1; then
  D="$ROOT/dart-web/main"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("dict-U1.a|" + fmts(f_dictU1_a())); } catch (_e) { print("dict-U1.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U2.a|" + fmts(f_dictU2_a())); } catch (_e) { print("dict-U2.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U3.a|" + fmts(f_dictU3_a())); } catch (_e) { print("dict-U3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("dict-U4.a|" + fmts(f_dictU4_a())); } catch (_e) { print("dict-U4.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-d1.b|" + fmts(f_Fd1_b())); } catch (_e) { print("F-d1.b|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-d2.a|" + fmts(f_Fd2_a())); } catch (_e) { print("F-d2.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "dict-U1.a|$VERDICT" >> "$OUT"
    echo "dict-U2.a|$VERDICT" >> "$OUT"
    echo "dict-U3.a|$VERDICT" >> "$OUT"
    echo "dict-U4.a|$VERDICT" >> "$OUT"
    echo "F-d1.b|$VERDICT" >> "$OUT"
    echo "F-d2.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/missing"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-d3.a|" + fmts(f_Fd3_a())); } catch (_e) { print("F-d3.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web missing: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web missing: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web missing: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d3.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/deliter"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-d4.a|" + fmts(f_Fd4_a())); } catch (_e) { print("F-d4.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web deliter: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web deliter: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web deliter: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-d4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/dassign"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// dict probe bodies - dart (Map: insertion-ordered by default; null on a miss).
Map<String, String> mkd() => <String, String>{"k": "v1"};
String f_dictU1_a() => mkd()["k"].toString();
String f_dictU2_a() { final d = <String, String>{"k": "v1"}; d["k"] = "v2"; return d["k"].toString(); }
String f_dictU3_a() { final d = mkd(); d.remove("k"); return fmtb(d.containsKey("k")); }
String f_dictU4_a() { final d = <String, int>{}; d["a"] = 1; d["b"] = 2; d["c"] = 3; return d.length.toString(); }
String f_Fd1_b() { final d = <int, String>{}; d[1] = "v"; return d[1].toString(); }
String f_Fd2_a() { final d = <String, int>{}; d["z"] = 1; d["a"] = 1; d["m"] = 1; return d.keys.join(","); }
String f_Fd3_a() => mkd()["nope"].toString();
String f_Fd4_a() {
  final d = <String, int>{"a": 1, "b": 2, "c": 3, "d": 4, "e": 5};
  for (final k in d.keys) { if (k == "c") { d.remove(k); } }
  return "ok";
}
String f_Fl9d_a() { final a = mkd(); final b = a; b["k"] = "99"; return a["k"].toString(); }


void main() {
  try { print("F-l9-dict.a|" + fmts(f_Fl9d_a())); } catch (_e) { print("F-l9-dict.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web dassign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web dassign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web dassign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9-dict.a|$VERDICT" >> "$OUT"
  fi
else
  echo "dart-web: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

echo ""
echo "=== run complete ==="
ls -l /out/dct_*.txt

