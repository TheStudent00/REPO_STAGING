---
id: hq.research.compiler_graph.gate.lean.progress
status: living
---

# PROGRESS — lean

- 2026-09-06: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/node_0_3_1_5_gate/CORE_0_3_1_5_gate.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-07: **task L1 ran the node's first runs; the node moves from
  skeleton to a working second discharger.** Status: the install is verified
  and the two questions the CORE's "first runs" section named are answered.
  Record: `PRIVATE/PseudoCoupHQ/DevComms/log_227_task_L1_lean_second_discharger.md`.
  Artifacts: `PRIVATE/PseudoCoupHQ/Research/op_pipeline/lean/`
  (a `lake` project `archproof/`, the translator `term_to_lean.py`, the
  drivers `edges_L1.py` and `run_edges_L1.py`, 27 lane scripts under
  `lanes_L1/`, and five result json files).
  - Lean 4.24.0 verified inside a lane; `lake new` and `lake build` run with
    the instance's `proxy = no`, so nothing fetched and no Mathlib is present.
  - Ten of t100's proved pairs stated as `bv_decide` theorems: **eight proved
    by Lean's kernel**, each in about 0.2 s; the other two refused BY CAUSE as
    floating point, which `bv_decide` has no theory for.
  - **The renderer's preservation theorem is PROVED for the whole integer
    subset** — all eighteen `Term` constructors, by structural induction, no
    `sorry`, and `#print axioms` shows only `propext`, `Classical.choice` and
    `Quot.sound`. This is the source-level half of log_221 §7's claim.
  - The three pairs t100 left UNDECIDED are NOT wide divisions: all 1,099 of
    them sit in three xmm (floating-point) machine type keys and carry no term
    at all. The cost question was answered separately on the division
    identity: 8 bits proves in 0.5 s; **16 bits proves in 283 s** once three
    ceilings in turn were raised (the SAT solver's 10 s default, Lean's
    elaborator heartbeat limit, and the instance's 1 GiB `/tmp`, which had
    truncated a proof certificate of 1,073,500,160 bytes); 32 and 64 bits
    remain undecided at a 600 s SAT ceiling.
  - Coverage of the subset over the pool, measured: 213 of the 862 pool5
    entries that print a layer-5 text use only operators the subset covers.
    Scope estimate for the rest is log_227 §8.
- 2026-09-07 (conversation branch): the owner's ruling that Lean is "a
  potentially fundamental part of the operator-mapping proof system",
  not a second discharger only; the float model as the owner stated it
  (sign, implicit-leading-1 significand, exponent, plus the five
  classes) with rounding as shift-compare-increment; proof cost
  re-placed: definitions and lemmas are days, bit-blasting is the
  fallback whose cost measures a lemma gap, and the only unavoidable
  per-instance check is the unverified compiler's output. Proposed
  order for this node in the log's §6, awaiting the owner. Record:
  `PRIVATE/PseudoCoupHQ/DevComms/log_228_lean_float_model_and_the_proof_system.md`.
- 2026-09-09: **task l3 closed the check's 19 DISCREPANCY rows and measured the
  proofs' trust classes; the check's guard value is now `259 / 172 / 87`,
  DISCREPANCY 0.** Status: the model_translator sub-node's check has no
  unadjudicated row left; the trust question is measured and one item of it is
  open for the owner. Record:
  `PRIVATE/PseudoCoupHQ/DevComms/log_251_task_l3_the_one_naming_and_the_trust_classes.md`.
  Artifacts: `PRIVATE/PseudoCoupHQ/Research/op_pipeline/lean/`
  (`check_L2.json` and `check_L2.json.before_task_l3`, `l3_trust_classes.json`,
  the updated `README.md`, 13 lane scripts under `lanes_l3/`).
  - The 19 were ONE defect in the check's own composer, not a disagreement
    about the machine: the theorem's two sides named the unit's arrivals by two
    different print-order rules, because `bound_variables` numbered the free
    symbols after a plain `z3.simplify` while the stored layer-5 line was named
    by `term.Term.normalize`, which orders every commutative operator's
    arguments before and after simplifying (task t104 added the first of those
    two ordering steps on 2026-09-07, after the check was written). Verified on
    the rows before anything was changed: the two rules agree on 214 of the 243
    rows with a proved term and differ on 29 — the 19 and 10 rows already
    refused for other causes.
  - `bound_variables` now calls `term.py`'s own `order_commutative` and
    `ordered_symbols` in that order, so the naming rule keeps ONE definition in
    the module that owns it, and it PROVES the naming per row against the
    stored line (refusal cause `NAMING_NOT_THE_STORED_ONE`; zero rows took it).
    `term.py` was not modified.
  - **All 19 now prove**, each by `bv_decide` in 0.40-0.99 s under 503 MB. The
    check reads 259 rows, 172 STATED (39 `rfl`, 133 `bv_decide`), 87 REFUSED,
    0 DISCREPANCY. **That tally is the guard value for every later task.**
  - The trust classes of the 172 proved theorems, from `#print axioms`: 20 with
    no axiom at all and 19 with `propext, Quot.sound` (both `rfl`), 61 with
    Lean's own three axioms (`bv_decide` closing in its rewriting stage), and
    72 that also carry `Lean.ofReduceBool` and `Lean.trustCompiler`, which
    trust the Lean compiler. None depends on `sorryAx`; `lake build` of the
    whole project is exit 0 over 318 jobs.
  - The brief's "61 native-evaluation proofs to be re-proved by `bv_decide`"
    does not exist as posed: `native_decide` occurs zero times in the project,
    the 61 is log 232 §6's figure over rows `bv_decide` had already closed, and
    the axiom is `bv_decide`'s own whenever it calls the SAT solver
    (`Lean/Elab/Tactic/BVDecide/Frontend/BVDecide.lean`, the
    `mkConst ``Lean.ofReduceBool`` at line 298 of the toolchain source);
    `BVDecideConfig` has no field that turns it off. Each of the 72 was
    re-posed with the definitions unfolded and `bv_normalize` — the tactic's
    own rewriting stage — alone: **0 of 72 closed**, all with `unsolved goals`,
    the attempt costing 26.2 s and peaking at 474 MB.
  - `model_translate.py model` was re-run before the check so `model_L2.json`'s
    census (173 mnemonics, 162 translated, 4,135 definitions) describes the
    `Model.lean` on disk after task ap5 widened the sweep's operand shapes.

- **2026-09-10 — task t2: the construction schemas' lemmas, in Lean,
  instantiated per shape and per limb.** The arch line's second tier
  (`Research/oracle/cross_construction/emulation/construct/`) rebuilds an
  operation the target has no holder for out of operations at the width it
  has, and the equality between the two is discharged HERE where it can
  be: `construct/lean/run_lemmas_t2.py` generates one Lean theorem per
  LIMB of each SHAPE at each word — the left is the z3 operation, the
  right is `schemas.lower`'s own output on it, both translated by
  `op_pipeline/lean/term_to_lean.py` with its own round-trip check, so a
  lemma cannot state something the tier does not do. **147 theorems proved
  by `bv_decide`**, at about a second each, covering the adder, the
  comparisons, the widening, the bitwise schema and the two logical shifts
  at 16/8, 32/16, 64/32, 65/64 and 128/64; the tier then discharged all
  fourteen of its proved places by lemma and asked z3 nothing.
  MEASURED AND OWED, in `construct/lean/OWED.md`: the high half of a
  product does not close at any width (a miter of two multipliers; the
  algebraic `BitVec.toNat` proof is stated there); the divider cannot be
  STATED at all, because a theorem's statement is the term written out and
  restoring division's steps read their own previous step three times, so
  the statement grows like 3^width — closing it needs a translation that
  can NAME an intermediate, which `term_to_lean.py` does not do; and the
  general-width lemma of every schema is owed, because `bv_decide` decides
  a goal at a fixed width and cannot be handed a variable one. TWO SEAMS
  MET FROM THE OTHER SIDE and recorded rather than worked around: the
  translator's round-trip check refuses a statement whose commutative
  EQUALITY the pipeline's `order_commutative` has reordered (log_251's
  subject, met here as `x == 0` against `0 == x`), and its width
  unification cannot pin a numeral whose width only a sibling fixes. No
  `.lean` file this task added carries `sorry`. Log:
  `PRIVATE/PseudoCoupHQ/DevComms/log_257_task_t2_the_second_tier_constructs_what_a_target_lacks.md`.

- 2026-09-11 — **task t4: a construction can be STATED as a theorem at
  all, which is task t2's own owed item closed.** `construct/lean/OWED.md`
  §3 recorded what was owed: "a theorem's statement is the term WRITTEN
  OUT, a printed term names no intermediate, and restoring division reads
  its own previous remainder three times per step — so the statement
  grows like 3^width", and what was owed was "a way to STATE it — a
  translation to Lean that names intermediates (`let`)".
  `construct/general/lean_general.py` is that translation: it walks the
  z3 node and writes one Lean `let` per DISTINCT node, in the project's
  own spellings. MEASURED, and it is the whole difference: the multiplier
  at 8 bits is 190 distinct nodes and at or above 200,000 nodes WRITTEN
  OUT; the divider at 64 is 8,839 distinct and past the same ceiling.
  With the `let` form, `run_lemmas_t4.py` closed 132 of the 196 (kind,
  width, word) instances the two stores use, 263 theorems run, one `lean`
  process each at a 90-second budget bounded at 4 GB of address space —
  the barrel shifter at 128 bits over a word of 128 in 3.301 s with 102
  bindings, the adder at 65 over 64 in 1.219 s with 122. EVERY INTEGER
  KIND the census names is proved at every width it uses, except multiply
  and the four divide/remainder kinds, where `bv_decide`'s SAT solver
  times out; the float kinds are not stated in Lean at all, because
  `BitVec` carries no float operation. STILL OWED and unchanged: the
  GENERAL statement over `BitVec w` for every `w`, which needs an
  induction over the limb count because `bv_decide` decides at a fixed
  width. Toolchain: `Lean (version 4.24.0, x86_64-unknown-linux-gnu,
  commit 797c613eb9b6d4ec95db23e3e00af9ac6657f24b, Release)`. Theorems:
  `.../construct/general/lean_t4/`, rows `.../lemmas_t4_1_of_2.json` and
  `.../lemmas_t4_2_of_2.json`. Log:
  `PRIVATE/PseudoCoupHQ/DevComms/log_262_t4_the_general_construction_tier.md`.
