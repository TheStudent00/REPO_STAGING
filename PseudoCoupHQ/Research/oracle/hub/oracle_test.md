# oracle_test -- task hub1, Hub v1's first oracle numbers

Generated by `hub.py tables` off `oracle_test.json` and `measure.json`.

## 1. The handful, node by node

One row per operator node of `Research/oracle/hub/handful/handful.go`: where it is, what go's own type checker says its operands and its result are, and which corpus go unit -- and so which cell -- the corpus attests for it.

| function | line:col | the node, LITERAL | lhs holder | rhs holder | result holder | corpus unit | cell |
|---|---|---|---|---|---|---|---|
| f1_i32_add_sub | 20:9 | `a + b` | int32 | int32 | int32 | go/op_312 | `add gpr_gpr 32` |
| f1_i32_add_sub | 20:9 | `a + b - c` | int32 | int32 | int32 | go/op_348 | `sub gpr_gpr 32` |
| f2_i32_add_mul | 25:9 | `(a + b) * c` | int32 | int32 | int32 | go/op_60 | `imul gpr_gpr 32` |
| f2_i32_add_mul | 25:10 | `a + b` | int32 | int32 | int32 | go/op_312 | `add gpr_gpr 32` |
| f3_i64_add_sub | 30:9 | `a + b` | int64 | int64 | int64 | go/op_319 | `add gpr_gpr 64` |
| f3_i64_add_sub | 30:9 | `a + b - c` | int64 | int64 | int64 | go/op_355 | `sub gpr_gpr 64` |
| f4_i64_mul | 35:9 | `a * b` | int64 | int64 | int64 | go/op_67 | `imul gpr_gpr 64` |
| f5_u64_shift | 40:9 | `(a << n) >> n` | uint64 | uint64 | uint64 | -- | -- |
| f5_u64_shift | 40:10 | `a << n` | uint64 | uint64 | uint64 | -- | -- |
| f6_i32_div | 45:9 | `a / b` | int32 | int32 | int32 | -- | -- |
| f7_f64_add_mul | 50:9 | `(a + b) * c` | float64 | float64 | float64 | go/op_88 | `mulsd xmm_xmm 64` |
| f7_f64_add_mul | 50:10 | `a + b` | float64 | float64 | float64 | go/op_340 | `addsd xmm_xmm 64` |
| f8_i32_select | 55:5 | `a != b` | int32 | int32 | bool | -- | -- |

## 2. The handful, function by function and target by target

| function | target | the gate's verdict | re-posed without `//go:noinline` | cause where it is a hole |
|---|---|---|---|---|
| f1_i32_add_sub | c | PROVED | -- | -- |
| f2_i32_add_mul | c | PROVED | -- | -- |
| f3_i64_add_sub | c | HOLE | -- | `a + b`: the dictionary has no proved entry for the cell `add gpr_gpr 64` on c: z3 found a starting state under which the two sides differ |
| f4_i64_mul | c | PROVED | -- | -- |
| f5_u64_shift | c | HOLE | -- | `(a << n) >> n`: go's own build of this construct at these holders is the corpus unit go/op_218, whose body `mov %rbx,%rcx; shr %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shr cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64`; `a << n`: go's own build of this construct at these holders is the corpus unit go/op_182, whose body `mov %rbx,%rcx; shl %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shl cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64` |
| f6_i32_div | c | HOLE | -- | `a / b`: go's own build of this construct at these holders is the corpus unit go/op_96, whose body `push %rbp; mov %rsp,%rbp; test %ebx,%ebx; je 47a658 <main.op_96+0x18>; cmp $0xffffffff,%ebx; jne 47a653 <main.op_96+0x13>; neg %eax; xor %edx,%edx; jmp 47a656 <main.op_96+0x16>; cltd; idiv %ebx; pop %rbp; ret; call 43f3c0 <runtime.panicdivide>; nop` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 8 arch-opcode row(s): `push gpr_one 64`, `test gpr_same 32`, `cmp imm_gpr 32`, `neg gpr_one 32`, `xor gpr_same 32`, `cltd none 64`, `idiv gpr_one 32`, `idiv gpr_one 32` |
| f7_f64_add_mul | c | PROVED | -- | -- |
| f8_i32_select | c | HOLE | -- | `a != b`: go's own build of this construct at these holders is the corpus unit go/op_492, whose body `cmp %eax,%ebx; setne %al; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 1 arch-opcode row(s): `cmp gpr_gpr 32` |
| f1_i32_add_sub | rust | PROVED | -- | -- |
| f2_i32_add_mul | rust | PROVED | -- | -- |
| f3_i64_add_sub | rust | PROVED | -- | -- |
| f4_i64_mul | rust | PROVED | -- | -- |
| f5_u64_shift | rust | HOLE | -- | `(a << n) >> n`: go's own build of this construct at these holders is the corpus unit go/op_218, whose body `mov %rbx,%rcx; shr %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shr cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64`; `a << n`: go's own build of this construct at these holders is the corpus unit go/op_182, whose body `mov %rbx,%rcx; shl %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shl cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64` |
| f6_i32_div | rust | HOLE | -- | `a / b`: go's own build of this construct at these holders is the corpus unit go/op_96, whose body `push %rbp; mov %rsp,%rbp; test %ebx,%ebx; je 47a658 <main.op_96+0x18>; cmp $0xffffffff,%ebx; jne 47a653 <main.op_96+0x13>; neg %eax; xor %edx,%edx; jmp 47a656 <main.op_96+0x16>; cltd; idiv %ebx; pop %rbp; ret; call 43f3c0 <runtime.panicdivide>; nop` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 8 arch-opcode row(s): `push gpr_one 64`, `test gpr_same 32`, `cmp imm_gpr 32`, `neg gpr_one 32`, `xor gpr_same 32`, `cltd none 64`, `idiv gpr_one 32`, `idiv gpr_one 32` |
| f7_f64_add_mul | rust | PROVED | -- | -- |
| f8_i32_select | rust | HOLE | -- | `a != b`: go's own build of this construct at these holders is the corpus unit go/op_492, whose body `cmp %eax,%ebx; setne %al; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 1 arch-opcode row(s): `cmp gpr_gpr 32` |
| f1_i32_add_sub | go | UNDECIDED | PROVED | -- |
| f2_i32_add_mul | go | UNDECIDED | PROVED | -- |
| f3_i64_add_sub | go | UNDECIDED | PROVED | -- |
| f4_i64_mul | go | UNDECIDED | PROVED | -- |
| f5_u64_shift | go | HOLE | -- | `(a << n) >> n`: go's own build of this construct at these holders is the corpus unit go/op_218, whose body `mov %rbx,%rcx; shr %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shr cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64`; `a << n`: go's own build of this construct at these holders is the corpus unit go/op_182, whose body `mov %rbx,%rcx; shl %cl,%rax; cmp $0x40,%rcx; sbb %rdx,%rdx; and %rdx,%rax; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 6 arch-opcode row(s): `mov gpr_gpr 64`, `shl cl_gpr 64`, `cmp imm_gpr 64`, `sbb gpr_same 64`, `sbb gpr_same 64`, `and gpr_gpr 64` |
| f6_i32_div | go | HOLE | -- | `a / b`: go's own build of this construct at these holders is the corpus unit go/op_96, whose body `push %rbp; mov %rsp,%rbp; test %ebx,%ebx; je 47a658 <main.op_96+0x18>; cmp $0xffffffff,%ebx; jne 47a653 <main.op_96+0x13>; neg %eax; xor %edx,%edx; jmp 47a656 <main.op_96+0x16>; cltd; idiv %ebx; pop %rbp; ret; call 43f3c0 <runtime.panicdivide>; nop` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 8 arch-opcode row(s): `push gpr_one 64`, `test gpr_same 32`, `cmp imm_gpr 32`, `neg gpr_one 32`, `xor gpr_same 32`, `cltd none 64`, `idiv gpr_one 32`, `idiv gpr_one 32` |
| f7_f64_add_mul | go | UNDECIDED | PROVED | -- |
| f8_i32_select | go | HOLE | -- | `a != b`: go's own build of this construct at these holders is the corpus unit go/op_492, whose body `cmp %eax,%ebx; setne %al; ret` is not one arch-opcode instruction plus chaff (task o2's narrow rule), so the corpus attests no single cell for this node; its own ledger holds 1 arch-opcode row(s): `cmp gpr_gpr 32` |

## 3. Body A and body B, per function and target

| function | target | body A, go's own, LITERAL | body B, the composition, LITERAL |
|---|---|---|---|
| f1_i32_add_sub | c | `add %ebx,%eax; sub %ecx,%eax; ret` | `lea (%rdi,%rsi,1),%eax; sub %edx,%eax; ret` |
| f2_i32_add_mul | c | `add %ebx,%eax; imul %ecx,%eax; ret` | `lea (%rdi,%rsi,1),%eax; imul %edx,%eax; ret` |
| f4_i64_mul | c | `imul %rbx,%rax; ret` | `mov %rdi,%rax; imul %rsi,%rax; ret` |
| f7_f64_add_mul | c | `addsd %xmm1,%xmm0; mulsd %xmm2,%xmm0; ret` | `addsd %xmm1,%xmm0; mulsd %xmm2,%xmm0; ret` |
| f1_i32_add_sub | rust | `add %ebx,%eax; sub %ecx,%eax; ret` | `lea (%rdi,%rsi,1),%eax; sub %edx,%eax; ret` |
| f2_i32_add_mul | rust | `add %ebx,%eax; imul %ecx,%eax; ret` | `lea (%rdi,%rsi,1),%eax; imul %edx,%eax; ret` |
| f3_i64_add_sub | rust | `add %rbx,%rax; sub %rcx,%rax; ret` | `lea (%rdi,%rsi,1),%rax; sub %rdx,%rax; ret` |
| f4_i64_mul | rust | `imul %rbx,%rax; ret` | `mov %rdi,%rax; imul %rsi,%rax; ret` |
| f7_f64_add_mul | rust | `addsd %xmm1,%xmm0; mulsd %xmm2,%xmm0; ret` | `addsd %xmm1,%xmm0; mulsd %xmm2,%xmm0; ret` |
| f1_i32_add_sub | go | `add %ebx,%eax; sub %ecx,%eax; ret` | `cmp 0x10(%r14),%rsp; jbe 47a80b <main.f1_i32_add_sub+0x2b>; push %rbp; mov %rsp,%rbp; sub $0x8,%rsp; mov %ecx,0x20(%rsp); call 47a6e0 <main.emu_add_gpr_gpr_32__primitive__go>; mov 0x20(%rsp),%ebx; nopl 0x0(%rax,%rax,1); call 47a700 <main.emu_sub_gpr_gpr_32__primitive__go>; add $0x8,%rsp; pop %rbp; ret; mov %eax,0x8(%rsp); mov %ebx,0xc(%rsp); mov %ecx,0x10(%rsp); call 475be0 <runtime.morestack_noctxt.abi0>; mov 0x8(%rsp),%eax; mov 0xc(%rsp),%ebx; mov 0x10(%rsp),%ecx; jmp 47a7e0 <main.f1_i32_add_sub>` |
| f2_i32_add_mul | go | `add %ebx,%eax; imul %ecx,%eax; ret` | `cmp 0x10(%r14),%rsp; jbe 47a86b <main.f2_i32_add_mul+0x2b>; push %rbp; mov %rsp,%rbp; sub $0x8,%rsp; mov %ecx,0x20(%rsp); call 47a6e0 <main.emu_add_gpr_gpr_32__primitive__go>; mov 0x20(%rsp),%ebx; nopl 0x0(%rax,%rax,1); call 47a720 <main.emu_imul_gpr_gpr_32__primitive__go>; add $0x8,%rsp; pop %rbp; ret; mov %eax,0x8(%rsp); mov %ebx,0xc(%rsp); mov %ecx,0x10(%rsp); call 475be0 <runtime.morestack_noctxt.abi0>; mov 0x8(%rsp),%eax; mov 0xc(%rsp),%ebx; mov 0x10(%rsp),%ecx; jmp 47a840 <main.f2_i32_add_mul>` |
| f3_i64_add_sub | go | `add %rbx,%rax; sub %rcx,%rax; ret` | `cmp 0x10(%r14),%rsp; jbe 47a8cb <main.f3_i64_add_sub+0x2b>; push %rbp; mov %rsp,%rbp; sub $0x10,%rsp; mov %rcx,0x30(%rsp); call 47a740 <main.emu_add_gpr_gpr_64__primitive__go>; mov 0x30(%rsp),%rbx; nopl (%rax); call 47a760 <main.emu_sub_gpr_gpr_64__primitive__go>; add $0x10,%rsp; pop %rbp; ret; mov %rax,0x8(%rsp); mov %rbx,0x10(%rsp); mov %rcx,0x18(%rsp); call 475be0 <runtime.morestack_noctxt.abi0>; mov 0x8(%rsp),%rax; mov 0x10(%rsp),%rbx; mov 0x18(%rsp),%rcx; jmp 47a8a0 <main.f3_i64_add_sub>` |
| f4_i64_mul | go | `imul %rbx,%rax; ret` | `cmp 0x10(%r14),%rsp; jbe 47a919 <main.f4_i64_mul+0x19>; push %rbp; mov %rsp,%rbp; sub $0x10,%rsp; call 47a780 <main.emu_imul_gpr_gpr_64__primitive__go>; add $0x10,%rsp; pop %rbp; ret; mov %rax,0x8(%rsp); mov %rbx,0x10(%rsp); call 475be0 <runtime.morestack_noctxt.abi0>; mov 0x8(%rsp),%rax; mov 0x10(%rsp),%rbx; jmp 47a900 <main.f4_i64_mul>` |
| f7_f64_add_mul | go | `addsd %xmm1,%xmm0; mulsd %xmm2,%xmm0; ret` | `cmp 0x10(%r14),%rsp; jbe 47a96b <main.f7_f64_add_mul+0x2b>; push %rbp; mov %rsp,%rbp; sub $0x10,%rsp; movsd %xmm2,0x30(%rsp); call 47a7a0 <main.emu_addsd_xmm_xmm_64__reg_xmm0__go>; movsd 0x30(%rsp),%xmm1; nop; call 47a7c0 <main.emu_mulsd_xmm_xmm_64__reg_xmm0__go>; add $0x10,%rsp; pop %rbp; ret; movsd %xmm0,0x8(%rsp); movsd %xmm1,0x10(%rsp); movsd %xmm2,0x18(%rsp); nopl (%rax); call 475be0 <runtime.morestack_noctxt.abi0>; movsd 0x8(%rsp),%xmm0; movsd 0x10(%rsp),%xmm1; movsd 0x18(%rsp),%xmm2; jmp 47a940 <main.f7_f64_add_mul>` |

## 4. The measure over the corpus's own go units

The corpus holds 590 go units. 138 of them are held by task o2's NARROW rule (the whole body is one arch-opcode instruction plus chaff) and 134 of those name exactly one cell; those are the units the dictionary can be asked about. The other go units of the corpus are the constructs go lowers to several cells, and they are named as such in the handful's own hole causes.

| target | units asked | composed | ledger rows the composed cells cover | built and carved | proved | proved under caller extension | disproved | undecided | ledger rows the proved cells cover | re-posed without `//go:noinline` | proved on that re-pose | ledger rows that re-pose proves |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| c | 134 | 100 | 56830 | 100 | 98 | 2 | 0 | 0 | 56830 | 0 | 0 | 0 |
| rust | 134 | 114 | 62745 | 114 | 112 | 2 | 0 | 0 | 62745 | 0 | 0 | 0 |
| go | 134 | 124 | 74677 | 124 | 0 | 0 | 0 | 124 | 0 | 124 | 108 | 68399 |

## 5. The measure, by cause

### c

| units | cause |
|---|---|
| 9 | the emulation `emu_and_gpr_gpr_32__primitive__c` declares its parameter 0 in c's truth holder `bool`, which collapses every non-zero value to one; the cell's own mapping is over 32 bits, so this entry cannot carry this node's operand |
| 9 | the emulation `emu_or_gpr_gpr_32__primitive__c` declares its parameter 0 in c's truth holder `bool`, which collapses every non-zero value to one; the cell's own mapping is over 32 bits, so this entry cannot carry this node's operand |
| 7 | the dictionary has no proved entry for the cell `add gpr_gpr 64` on c |
| 7 | the emulation `emu_not_gpr_one_32__primitive__c` declares its parameter 0 in c's truth holder `bool`, which collapses every non-zero value to one; the cell's own mapping is over 32 bits, so this entry cannot carry this node's operand |
| 2 | the unit's own body line `xor $0x1,%eax` carries an operand that is not a register, so no IN row stands for it |

### rust

| units | cause |
|---|---|
| 9 | the emulation `emu_and_gpr_gpr_32__primitive__rust` declares its parameter 0 in rust's truth holder `bool`, which collapses every non-zero value to one; the cell's own mapping is over 32 bits, so this entry cannot carry this node's operand |
| 9 | the emulation `emu_or_gpr_gpr_32__primitive__rust` declares its parameter 0 in rust's truth holder `bool`, which collapses every non-zero value to one; the cell's own mapping is over 32 bits, so this entry cannot carry this node's operand |
| 2 | the unit's own body line `xor $0x1,%eax` carries an operand that is not a register, so no IN row stands for it |

### go

| units | cause |
|---|---|
| 122 | body B: the reference: every path through this body leaves the unit or is unreachable, so the body leaves no answer to read |
| 4 | this task's holder table has no entry for the go holder `bool` |
| 2 | body B: the reference: arch opcode 'movups' has no entry in the opcode table -- no body in the corpus this table was built over spells it |
| 2 | the dictionary has no proved entry for the cell `subsd xmm_xmm 64` on go |
| 2 | the dictionary has no proved entry for the cell `subss xmm_xmm 32` on go |
| 2 | the unit's own body line `xor $0x1,%eax` carries an operand that is not a register, so no IN row stands for it |

