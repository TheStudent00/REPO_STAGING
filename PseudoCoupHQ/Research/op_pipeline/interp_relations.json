{
 "meta": {
  "generator": "interp_relations_build.py",
  "purpose": "Task 17(c) -- handler -> {class, relation kind, proof/witness}. NO table membership changes.",
  "table_read": "dominant_table24.json (901 classes, 42 type_pairs, read not modified)",
  "canon_attempt_read": "interp_canon_attempt.json"
 },
 "table_type_pairs_present": [
  "bool,None",
  "bool,bool",
  "bool,f32",
  "bool,f64",
  "bool,i32",
  "bool,i64",
  "bool,u64",
  "f32,None",
  "f32,bool",
  "f32,f32",
  "f32,f64",
  "f32,i32",
  "f32,i64",
  "f32,u64",
  "f64,None",
  "f64,bool",
  "f64,f32",
  "f64,f64",
  "f64,i32",
  "f64,i64",
  "f64,u64",
  "i32,None",
  "i32,bool",
  "i32,f32",
  "i32,f64",
  "i32,i32",
  "i32,i64",
  "i32,u64",
  "i64,None",
  "i64,bool",
  "i64,f32",
  "i64,f64",
  "i64,i32",
  "i64,i64",
  "i64,u64",
  "u64,None",
  "u64,bool",
  "u64,f32",
  "u64,f64",
  "u64,i32",
  "u64,i64",
  "u64,u64"
 ],
 "table_type_pairs_present_count": 42,
 "records": [
  {
   "lang": "ruby",
   "handler": "vm_opt_plus",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "anchor excerpt shown calls FIXNUM_2_P on both args before any ALU is visible in the stored sample -- a dispatcher/inline-cache check (log_095's own AGAINST reading), operand representation not directly established by ALU in the excerpt captured.",
   "type_pair_read": "opaque_VALUE_dispatch",
   "result_family_asserted": "VALUE_opaque",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'VALUE_opaque' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'opaque_VALUE_dispatch' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "ruby",
   "handler": "rb_fix_plus",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "ship excerpt: `and esi,0x1` / `test al,0x7` -- the argument register is TAG-TESTED before use, the Fixnum tagged-integer encoding (2n+1), not a plain integer register.",
   "type_pair_read": "tagged64",
   "result_family_asserted": "VALUE_opaque",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'VALUE_opaque' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'tagged64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "ruby",
   "handler": "rb_int_plus",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "ship excerpt: `test dil,0x1` -- same tag-test shape as rb_fix_plus, on the dispatch entry before the type check branches.",
   "type_pair_read": "tagged64",
   "result_family_asserted": "VALUE_opaque",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'VALUE_opaque' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'tagged64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "ruby",
   "handler": "rb_big_plus",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "ship excerpt shows BOTH shapes on the two arguments: `test sil,0x1` + `sar rsi,1` (rsi is a tagged Fixnum, untagged in place) and `movzx eax,BYTE PTR [rdi+0x1]` (rdi is DEREFERENCED at a fixed offset -- the pointer-to-struct pattern fix_cpython_type_key.py used for cpython's long_add). A single unit with two DIFFERENT argument representations.",
   "type_pair_read": "ptr64,tagged64",
   "result_family_asserted": "VALUE_opaque",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'VALUE_opaque' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,tagged64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "php",
   "handler": "add_function",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "excerpt for every one of the four handlers dereferences r14/r15 at fixed offsets 0x8 and 0xc (`mov eax,DWORD PTR [rax+0x8]` after `mov rax,r15`) -- the zval struct's value/type fields, the SAME pointer-to-struct dereference pattern as cpython's long_add and ruby's rb_big_plus rdi operand. Both operands are zval* pointers, never a plain integer register.",
   "type_pair_read": "ptr64,ptr64",
   "result_family_asserted": "zval_ptr",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'zval_ptr' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,ptr64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "php",
   "handler": "ZEND_ADD_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "excerpt for every one of the four handlers dereferences r14/r15 at fixed offsets 0x8 and 0xc (`mov eax,DWORD PTR [rax+0x8]` after `mov rax,r15`) -- the zval struct's value/type fields, the SAME pointer-to-struct dereference pattern as cpython's long_add and ruby's rb_big_plus rdi operand. Both operands are zval* pointers, never a plain integer register.",
   "type_pair_read": "ptr64,ptr64",
   "result_family_asserted": "zval_ptr",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'zval_ptr' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,ptr64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "php",
   "handler": "ZEND_ADD_LONG_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "excerpt for every one of the four handlers dereferences r14/r15 at fixed offsets 0x8 and 0xc (`mov eax,DWORD PTR [rax+0x8]` after `mov rax,r15`) -- the zval struct's value/type fields, the SAME pointer-to-struct dereference pattern as cpython's long_add and ruby's rb_big_plus rdi operand. Both operands are zval* pointers, never a plain integer register.",
   "type_pair_read": "ptr64,ptr64",
   "result_family_asserted": "zval_ptr",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'zval_ptr' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,ptr64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "php",
   "handler": "ZEND_ADD_LONG_NO_OVERFLOW_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "excerpt for every one of the four handlers dereferences r14/r15 at fixed offsets 0x8 and 0xc (`mov eax,DWORD PTR [rax+0x8]` after `mov rax,r15`) -- the zval struct's value/type fields, the SAME pointer-to-struct dereference pattern as cpython's long_add and ruby's rb_big_plus rdi operand. Both operands are zval* pointers, never a plain integer register.",
   "type_pair_read": "ptr64,ptr64",
   "result_family_asserted": "zval_ptr",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'zval_ptr' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,ptr64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  },
  {
   "lang": "cpython",
   "handler": "long_add",
   "class": null,
   "relation_kind": "incomparable_by_representation",
   "representation_evidence": "RATIFIED PRIOR RESULT (fix_cpython_type_key.py, task 5a): in0/in1 used directly as 64-bit values with no zx/sx wrapper AND as the base of ld64/ld32 fixed-offset loads -- the pointer-to-struct pattern. Carried forward unchanged, not recomputed, as the third instance of the SAME shape ruby's rb_big_plus and all four php handlers also show.",
   "type_pair_read": "ptr64,ptr64",
   "result_family_asserted": "ptr64_result",
   "canon_status": "refused (interp_canon_attempt.json -- no sem.anchor_registers, no pyvex sem block)",
   "dominance_attempt": {
    "has_projection": false,
    "detail": "dominance.RESULT_PROJECTION has no entry for 'ptr64_result' -- per dominance.py's own docstring: 'A result type this table does not name gets NO projection. The pair is recorded with the reason and no bridge is claimed.'"
   },
   "cross_unit_prover_attempt": {
    "candidate_exists": false,
    "detail": "cross_unit_prover.py's candidate step 1 requires a class key (type_pair) shared with an existing dom_ops class. type_pair 'ptr64,ptr64' is not among the 42 type_pairs present in dominant_table24.json's 901 rows (computed, not assumed -- see table_type_pairs_present below). No candidate pairing exists, so the prover is refused before a single z3 call is made."
   },
   "provenance_is_weaker": true,
   "evidence_class": "human interpretation of stated design (register tag-test / dereference shape read off the handler's own stored objdump excerpt) -- the same class fix_cpython_type_key.py used for cpython's long_add, extended here to ruby and php by the identical method."
  }
 ],
 "records_count": 9,
 "summary": {
  "incomparable_by_representation": 9,
  "candidate_present_unattempted": 0
 }
}