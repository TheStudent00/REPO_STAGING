---
id: hq.research.compiler_graph.pool.exception_families.progress
status: living
---

# PROGRESS — exception_families

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_7_pool.md`.
- 2026-09-03: guard families built in round 5, `EF0039` the `growing`
  family — **done** (`exception_families2.json`). Evidence:
  [pool realization](../CORE_0_3_1_7_pool.md).
- 2026-09-03: the round-5 file labelled stale against
  `the_pool3.json` rather than reused — **done**. Evidence:
  [pool realization](../CORE_0_3_1_7_pool.md).
- 2026-09-03: rebuild over `the_pool3.json` — **planned**. Evidence:
  [pool realization](../CORE_0_3_1_7_pool.md).
- 2026-09-03: CORRECTION to this CORE before any code ran (PROTOCOL
  §2, round-12 rule 2): task 61's brief named
  `exception_families3.json` for the rebuild, but that name is taken
  on disk by the round-5 line's own later build over `guards5.json`
  (2026-09-01), which this CORE's realization table already recorded.
  A superseded record is never edited, so the rebuild takes the next
  number, `exception_families4.json`. The settled rule and the
  realization table were corrected first. Evidence: this CORE, settled
  rules; `~/Programming/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: rebuilt over the current pool — **done**
  (`exception_families4.json`): 40 families over the 128 guard rows of
  `guards5.json` whose unit is a member of `the_pool4.json`, 186 rows
  excluded as `continue-with-a-different-answer`, 0 rows about units
  outside the pool. Each family now names the pool entries its members
  sit in, which is what makes it a rebuild OVER the pool. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: `exception_families5.json` rebuilt over `the_pool5.json`
  — 314 guard rows read, 128 considered, 186 excluded as
  `continue-with-a-different-answer`, 0 rows about units outside the
  pool, **40 exception families**. The number is the next free one
  after `exception_families4.json`, per this node's own naming
  correction — **done**. Evidence: `pool65_run.log`, `~/Programming/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
