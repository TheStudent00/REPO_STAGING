---
id: hq.research.compiler_graph.probes.type_inventory.progress
status: living
---

# PROGRESS — type_inventory

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_0_probes.md`.
- 2026-09-03: candidate extraction from each compiler's own type
  vocabulary — **done**. Evidence:
  [probes realization](../CORE_0_3_1_0_probes.md).
- 2026-09-03: witness compile per candidate, 85 of 208 demoted —
  **done**. Evidence:
  [probes realization](../CORE_0_3_1_0_probes.md).
- 2026-09-03: the accepted list banked as `type_inventory3.json` —
  **done**. Evidence:
  [probes realization](../CORE_0_3_1_0_probes.md).
- 2026-09-06: task t101, the join of language spellings to machine
  holders (master plan §4.2 step 3) — **stopped at step 1, flagged**.
  The stored DWARF parameter tables carry `name` and `location` only:
  64,398 rows over 348 store files, zero carrying a byte size or an
  encoding, so the class and width the holder table needs are not on
  disk and cannot be assigned by hand. `types101_holders.json`,
  `types101_spellings.json` and `types101_entry_holders.json` are not
  written. Evidence:
  [log 213](../../../../../DevComms/log_213_task_t101_dominant_types_dwarf_flag.md),
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/types101_report.md`,
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/types101_dwarf_flag.json`.
- 2026-09-06: task t101b, closing t101's flag — **done**. Re-read
  `DW_AT_byte_size` and `DW_AT_encoding` straight from the compiler
  (`types101_anchor_dwarf.py`: every accepted probe regenerated from
  its `meta`, compiled at anchor flags, `DW_AT_type` walked with
  pyelftools; 31,067 probes, 0 refused, 92,127 DWARF rows), then ran
  the join over those rows (`types101_join.py`, after fixing two
  `Counter`/`dict` mix-ups at cause, one line each). Result: 32
  holders (class × width), 88/88 machine `type_key`s covered, 9 DWARF
  vs. inventory class disagreements listed (not resolved, per the
  brief's stop rule), swift's 17 attested spellings UNDECIDABLE (its
  anchor DWARF carries no `DW_AT_encoding` on any base type — stdlib
  scalars are `DW_TAG_structure_type`, not `DW_TAG_base_type`).
  `types101_holders.json`, `types101_spellings.json`,
  `types101_entry_holders.json` written; spelling-ban guard 337/337
  PASS. Evidence:
  [log 217](../../../../../DevComms/log_217_task_t101b_dominant_types_join.md),
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/types101_report.md`.
