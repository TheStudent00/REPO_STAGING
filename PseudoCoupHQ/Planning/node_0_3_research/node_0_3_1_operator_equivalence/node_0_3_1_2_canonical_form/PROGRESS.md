---
id: hq.research.compiler_graph.canonical_form.progress
status: living
---

# PROGRESS — canonical_form

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: memory-wrapped form with absolute ledger, typed rows, positional labels: 30,436 of 31,078 wrapped and proved, 642 refused by name — **done**. Evidence: log_146, log_152 (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: IN-i rows numbered in arrival_contract order in the prelude (count the affected units, re-gate) — **planned**. Evidence: log_153 §9 (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: one module `canonical_form.py` carrying this node's name, superseding ledger48.py + canon38_*.py — **planned**. Evidence: this CORE (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: one module `canonical_form.py` carrying this node's name — class `CanonicalForm` with `wrap`, `assemble`, `refuse`, attributes `block_order` and `wrapped_text`, sub-nodes as the four classes `Prelude`, `Epilogue`, `Labels`, `Refuse`; it imports `ledger.py` and `gate.py` and imports nothing from `ledger47/48.py`, `gate48.py` or any `canon37/38_*` driver — **done**. Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_162_task60_canonical_form.md` §2.
- 2026-09-03: the IN-i ordering fix rendered over all 31,078 units → `canon39_*`; 30,432 proved / 646 refused against canon38's 30,436 / 642; the only movement is 4 swift units refused by the named cause `no runtime callee body`; `Gate.zero_regression` reports 0 regressions without a named cause — **done**. Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_162_task60_canonical_form.md` §5, §6 (`canon39_zero_regression.json`).
- 2026-09-03: assemble over the same 2,728-unit sample — 2,728 offered / 2,728 assembled / 0 failed / 8,948 `R_X86_64_PC32` ledger relocations, real `as --64` and `objdump` — **done**. Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_162_task60_canonical_form.md` §7 (`canon39_assemble.json`, `canon39_assemble_transcripts.txt`).
- 2026-09-03: unmodified guard over every canon39 artifact, ONE process — 335 paths, PASS 335, FAIL 0, exempt 0, exit 0 — **done**. Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_162_task60_canonical_form.md` §8 (`guard60_transcript.txt`).

- 2026-09-05: **the form was misread, and the correction is recorded
  before any code.** The arch-unit is essentially unchanged except for
  the loading and unloading of registers into a virtual memory;
  `region36.py` implemented "everything in memory" as rewriting every
  location relative to `%r15`, which is where the collision came from.
  `canonical_form.py` already wraps rather than rewrites — measured, 0
  of 1,779 compiled units' wrapped texts contain `%r15`. Planned: the
  eleven interpreter units move onto `canonical_form.py`; a seventh
  block kind, an arriving addressable area, joins the six, with
  `own-address` as its precedent. Provenance: the owner, 2026-09-05.

- 2026-09-05: **the eleven interpreter units are on
  `canonical_form.py`** — the re-carved handler bodies of task 94, put
  through `CanonicalForm.wrap` unmodified. Ten wrap; `ruby/vm_opt_plus`
  stays refused (`no canonical text`: the optimiser inlined the symbol
  away). **All five `%r15` refusals are gone**, and the re-measured
  count stands: 0 of the 1,763 wrapped texts among the 1,779 compiled
  units contain that register name. `region36.py` and
  `canon36_universal.py` gained one header block each saying superseded
  and naming the correction; neither was otherwise edited, and neither
  is imported by anything this lap wrote — **done**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_201_task96_interpreters_onto_canonical_form.md`
  §1, §2 (`t96_canonical.json`, `t96_wrapped_texts.txt`).
- 2026-09-05: **the seventh block kind exists** —
  `t96_arriving_area.py`, an arriving addressable area, following
  `own-address` line for line: an extent of `AREA_SPAN` = 0x100 (that
  file's own `OWN_SPAN`, unchanged), one uniform affine map whose
  origin was MEASURED to 0, the unit's own spacing preserved and only
  the base moving, and a displacement past the span refused by name.
  Its evidence is machine form only: a family the body dereferences
  before the body has written it. The mechanical difference from an
  `IN` row is one step against two — an area's ledger entry IS the
  address the body reaches through. Found and rendered: 2 areas in
  `cpython/long_add`, 1 in `ruby/rb_big_plus`, 5 across the three php
  `ZEND_ADD_*` handlers, 3 in `php/add_function` — **done**. Evidence:
  same log, §3.
- 2026-09-05: **the verdicts moved 10 × UNDECIDED →
  PROVED_BY_CONSTRUCTION**, against log_199's baseline of nothing
  proving. NOT a solver proof and not reported as one: the solver route
  answered for none of the eleven, and `gate.py`'s six mechanical
  checks carried all ten, 6/6 each. The movement is the FORM's, not the
  instrument's, and that was measured: `gate.py`'s own checks run
  against FORM 1's texts fail C1 on all five that exist, so the
  structural route was never available to a form that rewrites the body
  — **done**. Evidence: same log, §4 (`t96_analysis.json`).
- 2026-09-05: **two of the three shortfalls log_199 flagged are gone on
  every unit with a body; the third survives on two.** The `%r15`
  collision: gone 10/10. No block kind for memory obtained at run time:
  gone 10/10 — the canonical form rewrites nothing, so a store through
  an obtained pointer needs no block. An input block holds a pointer:
  gone on 6, not applicable on 2 (no arriving area exists), **surviving
  on `java/op_1` and `java/op_2`**, whose area is found correctly and
  then refused because they reach `0x538` into HotSpot's thread
  structure, past the 0x100 extent — **done, with one surviving
  shortfall recorded as a result**. Evidence: same log, §5.
- 2026-09-05: **widening `AREA_SPAN` past `own-address`'s 0x100 is
  awaiting the owner**, and so is what bounds a frame php reaches at
  `0x8(%r14,%rax,1)` with the index read out of the bytecode; and the
  seventh kind makes `IN-i` mean "the i-th VALUE arrival" rather than
  "argument i", which the CORE's settled rule words the other way —
  **planned**. Evidence: same log, §0.2.
