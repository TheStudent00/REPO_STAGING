---
id: pp.framework.tools.render_plan.progress
status: living
---

# PROGRESS — render_plan

- 2026-08-05 **done** (the owner, on a tree drawn in chat: "how come `node`
  and `connector` arent indented and underneath `ur`?"): **`--tree`
  prints the tree as plain text**, with `--with designation,status,id`
  for extra columns.
  - **Why it exists.** The HTML view is for READING a plan. Quoting one
    — in a message, a log, a CORE — had no output, so a tree in
    conversation was drawn from memory, and the one drawn from memory
    was wrong: two nodes were placed beside their parent instead of
    under it. A generated diagram cannot make that mistake.
  - It shows `realize: false` register entries, marked. The HTML view
    omits them, because they have no folder to draw — so the picture
    was silently missing structure the register states. `builder`'s
    six attributes and methods appear for the first time.
  - `--tree` writes no HTML and exits, so it costs nothing to run.

- 2026-08-05: `## components` is no longer special-cased; the renderer
  opens `## design`, which PROTOCOL §1b now states. See the framework
  node's PROGRESS.

- 2026-08-01: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PlanPlan/Planning/node_0_0_framework/node_0_0_3_tools/CORE_0_0_3_tools.md`. Skeleton only — definition,
  designation, and content pending.
