== populations
   pool2 member units                       30436
   pool3 member units                       30436
   units in both, the comparable population 30436
   in pool2 only -- absent from the newer pool 0
   in pool3 only -- absent from the older pool 0

== the join on member sets
   pool2 entries holding a shared unit       5274
   pool3 entries holding a shared unit       2247
   pool2 entries that SPLIT                  60
   pool3 entries that MERGED                 719

== SPLIT causes, computed, one entry may carry more than one
       60  the members carry more than one layer-3 wrapped text, so layer-3 identity does not join them
       15  14 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
       11  the eligible members normalize to more than one layer-5 text, so layer-5 identity does not join them
        9  12 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        7  10 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        5  6 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        2  28 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        2  32 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        2  7 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        2  26 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        2  30 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        1  2 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        1  13 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them
        1  5 member(s) have no eligible layer-5 text, so no layer-5 ground can reach them

== MERGE grounds, computed, one entry may carry more than one
      711  layer-3 wrapped-text identity
       13  layer-5 normalized-text identity
        0  no single crossing pair -- transitive chain only

-- wrote pool2_pool3_delta.json
