---
id: hq.research.compiler_graph.ledger.flag_rules.progress
status: living
---

# PROGRESS — flag_rules

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_3_ledger.md`.
- 2026-09-03: stem-based setter table covering comparisons, x87 and
  SSE compares and arithmetic setters — **done** (`ledger47.py`).
  Evidence: [ledger realization](../CORE_0_3_1_3_ledger.md).
- 2026-09-03: reader linked to the row that set the flags — **done**
  (`ledger48.py`); it withdrew 699 old proofs and is correct.
  Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.2.
- 2026-09-03: `jmp` removed as a flag reader — **done**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §2.4.
- 2026-09-03: `sbb` and `adc` modelled as flag SETTERS — **planned**;
  699 units wait on it, every other setter family is unaffected.
  Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.2.
- 2026-09-03: `## design` rule 7 REWRITTEN before any code, under
  round 12's binding rule 2: `sbb` and `adc` are both a flag reader
  and a flag setter, and each writes TWO rows — a value row
  (`written_half` "the difference" / "the sum") and a `flags only` row
  (`written_half` "the flags") that a following reader links to; the
  row that set the flags they read is the first operand of both.
  Provenance: log_158 TASK 59 (a), closing the gap log_153 §4.2 named.
  Evidence: this node's CORE, `## design` rule 7 and its settled
  rules.
- 2026-09-03: the rule REALIZED — **done**.
  `ledger.FLAG_RULES["carry_in_setters"]`, `ledger.carry_in_stem`, and
  the carry-in branch of `Ledger.walk_dataflow`. Population: 728 units
  of the 31,067 walked (regenerated 29,288 + wrapped 1,779) carry a
  flag pair whose setter stems to one of the two — c 284, cpp 390,
  rust 8, swift 46; by stem sbb 726, adc 2. log_153 §4.2's 699 is the
  subset that moved proved -> no-term on the flag link; this 728 is
  the ledger-side population. Evidence:
  `Research/op_pipeline/ledger_sample_walk_printed.txt`
  (`c/regen_34943`, `c/regen_35988` printed with both halves).
- 2026-09-03: rule 8 added — whether EVERY arithmetic setter should
  carry its own `flags only` row is NOT decided and NOT invented here.
  Evidence: this node's CORE, `## design` rule 8.
