---
id: hq.research.arch_unit_oracle.cross_construction.single_opcode_units.progress
status: living
---

# PROGRESS — single_opcode_units

- 2026-09-06: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_2_cross_construction/CORE_0_3_2_2_cross_construction.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-06: task o9 (log 223) — the opcode signature census.
  25 of 162 mnemonics carry a proved single-opcode row (243/259
  proved, task o8's own population); 262 (mnemonic, signature) rows;
  reading kinds read off the data (15 reading-blind, 13
  width-changing, 13 reading-changing, 8 sign-sensitive, 0
  flag-producing, 10 none); collapse 25 -> 20 ideas (5 sign-sensitive
  merges); guard partition over all 162 mnemonics, streamed from all
  332 canon40 shards. Spelling guard: 33 findings, all `and`/`or`/
  `xor`/`not` on the permitted `mnemonic` key — the same open item
  o2/o8 already carry, not re-litigated. Artifact:
  `Research/oracle/arch_opcodes/signatures/opcode_signatures.{json,md}`.
  Status: census complete, counting only, no proofs.
- 2026-09-07: task o10 (log 225) — the opcode signature census from
  LEDGER rows, over the 137 mnemonics o9's single-opcode-narrow
  population had no row for. Population widened from o9's 259
  single-instruction units to every ledger row of every canon40 unit
  (332 shards, 31,078 units); 112 of 162 mnemonics now carry a
  signature (up from 25), 1,508 (mnemonic, signature) rows; reading
  kinds (43 reading-blind, 55 width-changing, 47 reading-changing, 8
  flag-producing — zero in o9 — 88 sign-sensitive pairs, up from 5);
  collapse 112 -> 79 ideas (33 merges, read as an upper bound, not
  settled — see the log's own flag on `addsd`/`addss`); o9's five
  `lea`-driven pairings all reproduce over the fuller data, `lea`'s
  three still marked suspect as evidence, not proof, for the reason
  o9 already gave. Spelling guard: 58 findings, same shape as o9's
  own (`and`/`or`/`xor`/`not` on the permitted `mnemonic` key,
  confirmed identical to o9's own unshown findings), not
  re-litigated. Artifact:
  `Research/oracle/arch_opcodes/signatures/ledger_signatures.{json,md}`.
  Status: census complete, counting only, no proofs.
- 2026-09-08: task mn1 (log 235) — the `mnem` field rename. The guard
  already exempts a field named `mnem`, not `mnemonic`; o2's
  `single_opcode_units.py`/`unique_opcodes.py`, o9's
  `opcode_signatures.py`, o10's `ledger_signatures.py` and o8's
  `per_opcode.py` all wrote/read `mnemonic` instead — renamed in all
  five, then every artifact regenerated on the tower in dependency
  order (o2, o9+o10, o8). All headline counts unchanged (259 narrow
  rows, 162 unique mnemonics, o9 243/16, o10 31,078 units over 332
  shards, o8 243 rows / 197 LANDED / 155 byte-identical / 216
  proved-on-ship). Guard now PASSES clean on `unique_opcodes.json`,
  `per_opcode_population.json` and `per_opcode_held.json` (never
  before this task). Four causes remain, distinct from the field this
  task renamed, reported not fixed: (A) mnemonics used as dict keys
  themselves in `opcode_signatures.json` (33) and
  `ledger_signatures.json` (58) — the same open item o9/o10 already
  carried; (B) the compound fields `mnemonic_a`/`mnemonic_b`, folded
  into (A)'s counts; (C) `per_opcode.py`'s compound field
  `landed_mnemonic`, 57 findings in `per_opcode_results.json`, newly
  named; (D) `single_opcode_units.json`'s `zero_opcode_examples`
  records missing a `lang` field, 30 findings, newly visible (was
  masked inside o2's original 183-finding count). Artifacts:
  `Research/oracle/arch_opcodes/{single_opcode_units,unique_opcodes}.{json,md}`,
  `Research/oracle/arch_opcodes/signatures/{opcode_signatures,ledger_signatures}.{json,md}`,
  `Research/oracle/cross_construction/emulation/per_opcode/per_opcode_{population,held,results}.json`,
  `per_opcode_report.md`. Status: rename and regeneration complete;
  causes A-D awaiting the owner.
