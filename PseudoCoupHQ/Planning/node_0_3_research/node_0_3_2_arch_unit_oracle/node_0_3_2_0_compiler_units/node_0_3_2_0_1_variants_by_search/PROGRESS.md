---
id: hq.research.arch_unit_oracle.compiler_units.variants_by_search.progress
status: living
---

# PROGRESS — variants_by_search

- 2026-09-06: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_0_compiler_units/CORE_0_3_2_0_compiler_units.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-06: task o6 closed (the leftover of task o4, go only, dealt
  with per research CORE §5's recommended route — go/types run once
  as a type oracle over `src/cmd/compile`). Deliverable on disk:
  `Research/oracle/compiler_units/go_types_{oracle.go,sites.json.gz,
  join.py,join.json,report.md,package_shape_cost.json}`; report
  `DevComms/log_216_task_o6_go_types_oracle.md`, built from those
  artifacts and the `o6_l1`-`o6_l9` lane logs (no new analysis).
  Headline numbers, all LITERAL in log_216 with their source: the join
  matched 103,414 of o4's 103,475 sites one-to-one by position; typing
  those sites raises the resolved count from o4's 22,810 (search
  alone) to 79,799 (go/types), and distinct variants from 1,050 to
  1,568 (316 with both operands in the core inventory, up from 248).
  Cost measured for BOTH shapes of the run: the shape that reads the
  image's own copy of one generated file via an overlay types all
  106,063 sites in 7.2s / 616 MB (one cache) or 159.9s / 541 MB
  (per-package cache); the shape that omits the overlay (forcing
  go/types to resolve that generated file from source, pulling a much
  larger closure into every package's importer) was re-run once at
  DOUBLE the memory room (6 GB to 12 GB) per the standing "limit is a
  flag" rule and ABORTED AGAIN, `ABORT_MEMORY_O6: peak RSS 12474.0 MB
  > 12288 MB bound` at directory 35 of 77 — measured, not retried
  further: that shape needs meaningfully more than 12.5 GB, the
  overlay shape is the route. Final verifier
  (`check_conventions_log_claims.py --verify --timeout 20`, from
  instance `o6`) tallies **claims 4 | MATCHES 0 | DIFFERS 0 |
  UNVERIFIABLE 3 | REFUSED 1 | NOT_RERUNNABLE 0** — zero DIFFERS,
  first pass, no fix needed (the low claim count is the extractor not
  treating this log's pipe tables, copied verbatim from the already-
  checked `go_types_report.md`, as separately re-runnable shell
  claims). Spelling guard PASS on all three produced json files
  (`o6_l7_spelling_guard.sh`); `grep -c exempt` is 0 on every produced
  file (the only nonzero hit, 3, is the lane script's own comment text
  describing the check, not a data violation). Instance brought down.

  Nothing flagged as missing against this task's brief — its five §2
  items (site-level json fields, the join, resolution-vs-oracle
  agreement, the completed variant table, cost) are each present in
  `go_types_report.md` sections 1-6 and carried into log_216 unchanged.
  Awaiting the owner: none for this task specifically; task o5's PROGRESS
  entry (sibling node, `node_0_3_2_0_2_lowering_route_cut`) records
  open gaps against ITS OWN brief that may be worth deciding alongside
  this one, since both feed the same master-plan step.
