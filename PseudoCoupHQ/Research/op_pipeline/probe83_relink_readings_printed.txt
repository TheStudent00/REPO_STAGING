-- the population this probe asks about
   canon40 proved units whose term66 record carries the ledger's runtime-answer refusal: 3927
   readings handed in: 76 keys, 33 routine names

-- WHAT HAPPENS WHEN THE READINGS AND THE TOOLCHAIN ARE HANDED IN
     3927  relinked
           c/regen_119
           c/regen_17
           c/regen_2
           c/regen_329
           c/regen_343
           c/regen_385
        0  relink still refused
        0  rebuild disagreed

   peak resident 87060 kB against the stated 6 GB cap

-- THE VERDICT
   A DEFECT IN THE RELINK.  Every one of the 3927 refused units re-walks and its rebuild matches the stored canon40 ledger row for row once the same readings and toolchain the render was handed are handed to the relink.
