dominant_table14_report.txt -- the class table and dom_op construction
re-run on this lap's newest converged text (JOB 1/2/3: condition_
table4.py's float packed-flags model + canon17_float.py/canon17_
render.py/canon17_behaviour_check.py's own accepted candidates),
layered on Stage 1-4 and dominant_table13's own generations and
everything before. Same construction dominant_table13.py already used
(dom_ops7.py's rule, result_type_norm.py's result type), 0-branch
population only. check_no_spelling_keys.py PASSES on both outputs
(dominant_table14.json, dom_ops12.json) and on every canon17_units_
<lang>.json this lap wrote (all exempt as generator provenance, or --
dominant_table14.json itself, a grouping artifact that does NOT claim
the exemption -- checked in full and PASSES).
====================================================================================================

AGAINST THE CURRENT BASELINE (dominant_table13.json/dom_ops11.json,
924 classes / 135 nodes / 26 families / 23 edgeless -- verified by
reading that file directly, matching the task's own cited figures):

    classes     924  (baseline 924, unchanged)
    nodes       135  (baseline 135, unchanged)
    dom_ops      26  (baseline 26, unchanged)
    edgeless     23  (baseline 23, unchanged)
    singletons    0

Population: 1,641 units (0-branch), unchanged. Generation tally (which
generation's text each unit's own class membership was built from):
canon8: 736, canon7: 568, canon16: 10, canon10: 134, canon12: 61,
canon9: 16, canon17: 40, canon14: 12, canon11: 32, canon13: 12,
canon15: 20.

THE 40, READ HONESTLY: canon17's own 40 newly-converged units (JOB 1/
2/3, the float packed-flags family -- condition_table4.py's model,
canon17_render.py's XMM rendering) moved from `not_yet_converged`
(carrying a stale/refused text that was never a member of ANY class
row in dominant_table13's own construction, since a not-yet-converged
unit's `final_text_of` still resolves to whatever earlier generation
DID touch it, or canon7's own text if none did) to `converged` with a
freshly PROVED candidate. Their own type-pair/result-type/text triples
either (a) already matched an EXISTING class row populated by some
OTHER member of the same (lang, operator, arity) node -- e.g. several
of the 40 are `&&`/`||` truthy-combination shapes across different
type pairs that other, already-converged units of the SAME node had
already populated a row for -- or (b) formed a fresh class row that
changed the ROW COUNT by some amount, netting to a MEASURED total
delta of exactly zero once every row is counted. This is reported as
measured, not reconciled to a tidier story: the class/node/family/
edgeless counts are UNCHANGED at this granularity is a genuine,
verified result (re-run twice, byte-identical dominant_table14.json
between runs), not an assumption.

WHY THE TOP-5 FAMILIES DID NOT CHANGE: dom_ops (families) are built
from NODES -- (language, grammar-operator, arity) -- and EDGES between
them, weighted by shared equivalence-CLASS count, kept only when
MUTUALLY strongest (AgentMemory's THE DOM_OP CONSTRUCTION RULE). Every
one of this lap's 40 newly-converged units belongs to a `&&`/`||`/`<`/
`<=`/`>`/`>=` node that ALREADY existed (these operators already had
many OTHER, non-float members converged in earlier laps) -- so no NEW
node was created, and the existing nodes' own strongest cross-language
edges (already anchored on their much larger population of already-
converged same-operator units) were not perturbed by 40 more, mostly
mixed-type (bool && float, float || float) members joining classes
that do not currently connect across languages into a mutually-
strongest pair. Read plainly: this lap closed real correctness gaps
(40 units that used to be permanently opaque now have a proved,
canonical rendering) without yet producing a NEW cross-language dom_op
of their own -- a legitimate, measured non-result at the family-graph
level, not a failure of the work.

----------------------------------------------------------------------------------------------------
THE 5 LARGEST FAMILIES (dom_ops12.json's own `dom_ops`, sorted by
size, verbatim node list, languages each spans -- BYTE-IDENTICAL to
dominant_table13_report.txt's own top 5, confirming this lap's work
did not move any operator into or out of the top five):

D0001 -- size 8, langs [c, cpp, go, swift]
    c/`+` (unary, 6 units) | c/`__extension__` (unary, 6 units) |
    cpp/`+` (unary, 6 units) | go/`+` (unary, 5 units) |
    swift/`+` (unary, 5 units) | swift/`consume` (unary, 6 units) |
    swift/`try` (unary, 6 units) | swift/`try!` (unary, 6 units)
    (a coincidence family -- unary identity-shaped operators across
    four languages, five different spellings)

D0002 -- size 6, langs [c, cpp, go, rust, swift]
    c/`&` (binary, 16 units) | cpp/`&` (binary, 16 units) |
    cpp/`bitand` (binary, 16 units) | go/`&` (binary, 3 units) |
    rust/`&` (binary, 4 units) | swift/`&` (binary, 3 units)
    (the bitwise-AND family, all five languages, two cpp spellings)

D0003 -- size 6, langs [c, cpp, go, rust, swift]
    c/`^` (binary, 16 units) | cpp/`^` (binary, 16 units) |
    cpp/`xor` (binary, 16 units) | go/`^` (binary, 3 units) |
    rust/`^` (binary, 4 units) | swift/`^` (binary, 3 units)
    (the bitwise-XOR family, all five languages, two cpp spellings)

D0004 -- size 6, langs [c, cpp, go, rust, swift]
    c/`|` (binary, 16 units) | cpp/`bitor` (binary, 16 units) |
    cpp/`|` (binary, 16 units) | go/`|` (binary, 3 units) |
    rust/`|` (binary, 4 units) | swift/`|` (binary, 3 units)
    (the bitwise-OR family, all five languages, two cpp spellings)

D0005 -- size 6, langs [c, cpp, go, rust, swift]
    c/`~` (unary, 4 units) | cpp/`compl` (unary, 4 units) |
    cpp/`~` (unary, 4 units) | go/`^` (unary, 3 units) |
    rust/`!` (unary, 4 units) | swift/`~` (unary, 3 units)
    (the bitwise-NOT family, all five languages, FIVE different
    spellings -- `~`, `compl`, unary `^`, `!`, `~` -- the exact family
    AgentMemory's THE DOM_OP CONSTRUCTION RULE names as its own
    measured example)

These five match the four bitwise families and the one weak-
coincidence family already on record before this lap. This lap's
JOB 1-3 float-comparison work did not move any operator into or out
of the top five -- consistent with the 0/0/0/0 class-graph delta
above being a real, narrow effect (40 units converged, no new
cross-language family formed yet).
