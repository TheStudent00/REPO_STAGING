---
id: hq.research.compiler_graph.gate.zero_regression.progress
status: living
---

# PROGRESS — zero_regression

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_5_gate.md`.
- 2026-09-03: before/after comparison run for every round since
  round 8 — **done** (`canon37_zero_regression.py`,
  `canon38_zero_regression.py`, `zero_regression48/49/53.py`).
  Evidence: [gate realization](../CORE_0_3_1_5_gate.md).
- 2026-09-03: causes A (unreachable store, 392) and B (vacuous answer
  home, 214) computed per unit — **done**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_146_task47_memory_wrapped_form.md`
  §9.
- 2026-09-03: the 699 flag-link withdrawals recorded as a correction
  with its cause — **done**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.2.
- 2026-09-03: collapse the per-round scripts into one module named
  for this node — **planned**. Evidence:
  [gate realization](../CORE_0_3_1_5_gate.md).
- 2026-09-03: `Gate.zero_regression` written in `gate.py` with the
  CORE's three methods `compare` / `explain_losses` / `report`; the
  cause of every loss is COMPUTED from the unit's own record, and a
  loss with no computed cause is reported as an unexplained
  regression rather than smoothed -- **done**. Evidence: log_160
  §2.2, §7.
- 2026-09-03: run over all 30,436 canon38 units, task 53 against task
  58: **23,132 kept, 0 lost, 2,047 gained, 0 units missing, 0 lost
  proofs without a computed cause** -- **done**. Evidence: log_160
  §5, `Research/op_pipeline/audit58_printed.txt`.
- 2026-09-05 (task 91): a FOURTH named cause joins the three above —
  **the alternative stretches added together**, 245 units. Over log
  153's own 30,436, the round-17 re-gate keeps 22,887 of the 23,132
  proofs and loses **245**, and every one of the 245 has the cause
  checked over its own artifact rather than asserted: all 245 carry a
  positional label and a conditional transfer; all 245 were proved by
  ROUTE TWO ALONE in log 153 (the superseded simulator refuses a
  conditional transfer, so route one never proved one of them); and
  route two now refuses each of them by name — "this body transfers to
  'js L0', a label it defines itself, so the page order is not the run
  order and a text-order walk has no one answer to reach". The old
  walk ran both alternatives and added the two answers together, and
  the ledger's term did the same, so the two agreed — two walks
  sharing one error, the same shape as the flag link of log_153 §4.2.
  Under this node's own rule, "a withdrawn proof that was WRONG is a
  correction, not a loss to be avoided": **zero regressions holds in
  the ruled sense**. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/t91_lost_proofs_printed.txt`,
  `t91_lost_proofs.json`, `t91_audit_printed.txt`, and the fix itself
  in `gate.transfers_inside_the_unit` (log_168 §6.3).
