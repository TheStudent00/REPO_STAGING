i just wanna make a quick note about what im envisioning regarding the arch opcode modelling -- at least one aspect (and it might already be obvious):



lets say that an arch-unit contains a control-flow branch that contains identical behavior as the arch-opcode being modelled, we could use basic arch-unit control-flow to ensure that that particular branch covers that portion of the arch-opcode behavior and make the rest of the arch-unit branches unreachable.



also another thing is the idea that a lambda-like function and basic logical/arithmetic operations is more than expressive enough to use Lambda-calculus to approximate any arch-opcode. arch-opcode behavior is exceptionally simple. i would think most arch-opcodes are covered by a small finite number of cases of large spaces of induction-able behavior.



like a piece-wise logical interpolation of the mapping. if we can define the basis of the logical interpolation using basic arch-units, we would be able to fit the arch-opcodes, trivially. i think part of this has already been stated but i wanted to make it extra clear.


































































