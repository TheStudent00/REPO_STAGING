---
id: pcv5.tools.ledgerer.builder.progress
status: living
---

# PROGRESS — builder

- 2026-08-02: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/Programming/PseudoCoup_v5/Planning/node_0_0_tools/node_0_0_0_ledgerer/CORE_0_0_0_ledgerer.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-08-02 **done** (the owner: "we are in agreement include `builder`.
  proceed"): renamed from `ledger_master` (the `ledgerer.ledger_master`
  repetition was the awkwardness), designation refined `code (module)`
  → `code (class)`, and `ts_to_ur_mapper` moved under this node —
  ledgerer calls funnel through the builder. folders renumbered:
  this node `node_0_0_0_3_ledger_master` → `node_0_0_0_2_builder`;
  the mapper `node_0_0_0_2_ts_to_ur_mapper` →
  `node_0_0_0_2_0_ts_to_ur_mapper` beneath it. nothing outside the
  chain referenced the old addresses.
- 2026-08-02 **done**: the four attributes and two methods registered
  as `realize: false` entries in this CORE's `sub_nodes` — the first
  instance of the rule added to PROTOCOL §1 the same day. plan
  structure with reserved address slots, no folders; each graduates
  by deleting its key when it grows design of its own. the
  `## components` overview stays as commentary beside the register.
- 2026-08-05 **done** (the owner: "perfect"): `sequencer` added to the
  register and design — the two-level logical clock assigning
  spacetime coordinates at admission. held state beside the
  ledger-under-construction; the id value class is `ur.id`.
