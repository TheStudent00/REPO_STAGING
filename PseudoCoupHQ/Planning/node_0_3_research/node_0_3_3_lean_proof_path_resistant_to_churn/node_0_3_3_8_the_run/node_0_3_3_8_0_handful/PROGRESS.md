---
id: hq.research.lean_proof_path_resistant_to_churn.the_run.handful.progress
status: living
---

# PROGRESS — handful

- 2026-09-14: planned (status: planned). Written fresh at the owner's order of 2026-09-14 ("start over. in a different research node ... the parts that are fucked up are deleted before you start them over again"); nothing of the superseded node's `handful` was copied.
- 2026-09-14: lanes on the tower: l49/l50 (corpus meanings; every certificate failed on a phantom `Sail.Vector` in the unfold list: the library index keyed lean-sail's `abbrev Vector.length` under `Vector`; fixed twice in `strip.py`), l52 (corpus meanings, third try), l53 (operator_for), l54 written (pass B: render, lower, prove, eye table), to be submitted once l53 shows a table (status: in-progress).
- 2026-09-15: THE GATE (the owner: verify before long runs): lane l61, 158 s, twelve c compiler-operators by stride over the certified corpus, through every stage to the eye table; 3 keys filled by proof, 3 emulations rendered, 2 proved equal to their definition, 1 not (the unit's int32_t parameter, sign-extended by the ABI). Log 282. Nothing wider runs until the owner reads it (status: done, awaiting the owner's read).
- 2026-09-15: the owner: "run it" (the gate with the width rule, then all 346 c units, then cpp, rust, go). The width rule coded (holders from the manifest's reps in the units file; entries of operator_for carry them; render takes a unit only at 64): lane l62 submitted, the gate again; l63 (operator_for over the 346) and l64 (render, lower, prove, eye table over the whole table) written, submitted after l62 is read (status: in-progress).
- 2026-09-15: the gate for cpp, rust and go (lane l66, queued after l65): the render plumbing of each is new code (a call casts to the unit's own parameter types; go carries only the function block), so each language gets its dozen through every stage and its eye table before its wide run (status: in-progress).
- 2026-09-15: the gates for cpp, rust, go (lane l66): rust 5 keys, 6 rendered, 6 proved (its plumbing works: a call casts to the unit's own types); cpp 1 key held only at (64, 32), nothing rendered (the dozen by plain stride fell on narrow holders); go 0 keys: its complement (`xori a, -1`) survived evaluation and failed every stage because the meaning's `sign_extend` was never unfolded. Two changes: the provers unfold the unit's own definitions; the gate's dozen is drawn first from units whose holders are all 64. Lane l70 (cpp and go gates again) queued (status: in-progress).
- 2026-09-15: the cpp and go gates again (lane l70): cpp 3 keys, 4 rendered, 4 proved (SRA, XOR, SUB); go 3 keys, 3 rendered, 0 lowered: two refused by go's compiler (a unit returning `bool` cannot be converted to uint64) and one by the walk (a `c.nop` the compiler inserted, whose clause the strip never read). Two fixes: go's plumbing reads the unit's return type off its own signature and routes a bool through `b2u`; the walk composes a clause whose body is only the retire (C_NOP, C_NTL, NTL, PAUSE, ZCMOP, found by shape) as no change and unfolds it in the certificate (status: fixed; l72's go render is the test).
