---
id: hq.research.compiler_graph.term.render_back.progress
status: living
---

# PROGRESS — render_back

- 2026-09-03: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_6_term.md`.
- 2026-09-03: the debt named and recorded — **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_147_task48_layer4_ledger_census.md`
  §8.1.
- 2026-09-03: `render` — z3 term back to arch text — **planned**;
  nothing is built. Evidence:
  [term realization](../CORE_0_3_1_6_term.md).
- 2026-09-03: `verify` — gate the rendered text against its term —
  **planned**, and blocked on `render`. Evidence:
  [term realization](../CORE_0_3_1_6_term.md).
- 2026-09-03: still **planned**, deliberately. Task 61's brief says
  `render_back` is not attempted; `term.py`'s `Term.render_back`
  raises with the debt named rather than returning something that
  looks like a rendering. Evidence: `PRIVATE/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: the CORE's `## design` was INSUFFICIENT for the work and
  was corrected FIRST (PROTOCOL §2; log_158's binding rule). It named
  only `render` and `verify` and no shape for: the inverse map's
  provenance, the register discipline, where temporaries live, which
  arrival families a rendering uses, or which gate outcome counts.
  Five settled rules and a `template_table` / `temp_pool` /
  `wrap_rendered` / `refuse` design were written with their decision
  sources — **done**. Evidence: this node's CORE; log_166 TASK 66
  (`PRIVATE/PseudoCoupHQ/DevComms/`).
- 2026-09-03: `render` and `wrap_rendered` BUILT — `term.RenderBack`
  in `term.py`, reached through `Term.render_back`. The inverse map is
  `Reference.opcode_table`'s own builders read backwards (a template
  exists only where that table holds the mnemonic WITH a builder, and
  the constructor refuses otherwise); free symbols are the IN rows the
  prelude fills; the root reaches OUT-0 through the epilogue;
  intermediates become TEMP rows when the ledger walks the rendered
  body — **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/term.py` section 5;
  `PRIVATE/PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: `verify` BUILT — the rendered text is wrapped by the same
  `CanonicalForm.wrap` and gated by the same `Gate.prove_wrapped`
  against the unit's OWN ship body; only `PROVED_ON_SHIP` counts —
  **done**. Evidence: `render_back_run.py`;
  `PRIVATE/PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: POPULATION ONE, the 158 members of pool entry `E00029`
  (one layer-5 text `v0 + v1`, seven languages): **158 rendered, 158
  assembled by `as`, 158 round-tripped through `objdump -d`, 158
  PROVED_ON_SHIP, 0 refused**. The collapse: 12 distinct layer-3
  wrapped texts to 5 distinct rendered texts; 0 are
  character-identical to their layer-3 text — **done**. Evidence:
  `render_back_E00029_printed.txt`, `render_back_E00029.json`.
- 2026-09-03: POPULATION TWO, all 26,040 proved terms of the 30,432
  canon39-proved units: **5,909 rendered, 5,909 assembled by `as`,
  5,909 round-tripped through `objdump -d`, 5,873 PROVED_ON_SHIP,
  20,131 refused across 17 named causes**. The collapse over the same
  26,040 units: 105 distinct rendered texts against 1,905 distinct
  layer-3 wrapped texts; 0 rendered texts are character-identical to
  their layer-3 text, because the rule always routes through the
  fixed temp pool where the compiler wrote in place — **done**.
  Evidence: `render_back_tally_printed.txt`,
  `render_back_tally.json`, `render_back_store/` (332 shards);
  `PRIVATE/PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: guard: `guard66.py` runs the UNMODIFIED
  `check_no_spelling_keys.py` as one process over all 335 JSON
  artifacts — 335 PASS, 0 FAIL, `grep -c exempt` 0 — **done**.
  Evidence: `guard66_transcript.txt`, `guard66.json`.
- 2026-09-03: the largest remaining cause is `if` (13,027 units): a
  term carrying a conditional has no template, because inverting one
  means rendering the CONDITION as a comparison and the choice as a
  conditional move — `build_set_condition` / `build_move_condition`
  read a flag triple this rule does not yet produce. Named, not worked
  around — **planned**. Evidence: `render_back_tally_printed.txt`.
- 2026-09-03: task 66's rendered texts counted beside layers 5 and 3
  over the units it reached (5,878 of this round's proved set): **102
  distinct rendered texts**, against 38 layer-5 texts and 164 layer-3
  texts over those same units — **done** (a reading of task 66's
  artifacts, no new render). Evidence: `collapse65_printed.txt`, `PRIVATE/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03 (task 80): the CORE's `## design` was INSUFFICIENT for the
  conditional and was corrected FIRST (PROTOCOL section 2; log_158's
  binding rule). It had no shape for a sub-term whose sort is Bool —
  the walk it described computes every sub-term into a pool register,
  and a predicate is never in a register on this machine; it is in the
  flags, and a suffix reads them. `condition_table`, `emit_condition`
  and `emit_choice` were written into the design, with four settled
  rules and their decision sources — **done**. Evidence: this node's
  CORE; `PRIVATE/PseudoCoupHQ/DevComms/log_187_task80_render_back_conditional.md`.
- 2026-09-03 (task 80): the conditional shape READ OFF THE CORPUS
  before any template was written — 31,078 units of the 332 canon39
  shards walked, 15,884 of them spelling a flag-reading arch opcode:
  20,136 reads that write a place and bind to a flag setter in the
  same body (18,788 `set<cc>`, 1,348 `cmov<cc>`), 15,604 of the
  `set<cc>` reads exactly one line after their own flag setter; the most frequent shape
  `test <0>,<0>; setne <1>` at 5,691; the 14 condition suffixes the
  corpus writes are the 14 the inverse rule picks — **done**.
  Evidence: `PRIVATE/PseudoCoupHQ/Research/op_pipeline/probe80_conditional_shapes_printed.txt`.
- 2026-09-03 (task 80): the CONDITIONAL TEMPLATE BUILT —
  `term.RenderBack.condition_of` / `.emit_condition` / `.emit_choice` /
  `.emit_set_choice` / `.emit_move_choice`. The condition suffix is
  `condition_table.SUFFIX_TO_COND` read backwards; the complement is
  proved out of `condition_table.cond_to_z3` at construction; the
  flag-reading mnemonic is kept only where the one opcode table holds
  it with a builder (which is why `cmovg` has no template: no body in
  the corpus spells one) — **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/term.py` section 5.
- 2026-09-03 (task 80): POPULATION TWO re-run, the same 26,040 proved
  terms of the 30,432 canon39-proved units: **11,911 rendered, 11,911
  assembled by `as`, 11,911 round-tripped through `objdump -d`, 11,876
  PROVED_ON_SHIP, 14,129 refused across 20 named causes** — against
  round 13's 5,909 / 5,909 / 5,909 / 5,873 / 20,131. The collapse over
  the same 26,040 units: **420 distinct rendered texts against 1,905
  distinct layer-3 wrapped texts** (round 13: 105 against the same
  1,905); 0 character-identical to their layer-3 text — **done**.
  Evidence: `render_back80_tally_printed.txt`,
  `render_back80_tally.json`, `render_back80_store/` (332 shards).
- 2026-09-03 (task 80): THE TEMPLATE'S OWN RESULT, measured on the
  units it reached: the 13,027 units round 13 refused for the `if`
  cause are now **6,002 rendered, 6,002 assembled, 6,002 round-tripped
  and 6,002 PROVED_ON_SHIP — no unit rendered by the conditional
  template failed to prove** — and 7,025 still refused under six
  finer causes, the largest being a choice at 8 bits, which has no
  conditional-move instruction on this machine (1,618) — **done**.
  Evidence: `render_back80_tally_printed.txt`; the literal walk
  `render_back80_one_unit_printed.txt`.
- 2026-09-03 (task 80): POPULATION ONE, the 158 members of pool entry
  `E00029`: **158 rendered, 158 assembled, 158 round-tripped, 158
  PROVED_ON_SHIP, 0 refused; 5 distinct rendered texts against 12
  layer-3 texts** — the same figures as round 13, which is expected:
  the entry's one layer-5 text carries no conditional — **done**.
  Evidence: `render_back80_E00029.json`.
- 2026-09-03 (task 80): guard: `guard80.py` runs the UNMODIFIED
  `check_no_spelling_keys.py` as one process over all 336 JSON
  artifacts — 336 PASS, 0 FAIL, `grep -c exempt` 0 — **done**.
  Evidence: `guard80_transcript.txt`, `guard80.json`.
- 2026-09-03 (task 80): the largest remaining causes are now named and
  still **planned**: a choice at 8 bits with no conditional move
  (1,618), a comparison whose sides are one bit wide (564, the corpus's
  own shape for it is `test $imm,<reg>` — 272 sightings), and a
  predicate that is a conjunction or a disjunction of comparisons
  (1,452 + 240, the corpus's own shape is two flag reads combined by a
  bit operator — 188 sightings of
  `test <0>,<0>; setne <1>; or <2>,<3>; setne <4>`). Evidence:
  `render_back80_tally_printed.txt`,
  `probe80_conditional_shapes_printed.txt`.
