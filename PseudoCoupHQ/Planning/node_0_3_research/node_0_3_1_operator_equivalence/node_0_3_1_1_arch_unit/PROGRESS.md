---
id: hq.research.compiler_graph.arch_unit.progress
status: living
---

# PROGRESS — arch_unit

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: extract, arrival_contract, context, interp_unit for all populations — **done**. Evidence: log_146 §5.4, log_124 (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: runtime_callee: extract the compiler-runtime routine a body calls and attach it (308 units) — **planned**. Evidence: log_157 correction; the owner 2026-09-03 (`~/Programming/PseudoCoupHQ/DevComms/`).

- 2026-09-05: **the unit's boundary is a function body — RULED by the owner.**
  From just after the wrapper-function call to just before the return;
  for an interpreter the handler's own function is the wrapper. This
  retires the lineage carve as a BOUNDARY rule (it stays as the record
  and as evidence). The compiled population already met the rule; the
  interpreter population did not, and `cpython/long_add_fastpath` — four
  instructions carved from a function that allocates and refcounts — is
  the case that showed it. Ruled with its cost in view: whole handler
  bodies write memory and answer with pointers, and the owner ruled the same
  day that the canonical form's virtual memory carries that traffic and
  the super-op miner covers recurring machinery. Status: **planned**
  (round 18 re-carves). Provenance: the owner, 2026-09-05, in session.

- 2026-09-05 (TASK 94 round 18, log_199): **the eleven interpreter units
  re-carved to their handler function's body — done, with three named
  shortfalls.** Bounds READ: the ELF symbol table for nine units, DWARF
  agreeing exactly on the four it carries a `DW_TAG_subprogram` for
  (`ruby/rb_fix_plus`, `ruby/rb_int_plus`, `ruby/rb_big_plus`,
  `php/add_function`); no DWARF subprogram for `cpython/long_add` or the
  three php `ZEND_ADD_*` handlers, so those rest on the symbol table
  alone (flagged, not resolved). The two java units are JIT nmethods: no
  symbol table, no DWARF, so their bounds are the JVM's own printed
  nmethod base and length — read, not computed, but not one of the
  ruling's two named sources (flagged). `ruby/vm_opt_plus` has no ship
  body and is a recorded absence. Sizes: `cpython/long_add` 1 own
  instruction -> 92; `ruby/rb_int_plus` 1 -> 190; `php/add_function`
  2 -> 55; the full table is in the log. VERDICTS: 9 PROVED ->
  UNDECIDED, 1 NO_CANONICAL_TEXT -> UNDECIDED, 1 -> NO_BODY; none
  DISPROVED, none still PROVED. No verdict was solver-bound, so the
  120,000 ms re-run never triggered. SHORTFALLS in the universal form's
  virtual memory, all three needing a ruling rather than a patch: (1)
  `%r15` is the region base and also php's and HotSpot's own frame
  register — 5 units refused by name; (2) no block kind for memory a
  unit obtains at run time — 9 bodies store through such a pointer;
  (3) an input block holds a pointer, not the object it addresses — 4
  bodies read at a displacement from an arriving pointer. SUPER-OP
  ROUTE: sound in shape, empty in content — the miner's population is
  the five compiled languages, and searching its 2,173 candidates
  against these bodies with its own normalizer finds ONE hit
  (`pop <reg>; ret`, mined from go), covering none of the four recurring
  paths. Artifacts: `Research/op_pipeline/t94_read_bounds.py`,
  `t94_recarve.py`, `t94_analysis.py` and their JSON.
  `lineage_carve.py` keeps its taint propagation and its outputs; one
  header block was added saying the BOUNDARY use is retired 2026-09-05
  and by what. Full report:
  `DevComms/log_199_task94_interpreter_function_bodies.md`.
