---
id: hq.research.arch_unit_oracle.architectures.riscv64.lean_proof_path.the_run.handful.progress
status: living
---

# PROGRESS — handful

- 2026-09-13: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_3_architectures/node_0_3_2_3_1_riscv64/node_0_3_2_3_1_3_lean_proof_path/node_0_3_2_3_1_3_8_the_run/CORE_0_3_2_3_1_3_8_the_run.md`. Skeleton only — definition,
  designation, and content pending.

- 2026-09-13: task lp1 attempted the handful on the tower. Reached 0 of
  10 units: BLOCKED by the model build. The cached emit does not compile
  under the pinned `leanprover/lean4:v4.29.0` + `lean-sail` v4
  (`LeanIM/Defs.lean`, 12 errors, sail 0.20.2's Lean backend leaked Sail
  call syntax `is_sv32_mode(k_v)` into a Lean type abbrev; 0 of 116
  modules build). `meaning` and `equals` both need the built model, so
  none of the ten units, and not the `mulh`-on-c headline, could run.
  Flagged for the coordinator (a newer sail is a fetch/install outside
  lane 1). Report: `PRIVATE/PseudoCoupHQ/DevComms/log_275_lp1_the_cached_emit_does_not_build_under_the_pinned_toolchain.md`.

- 2026-09-14: task lp1, second launch. Reached 0 of 11 units (the ten of
  rv1 plus `mulh` on c): BLOCKED AGAIN by the model build. The re-emit
  with sail built from source at `5745ea9e53` (branch `sail2`,
  2026-08-26) still prints `is_sv32_mode(k_v)` — a Sail type-level
  function of the model commit 3243f93 (`core/vmem_types.sail`) — into
  `LeanIM/Defs.lean`, and `lake build` fails there with 14 error lines
  (0 of 118 modules). `meaning`, `equals`, the `mulh` headline and the
  per-stage speed are unreached; brief §4 stops the task at this flag.
  Report: `PRIVATE/PseudoCoupHQ/DevComms/log_276_lp1_the_re_emit_with_sail_5745ea9e_does_not_build_either.md`.
