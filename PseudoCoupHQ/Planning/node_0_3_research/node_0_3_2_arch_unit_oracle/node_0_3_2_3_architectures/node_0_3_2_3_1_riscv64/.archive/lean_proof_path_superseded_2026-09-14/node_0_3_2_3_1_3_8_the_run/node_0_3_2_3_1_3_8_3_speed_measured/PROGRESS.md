---
id: hq.research.arch_unit_oracle.architectures.riscv64.lean_proof_path.the_run.speed_measured.progress
status: living
---

# PROGRESS — speed_measured

- 2026-09-13: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_3_architectures/node_0_3_2_3_1_riscv64/node_0_3_2_3_1_3_lean_proof_path/node_0_3_2_3_1_3_8_the_run/CORE_0_3_2_3_1_3_8_the_run.md`. Skeleton only — definition,
  designation, and content pending.

- 2026-09-13: task lp1 measured only the steps that need no built model
  (the model build is blocked; see log 275). Beside log 274 §7.1:

| step | expected (log 274 §7.1) | measured (lp1, tower) |
|---|---|---|
| `definitions()` emit | 45 min, 17 GB, once/commit | cached (log 274 l5); not re-run |
| toolchain fetch + unpack | plumbing | curl 11.1 s (839 MB zip) + unpack ~12 s |
| `lean-sail` v4 library build | plumbing | 5.7 s, peak 1.56 GB (7 jobs) |
| definitions text listing | seconds | 27.8 s wall, peak 23,752 kB (29 clauses / 49 keys) |
| `meaning` per unit | seconds expected | UNMEASURED — model build blocked |
| `equals` int level / bv_decide | under 1 s expected | UNMEASURED — model build blocked |
| `equals` on `mulh` at int level | the headline | UNMEASURED — model build blocked |
| pass A over 255 x 4 | ~1 hour expected | UNMEASURED — model build blocked |

  Report: `PRIVATE/PseudoCoupHQ/DevComms/log_275_lp1_the_cached_emit_does_not_build_under_the_pinned_toolchain.md`.

- 2026-09-14: task lp1, second launch: the steps that ran, measured on
  the tower (instance `lp1`, 8 cpus / 20g), beside log 274 §7.1. The
  model build failed (log 276 §4), so everything downstream of it is
  still UNMEASURED.

| step | expected (log 274 §7.1) | measured (lp1 second launch, tower) | of |
|---|---|---|---|
| sail built from source at `5745ea9e53` (clone, deps, `make install`) | not expected (the image's sail was to serve) | 85.2 s lane wall; clone 18.3 s; deps "Nothing to do." 3.9 s; `make install` 26.1 s, peak 1.21 GB | 1 build |
| `definitions()` emit, this sail | 45 min, 17 GB (sail 0.20.2, log 274 l5) | 216.5 s wall, peak resident 552 MB (lane 7b, rc=0) | 117 files / 63,066 lines |
| `lean-sail` v5 fetch (`lake update`) | plumbing | 0.9 s (commit 0794631) | 1 |
| `lean-sail` v5 library build | plumbing | 7.4 s, peak 1.50 GB (10 jobs) | 10 jobs |
| `lake build LeanIM.Defs` (the first model module) | plumbing | FAILED at 30.2 s, peak 1.92 GB, 14 error lines | 0 of 118 modules |
| definitions text listing through the module | seconds | 0.3 s lane wall, peak 22.6 MB | 29 clauses / 74 keys |
| `meaning` per unit | seconds expected | UNMEASURED — the build fails | — |
| `equals` int level / bv_decide | under 1 s expected | UNMEASURED — the build fails | — |
| `equals` on `mulh` at int level | the headline | UNMEASURED — the build fails | — |
| pass A over 255 x 4 | ~1 hour expected | UNMEASURED — the build fails | — |

  Report: `PRIVATE/PseudoCoupHQ/DevComms/log_276_lp1_the_re_emit_with_sail_5745ea9e_does_not_build_either.md`.
