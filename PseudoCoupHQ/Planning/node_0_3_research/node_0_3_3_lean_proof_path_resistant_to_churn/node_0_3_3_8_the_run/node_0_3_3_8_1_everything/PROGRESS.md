---
id: hq.research.lean_proof_path_resistant_to_churn.the_run.everything.progress
status: living
---

# PROGRESS — everything

- 2026-09-14: planned (status: planned). Written fresh at the owner's order of 2026-09-14 ("start over. in a different research node ... the parts that are fucked up are deleted before you start them over again"); nothing of the superseded node's `everything` was copied.
- 2026-09-15: the gate with the width rule (l62, 148 s): 3 keys, 2 usable at 64, 2 rendered, 2 proved, RTYPE SUB refused at render with its widths named. the owner: "run it". Submitted in order: l63 (operator_for over all 346 certified c units, entries with holders), l64 (render every definition from that table, lower, prove, eye table). cpp, rust, go follow once their corpora are lowered (status: in-progress).
- 2026-09-15: the wide c run: l63 (the table: 7 keys, 92 entries, 28 min) and l64 (render every definition from it: 7 rendered, all 7 lowered, read back and PROVED equal to their definition by the same text; 68 refused for want of a key). Then a defect found in the typing pass: Lean prints a long #check result over several lines and the one-line regex dropped every such candidate (the shifts, the multiplies, the 32-bit forms); fixed (continuation lines joined; a width written as literal arithmetic evaluated). Queued behind l65/l66: l67 (the gate again), l68 (the table again), l69 (the render again) (status: in-progress).
- 2026-09-15: queued after the two fixes (typing joined, own definitions unfolded): l73/l74 (c table and render again; l68/l69 withdrawn unrun), l71 (the tables of cpp, rust, go over every certified unit), l72 (their renders, lowered, proved, eye tables) (status: in-progress).
- 2026-09-15: correction: l68 (the c table after the typing fix alone) had already started when the withdrawal reached the queue; it runs to its end and its table is a data point for the typing fix; l69 was withdrawn unrun; l73/l74 follow with both fixes.
- 2026-09-15: the renders of cpp, rust, go (lane l72): cpp 13 rendered, 13 lowered, 13 proved; rust 17 rendered, 17 lowered, 16 proved (the one not proved, C_NOT from rust's `!`, is an immediate-carrying definition `equals` has no candidate for); go 3 rendered, 0 lowered: the carve ran past the return into the zero-word padding after the function, so the return was read as a body instruction. Fixed in `walk.carve` (trailing zero words dropped); lane l75 (go again) queued after l73/l74 (status: in-progress).
- 2026-09-15: the run over four languages complete (lanes l62-l75): c 346 meanings, 11 keys, 13 rendered, 13 proved; cpp 418, 11 keys, 13 rendered, 13 proved; rust 90, 12 keys, 17 rendered, 16 proved; go 21, 4 keys, 3 rendered, 2 proved. Log 284 holds the tables, the key matrix, the seven rules, the residues (status: done; the residues in §6 of log 284 awaiting the owner).
- 2026-09-15: correction of the row above: "the residues in §6 of log 284" — the first residue (multiply, divide, remainder with no equal 64-bit compiler-operator) was a defect of the candidate reader, not a residue; fixed by the flag rule (operator_for PROGRESS, gate l76/l77: MUL, DIV, REM render and prove by the same text). Log 284 §6a. The wide run with the rule is in progress (lanes from l78) (status: in-progress).
- 2026-09-15: the run over four languages again under the flag rule (lanes l78, l79, l80, l81; 3375 s, 191 s, 5436 s, 476 s; all exit 0). The corpora and their walks were not re-lowered; the candidate set was, at 186 subterms read at every value of a pure form's non-register parameters (120 typed at 64, against 136 / 97), and the definition arms at 93 (was 75). Final numbers, per language (compiler-operators / arch-units expressed in Lean / keys / entries / keys at 64 / rendered / lowered and read back / proved): c 750 / 346 / 23 / 163 / 15 / 18 / 18 / 18; cpp 1002 / 418 / 23 / 214 / 15 / 18 / 18 / 18; rust 858 / 90 / 17 / 48 / 12 / 18 / 18 / 17; go 744 / 21 / 5 / 8 / 4 / 4 / 4 / 3. Against the run of §2 of log 284: keys 11 -> 23, 11 -> 23, 12 -> 17, 4 -> 5; proved 13 -> 18, 13 -> 18, 16 -> 17, 2 -> 3, and nothing that proved before stopped proving. What is new and proved: the 64-bit multiply in all four languages, and the signed and unsigned divide and remainder in c and cpp — each rendered from an integer-level subterm of Sail's own definition, lowered at the language's ship flags, read back as the one instruction the definition names, and proved by the same text. What still does not prove: the eight W-form keys of c and cpp (in the table by proof, refused at render because their arch-units are held at 32-bit holders and the width rule wants the definition's operand widths — a width question for the plan, not a defect); `C_NOT` in rust and go (an immediate-carrying definition `equals` has no candidate for); the composite meanings (one definition applied to another, which no single subterm can be — `pass_c`'s object); and the 75 of 93 arms (89 for go) whose keys are bit manipulations, rotates, immediate forms, sign extensions and mixed-signedness multiplies. The summary and the key matrix are rebuilt by `runs/run_summary.py` into `runs/run_summary_2026-09-15_flag_rule.json` and `runs/key_matrix_2026-09-15_flag_rule.md`; the tables are log 284 §10 (status: done; the width question and the immediate residue in §10 awaiting the owner).
- 2026-09-15: the width rule by the definition's own use (the owner's decision of the day, answering the width question of log 284 §10): a definition's operand width, per read, is the width the definition's own subterm uses of that read, read off the key's own text (a read sliced from bit 0 by literal bounds is used at the slice's width, a read used whole at 64); `operator_for` records the widths beside each key, `render` takes an arch-unit whose holder widths equal them. Gate first (lane l82, 313 s): 12 arch-units expressed in Lean, 12 subterms filled (8 read at (32, 32)), 11 usable at render, 11 rendered, 11 lowered, 11 proved — the seven W forms among them, each from an arch-unit held at (32, 32). Then the wide run for the two languages with W-form keys (lanes l83, l84, l85, l86; 3375 s, 273 s, 4398 s, 270 s; all exit 0), per language (arch-units expressed in Lean / subterms filled / usable at render / rendered / lowered / proved): c 346 / 23 / 22 / 25 / 25 / 25 (was 346 / 23 / 15 / 18 / 18 / 18); cpp 418 / 23 / 22 / 25 / 25 / 25 (was 418 / 23 / 15 / 18 / 18 / 18). Newly proved in each: RTYPEW ADDW, SUBW, SLLW, SRAW, MULW, DIVW (signed), REMW (signed) — seven definitions, each lowered to one instruction and the return and proved by the same text; nothing that proved before stopped proving. Still refused: RTYPEW SRLW (its arch-units are held at 1-bit holders, none at (32, 32)); the unsigned 32-bit divide and remainder (no key). rust and go not re-run (their tables of record are the l80 ones; measured locally, the rule makes rust 17 of 17 keys usable and go 5 of 5). Spelling guard PASS on every JSON written. Log 284 §11 (status: done for c and cpp).
