---
id: hq.research.kind_fuzz_clustering.check
---

# CHECK — kind_fuzz_clustering

- [~] phase 6: RANGE / BOUNDARY — the value classes read as points on an axis, and the place between them located (2026-08-20, log 042, on the owner's ruling *"i do want things like the boundaries"*)
  - [x] 6a the TARGET LIST derived host-side from the stored answers, nothing re-run — `l3_boundary_targets.py` + `boundary_targets.json`, **8,067 adjacent pairs disagree, 1,623 already exact because their samples are adjacent integers, 6,444 with a gap to search** *(measured)*. Answer class is decision B1: outcome plus result type plus fidelity, where fidelity asks only whether the answer equals the exact result over the rationals and reads `na` for division, remainder, the shifts and any non-numeric operand. **Swift contributes zero targets** — its 1,066 codegen refusals remove the large literals before any pair can disagree
  - [x] 6b **DECISION B3 — the varying operand is a RUN TIME variable of the holder's own type, not a literal.** Sixty compiles per target is not a run; one compiled program per language is. The cost is named rather than hidden: a boundary that only constant folding would create is invisible, so **both endpoints are re-measured at run time** and any disagreement with the stored answer prints as `no move`
  - [x] 6c **DECISIONS B4 and B5 — the scope, with the remainder counted.** Phase A is the 2,798 targets whose fixed operand is also a whole holder. Deferred with reason: **3,445** fixed operand not a whole holder, **127** unbounded operation (`**`), **74** shift on a holder with no width. The first python run was OOM-killed before B5 existed, which is what produced the rule
  - [x] 6d **THE RUN — one lane, 8.8 s, eleven languages, one compile each, 96,474 probes, worst target 65 probes** *(measured)*: cpp 1,078 targets / 47,442 probes, java 280 / 7,084, python 270 / 5,684, php 240 / 7,316 (188 located, 52 no-move), csharp 202 / 3,206, rust 188 / 7,366, ruby 184 / 7,808, dart 154 / 5,646, go 82 / 2,346, kotlin 76 / 1,152 (68 located, 8 no-move), typescript 44 / 1,424. Smoke run first, six targets per language, twice; it caught that **this php has neither ext-gmp nor ext-bcmath**, so php's reference arithmetic is decimal strings written in the harness
  - [x] 6e **TEN OF ELEVEN LANGUAGES SHARE THE 2^63 WALL AND FAIL AT IT FOUR DIFFERENT WAYS** — go wraps, rust panics, php changes the answer's TYPE to `double`, python and ruby have no wall to reach. **Zero is a wall too**: every unsigned holder in go, c-sharp, rust and c++ has a boundary at the unsigned floor. 447 pinned walls in all, per holder, in `boundaries_<lang>.json`
  - [x] 6f **A WALL IN RESULT SPACE IS NOT A BOUNDARY IN OPERAND SPACE, and the operand boundary MOVES with the other operand** — `go.+` on int64 against 42 breaks at `2^63 - 43`, `go.*` on the same holders against the same 42 breaks at `ceil(2^63/42) = 219604096115589901`, which is not a power of two and not near one. **Only 447 of 2,738 located boundaries pin a power of two**; the other 2,178 sit at divisibility, at truthiness, at a shift width or at a holder's edge
  - [x] 6g **RUST'S SHIFT BOUNDARY FOLLOWS THE LEFT HOLDER AND IGNORES THE HOLDER OF THE SHIFT AMOUNT** — `i32 << i128` at 32, `i64 << i32` at 64, `i128 << i32` at 128 *(measured, 56 cells)*. The same operation with the same varying operand has three boundaries in one language depending on what sits opposite
  - [x] 6h **THE 2^53 EROSION IS REACHED THROUGH COMPARISONS, NOT ARITHMETIC** — 101 boundaries sit at 2^53 + 1 on the operand axis and they are `==`, `<` and their kin: cpp 47, ruby 20, dart 12, java 8, php 8, typescript 4, and one each in go, csharp and rust
  - [x] 6i **PHP ANSWERS A THREE-CLASS SPAN AND NOBODY ELSE DOES** — 44 php targets pass through `integer|exact`, then `double|exact`, then `double|inexact`. The promotion and the loss of exactness are two boundaries where every other language has one. All 44 in the run are php's
  - [x] 6j **607 BOUNDARIES ARE A HOLDER RUNNING OUT OF ROOM, 472 OF THEM ONE C++ HOLDER** — c++ accepts `int32_t v = 9223372036854775807;` with truncation, so those stored answers were never about the axis value: the class is named `i64max` and the holder holds `-1`. **A value class NAME can lie about what the holder holds**, and the rows are counted apart in `boundaries.md` so they cannot be read as arithmetic
  - [x] 6k **TWO OF THE SIXTY NO-MOVES ARE THIS NODE'S OWN INSTRUMENTS** — kotlin's 8 are `BigInteger` subtraction whose stored payload the host-side decoder in `l3_boundary_targets.py` reads wrongly, so those targets exist only because of the decoder; php's 52 are `int` comparisons at 2^63 and above, where the stored run compiled a literal php turned into a float and the re-measured run assigns an integer that saturates — **decision B3's named cost appearing exactly where it was predicted and nowhere else**
  - [ ] 6l **THE AXIS IS ONE-SIDED, SO ASYMMETRY IS UNMEASURED** (2026-08-20, log 042) — the whole-number value classes are 0, 42, 2^53+1, 2^63-1, 2^63, 2^64-1 and not one is negative. Whether a language's negative wall sits at `-2^63` exactly, or one short of it, is unverified, and no Hub sentence may read "±2^63" on this node's evidence. It is a gap in log 024's probe design, found by using it for something it was not built for, and it is the cheapest thing left to add
  - [ ] 6m the 3,646 deferred targets are NOT run (2026-08-20, log 042) — 3,445 of them have a fractional, text or container operand opposite the varying one, which needs rational rather than integer reference arithmetic in eleven harnesses; 127 are `**` and 74 are shifts on a holder with no width, both held out for unbounded cost. The kotlin decoder fault named in 6k should be repaired before that pass, since it manufactures targets
  - [ ] 6n the host-side decoder's big-integer reading is UNREPAIRED (2026-08-20, log 042) — `l3_boundary_targets.py` mis-reads kotlin's stored `BIGINT` payload, which `answers_encoding.md` says may be java's own byte array rather than sign-then-magnitude. Eight false targets are the whole measured consequence; whether it also mis-reads any dart or typescript bigint answer is unverified
- [~] phase 5: layer 3 extended to CONSTRUCTS — access / flow / binding, blessed by the owner 2026-08-19 (log 036)
  - [x] 5a design artifact written BEFORE generation — `Research/kind_fuzz_clustering/construct_design.md`, **16 numbered overturnable decisions**, the scaffold principle in the owner's own words, the slot table, the trace spec extending `answers_encoding.md`, and the derived probe count per language
  - [x] 5b the construct list DERIVED from each grammar's own kinds file, never hand-listed — `construct_catalogue.py` resolves a ROLE against candidate kind names and asks `kinds_<lang>.json` which it declares. **130 of 144 role slots resolved, 14 ABSENT.** Two kinds of absence kept apart and neither inferred from the other: go really has no `while` and no `try`, seven languages really have no slice syntax, while kotlin's `break`/`continue` and four of swift's roles exist in the language and are unnamed by the pinned grammar (swift declares 72 named kinds against c++'s 223)
  - [x] 5c the probe space DERIVED and PRINTED before anything ran — `construct_space.py`, **647,190 probes** across the twelve, about a ninth of the operator pass's 5.79M
  - [x] 5d the TRACE observation standard built and round-tripped — `PROBE_ID|TYPE|TRACE:<n>[...]` with joints `BR= IT= STOP= SKIP= BIND= THROW=`, every payload an `answers_encoding.md` encoding verbatim. **go's break traces and python's break traces are byte-identical**, produced by independently written encoders in two languages from one written spec
  - [x] 5e PROOF PASS, one executed language and one checked — python `kc_python.sh` **59,797 probes, 16.9 s, gate COMPLETE** (1,538 answers, 6,502 traces, 51,757 raises, 0 refusals); go `kg_go.sh` route A2 **1,710 probes, 26.8 s, gate COMPLETE** (104 accept, 1,606 refuse)
  - [x] 5f the other two route-C languages — ruby `kc_ruby.sh` **48,297 probes, 0.8 s, COMPLETE**; php `kc_php.sh` **33,660 of 33,680, 10.4 s, TWENTY SHORT** on uncatchable php fatals, each recorded in the lane log with the probe number it died on
  - [x] 5g three harness faults found by the proof pass and fixed — (i) go imports live at the FILE head and a duplicate import is also a refusal, two runs to clear; (ii) ruby evaluates `#{...}` when the DRIVER is parsed, so the first ruby lane died in 0.1 s having run nothing; (iii) php refuses a class declared twice and a php fatal is not catchable, the log 027 §5.1 fault met again in a new place, fixed the way java's was — the declared name suffixed per probe
  - [x] 5h **DECISION 16 — go's unpack cell is NOT_APPLICABLE with its reason** rather than faked. go's `expression_list` destructures a call's multiple returns, never a value; `_x, _y := a, a` would have measured the harness
  - [x] 5i the COMPLETENESS GATE built for constructs in `l3_construct_read.py`, and it caught its own fault: summing the closing summaries scored php short, because php restarts past a fatal and only its LAST run writes one. The plan now comes from the lane's own OPENING line
  - [x] 5j **the TRUTHINESS table exists as its own readable artifact** — `truthiness_table.md`, twelve languages by the eight forms, four filled in. **The four disagree completely**: go admits ONE form into a condition and refuses seven; ruby admits every form and answers `then` for all but nothing; python and php both split on emptiness and on zero, by different counts
  - [x] 5k **a `for` over 1, 2, 3 does NOT see the same trace in every language** — `trace_compare.md`. python, ruby and php walk 1, 2, 3; **go walks 0, 1, 2**, because go's one-variable `range` hands back the INDEX. Invisible to any observation that only recorded whether the loop finished. **break at k agrees in all four byte for byte**, which locates the disagreement precisely in how a language's loop reads a container and nowhere else
  - [x] 5l **THE EIGHT REMAINING CONSTRUCT LANES — DONE** (2026-08-19, log 037) — `l3_construct_lang.py` carries the two parts `HARVEST_constructs.md` said would change and reuses the rest, including the value encoders, which are lifted verbatim from `l3_exec.py` and were already run over the whole value matrix in logs 032 and 033. Two stages in one dropped lane: acceptance through each language's own proven instrument (rust `--emit=metadata`, c++ `-fsyntax-only`, swift the FULL compiler, dart `--format=machine` at severity ERROR only, and the in-process checkers for typescript, c-sharp, java and kotlin), then execution of the accepted set in chunks whose binary takes a start index. **21,342 probes, 1,757 accepted, 508 s, every lane COMPLETE**: typescript 2,287/174, csharp 3,850/269, rust 2,122/63, cpp 2,482/309, dart 2,685/426, java 4,298/269, swift 706/55, kotlin 2,912/192. Kotlin went through the warm-JVM route at **70 ms/probe** against a cold `kotlinc`'s 2.4 s
  - [x] 5n **a SMOKE RUN of about sixty probes per language caught a fault that would have published a language refusing everything** (2026-08-19, log 037) — typescript's acceptance route builds a ts Program with `types: []`, so the recorder's `process.stdout.write` had no declaration and **64 of 64 probes scored REFUSE**; one `declare var process: any;` line, which transpilation erases, took the same subset to 38 ACCEPT. The same smoke run surfaced a counting rule rather than a fault: a flow probe that recorded no joint wrote no row at all, so the trace dump now fires even when the trace is empty (decision 20) and every probe leaves exactly one row
  - [x] 5o **DECISIONS 17 to 20** (2026-08-19, log 037) — 17 the constructs get their OWN dendrogram, on a measured difference of grain, overturnable by one flag; 18 rust's `flow.try` is probed as `?` inside a closure returning an option, because `try_expression` is the kind the catalogue resolved; 19 java's `binding.unpack` is NOT APPLICABLE, since java's record pattern destructures inside `instanceof` and `switch` and never in a binding statement — the same standing go's unpack cell already has; 20 an empty trace is still written down
  - [x] 5p **THE TWELVE-LANGUAGE TRUTHINESS TABLE** (2026-08-19, log 037) — **seven languages refuse every non-boolean condition** (go, rust, swift, dart, csharp, kotlin, java) and **five coerce** (cpp, typescript, python, ruby, php), and the line is NOT the static-against-open line: c++ and typescript are statically checked and coerce. C++ and typescript coerce differently — c++ admits whole and fractional and refuses keyed and nesting outright, typescript admits all eight. The open three share exactly one cell, `nothing` to the else arm: **ruby sends every other form to then without exception** while python and php both split on zero and on emptiness
  - [x] 5q **the index-against-value split lands at exactly ONE language of twelve** (2026-08-19, log 037) — go, whose one-variable `range` hands back the INDEX, so its for-trace over 1, 2, 3 reads 0, 1, 2 while the other eleven read 1, 2, 3. **Typescript agrees on the values and disagrees on the type**: FLOAT:64 for all three, because a javascript number is a double. **Break at k agrees in SHAPE across all eleven languages that have a break**, differing only in integer width (32 bits in cpp, csharp, java, rust; 64 in the rest). Kotlin's row reads ABSENT rather than MISSING, because its grammar declares no break kind
  - [x] 5r **CONSTRUCT CLUSTERING BUILT AND SWEPT** (2026-08-19, log 037) — `l3_construct_cluster.py` + `clusters_constructs.json`: **149 signatures, 129 with a non-empty domain, 20 admitted-and-empty**, families access 13, flow 67, binding 49. Same method as logs 030/031/033 — jaccard on domains, average linkage, cut swept and never chosen. **No plateau is wider than 0.06**: 42 clusters over [0.81, 0.87], 27 over [0.53, 0.59], 17 over [0.39, 0.45], so log 030's no-elbow finding holds for the constructs too. **87 of the 129 leaves are ONE-SLOT and share no key space with an operator**, said rather than recorded as a zero; for the 42 two-slot leaves the nearest operator signature has **median jaccard exactly 1.000**, and 20 of 42 sit nearest an operator of their own language
  - [x] 5s the VISUAL built for the constructs (2026-08-19, log 037) — `Research/kind_fuzz_clustering/dendrogram_constructs.html`, 129 leaves, 257 nodes, same conventions and same template as the three dendrograms before it, self-contained. **Validated under `domstub.js`**: 1 script run, 0 threw, icicle and curve each append their drawing, `#count` reads 34 at the default 0.700. The stub also names three EMPTY PANELS which belong to log 033's page and do not exist here — a checklist mismatch, not a broken picture, and the stub was left alone because it is log 033's instrument. `dendrogram_all12.html` re-validated unchanged
  - [x] 5t **THE VALUE GRAIN FOR THE NINE CHECKED CONSTRUCTS — CLOSED, ALL NINE FOLDED** (2026-08-19, log 038 postscript) — the two lanes left running landed and were harvested by the six steps of `HARVEST_constructs.md` in order: kv_kotlin_00..08, **64,782 probes in 4,931 s at 76 ms a probe**, and kv_dart_04, **8,000 in 22 s**, taking dart to its full 51,867. Both stages ran in-lane, so no separate execution stage was owed. Kotlin's re-dropped shard 05 folded **once** — 8,000 rows, 8,000 distinct probe ids, checked against its frozen manifest and not against the two `.done` entries the re-drop left. **481,978 of 481,978 rows, 100 percent, all nine COMPLETE against the summed manifests and ZERO suspect rows**; 11,844 s of lane time over the 68 shards. With the three open languages the gate reads COMPLETE for all twelve, **623,752 rows**. **Kotlin's truth column moves `T`>`S` like the other eight, its other seven read `R`**, so log 038's finding is now complete without exception; movement over all nine is **1,045 of 22,972 holder keys, 4.5 percent**, up from 693 of 19,760 at eight-ninths. **Kotlin splits more keys than any other checked language, 331, and splits them somewhere else** — 159 on `+=` and 124 on `-=`, 283 of 331, where every other language's biggest splitter is `subscript` or `+=` alone; derived, `plusAssign`/`minusAssign` resolve on the receiver's declared type and that type changes with the value class. **Kotlin has no kotlin-only construct leaves**: elvis, `in` and `..` are operators, measured in the operator pass, and were never in this space. Re-folded at matched grain for all nine and DOM-stub validated — 1 script run, 0 threw, `#count` 34. Nothing pending in `HARVEST_constructs.md`
  - [x] 5t-partial (the mid-pass wording, kept for the record and now closed by 5t above) SEVEN OF NINE FOLDED, three lanes running (2026-08-19, log 038) — `l3_construct_value.py` reuses log 037's scaffolds, recorders, encoders and lane machinery untouched and changes only what fills a slot: every value class instead of the base, with the class named in the probe id per decision 14. **481,978 probes generated across the nine, 68 shards, 68 frozen manifests, and the 323-probe difference from the design's 482,301 fully accounted for** — 80 are `break`/`continue`, which open no operand slot and have no value grain, and 243 are go's and java's NOT-APPLICABLE unpack roles. **409,196 rows folded — 84.9 percent — every folded language COMPLETE, zero suspect rows**: typescript 40,700, csharp 85,587, java 89,698, rust 47,736, go 39,104, cpp 49,170, swift 13,334, dart 43,867 of 51,867; 6,919 s of lane time. **The holder grain was not merely coarse but WRONG**: 79 of the 226 holders of the nine change their DECLARED TYPE with the value class, so a verdict measured at one value was never a verdict for the holder. Still owed: kv_kotlin_00..08 (64,782, first shard running at 87 ms a probe, about two hours) and kv_dart_04 (8,000, thirty seconds) — queued and running, harvest steps in `Research/kind_fuzz_clustering/HARVEST_constructs.md`
  - [x] 5v **THE TRUTHINESS TABLE'S `T` WAS AN ARTIFACT OF THE BASE VALUE** (2026-08-19, log 038) — all eight folded checked languages read always-then for the truth form at the holder grain and **SPLIT** at the value grain, because a condition slot handed only `true` can only answer `then`. C++ and typescript, the two checked languages log 037 found to COERCE, moved in more columns than that one (cpp on whole and fractional, typescript on whole, fractional and text); the six refusing languages moved in exactly one and **could not have moved in any other**, since a form refused outright has nothing to split. Beyond the condition slot, **693 of 19,760 holder keys — 3.5 percent — hide a verdict that depends on the value**, and nothing at the holder grain said which
  - [x] 5w **break-at-k does NOT depend on the value, and structurally cannot** (2026-08-19, log 038) — `break` and `continue` open no operand slot, so they have no value grain at all; log 037's shape-agreement finding stands unchallenged. What does depend on the value is the loop around them, which the value grain could reach for the first time: **a while guard records either 0 steps or 9 and nothing between** in every language that has a while and admits more than one value into its guard, which is the truthiness finding seen from inside a loop and measured by different probes. A for-iterable's trace length is the container's length and the value class sets it; rust and c++ **disagree about the same text value class**, 2 steps against 1, a layer-1 disagreement surfacing through a layer-3 construct
  - [x] 5x **DECISION 21 — constructs and operators share ONE tree, divided by KEY SPACE rather than by grain** (2026-08-19, log 038) — decision 17's stated reason was the measured difference of grain, and this pass removes it for seven of the nine, so decision 17 is overturned for everything it covered. The second objection stands and was never about grain: an operator opens TWO operand positions and a one-slot construct opens ONE. `clusters_joint.json` — **284 leaves, 239 operator and 45 two-slot construct; 78 one-slot constructs keep their own tree**; nearest leaf to a two-slot construct is a CONSTRUCT 32 times and an operator 13, median jaccard 1.000, plateaus 75 clusters over [0.85, 0.90] and 10 over [0.24, 0.29]. The constructs' own tree re-folded at the value grain: 133 signatures, 123 non-empty, families access 18 / binding 54 / flow 51, **no non-degenerate plateau wider than 0.06**, so log 030's no-elbow finding holds again. Visual re-folded to 123 leaves / 245 nodes and **validated under `domstub.js`: 1 script run, 0 threw, `#count` 35 at 0.700**. **What is IN the picture: eight languages at the value grain and kotlin still at log 037's holder grain**
  - [x] 5y **two instrument faults, both of the "exit 0 is not evidence" family** (2026-08-19, log 038) — (i) the completeness gate takes its PLANNED count from the shard summary lines, so a shard that wrote an EMPTY file contributes zero planned and zero rows and the gate reads COMPLETE one shard short; the 68 frozen manifests are the independent check and dart was caught by exactly that. (ii) **a script sitting in the drop directory is not a queued script** — the daemon fires on a file EVENT, and ten shards sat unrun because their events had been spent on an earlier run that refused for want of scratch; re-writing each file in place fired them. A third, found and fixed inside this pass: `truthiness.json` is rewritten by `l3_construct_read.py` at whatever grain its LANES table names, so movement measured against it compares a run with ITSELF and prints nothing; log 037's baseline is now frozen as `truthiness_holder037.json`. Also from this pass, go's first value run is VOID and counted nowhere — its driver kept every probe's binary, filled the 4 GB scratch, and recorded 1,031 disk failures as language refusals which the gate read as COMPLETE
  - [ ] 5t-old (the log 037 wording, kept for the record and now answered by 5t above) the ANSWER GRAIN for the nine checked constructs is UNBUILT (2026-08-19, log 037) — this pass measured them at the HOLDER grain, every holder at its base value, which is 21,342 probes against the design's derived 624,166 for the full value matrix. Whether a construct's domain moves when the value varies is **unverified** for the nine, and it is exactly the question decision 9 answered for the operators by measuring it
  - [x] 5u **rust's four HARNESS REFUSALS — CLOSED, and they were TWO different things** (2026-08-19, log 038) — the messages were READ rather than guessed at. Two named the recorder's own encoder method, `the method 'db' exists for reference '&R3' but its trait bounds were not satisfied`, and are genuine harness refusals; they are repaired and `Kbinding.assign_19` and `Kbinding.assign_21` now record BIND traces across their value classes. Two named rust's own `Borrow` trait, `the trait bound 'String: Borrow<[i64]>' is not satisfied`, which is rust refusing to index a string-keyed map with a slice of whole numbers — **rust's trait, not the recorder's**. Log 037's classifier matched the bare token "trait bound", which is too wide, and swept them up; narrowed to the recorder's method name, **44 `Borrow` rows now read as what they are and rust's value-grain lane records ZERO harness refusals**. The two in `raw/kb_rust.txt` are left exactly as log 037 wrote them
  - [ ] 5u-old (the log 037 wording, kept for the record and now answered by 5u above) rust's four HARNESS REFUSALS are counted apart and not repaired (2026-08-19, log 037) — rust's encoder is a trait rather than a function over a dynamic value, so a holder whose type it has no impl for is refused with a message naming the recorder. **4 of 2,122 probes, 0.2 percent**; they are never read as findings about rust, and repairing them means adding impls to a measured encoder
  - [x] 5m-done **php's twenty fatals RECOVERED** (2026-08-19, log 037) — the messages were READ, not guessed: all twenty say `Cannot redeclare class`, which is the harness suffixing a declared class name per PROBE while a probe whose two slots are the same class-declaring holder declares it twice. The suffix is now per SLOT as well as per probe, the twenty ran one php process each so a residual fatal would still have cost one probe, **none died again**, and all twenty are the same holder paired with itself — which confirms the diagnosis from the other side. The recovered rows are folded BESIDE the original and `raw/kc_php.txt` is untouched. **php is now 33,680 of 33,680 and all twelve languages read COMPLETE**, 164,826 of 164,826 rows
  - [x] 5l-old (the log 036 wording, kept for the record and now answered by 5l above) the eight remaining construct lanes — UNBUILT, not running and not queued. Cost order and the four parts of a lane are in `Research/kind_fuzz_clustering/HARVEST_constructs.md`: typescript, csharp, kotlin, rust, cpp, dart, java, and swift last because swift needs the FULL compiler and not `-typecheck`
  - [x] 5m php's twenty fatals — RECOVERED, see 5m-done above; the original wording kept for the record: UNRECOVERED — re-running them one at a time would recover them; the gate says so rather than rounding it off
  - [ ] 5n the construct signatures are NOT yet clustered. They are written beside the operator signatures in the same shape — domain accepted plus answer per cell — so the clustering machinery can take them without changes; nothing has pointed it at them
- [x] phase 0: kind extraction + legal-pair table + probe-space size (2026-08-15, log 021)
- [x] phase 1: probe design, written 2026-08-17 as `Research/kind_fuzz_clustering/probe_design.md` (log 024 §2) — AWAITING the owner's review, which gates phase 3
- [x] phase 2: generate + run, one language (python, 2026-08-17: 20,024 probes, 64.5 s, log 024)
- [~] phase 3: generate + run, all twelve under the 2026-08-18 design (log 027)
  - [x] 3a instrumentation: shared progress module, [done/total] + elapsed + ETA (2026-08-18)
  - [x] 3b route A2 verification for kotlin, swift, dart, csharp — swift/dart PROVEN, csharp promoted to A1, kotlin A2 REFUTED with a measured 20x mitigation (2026-08-18)
  - [x] 3c bisect/batching BUILT and raced — answered AGAINST batching, per-file parallel wins 5.4x (2026-08-18)
  - [x] 3d generators: full ordered holder pairs x grammar operator menu, counts printed before running (2026-08-18)
  - [x] 3e routes A1/A2: ALL SEVEN acceptance runs complete (2026-08-18) — 71,313 probes, 5,842 accept, 65,471 refuse, 1,644 s; go 179, rust 236, cpp 1,102, swift 80, dart 1,170, csharp 1,127, kotlin 1,948
  - [x] 3e-B route B lift run (2026-08-18) — go map + java 86 rows extracted; rust part DID NOT RUN, `rust-src` absent in the container
  - [x] 3e-C route C ALL THREE complete (2026-08-18) — python 342,225 probes / 100,597 answers / 381.8 s, ruby 261,382 / 60,428 / 2.8 s, php 215,306 / 94,690 / 0.8 s; 818,913 probes, 0 refusals (the expected shape: open dispatch refuses nothing at compile time)
  - [x] 3i completeness gate built in `l3_read.py` (2026-08-18) — three lanes died mid-run and all three exited 0 (rust EBUSY, php class-redeclare fatal, python OOM); every result file now states `complete`
  - [x] 3f java and typescript route-A1 harnesses BUILT and RUN (2026-08-18, log 028) — java `javax.tools` 20,480 probes / 981 accepts / 3.41 ms, typescript shipped checker 12,167 / 2,318 / 0.34 ms; all twelve languages now have layer-3 acceptance evidence
  - [x] 3h route-B certification diffs RUN (2026-08-18, log 028) — rust **CERTIFIED** 490 of 490 cells after `rustup component add rust-src` and a 407-row mechanical expansion; java 785 of 931 agree with **146 findings**, all javac-ACCEPT against lift-REFUSE, cause measured as binary numeric promotion upstream of the table; go re-counted
  - [x] 3j the java 23-versus-86 row count RESOLVED (2026-08-18, log 028) — one table, 86 rows and 23 distinct ordered operand pairs; the A1 evidence gives seven distinct accepting pair sets over twenty operations, so the verdict is not a function of the pair alone and **86 is the count that governs acceptance**
  - [x] 3g full value matrix for the nine statically checked languages — **ALL NINE COMPLETE** (2026-08-18, log 030): 2,221,643 probes over 26 shard lanes, every one folded and every one passing its own `complete` gate. Splits per language: kotlin 1,234, typescript 876, java 759, csharp 718, dart 659, rust 136, go 103, cpp 94, swift 9. Earlier state below, kept for the record
  - [~] 3g-earlier full value matrix — BUILT, SIZED, SHARDED and RUNNING (2026-08-18, log 028): 2,221,643 probes, 26 shard lanes, 10.32 h projected, cost order, 5 shards landed at close-out. Harvest with `l3_matrix_read.py`; read `split_cells` first. **First complete fold REFUTES the value-independence assumption: typescript splits on 876 of 12,167 cells, all operation-level** (`false == false` accepts, `false == true` refuses — literal-type narrowing)
  - [x] 3l classify every split — DONE (2026-08-18, log 030): all nine `ld_<lang>_00.sh` load-check lanes run and folded, 1,046 probes, **zero unclassified**. Of 4,588 split cells, **3,267 are layer 2 leaking in (71 percent), 1,053 are layer 3 moving and 268 are mixed**. Typescript is the pure layer-3 case (876 of 876); dart is the pure layer-2 case (659 of 659). **Two of the nine load-check lanes were themselves void and were re-run** — go refused all 94 on `declared and not used` plus `"fmt" imported and not used`, both hard go errors caused by a body-less probe, and dart refused all 113 on the severity fault of 4g; after the fix go refuses 21 of 94 and dart 12 of 113 with `INTEGER_LITERAL_OUT_OF_RANGE`
  - [x] 3m `vm_dart_01` QUEUED AND LANDED (2026-08-18, log 030) — dart's matrix is complete; **all nine value matrices now pass the completeness gate**, 2,221,643 probes folded
  - [x] 3k **route C for the nine checked languages — DONE, ALL NINE** (2026-08-19, log 032) — the accepted subset only, emitted one probe per function into a small number of CHUNK files per language, one compile per chunk, run once: **170,419 probes, 162,853 answers, 6,020 raises, 480 deaths, 1,066 codegen refusals, 301.7 s of wall time**, every language passing the completeness gate. Answers are recorded as BITS and TYPE, never as printed text — two's complement bytes, IEEE bit patterns, byte length plus both length notions for text, containers recursively; the rule is `answers_encoding.md` and the products are `answers_<lang>.json`. Compilation is the cost in eight of nine, which is the whole point of the single-file design. Four mechanisms carry it: a START INDEX on every chunk binary so an uncatchable death (c++ `SIGFPE`, swift trap) costs ONE probe and not a chunk; a 90 s stall watchdog so a hang costs one probe too; a `// __PROBE__` marker per probe so a compiler that refuses the chunk NAMES the probes to drop and the chunk is rebuilt without them, which is a repair step and not a bisect; and exactly two allowed rust lints, `unconditional_panic` and `arithmetic_overflow`, both of which fire after the `--emit=metadata` stage every rust verdict was taken at
  - [x] 3k-1 **swift's type checker and swift's compiler disagree on 40.7 percent of swift's accepted set** (2026-08-19, log 032) — `swiftc -typecheck`, the instrument behind every swift verdict in this node, accepts `let b: Int = 9223372036854775808`; full compilation refuses it. **1,066 of 2,618 accepted probes are CODEGEN_REFUSE**, 1,032 of them arithmetic or literal overflow, each carrying the compiler's own message. The other eight languages recorded zero. Same shape as log 030's dart severity fault: the instrument was answering a slightly different question
  - [x] 3k-2 **kotlin ran twice and the first run is QUARANTINED** (2026-08-19, log 032) — kotlin's `..` returns a `LongRange`, a `LongRange` IS an `Iterable`, and the recorder walked it; one chunk spent nine minutes materialising a range. Fixed three ways (ranges tested before iterables, containers truncated past 1000, the stall watchdog) and a second fault found by diffing the two runs: java arrays were being recorded through `toString`, which embeds an **identity hash** and so differs between two runs of the same probe. `raw/SUPERSEDED_ex_kotlin_rangebug.txt` is kept and not folded
- [~] phase 4: relate + cluster; cross-check against kind_clustering (log 029)
  - [x] 4ww **THE SIGNATURES REBUILT AT THE VALUE GRAIN, ANSWER-VALUED ELEMENTS** (2026-08-20, log 043, on the owner's ruling *"each row isnt a row element. its a vector"*) — `l3_valuegrain.py` + `signatures_valuegrain.json`. An element is (lhs holder + value class, rhs holder + value class) → answer token, keyed by the input cell `form_a|vc_a|form_b|vc_b` so it crosses a language boundary through the layer-1 value classes decision 41 measured to be contained in one vocabulary. **The grid goes from 64 form pairs to 1,156 elements, and the agreement rate was ALREADY measured there** — only the ORDER was being built at the form grain, so no new data was needed. **239 leaves, not 238**: `python.@` raises on all 1,156 and a raise is a token, so an all-refusal vector is a vector where an empty domain was not a leaf at all. **121,100 elements spoken, 6,155 distinct tokens, 6,005 distinct element values, and 64,331 of the 121,100 (53.1 percent) are elements where a leaf's OWN HOLDERS disagree** *(all measured)* — which is why the grain is a choice and not a formality. Sparse format, documented in the artifact's own `format` field: three intern tables plus one `[cell id, value id]` list per leaf, an absent cell id meaning SILENT. 4.7 MB
  - [x] 4xx **THE TWO ORDERS COLLAPSE INTO ONE, AND `php.==` SITS ABOVE NOTHING** (2026-08-20, log 043) — `l3_dominance_vg.py` + `dominance_valuegrain.json`. X ≤ Y when Y carries the SAME element value as X on EVERY element X speaks on. **Containment-without-faithfulness has no referent**: there is no second order for it to live in, and god status is impossible by construction because a leaf gets above another only by repeating it. Checked rather than asserted: **15 super-chains of length two, no transitivity failure**. **`php.==` speaks on 1,156 of 1,156 elements, 160 leaves ask only questions it also answers, and it CONTRADICTS all 160, so it sits above ZERO** *(measured)*. The first denial is `dart.==` at `fractional|base_1_5|text|base_empty` — dart answers `truth:false`, php answers `truth:false` on some holder pairs and `raise:Error` on others. Nothing in the machinery knows which leaf `php.==` is
  - [x] 4yy **THE DOMINANCE RERUN AGAINST LOG 041: 209 MAXIMAL OF 239 ON 202 FAITHFUL EDGES OF 18,479** (2026-08-20, log 043) — against log 041's 184 of 238 on 189 of 13,831. **1.09 percent of nesting pairs survive the answers, against the form grain's 1.4** *(derived)*. The nesting count rose because speaking sets are finer than domains and decision 55's support bar is gone; the faithful count barely moved; the maximal SHARE went from 77.3 to 87.4 percent. Per family, **no dominant vector anywhere, same as log 041**, but two families moved hard: **arithmetic collapsed to 59 maximal of 60 on 2 faithful edges of 1,303**, which says its form-grain agreements were about which forms were accepted and not about what was computed; **logical REVERSED** from log 041's 36-of-36 to 24 of 36 on 108 faithful edges of 434, because a four-element short-circuit pair fell under decision 55 and can now be read. **Shift and other have ZERO faithful edges.** **218 distinct vectors over 239 leaves and thirteen exact identities**, headed by `csharp.&&` = `go.&&` = `java.&&` = `kotlin.&&` = `rust.&&` = `swift.&&` and the same six for `||` — an identity rather than a similarity score, on **four elements each**, which travels with it. `java.>>` with `java.>>>` is a PROBE-SPACE GAP named as one: no probe value made the sign bit matter
  - [x] 4zz **DECISIONS 58 to 61, two FLAGGED for the owner with their cost measured** (2026-08-20, log 043) — **58** the element key is the input cell and not the holder, because holders are language-private and a holder key would make every cross-language element distinct, producing a result from the key rather than the data. **60** a backend refusal is the token `refuse:codegen`: a row that is ABSENT was never asked, a row that says CODEGEN_REFUSE was asked and declined. **61, FLAGGED — the grain.** Holder-pair (element value is the whole token set over the leaf's holder pairs, matched by EQUALITY) is the default, for three stated reasons: it is the literal reading of the owner's sentence, it is the stricter of the two so nothing it certifies would be uncertified by the other, and intersection carries no guarantee of transitivity — though **on this data it does not fail, 0 failures, measured**. Cost: **form-pair would give 349 faithful edges and 172 maximal against 202 and 209, a difference of 147 edges and 37 leaves**, and neither grain gives any family a single top. **59, FLAGGED — refusal against raise.** Silence is FREE (permitted widening), because a leaf is silent where nobody ever ran it and absence of a claim cannot contradict a claim; a raise, a death and a backend refusal are CLAIMS and must be matched. Cost measured: reading a raise as silence gives **455 faithful edges and 205 maximal against 202 and 209**, so the decision is worth a great deal to the edges and almost nothing to the headline — only 53 of 6,005 element values are wholly a claim of refusal. **Decision 55 does NOT apply and is not silently reused**: the merged order computes no rate, so there is no thin rate to guard; instead every table carries the elements-spoken count beside the verdict
  - [x] 4ab **THE HUB-FACING UNION CANDIDATES: 14 WITHIN A FAMILY AGAINST 5,092 BLOCKED** (2026-08-20, log 043) — `make_union_candidates.py` + `union_candidates.md`. Two maximal leaves can be laid on top of one another when they never carry different values on an element both speak on; compatibility is pairwise and that is SUFFICIENT for a group of any size, so the assemblies are the maximal cliques of the compatibility graph, enumerated exactly by Bron-Kerbosch. Agreeing edges (they share elements and agree) are kept apart from VACUOUS ones (they share none and cannot contradict, but nothing was measured either). **Down from log 041's 31, with ten of the fourteen in the logical family and four of the fourteen cross-language — all four on FOUR shared elements, `cpp.&&` with `dart.&&` and its co-spellings.** **Arithmetic offers NOTHING: 59 maximal leaves, 1,711 pairs, every one blocked, no assembly of two exists.** **Log 041's headline candidate does not survive**: `dart.==` with `php.===` share 1,089 elements and differ on 660 *(measured)*, blocked at the same element that denies `php.==` the top — invisible at the form grain, where both leaves merely "accepted `fractional|text`". Ten of the fourteen are same-language spelling pairs, which are logs 034 and 035's positive controls turning up unasked
  - [x] 4ac **THE VALUE-GRAIN VISUAL** (2026-08-20, log 043) — `make_dominance_lattice_vg.py` + `dominance_lattice_vg.html`, a Hasse diagram per family over the 218 VECTOR CLASSES rather than domain classes, height in elements spoken. **Every line in it is faithful by construction, so log 041's red edges are ABSENT rather than recoloured**, and each family panel carries the count of nesting pairs the answers threw out so a reader is told why the picture is sparse. **DOM-stub validated: 1 script run, 0 threw, 8 panels filled, the lattice appended, exit status 0.** `dominance_lattice.html`, `dominance_lattice.json` and `l3_dominance.py` are untouched, so the two readings can be set against each other
  - [ ] 4ad **DECISION 61 IS DEE'S TO RULE and it is worth 147 edges** (2026-08-20, log 043) — holder-pair against form-pair. Both are computed on every run and published, and the log takes neither as settled. Also open and stated rather than dropped: the constructs are not carried into the value-grain order, the same gap log 041's 4vv records, and `construct_answers_<lang>.json` holds what would close it
  - [x] 4oo **THE PRODUCT METRIC'S THREE ALTERNATIVE READINGS BUILT, EACH AS ITS OWN VISUAL** (2026-08-20, log 041, on the owner's ruling) — `l3_alt_readings.py` re-reads `clusters_all12.json` and nothing else, so J and A are the SAME numbers the product tree stands on and only the way they are combined changes. **AGREEMENT ONLY** (similarity = A) `clusters_agreement.json` + `dendrogram_agreement.html`, widest non-degenerate plateau [0.114, 0.189) → 29 clusters; **CONTAINMENT WEIGHTED** (similarity = C x A, C = shared cells over the SMALLER domain) `clusters_containment.json` + `dendrogram_containment.html`, [0.010, 0.077) → 4; **DOMAIN ONLY** (similarity = J) `clusters_domain.json` + `dendrogram_domain.html`, [0.060, 0.129) → 2. Same 238 leaves, same average linkage, same tie-breaking, no cut chosen anywhere. **No probe ran and no lane ran.** All three DOM-stub validated: 2 scripts each, 0 threw, five panels filled, icicle and curve each appending, and each page's own cluster-count self-check silent
  - [x] 4pp **THE MACHINERY HAS A POSITIVE CONTROL AND IT PASSES** (2026-08-20, log 041) — the domain-only reading is a tree log 033 already computed as its `control_domain_only` summary, so if the new linkage is the old linkage the plateau list must come out identical. **It reproduces all twelve published plateaus, bound for bound and count for count** *(measured)*, and the script ASSERTS it rather than printing it, so a future tie-breaking drift stops the run. The first attempt did NOT reproduce it — a new node was being kept in place of its left sub-node instead of appended at the end of the live list, which changes nothing except which of many exact ties wins, and in a matrix carrying bulk 1.000s and 0.000s that was enough to move the root merge from 0.060 to 0.105
  - [x] 4qq **DECISION 55 — the support threshold, 32 shared input cells, with its cost stated** (2026-08-20, log 041) — an agreement rate over three cells is not a rate. **5,633 of 28,203 pairs (20.0 percent) fall below the bar and 1,264 of those share no input cell at all**; a below-bar pair is left OUT of every average rather than scored zero, because zero would claim the two never agree and that is precisely what was not measured. 22 of 237 merges are made with no supported pair beneath them and are counted as such. Sensitivity printed on every run: 3,674 excluded at 16, 10,230 at 64. **The cost is named rather than hidden** — `go.&&` against `java.&&` has agreement exactly 1.000 over FOUR shared cells and `go.<<` against `java.<<` over sixteen, so the agreement tree places both apart where the product tree, which reads every rate however thin, puts them together
  - [x] 4rr **THE SURPRISING LANGUAGE SORTING IS THE DOMAIN FACTOR, NOT THE ANSWERS** (2026-08-20, log 041) — compared at MATCHED GRANULARITY under log 040's decision 51, cut to 21 groups each: the **domain-only** tree sorts by language MORE strongly than the product (same-language rate 0.246 against 0.223) and by operator family much LESS (0.418 against 0.674), while **containment** carries the highest family sorting of all four (0.703) on the lowest language sorting (0.128) *(all measured)*. **The agreement tree cannot be cut at 21 at all**: 24 of its merges sit at similarity exactly 0.000, so it steps from 25 groups straight to 1 and stands in nothing between. At 25 it holds **214 of 238 leaves in one group** with 24 singletons, and **sixteen of the 24 are a short-circuit `&&` or `||`** — starved of shared cells by decision 55, not disagreeing. `go.+` with `java.+` and `go.+` with `cpp.+` are apart under product and domain-only and TOGETHER under agreement and containment; `java.+` with `cpp.+` stays apart under all but the two answer-blind readings, and it should, since C is 0.500 and A is 0.586 there
  - [x] 4ss **DEE'S DOMINANCE PARADIGM BUILT, AND THE ANSWER IS NO** (2026-08-20, log 041) — `l3_dominance.py` + `dominance_lattice.json` + `dominance_lattice.html`. **The CONTAINMENT order flattens**: 238 leaves carry 59 distinct domains and there is **exactly ONE maximal element**, a class of all 64 cells held by 35 leaves (fifteen php, eight ruby, six python, three typescript, two dart, one kotlin), with **six of the twelve languages putting nothing in it** — c++, c-sharp, go, java, rust and swift. That answer costs nothing, because a domain that accepts everything contains everything without earning it. **The DOMINANCE order does NOT flatten**: under decision 56, **184 of 238 leaves are MAXIMAL** where a dominant vector would leave exactly one, and of **13,831 supported strict containments only 189 never disagree once — 1.4 percent** *(derived)*. **No family flattens**, and the logical family fails as hard as it can: 36 leaves, 36 maximal. Three families do flatten by containment (comparison, logical, other, all at 64 cells) and three do not (arithmetic 6 maximal classes, bitwise 4, shift 2); arithmetic's union runs to 61 of 64 while its widest single domain reaches 38, so **three cells of the grid are refused by every arithmetic operation in every one of the twelve**
  - [x] 4tt **DECISIONS 56 and 57, and the union-versus-fork short list** (2026-08-20, log 041) — **56**: dominance is strict containment PLUS support PLUS an agreement rate of exactly 1.000, because the CORE's own wording for `nests` is "same answers, wider domain" and one disagreeing cell is a measured counter-example; the looser 0.99 reading is computed beside it and is worth eight leaves (176 against 184), so the strictness has a price and it is small. **57**: the lattice is drawn over the 59 DOMAIN CLASSES and per family, since a leaf-wise drawing would stack 179 nodes on others with identical edges, while the dominance order is computed over LEAVES always, because two leaves with one domain can answer differently. **The UNION VECTOR is the whole 64-cell grid and that is not news** — 35 leaves already carry it alone. The news is beneath it: **31 within-family union candidates against 179 forks**, headed by **`dart.==` with `php.===` agreeing on all 64 cells** — two languages sharing no runtime, no type discipline and no lineage — and the widest forks are every pairing of a three-way `<=>` against an equality at agreement 0.000, which is log 033's and log 035's contradictions **re-derived by an analysis that was told nothing about them**
  - [x] 4uu **THE TWO-TREE READING GIVES A MEASURED NESTS LIST, 13,204 CASES** (2026-08-20, log 041) — the agreement tree beside the domain tree, both cut to 21 groups: **20,593 pairs placed differently, 13,204 of them a true domain containment**, and the asymmetry is the shape of it — 20,355 together only in the agreement tree against 238 only in the domain tree, because agreement does not care how wide either domain is. Every top row has agreement exactly 1.000 and a strict containment, so every one is a `nests` case in the CORE's sense: `csharp.==` inside `python.==` at 322 shared input cells with a product of 0.406, `csharp.!=` inside `typescript.!==` at 310 with 0.619, `cpp.==` inside `python.==` at 282 with 0.312, `go.==` inside `dart.==` at 181 with 0.281. **`cpp.not_eq` appears beside `cpp.!=` on that list**, which is log 034's positive control turning up unasked. Stated with the finding: a `nests` relation here is measured over the cells the NARROWER operation accepts and says nothing about the cells it refuses
  - [ ] 4vv the alternative readings are OPERATORS ONLY, and the joint construct tree is not attempted (2026-08-20, log 041) — the three distances need an agreement rate on shared input cells and `clusters_joint.json` carries J and no A, because decision 21 built it to answer a key-space question. **Said rather than quietly dropped.** The construct answers exist in `construct_answers_<lang>.json`, so an agreement rate over the 45 two-slot construct leaves is one pass; whether it is worth taking is the owner's call
  - [x] 4a **manifests FROZEN, all twelve** (2026-08-18, log 029) — `manifest_<lang>.json` replays the generator's enumeration with no probes run, mapping each positional probe id to operation + both operands' form, holder, value class and literal declaration text; in-flight runs (cpp, kotlin) got theirs too
  - [x] 4b manifests VERIFIED three ways (2026-08-18, log 029) — every landed shard's line count matches; **520 of 520 spot-checked ids present**, 449 of the 460 grid-comparable agreeing and **all 11 differences confirmed to sit inside measured `split` cells**; and, independently of the enumeration replay, **all nine route-A manifests agree with their lane script's own embedded frozen table on operations, holders and value classes, zero mismatches**
  - [x] 4c value-matrix harvest under the completeness gate (2026-08-18, log 029) — 23 of 26 shards landed; **seven matrices now COMPLETE** (typescript, csharp, java, rust, go, swift, kotlin — kotlin's last shard landed mid-session), two refused by the gate (cpp, dart) and contributing nothing
  - [x] 4d **PRELIMINARY clustering built and run** (2026-08-18, log 029) — `clusters_preliminary.json` + `clusters_preliminary.md`: 233 operation signatures over twelve languages projected onto the shared 64-cell layer-1 form space, 46 clusters at the 0.70 cut / 58 at 0.85, **2,909 relation edges** (1,009 nested_by, 687 equals, 618 overlaps, 588 nests, 7 contradicts), eleven numbered overturnable decisions
  - [x] 4e **the shift mixed-holder exemption REDISCOVERED, not hard-coded** (2026-08-18, log 029) — a statistic naming no operation and no language ranks `>>>` 0.80, `<<`/`>>` 0.619 above `+` 0.482, `-` 0.384, `*` 0.325. **And it corrects the claim**: the exemption is total in go and rust only (shifts 0.75 against arithmetic 0.00–0.14) and barely measurable in java (0.80 against 0.78), c++ and c-sharp, where implicit numeric promotion already mixes arithmetic holders
  - [x] 4f the acceptance grid's error bar MEASURED for seven languages (2026-08-18, log 029) — go 0 of 7,600 cells wrong, kotlin 150 of 11,492, typescript 704 of 12,167 (5.8 percent); kotlin carries the most value-dependent cells measured, 1,234 splits of 11,492 (10.7 percent). C++ and dart have no error bar
  - [x] 4g **dart `||` DIAGNOSED AND FIXED, not flagged** (2026-08-18, log 030) — the dart lane scraped `dart analyze` at EVERY severity and recorded no detail, so `dead_code` (severity **WARNING**) on the right operand of `true || b` and `false && b` was read as a type refusal; the one-value-per-holder grid drew `true`, the dead-code case for `||` and the live case for `&&`. Confirmed verbatim on four isolated probes. Fixed to `--format=machine`, severity ERROR only — which makes dart CONSISTENT with the other eight, since g++/javac/rustc do not fail on warnings either. Dart re-run whole: **50,381 of 217,073 matrix probes moved REFUSE→ACCEPT and ZERO moved the other way**; `&&|3|3` and `|||3|3` are now uniform ACCEPT
  - [x] 4l **ALL SEVEN `contradicts` EDGES ARE GONE** (2026-08-18, log 030) — the final pass has **zero**. Every one was the dart lint. So this node has not measured a single genuine cross-language contradiction at acceptance grain; the CORE's answer-grain `contradicts` remains untested and is gated on 3k
  - [x] 4m **the completeness gate CANNOT see an out-of-scratch lane** (2026-08-18, log 030) — found while chasing an incoherent dart cell. `/work` (4 GB tmpfs) was holding 3.7 GB of phase-3 leftovers; the lane could not write its probe sources, a file that was never written draws no diagnostic, and the harness scores "no diagnostic" as ACCEPT. The lane **exited 0, printed its `__SUMMARY__` and reported its full probe count**. 44,268 `Errno 28` lines in its log. Swept, re-run clean, and `vm_dartfix_00` re-run on clean scratch as a control **reproduced its 17,101 accepts exactly**. Every other vm/ac/ld lane log in the node scanned: **exactly one file matches, the void run itself**. Standing rule proposed: grep a lane's log for ENOSPC before folding its product
  - [x] 4h phase 4 re-run as NON-preliminary (2026-08-18, log 030) — decision 9's promotion FIRED: the value matrix is now the PRIMARY domain for the nine checked languages under decision 10, python/ruby/php stay route-C. **23 of 233 signatures moved domain, every one a widening, none narrowed, none lost.** Typescript's four equality spellings gained the same 14 `keyed`/`nesting` cells each; rust's seven relationals gained exactly `nothing|sequence` and `sequence|nothing`; go, cpp and java did not move one cell. `clusters_final.json` supersedes `clusters_preliminary.json`
  - [x] 4n **THRESHOLD SWEEP, no chosen cut** (2026-08-18, log 030, on the owner's ruling) — the full merge history is computed (228 merges over 229 leaves, average linkage, monotone so no inversions) and the cluster count at any threshold follows exactly from it; verified against an independent tree walk at all 167 breakpoints, **zero mismatches**. The curve has **no elbow**, which is the direct reason no cut can be picked from the data. Widest non-degenerate plateau **[0.137, 0.196) → THREE clusters**, and they are not by language and not by operator but by WIDTH: 147 permissive (median domain 18 of 64), 51 strict (median 3), 31 integer-and-truth-only spelled `% & && << >> ^ | ||` (median 2). Log 029's 0.70 sits on the upper edge of the [0.667, 0.700) → 44 plateau, so it was an unmarked reading rather than a bad one. **229 signatures collapse to 61 exact distinct domains**
  - [x] 4o the named stories tracked across the sweep (2026-08-18, log 030) — log 029's cluster 5 **survives and is stronger than log 029 could say**: its twelve members have IDENTICAL domains, the single cell `whole|whole`, pure to threshold 1.000 and holding to 0.500. Cluster 6 survives, peaking at all ten at 0.800. **`+` never forms a pure cluster larger than TWO at any threshold in the entire sweep**, and **`==` never forms one at all** — a strictly stronger statement than log 029's "`+` does not cluster together". The shift spellings alone never exceed two either: shifts do not form a shift cluster, the bitwise operations carry them
  - [x] 4p the VISUAL built (2026-08-18, log 030) — `Research/kind_fuzz_clustering/dendrogram_sweep.html`, a copy-and-adapt of `kind_signature_clustering/dendrogram_explorer.html`: same icicle rendering, same draggable threshold line, same live cluster count, same yellow outline on current clusters, same search and hover card. All 229 leaves labelled `language.operation`, axis is similarity, plus a cluster-count-versus-threshold panel with the plateaus shaded. **Self-contained — no d3, no CDN, opens from `file://`**, unlike the earlier explorer
  - [x] 4g-earlier **dart `||` is a probable harness fault, NOT reconciled** (2026-08-18, log 029) — all 7 `contradicts` edges involve it; dart records `bool && bool` ACCEPT and `bool || bool` REFUSE with an empty error detail. Found by the relation machinery unprompted; recorded as a finding, cause **unverified**
  - [x] 4h-earlier re-run phase 4 as NON-preliminary when the cpp and dart matrices land — DONE, see 4h above — decision 9 says the overlay then BECOMES the primary domain for the nine checked languages
  - [x] 4k the shift tie CANNOT be broken by any run in flight (2026-08-18, log 029) — kotlin and swift have **no shift spellings in their grammars at all** (kotlin spells them `shl`/`shr`, builtins and out of scope by the CORE's own rule), and cpp's mixed-holder row already exists because the statistic runs off the acceptance grid, not the matrix. Go-and-rust against java-and-cpp stands two-all; settling it needs more LANGUAGES, not more values
  - [x] 4q **ANSWER-GRAIN CLUSTERING BUILT AND RUN** (2026-08-19, log 031) — `l3_answers.py` + `clusters_answers.json`: 69 signatures over python, ruby and php, distance = Jaccard on domains **times** the agreement rate on shared accepted input cells (decision 27), full merge history, no chosen cut. **Decisions 18 to 30**, every canonicalization reversible with the pre-canonical raws kept. Widest plateau **[0.601, 0.700) → 45 clusters**; the domain-only CONTROL on the same 69 leaves gives **one cluster of 68** over [0.095, 0.194) and **25 distinct classes at threshold 1.000 against the answers' 68**
  - [x] 4r **the answers re-sort the logicals by what they RETURN** (2026-08-19, log 031) — `python.and` with `ruby.&&` and `python.or` with `ruby.||` (operand-returning) against `php.&&` and `php.||` sitting with `php.!=`, `php.<`, `python.!=`, `python.is not` and `ruby.!=` (boolean-returning). The bitwise operators form **no** cross-language group at any threshold, which points the other way from log 030's shift finding
  - [x] 4s **the CORE's `contradicts` arm TESTED at the answer grain, and it answers with a RATE not a boolean** (2026-08-19, log 031) — 55 same-spelling language pairs sharing 19,090 input cells: **zero disagree on every shared cell; 44 of 55 disagree on some cell; 11 agree perfectly**. `php.||` vs `ruby.||` 1,118 of 1,156 (96.7 percent), `php.+` vs `python.+` 190 of 365 (52.0). Worked examples with the actual answers in log 031 §5
  - [x] 4t **integer overflow is the fracture that fractured; the others did not** (2026-08-19, log 031) — `i64max + 42` gives `whole:9223372036854775849` in python and ruby against `fractional:9.2233720368548e+18` in php; `+` on `whole|whole` agrees 1.000 python-with-ruby and 0.306 for php against either. **2^53+1, −0.0, NaN arithmetic, NaN equality, inf-against-NaN and e-acute equality all AGREE** across the three. python's own `ctypes.c_int64` holder contradicts its `int` holder on `u64max + 42` (41 against 18446744073709551657), kept visible by decision 26's split marker
  - [x] 4u **php's `and`, `or` and `xor` are VOID — a harness precedence fault the answer grain caught** (2026-08-19, log 031, decision 30) — `$__r = ($a) $op ($b);` parses as `($__r = ($a)) and ($b)` because php's word logicals bind looser than assignment, so all **6,241 answer cells record the LEFT OPERAND**. Invisible to every acceptance pass, since a wrong answer is still an ACCEPT. Excluded from the clustering, kept in the artifact with the diagnosis; `&&` and `||` are unaffected. Fix is one pair of parentheses in `l3_routec.py` plus a re-run
  - [x] 4v the VISUAL extended to the answer grain (2026-08-19, log 031) — `Research/kind_fuzz_clustering/dendrogram_answers.html`, same conventions as `dendrogram_sweep.html` (icicle, similarity axis, draggable threshold line, live count, yellow outline, search, hover card, shaded plateau curve) plus two panels the domain-grain file had no data for: the same-spelling agreement table and the known-fracture table. **Self-contained, opens from `file://`**
  - [x] 4i **ANSWER GRAIN FOR ALL TWELVE — DONE** (2026-08-19, log 033) — `l3_answers12.py` + `clusters_all12.json`: **226 leaves over twelve languages, 111,410 input cells, 399,845 value answers, 549,231 raises, 480 deaths**. The two answer encodings are bridged as **decisions 31 to 42**, all overturnable by a re-read: integers bridge by value with width dropped, floats keep bits AND a 14-digit shadow used only where a print-grain language is in the pair (**13,179 of 25,425 edges**), text bridges straight because decision 24 already chose bytes, a **raise and a death are answer classes** (decision 31), swift's 1,066 `CODEGEN_REFUSE` rows are **excluded with their count** (decision 40). **457 canonical tokens are reached from both a printed and a bit pre-form.** Widest plateau **[0.465, 0.499) → 80 clusters**; the domain-only CONTROL on the same leaves gives **two clusters** over [0.059, 0.130) and **58 distinct classes at threshold 1.000 against the answers' 195**
  - [x] 4w **the CORE's `contradicts` arm has its FIRST instances, and they cross the static/open divide** (2026-08-19, log 033) — 974 same-spelling language pairs, **3 disagree on every shared cell**: `cpp.<=>` vs `php.<=>` and vs `ruby.<=>` at 211 of 211, `kotlin...` vs `rust...` at 112 of 112. **631 of 974 disagree somewhere and 343 agree everywhere.** Cell-level disagreement rate 0.460 open-with-open, 0.283 static-with-open, 0.130 static-with-static. Every total pair is a kind mismatch that **decision 39 keeps apart**, stated so overturning it is a known move. **Decision 31 changed a quantity log 031 published** — `php.+` vs `python.+` was 0.480 there and is 0.945 here, because raises now agree with raises; the two logs' A factors are not the same measurement
  - [x] 4x **the named stories re-read at twelve** (2026-08-19, log 033) — **`+` splits into four families** and the answers name the principle: the five checked languages whose `+` concatenates a string, `go.+` with `rust.+`, `php.+` with `python.+`, `ruby.+` alone; dart has no `+` leaf. **The five bit-identical wrap languages never cluster** — `go.+` pairs with panicking `rust.+` at 0.907 on an identical domain while `go.+` vs `java.+` is 0.094 with A exactly 1.000. **`dart./` and `typescript./` are at similarity 1.000** and sit with `java./` and `kotlin./`. The shift group **returns**, against log 031's three-language reading: dart, go, java, ruby and rust shifts group and hold from [0.085, 0.104) up
  - [x] 4y the VISUAL extended to twelve (2026-08-19, log 033) — `Research/kind_fuzz_clustering/dendrogram_all12.html`, same conventions as `dendrogram_sweep.html` and `dendrogram_answers.html`, 226 leaves labelled `language.operation`; the three-column agreement table becomes a twelve-by-twelve matrix with a per-operation roll-up beside it, and the fracture table carries twelve columns. **Validated under a DOM stub** (`domstub.js`): both scripts run with zero throws, icicle and curve each append their drawing, `#count` reads 115 at the default 0.700 which matches the [0.697, 0.730) plateau exactly
  - [ ] 4nn `clusters_all12.json` is NOT byte-reproducible between two runs, in two DISPLAY fields only (2026-08-19, log 035) — chased rather than shrugged at: the differences are confined to `answer_classes`'s `top` (648 same-set-order-only, 232 a four-way truncation tie) and `kinds` (299), because both are sorted by count with ties broken by the iteration order of a SET. **`tokens`, `shape`, every domain, every edge, the whole merge history, every plateau, every snapshot and every contradiction are identical, and `dendrogram_all12.html` comes out BYTE-IDENTICAL** *(measured)*. A presentation fault, not a measurement fault; recorded and not fixed, since fixing it means changing a sort key this pass had no other reason to touch
  - [x] 4j **THE CROSS-CHECK IS DONE — the symmetry HOLDS for the operators and is ABSENT for the constructs, and the cause is measured** (2026-08-20, log 040) — `xcheck_signature.py` + `xcheck_compare.py` + `xcheck_signature.json`. The mapping is stated as **decisions 45 to 54**, all overturnable, and what could not be carried is named on both sides rather than dropped: **212 of 238 operator leaves and 122 of 123 construct leaves reach a declared host kind**, while **1,728 named kinds inside the twelve grammars are reached by no behavior leaf at all**. Compared at MATCHED cluster counts over the whole sweep, never at one cut, with each population's headline read at the behavior tree's own widest plateau. **Operators at 21 groups: agreement 0.8795, ARI +0.1193, against a 2,000-draw permutation null with mean 0.0002 whose largest draw was 0.0343 — real and weak, p < 0.0005**; ARI peaks at **+0.1638 at 14 groups**. **Constructs at 12 groups: agreement 0.6365, ARI −0.0057, p 0.552 — not distinguishable from unrelated**, and unstable near zero (rounding the distances to six places moves it to +0.0079). **THE CAUSE: over the 212 mapped operator leaves the declared-signature source holds exactly TWELVE distinct feature vectors and not one is shared across two languages**, against the behavior source's 183 — the declaration's whole operator vocabulary is one word per language. **So every one of the 861 type-A disagreements is a BLINDNESS and not a contrary claim**: all 861 sit at declared distance exactly 0.000000 and all 861 are within one language. **Of the 1,833 type-B disagreements, 1,578 cross a language and 36 sit at behavior similarity 0.999 or better** — the unification short list, headed by `php.===` with `python.is` at declared distance 0.669421 and behavior agreement 0.999135 over 1,156 shared cells. Worked type-A case: `cpp.!=` against `cpp.+`, one feature vector, **0.0000 agreement over 232 shared cells**. Per family the strongest is bitwise (ARI +0.4697, carried by the c++ word spellings) and the weakest is access (agreement 0.3676, ARI −0.0334). **Leaf ordering was VERIFIED before use, not assumed** — 237 of 237 operator merges reproduce from the sorted signature order against the artifact's own member lists, zero mismatches; the constructs artifact records no member lists and its ordering is stated as unverified by an independent route. Mapping artifacts counted APART from findings: dart's 17 operator leaves and swift's 6 have no host kind because those grammars name no binary operator token in `node-types.json` at all, which makes dart and swift the two languages weak on BOTH sources at once
  - [~] 4z **SWIFT RETAKEN WITH THE REAL COMPILER** (2026-08-19, log 034, the owner's JOB A) — every swift verdict in this node was `swiftc -typecheck`, which log 032 measured accepting **1,066 of 2,618** probes the full compiler refuses (40.7 percent, 1,032 of them arithmetic or literal overflow diagnosed in SILGen). `swiftc -c` MEASURED at **155 ms/probe against `-typecheck`'s 154**, effective **34 ms/probe at -P 8 on 6 cores**, so the WHOLE 76,614-probe matrix is retaken rather than only its accepts — an accepts-only redo would have rested on the unmeasured assumption that full swiftc refuses everything `-typecheck` refuses. Two lanes `vs_swift_00/01`, generator `l3_swiftfull.py`, reader `l3_swiftfull_read.py` with the standing completeness gate. `valuematrix_swift.json` KEPT and marked superseded-with-reason. **Still owed: the execution redo over the new accepted set, and the re-fold**
  - [x] 4aa **DECISION 43 — a MECHANICAL library-versus-language criterion for word-spelled operations, all twelve languages** (2026-08-19, log 034, the owner's JOB B) — two legs, both run, conjunction required. **leg G**: the grammar declares the word among the anonymous tokens of the operator slot of a binary-shaped expression node, over `raw_all/<L>.node-types.json` and `kinds_<L>.json`, the same two files `l3_accept.ops()` already reads; three grammar shapes accepted (left/operator/right, a repeated `operators` field, and `<word>_expression` with left/right), the assignment family excluded mechanically, a TYPE in the right slot excluded. **leg R**: the language's own checker REFUSES an ordinary variable named with that word. **ADMITS cpp `and` `bitand` `bitor` `not_eq` `or` `xor`, ruby `and` `or`, typescript `instanceof`, php `instanceof`** — nine new leaves over four languages — and **re-derives python's six, kotlin's `in`, typescript's `in` and php's three from the standing menu without being told**. The three standing entries it does NOT re-derive are `java.instanceof`, `csharp.is` and `csharp.as`, all three because their grammar node puts a TYPE in the right slot; decision 43 is ADDITIVE so they stay and the disagreement is a finding. Leg R is the right question because **every swift operator including `+` is a library declaration**, so "is this a library symbol" would strip swift bare, while "is this spelling available as an identifier" does not
  - [x] 4bb **the owner's kotlin premise CHECKED and REFUTED; the shift tie breaks by EXCLUSION** (2026-08-19, log 034) — the ruling asked whether kotlin's grammar declares `shl`/`shr`/`ushr`/`and`/`or`/`xor` as infix operators. **It does not**: kotlin's `infix_expression` node carries an ordinary `identifier` where an operator token would be, and `binary_expression` declares no bitwise or shift token at all *(measured)*. Leg R agrees — `val shl: Int = 1` compiles *(measured)*. Both legs fail, so kotlin's shifts are LIBRARY, log 030 item 4k's builtin ruling is CONFIRMED rather than bent, and log 030's two-sharp-against-four-blunt tally is settled by kotlin being permanently out on measured grounds rather than by kotlin joining. Measured anyway as a LABELLED ASIDE (`ka_kotlin_00..03`, deferred off the critical path) so the tally can be read both ways; **the aside must never be folded into the default clustering**
  - [x] 4cc **the php parenthesis fault is the SAME fault in ruby** (2026-08-19, log 034) — log 033 item 4u's `$__r = ($a) $op ($b);` binding as `($__r = $a) and $b` is the identical line in ruby's route-C driver, and ruby's `and`/`or` have the same precedence relation to `=`. It was invisible because ruby's menu carried no word-spelled operation until decision 43 admitted one — the harness fault and the missing operation were the same blind spot. Both drivers repaired in `l3_wordops.py`; **both route-C languages re-run WHOLE rather than patched**, so the symbol-spelled cells can be diffed against the pre-repair product and confirmed unchanged rather than assumed unchanged. Python needed no repair: its word logicals bind ABOVE assignment
  - [x] 4dd **a probe harness needs a CONTROL, and this one cost four passes for want of it** (2026-08-19, log 034) — leg R's first pass carried the control word `zzfoo` for seven of twelve languages, and **two of the five without one were measuring their own broken harness**: c-sharp's compiler refused every probe including the control for want of an entry point, and the typescript lib-path search found no checker at all. Both looked exactly like a measurement — the same shape as log 030 item 4m's out-of-scratch lane. **26 verdicts over 7 languages DROPPED rather than read**; `wordop_criterion.py` refuses any verdict whose pass did not also ACCEPT the control. C-sharp's error codes had separated the two classes before the repair: `and`/`or` drew only the link-stage `CS5001` while `is`/`as` drew `CS1001 Identifier expected` at the declaration column
  - [x] 4ee **the c++ six are the clustering's first POSITIVE CONTROL** (2026-08-19, log 034) — `and` `bitand` `bitor` `not_eq` `or` `xor` are the standard's alternative token spellings of `&&` `&` `|` `!=` `||` `^`, declared in `binary_expression` beside the symbols and reserved with no header included *(measured)*. They carry a prediction nothing else in this node has: each must land at distance exactly ZERO from its symbol twin, identical domain and identical answer on every cell. **A re-fold that does not put `cpp.and` on top of `cpp.&&` is evidence against the machinery, not against c++**
  - [x] 4ff **the RE-FOLD — DONE** (2026-08-19, log 035) — `clusters_all12.json` and `dendrogram_all12.html` rebuilt: **238 leaves over twelve languages, 119,944 input cells, 446,491 value answers, 28,203 edges, and ZERO rows excluded as codegen refusals against log 033's 1,066**. Twelve leaves added — c++'s six words, ruby's `and`/`or`, php's `instanceof`, and php's `and`/`or`/`xor` returning from VOID — and **none lost**. Log 034 §5 predicted 235 by not counting the three de-VOIDed rows. Every lane in log 034's queue had finished, all passed the gate, all had zero `ENOSPC`, scratch left at 2,566 MB. Visual validated under `domstub.js`: 2 scripts run, 0 threw, `#count` reads 115 at the default 0.700 which is inside the [0.694444, 0.706118) → 115 plateau
  - [x] 4gg **BOTH POSITIVE CONTROLS PASS, exactly** (2026-08-19, log 035) — log 034 item 4ee's prediction was made before the run and the run agrees. Each of c++'s six word spellings lands at **similarity 1.000, Jaccard 1.000, agreement 1.000** on its symbol twin (`and`~`&&` 562 shared, `not_eq`~`!=` 282, `or`~`||` 562, `bitand`~`&`, `bitor`~`|`, `xor`~`^` 64 each), and scored on the answers directly before any clustering, **0 disagreements of 8,526 shared cells**. `ruby.and` lands at 1.000 on `ruby.&&`; its 1,218 differing cells of 11,881 **all carry a hexadecimal object address** and are the log-034 §4.3 recorder fault, not a disagreement. **A third control fell out unplanned**: `php.and` on `php.&&` at 1.000 after the parenthesis repair. And log 034 §4.2's one UNVERIFIED line is closed — `ruby.or` against `ruby.||` reads 11,881 shared, 1,274 differing, **all 1,274 carrying an address**; the earlier check could not read it because it split the raw line from the LEFT and `||` is the field separator, log 027 finding 11 for the third time
  - [x] 4hh **the swift redo changed swift's PROVENANCE, not swift's position** (2026-08-19, log 035) — **all 1,335 edges touching a swift leaf keep their log-033 similarity to the last digit, and all six signatures have byte-identical domains and answer classes** *(measured)*. What moved is that decision 40's 1,066 excluded rows are gone: they were never in the accepted set to begin with. **That dropping them and never admitting them give the same answers is the strongest available evidence that decision 40 was doing the right thing with the wrong input.** The execution redo's own check passed — the codegen-repair loop fired **zero** times against 1,066 under the old accepted set — and three independent numbers agree to the probe: full-compiler accepts 1,552, log 032's answer count 1,552, the redo's answer count 1,552. Swift stays at six leaves for a GRAMMAR reason (log 028's `custom_operator`), unchanged and still the largest known gap in the picture
  - [x] 4ii **DECISION 44 — decision 30's voiding of php's `and`/`or`/`xor` is LIFTED** (2026-08-19, log 035) — the parenthesis fault is repaired, php re-run WHOLE as `rc_php2`, and the repair is shown to have reached the clustering rather than asserted: **57 of `php.and`'s 64 domain cells changed answer class**, `fractional|fractional` going from `fractional:1.5, fractional:inf, fractional:nan` — the LEFT OPERAND — to `truth:true, truth:false`, the operation. The voiding is KEPT in the code beside the lifting because logs 031 and 033 published numbers under it. **Consequence stated rather than left to be discovered: those two logs' php word-logical numbers are not comparable with log 035's**, the same way decision 31 made their A factors incomparable
  - [x] 4jj **a FOURTH total contradiction, and it is a spelling collision** (2026-08-19, log 035) — `cpp.xor` against `php.xor`, **64 of 64 shared cells disagreeing**. On `truth|truth` c++ answers `whole:0, whole:1` and php answers `truth:false, truth:true`: c++'s `xor` is the standard's alternative token for bitwise `^`, php's is a logical exclusive-or. **It is the first contradiction in this node that decision 39 does NOT explain as a kind mismatch kept apart** — same operands, same operation name, different answers because the operations are different. Log 033 item 4w's three all STAND unchanged at the same rates. The agree-everywhere count stays at exactly 343 while the twelve new leaves added 13 same-spelling pairs, not one of which agrees everywhere
  - [x] 4kk **the newly admitted operations joined their own families, and one pair changed the coarse reading** (2026-08-19, log 035) — at the stable [0.155, 0.185) cut every one of the twelve lands with its own language and none crossed a language: c++'s `and`/`or`/`not_eq` to the boolean-returning cluster beside `cpp.&&`/`cpp.||`/`cpp.!=`, c++'s `bitand`/`bitor`/`xor` to the integer-and-truth cluster beside `cpp.&`/`cpp.|`/`cpp.^` — **the standard's split, re-derived with nothing told to the machinery**. The **eleven-member shift group returns with no twelfth**, exactly as log 034 §5 predicted, because decision 43 rules kotlin's `shl` LIBRARY and it is not in the fold. **The one structural change is a MERGE caused by `ruby.and` and `ruby.or`**: log 033's four-member and eight-member short-circuit clusters become one of twelve, so twelve leaves added give one FEWER cluster, 22 to 21. That answers log 031 item 4r's open question — with ruby's word spellings in, the operand-returning logicals do form one group at this reading. **The [0.155, 0.185) plateau holds its bounds to six decimal places across the twelve-leaf change** and becomes the widest only because the two above it narrowed; log 030 item 4n's no-elbow finding stands and no cut is chosen
  - [x] 4ll **two faults in log 035's own new code, both caught by a checked number rather than by a failure** (2026-08-19, log 035) — (i) `l3_wordops.with_ops` rebinds `ops` on three modules, but `l3_exec.py` line 51 does `from l3_accept import ..., ops`, binding the function OBJECT at import, so the patch never reached the exec path. The first `xw_cpp_00` therefore compiled c++'s STANDING menu — `k=3` means `not_eq` in the word list and `/` in the standing one — and **2,466 of 8,526 probes returned `CODEGEN_REFUSE` reading `no match for 'operator/'` while the lane exited 0 and wrote a full, gate-passing `__SUMMARY__`**. The sixth lane in this node to lie about finishing and the first to lie by reporting a complete measurement of the wrong question; quarantined as `raw/VOID_xw_cpp_00_wrongops.txt`, and `l3_wordexec.py` now asserts the op list reached the table before emitting. (ii) `l3_answers12.load_three` iterates the FROZEN `manifest_<lang>.json` operation list, frozen in log 029 before decision 43 admitted anything, so the first re-fold **silently dropped `ruby.and`, `ruby.or` and `php.instanceof`** — and produced **235 leaves, exactly log 034's predicted number**. Caught by listing what the fold ADDED instead of counting what it produced. **A count that matches a prediction is the weakest evidence in this node.** Repaired by appending the admitted operations at READ time rather than re-freezing a manifest, since a manifest is a record of what was probed and re-freezing one to fix a reader destroys the thing it exists to preserve
  - [x] 4mm typescript's `instanceof` is ADMITTED with an EMPTY DOMAIN (2026-08-19, log 035) — `vw_typescript_00` accepted **0 of 10,000 probes** *(measured)*, because layer 3's holders are all values and `x instanceof 5` is a type error for every one of them. It needed no execution lane and contributes no leaf. **Admitted-and-empty is not the same as excluded** and `refold_index.json` records it as the former; whether a probe space carrying type operands would give it a domain is unverified, and it is the same gap that keeps `java.instanceof` and `csharp.is` out
