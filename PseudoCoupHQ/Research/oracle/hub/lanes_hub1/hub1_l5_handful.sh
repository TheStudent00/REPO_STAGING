#!/usr/bin/env bash
# hub1_l5_handful.sh -- task hub1, lane 5: the handful.
# Step 5 of the master order, on eight explicitly typed go functions: the
# front end (tree-sitter + go's own type checker), the resolution of each
# typed node to the cell the corpus attests, the source composition per
# target, and the gate over go's own carved body and the composed carved
# body. Memory bound 6 GB, named abort ABORT_MEMORY_HUB1.
set -uo pipefail
cd PseudoCoupHQ/Research/oracle/hub
python3 hub.py handful
