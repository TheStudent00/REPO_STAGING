---
id: hq.research.arch_unit_oracle.cross_construction.progress
status: living
---

# PROGRESS — cross_construction

- 2026-09-05: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/CORE_0_3_2_arch_unit_oracle.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-05: task o1 done — length-one and depth-2 length-two
  construction matrices over the pool's five languages;
  `~/Programming/PseudoCoupHQ/DevComms/log_207_task_o1_cross_construction_map.md`.
  Term-level only; no gate proof yet (task o2). Parser round-trip: 22
  of 1,267 distinct layer-5 texts fail on one cause (`*` spacing
  ambiguity between integer and float print forms), excluded not patched.
- 2026-09-06: task o7 done — does clang collapse a c EMULATION of a
  go / rust / swift unit's term back into a c arch-unit?
  `~/Programming/PseudoCoupHQ/DevComms/log_218_task_o7_emulation_collapse.md`;
  artifacts under
  `~/Programming/PseudoCoupHQ/Research/oracle/cross_construction/emulation/`.
  206 pool entries with an x member and no c member; 204 rendered and
  compiled at the corpus's ship flags. Behaviour: 168 of 204 proved
  equal to the x unit, 193 of 204 once narrow arguments are
  zero-extended from their holder width. Bytes: 5 of 204 carry bytes
  an existing c unit already has, read against the control's ceiling
  of 14 of 33 (emulate a c member from its OWN term and ask whether
  clang lands back on that member's bytes). Of the 11 that stay
  disproved, 6 have an x body with a guarded case the term does not
  carry; 5 are unattributed and OPEN. This task did NOT unfreeze the
  node's term-level composition; whether it warrants a sub-node is
  the coordinator's call.
- 2026-09-06: task o8 done — for each of task o2's single-arch-opcode
  units (five languages, narrow rule), does clang land its own
  polyfill back on the SAME opcode?
  `~/Programming/PseudoCoupHQ/DevComms/log_220_task_o8_per_opcode_polyfill.md`;
  artifacts under
  `~/Programming/PseudoCoupHQ/Research/oracle/cross_construction/emulation/per_opcode/`.
  259 rows (c 83, cpp 82, rust 48, go 27, swift 19); 243 had a proved
  term (16 c/cpp `call` rows over wide-integer runtime callees did
  not); 217 of 243 rendered and compiled. Q0 (the owner's own question):
  197 of 217 (91%) LANDED on the row's own mnemonic; 2 (both go
  `add`) LANDED_ELSEWHERE, on `lea`; 18 NOT_COLLAPSED (5 of those are
  a zero-opcode sub-case — a pointer-store row whose term is just its
  own argument). Q3: 216 of 217 PROVED_ON_SHIP, 1 DISPROVED on first
  pose and PROVED under the same caller-extension re-posing o7 used.
  Reused o7's renderer/compile/carve/gate steps and o2's own chaff
  rule by import; wrote no line under `Research/op_pipeline/` or
  `Research/oracle/arch_opcodes/`. The `and`/`or`/`xor`/`not`
  mnemonic/operator-spelling guard collision task o2 found (log 208)
  recurs here on the same cause, not re-litigated. Did NOT unfreeze
  the node's term-level composition.
- 2026-09-07: task o11 done — AutoPoly with RUST as the target: the
  coverage table (the term language's operators x the target
  languages) and the same collapse question o7 asked of clang, asked
  of rustc.
  `~/Programming/PseudoCoupHQ/DevComms/log_226_task_o11_rust_renderer.md`;
  artifacts under
  `~/Programming/PseudoCoupHQ/Research/oracle/cross_construction/emulation/rust/`.
  THE COVERAGE TABLE, built before any run and measured against
  rustc's own emission (four probe lanes, `rust_facts*.json`): 62
  operator rows; rust spells 42 directly, needs a named idiom for 20,
  and has NO operator hole. The one hole is a SORT: rustc 1.96.1
  refuses `f16` and `f128`, so there is no 16-bit float holder, and
  that single missing sort caused 43 of the run's 80 refusals. The
  idioms have one cause: rust's operator carries a check the term does
  not. Plain `+`/`*`/`-` branch to a panic routine at
  `debug-assertions=on` (the corpus's ship flags carry
  `debug-assertions=off`, so they wrap there), so the renderer writes
  `wrapping_*`; `/` and `%` check a zero divisor in BOTH builds and
  the signed forms check the extreme as well, so division is written
  under `core::hint::unreachable_unchecked` at exactly the conditions
  the machine's divide leaves undefined (`assert_unchecked` alone does
  NOT remove the signed-extreme check); plain float-to-integer `as`
  saturates, so `to_int_unchecked` is used.
  THE RUN: 1,175 pool entries have a c, go or swift member and no rust
  member; 561 carry a layer-5 text the parser round-trips; 400 drawn
  uniformly (seed 20260907). 320 rendered and 320 of 320 compiled —
  rustc refused nothing. Behaviour: 243 of 320 proved equal to the x
  unit, 284 of 320 once narrow arguments are zero-extended from their
  holder width. Bytes: 2 of 320, read against the control's ceiling of
  27 of 39 (69%) — measurably HIGHER than clang's 14 of 33 (42%) on
  the same control in o7, so rustc's lowering of the renderer's long
  nested source is the more predictable of the two. Of the 18 that
  stay disproved: 7 have an x body reading a memory row the term does
  not carry, 2 a conditional branch it does not carry, 9 unattributed
  and OPEN (three of those nine are the same entries o7 left
  unattributed with c as the target, so the cause is not rust's).
  18 UNDECIDED, 16 of them because the shared opcode table has no
  entry for `cmpordss` / `cmpordsd`, which rustc emits and no corpus
  body spells — flagged, not fixed (that file is out of scope).
  TASK o8's QUESTION WITH RUST AS THE TARGET, over the single-opcode
  rows of c, go and swift: 121 rows with a proved term, 107 rendered
  and compiled, 99 (93%) LANDED on the row's own mnemonic, 2 (both go
  `add`) LANDED_ELSEWHERE on `lea` — the same substitution clang made
  in o8 — and 6 NOT_COLLAPSED, all c rows whose term joins a 32-bit
  answer into a 64-bit holder. All 107 proved; none disproved.
  Task o7's `emulate.Renderer` was DERIVED FROM, not forked
  (`RustRenderer` overrides one method per coverage-table row);
  `prove_against_x`, `pipeline_extractor`, `recorded_facts`,
  `Population`, `collect` and `fork_one` are imported unchanged, with
  this task's worker and memory ceiling bound in by two assignments in
  `install_worker()`. Nothing under `Research/op_pipeline/`,
  `per_opcode/` or `arch_opcodes/` was written.
  The `and`/`or`/`xor`/`not` mnemonic-versus-spelling collision that
  task o2 (log 208 §6) and task o8 (log 220) both left failing has a
  resolution here that is neither a new key nor an exemption: the
  field is spelled `mnem`, the guard's own machine-form name, and the
  landed mnemonic sits at `landed.mnem`. Every json this task wrote
  PASSes the unmodified guard. Whether o2's and o8's artifacts are
  renamed the same way is the coordinator's call.
  Did NOT unfreeze the node's term-level composition.
