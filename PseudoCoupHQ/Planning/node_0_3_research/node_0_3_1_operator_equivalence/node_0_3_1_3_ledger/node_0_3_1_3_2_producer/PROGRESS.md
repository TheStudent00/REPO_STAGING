---
id: hq.research.compiler_graph.ledger.producer.progress
status: living
---

# PROGRESS — producer

- 2026-09-03: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_3_ledger.md`.
- 2026-09-03: producer written as `{kind, mnem}` everywhere —
  **done** (`ledger48.producer_object`). Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_147_task48_layer4_ledger_census.md`
  §13.3.
- 2026-09-03: the typed shape checked by the unmodified guard over
  every canon38 artifact — **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §6.1.
- 2026-09-03: `runtime_callee` as a fourth kind — **planned**, waiting
  on the `call` destination rule. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §9.2.
- 2026-09-03: the third field written into the CORE before any code,
  under round 12's binding rule 2: `runtime_callee` carries `callee`
  where the other kinds carry `mnem`, because what produced the value
  is a routine and not an instruction. Provenance: log_158 TASK 59 (b)
  states the shape `{"kind": "runtime_callee", "callee": "__divti3"}`.
  Evidence: this node's CORE, `## design`.
- 2026-09-03: `Producer` REALIZED as a class under this node's name —
  **done**. `ledger.Producer` with `arch_opcode`, `flag_pair`,
  `non_opcode_phrase`, `runtime_callee` and `of`; `PRODUCER_KINDS` is
  the closed vocabulary and an unknown kind is refused BY NAME.
  Evidence: `Research/op_pipeline/ledger.py`.
- 2026-09-03: the `runtime_callee` kind measured — 104 such rows over
  the 241-unit computed sample; 304 of the 308 recorded callers
  attached. Evidence:
  `Research/op_pipeline/ledger_sample_walk_printed.txt` and
  `Research/op_pipeline/runtime_callee_attachments.json`.
- 2026-09-03: the unmodified guard, one process, over every JSON task
  59 wrote — 3 artifacts, all PASS, `grep -c exempt` = 0. Evidence:
  `Research/op_pipeline/ledger_guard_transcript.txt`.
