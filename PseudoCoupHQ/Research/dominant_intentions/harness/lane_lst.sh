#!/bin/sh
# list census verification run -- generated by
# Research/dominant_intentions/harness/generate_run.py . Do not hand-edit;
# regenerate. Every line printed to /out/lst_<lang>.txt is
# FACT.PROBE|RESULT.
set -u
ROOT=/work/lst
rm -rf "$ROOT"; mkdir -p "$ROOT"
export HOME=/work
export GOFLAGS=-mod=mod
export GOCACHE=/work/.gocache
export GOPATH=/work/.gopath
export PATH=/persist/dart-sdk/bin:$PATH

# swift's binaries want an ncurses name the image does not ship under; the fix
# is per-container, so every script that may touch swift re-applies it.
ncurses_fix() {
  if [ -f /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 ]; then
    ln -sf /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 \
       /usr/lib/x86_64-linux-gnu/libncurses.so.6 2>/dev/null
    ldconfig 2>/dev/null
  fi
}
ncurses_fix

echo "=== list census verification run ==="
date -u +%Y-%m-%dT%H:%M:%SZ


# ---------------------------------------------------------------- python
echo ""; echo "--- python ---"
OUT=/out/lst_python.txt
: > "$OUT"
if command -v python3 >/dev/null 2>&1; then
  D="$ROOT/python/main"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# list probe bodies - python (mixed elements, reference semantics, copy slices).
def J(v): return ",".join(str(x) for x in v)
def mk(): return [1, 2, 3]
def f_listU1_a():
    a = [1, 2]; a.append(3); return J(a)
def f_listU2_a(): return fmts(mk()[1])
def f_listU3_a(): return J([x for x in mk()])
def f_listU4_a(): return fmts(len(mk()))
def f_Fl1_a(): return J([1, "a"])
def f_Fl3_a():
    a = [1, 2]; a.append(3); a.append(4); return fmts(len(a))
def f_Fl4_a(): return fmts(mk()[10])
def f_Fl5_a(): return fmts(mk()[-1])
def f_Fl6_a():
    a = mk(); s = a[0:2]; s[0] = 99; return fmts(a[0])
def f_Fl7_a(): return fmtb([1, 2, 3] == mk())
def f_Fl8_a():
    a = [10, 9, 1]; a.sort(); return J(a)
def f_Fl8_b():
    a = [10, 9, 1]; sorted(a); return J(a)
def f_Fl9_a():
    a = mk(); b = a; b[0] = 99; return fmts(a[0])


try:
    print("list-U1.a|" + fmts(f_listU1_a()))
except BaseException as _e:
    print("list-U1.a|<raise:" + type(_e).__name__ + ">")
try:
    print("list-U2.a|" + fmts(f_listU2_a()))
except BaseException as _e:
    print("list-U2.a|<raise:" + type(_e).__name__ + ">")
try:
    print("list-U3.a|" + fmts(f_listU3_a()))
except BaseException as _e:
    print("list-U3.a|<raise:" + type(_e).__name__ + ">")
try:
    print("list-U4.a|" + fmts(f_listU4_a()))
except BaseException as _e:
    print("list-U4.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-l1.a|" + fmts(f_Fl1_a()))
except BaseException as _e:
    print("F-l1.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-l3.a|" + fmts(f_Fl3_a()))
except BaseException as _e:
    print("F-l3.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-l6.a|" + fmts(f_Fl6_a()))
except BaseException as _e:
    print("F-l6.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-l8.a|" + fmts(f_Fl8_a()))
except BaseException as _e:
    print("F-l8.a|<raise:" + type(_e).__name__ + ">")
try:
    print("F-l8.b|" + fmts(f_Fl8_b()))
except BaseException as _e:
    print("F-l8.b|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l1.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
    echo "F-l8.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/oob"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# list probe bodies - python (mixed elements, reference semantics, copy slices).
def J(v): return ",".join(str(x) for x in v)
def mk(): return [1, 2, 3]
def f_listU1_a():
    a = [1, 2]; a.append(3); return J(a)
def f_listU2_a(): return fmts(mk()[1])
def f_listU3_a(): return J([x for x in mk()])
def f_listU4_a(): return fmts(len(mk()))
def f_Fl1_a(): return J([1, "a"])
def f_Fl3_a():
    a = [1, 2]; a.append(3); a.append(4); return fmts(len(a))
def f_Fl4_a(): return fmts(mk()[10])
def f_Fl5_a(): return fmts(mk()[-1])
def f_Fl6_a():
    a = mk(); s = a[0:2]; s[0] = 99; return fmts(a[0])
def f_Fl7_a(): return fmtb([1, 2, 3] == mk())
def f_Fl8_a():
    a = [10, 9, 1]; a.sort(); return J(a)
def f_Fl8_b():
    a = [10, 9, 1]; sorted(a); return J(a)
def f_Fl9_a():
    a = mk(); b = a; b[0] = 99; return fmts(a[0])


try:
    print("F-l4.a|" + fmts(f_Fl4_a()))
except BaseException as _e:
    print("F-l4.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/neg"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# list probe bodies - python (mixed elements, reference semantics, copy slices).
def J(v): return ",".join(str(x) for x in v)
def mk(): return [1, 2, 3]
def f_listU1_a():
    a = [1, 2]; a.append(3); return J(a)
def f_listU2_a(): return fmts(mk()[1])
def f_listU3_a(): return J([x for x in mk()])
def f_listU4_a(): return fmts(len(mk()))
def f_Fl1_a(): return J([1, "a"])
def f_Fl3_a():
    a = [1, 2]; a.append(3); a.append(4); return fmts(len(a))
def f_Fl4_a(): return fmts(mk()[10])
def f_Fl5_a(): return fmts(mk()[-1])
def f_Fl6_a():
    a = mk(); s = a[0:2]; s[0] = 99; return fmts(a[0])
def f_Fl7_a(): return fmtb([1, 2, 3] == mk())
def f_Fl8_a():
    a = [10, 9, 1]; a.sort(); return J(a)
def f_Fl8_b():
    a = [10, 9, 1]; sorted(a); return J(a)
def f_Fl9_a():
    a = mk(); b = a; b[0] = 99; return fmts(a[0])


try:
    print("F-l5.a|" + fmts(f_Fl5_a()))
except BaseException as _e:
    print("F-l5.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/eq"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# list probe bodies - python (mixed elements, reference semantics, copy slices).
def J(v): return ",".join(str(x) for x in v)
def mk(): return [1, 2, 3]
def f_listU1_a():
    a = [1, 2]; a.append(3); return J(a)
def f_listU2_a(): return fmts(mk()[1])
def f_listU3_a(): return J([x for x in mk()])
def f_listU4_a(): return fmts(len(mk()))
def f_Fl1_a(): return J([1, "a"])
def f_Fl3_a():
    a = [1, 2]; a.append(3); a.append(4); return fmts(len(a))
def f_Fl4_a(): return fmts(mk()[10])
def f_Fl5_a(): return fmts(mk()[-1])
def f_Fl6_a():
    a = mk(); s = a[0:2]; s[0] = 99; return fmts(a[0])
def f_Fl7_a(): return fmtb([1, 2, 3] == mk())
def f_Fl8_a():
    a = [10, 9, 1]; a.sort(); return J(a)
def f_Fl8_b():
    a = [10, 9, 1]; sorted(a); return J(a)
def f_Fl9_a():
    a = mk(); b = a; b[0] = 99; return fmts(a[0])


try:
    print("F-l7.a|" + fmts(f_Fl7_a()))
except BaseException as _e:
    print("F-l7.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/python/assign"; mkdir -p "$D"; cd "$D"
  cat > main.py <<'PROBE_SRC_EOF'
# runner template - python. The probe placeholder below is filled by generate_run.py.
def identb(x): return x
def notb(x): return (not x)
def d(x): return float(x)
def z(): return 0.0
def one(): return 1.0
def negone(): return -1.0
def nan(): return float('nan')
def inf(): return float('inf')
def ninf(): return float('-inf')
def fmtb(v): return "true" if v else "false"
def fmtf(v):
    if v != v: return "nan"
    if v == float('inf'): return "inf"
    if v == float('-inf'): return "-inf"
    return repr(v)

def fmts(v): return str(v)

# list probe bodies - python (mixed elements, reference semantics, copy slices).
def J(v): return ",".join(str(x) for x in v)
def mk(): return [1, 2, 3]
def f_listU1_a():
    a = [1, 2]; a.append(3); return J(a)
def f_listU2_a(): return fmts(mk()[1])
def f_listU3_a(): return J([x for x in mk()])
def f_listU4_a(): return fmts(len(mk()))
def f_Fl1_a(): return J([1, "a"])
def f_Fl3_a():
    a = [1, 2]; a.append(3); a.append(4); return fmts(len(a))
def f_Fl4_a(): return fmts(mk()[10])
def f_Fl5_a(): return fmts(mk()[-1])
def f_Fl6_a():
    a = mk(); s = a[0:2]; s[0] = 99; return fmts(a[0])
def f_Fl7_a(): return fmtb([1, 2, 3] == mk())
def f_Fl8_a():
    a = [10, 9, 1]; a.sort(); return J(a)
def f_Fl8_b():
    a = [10, 9, 1]; sorted(a); return J(a)
def f_Fl9_a():
    a = mk(); b = a; b[0] = 99; return fmts(a[0])


try:
    print("F-l9.a|" + fmts(f_Fl9_a()))
except BaseException as _e:
    print("F-l9.a|<raise:" + type(_e).__name__ + ">")
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (python3 main.py) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "python assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "python assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "python: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- ruby
echo ""; echo "--- ruby ---"
OUT=/out/lst_ruby.txt
: > "$OUT"
if command -v ruby >/dev/null 2>&1; then
  D="$ROOT/ruby/main"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# list probe bodies - ruby (mixed elements, reference semantics, copy slices).
def mkl() = [1, 2, 3]
def f_listU1_a()
  a = [1, 2]; a.push(3); a.join(",")
end
def f_listU2_a() = fmts(mkl()[1])
def f_listU3_a()
  out = []; mkl().each { |x| out.push(x) }; out.join(",")
end
def f_listU4_a() = fmts(mkl().length)
def f_Fl1_a() = [1, "a"].join(",")
def f_Fl3_a()
  a = [1, 2]; a.push(3); a.push(4); fmts(a.length)
end
def f_Fl4_a() = fmts(mkl()[10].inspect)
def f_Fl5_a() = fmts(mkl()[-1])
def f_Fl6_a()
  a = mkl(); s = a[0, 2]; s[0] = 99; fmts(a[0])
end
def f_Fl7_a() = fmtb([1, 2, 3] == mkl())
def f_Fl8_a()
  a = [10, 9, 1]; a.sort!; a.join(",")
end
def f_Fl8_b()
  a = [10, 9, 1]; a.sort; a.join(",")
end
def f_Fl9_a()
  a = mkl(); b = a; b[0] = 99; fmts(a[0])
end


begin; puts("list-U1.a|" + fmts(f_listU1_a())); rescue Exception => _e; puts("list-U1.a|<raise:" + _e.class.name + ">"); end
begin; puts("list-U2.a|" + fmts(f_listU2_a())); rescue Exception => _e; puts("list-U2.a|<raise:" + _e.class.name + ">"); end
begin; puts("list-U3.a|" + fmts(f_listU3_a())); rescue Exception => _e; puts("list-U3.a|<raise:" + _e.class.name + ">"); end
begin; puts("list-U4.a|" + fmts(f_listU4_a())); rescue Exception => _e; puts("list-U4.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-l1.a|" + fmts(f_Fl1_a())); rescue Exception => _e; puts("F-l1.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-l3.a|" + fmts(f_Fl3_a())); rescue Exception => _e; puts("F-l3.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-l6.a|" + fmts(f_Fl6_a())); rescue Exception => _e; puts("F-l6.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-l8.a|" + fmts(f_Fl8_a())); rescue Exception => _e; puts("F-l8.a|<raise:" + _e.class.name + ">"); end
begin; puts("F-l8.b|" + fmts(f_Fl8_b())); rescue Exception => _e; puts("F-l8.b|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l1.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
    echo "F-l8.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/oob"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# list probe bodies - ruby (mixed elements, reference semantics, copy slices).
def mkl() = [1, 2, 3]
def f_listU1_a()
  a = [1, 2]; a.push(3); a.join(",")
end
def f_listU2_a() = fmts(mkl()[1])
def f_listU3_a()
  out = []; mkl().each { |x| out.push(x) }; out.join(",")
end
def f_listU4_a() = fmts(mkl().length)
def f_Fl1_a() = [1, "a"].join(",")
def f_Fl3_a()
  a = [1, 2]; a.push(3); a.push(4); fmts(a.length)
end
def f_Fl4_a() = fmts(mkl()[10].inspect)
def f_Fl5_a() = fmts(mkl()[-1])
def f_Fl6_a()
  a = mkl(); s = a[0, 2]; s[0] = 99; fmts(a[0])
end
def f_Fl7_a() = fmtb([1, 2, 3] == mkl())
def f_Fl8_a()
  a = [10, 9, 1]; a.sort!; a.join(",")
end
def f_Fl8_b()
  a = [10, 9, 1]; a.sort; a.join(",")
end
def f_Fl9_a()
  a = mkl(); b = a; b[0] = 99; fmts(a[0])
end


begin; puts("F-l4.a|" + fmts(f_Fl4_a())); rescue Exception => _e; puts("F-l4.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/neg"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# list probe bodies - ruby (mixed elements, reference semantics, copy slices).
def mkl() = [1, 2, 3]
def f_listU1_a()
  a = [1, 2]; a.push(3); a.join(",")
end
def f_listU2_a() = fmts(mkl()[1])
def f_listU3_a()
  out = []; mkl().each { |x| out.push(x) }; out.join(",")
end
def f_listU4_a() = fmts(mkl().length)
def f_Fl1_a() = [1, "a"].join(",")
def f_Fl3_a()
  a = [1, 2]; a.push(3); a.push(4); fmts(a.length)
end
def f_Fl4_a() = fmts(mkl()[10].inspect)
def f_Fl5_a() = fmts(mkl()[-1])
def f_Fl6_a()
  a = mkl(); s = a[0, 2]; s[0] = 99; fmts(a[0])
end
def f_Fl7_a() = fmtb([1, 2, 3] == mkl())
def f_Fl8_a()
  a = [10, 9, 1]; a.sort!; a.join(",")
end
def f_Fl8_b()
  a = [10, 9, 1]; a.sort; a.join(",")
end
def f_Fl9_a()
  a = mkl(); b = a; b[0] = 99; fmts(a[0])
end


begin; puts("F-l5.a|" + fmts(f_Fl5_a())); rescue Exception => _e; puts("F-l5.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/eq"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# list probe bodies - ruby (mixed elements, reference semantics, copy slices).
def mkl() = [1, 2, 3]
def f_listU1_a()
  a = [1, 2]; a.push(3); a.join(",")
end
def f_listU2_a() = fmts(mkl()[1])
def f_listU3_a()
  out = []; mkl().each { |x| out.push(x) }; out.join(",")
end
def f_listU4_a() = fmts(mkl().length)
def f_Fl1_a() = [1, "a"].join(",")
def f_Fl3_a()
  a = [1, 2]; a.push(3); a.push(4); fmts(a.length)
end
def f_Fl4_a() = fmts(mkl()[10].inspect)
def f_Fl5_a() = fmts(mkl()[-1])
def f_Fl6_a()
  a = mkl(); s = a[0, 2]; s[0] = 99; fmts(a[0])
end
def f_Fl7_a() = fmtb([1, 2, 3] == mkl())
def f_Fl8_a()
  a = [10, 9, 1]; a.sort!; a.join(",")
end
def f_Fl8_b()
  a = [10, 9, 1]; a.sort; a.join(",")
end
def f_Fl9_a()
  a = mkl(); b = a; b[0] = 99; fmts(a[0])
end


begin; puts("F-l7.a|" + fmts(f_Fl7_a())); rescue Exception => _e; puts("F-l7.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/ruby/assign"; mkdir -p "$D"; cd "$D"
  cat > main.rb <<'PROBE_SRC_EOF'
# runner template - ruby.
def identb(x) = x
def notb(x) = (!x)
def d(x) = x.to_f
def z() = 0.0
def one() = 1.0
def negone() = -1.0
def nan() = Float::NAN
def inf() = Float::INFINITY
def ninf() = -Float::INFINITY
def fmtb(v) = (v ? "true" : "false")
def fmtf(v)
  return "nan" if v.nan?
  return "inf" if v == Float::INFINITY
  return "-inf" if v == -Float::INFINITY
  v.to_s
end
def fmts(v) = v.to_s

# list probe bodies - ruby (mixed elements, reference semantics, copy slices).
def mkl() = [1, 2, 3]
def f_listU1_a()
  a = [1, 2]; a.push(3); a.join(",")
end
def f_listU2_a() = fmts(mkl()[1])
def f_listU3_a()
  out = []; mkl().each { |x| out.push(x) }; out.join(",")
end
def f_listU4_a() = fmts(mkl().length)
def f_Fl1_a() = [1, "a"].join(",")
def f_Fl3_a()
  a = [1, 2]; a.push(3); a.push(4); fmts(a.length)
end
def f_Fl4_a() = fmts(mkl()[10].inspect)
def f_Fl5_a() = fmts(mkl()[-1])
def f_Fl6_a()
  a = mkl(); s = a[0, 2]; s[0] = 99; fmts(a[0])
end
def f_Fl7_a() = fmtb([1, 2, 3] == mkl())
def f_Fl8_a()
  a = [10, 9, 1]; a.sort!; a.join(",")
end
def f_Fl8_b()
  a = [10, 9, 1]; a.sort; a.join(",")
end
def f_Fl9_a()
  a = mkl(); b = a; b[0] = 99; fmts(a[0])
end


begin; puts("F-l9.a|" + fmts(f_Fl9_a())); rescue Exception => _e; puts("F-l9.a|<raise:" + _e.class.name + ">"); end
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (ruby main.rb) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "ruby assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "ruby assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "ruby: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- php
echo ""; echo "--- php ---"
OUT=/out/lst_php.txt
: > "$OUT"
if command -v php >/dev/null 2>&1; then
  D="$ROOT/php/main"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// list probe bodies - php. php's "list" IS its ordered hash -- the impostor the
// census flags -- so probe F-l2.b asks it for its keys.
function mkl() { return [1, 2, 3]; }
function f_listU1_a() { $a = [1, 2]; $a[] = 3; return implode(",", $a); }
function f_listU2_a() { $a = mkl(); return fmts($a[1]); }
function f_listU3_a() { $o = []; foreach (mkl() as $x) { $o[] = $x; } return implode(",", $o); }
function f_listU4_a() { return fmts(count(mkl())); }
function f_Fl1_a() { return implode(",", [1, "a"]); }
function f_Fl2_b() { $a = [1, 2, 3]; unset($a[1]); return implode(",", array_keys($a)); }
function f_Fl3_a() { $a = [1, 2]; $a[] = 3; $a[] = 4; return fmts(count($a)); }
function f_Fl4_a() { $a = mkl(); $v = @$a[10]; return $v === null ? "null" : fmts($v); }
function f_Fl5_a() { $a = mkl(); $v = @$a[-1]; return $v === null ? "null" : fmts($v); }
function f_Fl6_a() { $a = mkl(); $s = array_slice($a, 0, 2); $s[0] = 99; return fmts($a[0]); }
function f_Fl7_a() { return fmtb([1, 2, 3] == mkl()); }
function f_Fl8_a() { $a = [10, 9, 1]; sort($a); return implode(",", $a); }
function f_Fl9_a() { $a = mkl(); $b = $a; $b[0] = 99; return fmts($a[0]); }


try { echo "list-U1.a|" . fmts(f_listU1_a()) . "\n"; } catch (Throwable $_e) { echo "list-U1.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "list-U2.a|" . fmts(f_listU2_a()) . "\n"; } catch (Throwable $_e) { echo "list-U2.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "list-U3.a|" . fmts(f_listU3_a()) . "\n"; } catch (Throwable $_e) { echo "list-U3.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "list-U4.a|" . fmts(f_listU4_a()) . "\n"; } catch (Throwable $_e) { echo "list-U4.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-l1.a|" . fmts(f_Fl1_a()) . "\n"; } catch (Throwable $_e) { echo "F-l1.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-l2.b|" . fmts(f_Fl2_b()) . "\n"; } catch (Throwable $_e) { echo "F-l2.b|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-l3.a|" . fmts(f_Fl3_a()) . "\n"; } catch (Throwable $_e) { echo "F-l3.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-l6.a|" . fmts(f_Fl6_a()) . "\n"; } catch (Throwable $_e) { echo "F-l6.a|<raise:" . get_class($_e) . ">\n"; }
try { echo "F-l8.a|" . fmts(f_Fl8_a()) . "\n"; } catch (Throwable $_e) { echo "F-l8.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l1.a|$VERDICT" >> "$OUT"
    echo "F-l2.b|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/oob"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// list probe bodies - php. php's "list" IS its ordered hash -- the impostor the
// census flags -- so probe F-l2.b asks it for its keys.
function mkl() { return [1, 2, 3]; }
function f_listU1_a() { $a = [1, 2]; $a[] = 3; return implode(",", $a); }
function f_listU2_a() { $a = mkl(); return fmts($a[1]); }
function f_listU3_a() { $o = []; foreach (mkl() as $x) { $o[] = $x; } return implode(",", $o); }
function f_listU4_a() { return fmts(count(mkl())); }
function f_Fl1_a() { return implode(",", [1, "a"]); }
function f_Fl2_b() { $a = [1, 2, 3]; unset($a[1]); return implode(",", array_keys($a)); }
function f_Fl3_a() { $a = [1, 2]; $a[] = 3; $a[] = 4; return fmts(count($a)); }
function f_Fl4_a() { $a = mkl(); $v = @$a[10]; return $v === null ? "null" : fmts($v); }
function f_Fl5_a() { $a = mkl(); $v = @$a[-1]; return $v === null ? "null" : fmts($v); }
function f_Fl6_a() { $a = mkl(); $s = array_slice($a, 0, 2); $s[0] = 99; return fmts($a[0]); }
function f_Fl7_a() { return fmtb([1, 2, 3] == mkl()); }
function f_Fl8_a() { $a = [10, 9, 1]; sort($a); return implode(",", $a); }
function f_Fl9_a() { $a = mkl(); $b = $a; $b[0] = 99; return fmts($a[0]); }


try { echo "F-l4.a|" . fmts(f_Fl4_a()) . "\n"; } catch (Throwable $_e) { echo "F-l4.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/neg"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// list probe bodies - php. php's "list" IS its ordered hash -- the impostor the
// census flags -- so probe F-l2.b asks it for its keys.
function mkl() { return [1, 2, 3]; }
function f_listU1_a() { $a = [1, 2]; $a[] = 3; return implode(",", $a); }
function f_listU2_a() { $a = mkl(); return fmts($a[1]); }
function f_listU3_a() { $o = []; foreach (mkl() as $x) { $o[] = $x; } return implode(",", $o); }
function f_listU4_a() { return fmts(count(mkl())); }
function f_Fl1_a() { return implode(",", [1, "a"]); }
function f_Fl2_b() { $a = [1, 2, 3]; unset($a[1]); return implode(",", array_keys($a)); }
function f_Fl3_a() { $a = [1, 2]; $a[] = 3; $a[] = 4; return fmts(count($a)); }
function f_Fl4_a() { $a = mkl(); $v = @$a[10]; return $v === null ? "null" : fmts($v); }
function f_Fl5_a() { $a = mkl(); $v = @$a[-1]; return $v === null ? "null" : fmts($v); }
function f_Fl6_a() { $a = mkl(); $s = array_slice($a, 0, 2); $s[0] = 99; return fmts($a[0]); }
function f_Fl7_a() { return fmtb([1, 2, 3] == mkl()); }
function f_Fl8_a() { $a = [10, 9, 1]; sort($a); return implode(",", $a); }
function f_Fl9_a() { $a = mkl(); $b = $a; $b[0] = 99; return fmts($a[0]); }


try { echo "F-l5.a|" . fmts(f_Fl5_a()) . "\n"; } catch (Throwable $_e) { echo "F-l5.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/eq"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// list probe bodies - php. php's "list" IS its ordered hash -- the impostor the
// census flags -- so probe F-l2.b asks it for its keys.
function mkl() { return [1, 2, 3]; }
function f_listU1_a() { $a = [1, 2]; $a[] = 3; return implode(",", $a); }
function f_listU2_a() { $a = mkl(); return fmts($a[1]); }
function f_listU3_a() { $o = []; foreach (mkl() as $x) { $o[] = $x; } return implode(",", $o); }
function f_listU4_a() { return fmts(count(mkl())); }
function f_Fl1_a() { return implode(",", [1, "a"]); }
function f_Fl2_b() { $a = [1, 2, 3]; unset($a[1]); return implode(",", array_keys($a)); }
function f_Fl3_a() { $a = [1, 2]; $a[] = 3; $a[] = 4; return fmts(count($a)); }
function f_Fl4_a() { $a = mkl(); $v = @$a[10]; return $v === null ? "null" : fmts($v); }
function f_Fl5_a() { $a = mkl(); $v = @$a[-1]; return $v === null ? "null" : fmts($v); }
function f_Fl6_a() { $a = mkl(); $s = array_slice($a, 0, 2); $s[0] = 99; return fmts($a[0]); }
function f_Fl7_a() { return fmtb([1, 2, 3] == mkl()); }
function f_Fl8_a() { $a = [10, 9, 1]; sort($a); return implode(",", $a); }
function f_Fl9_a() { $a = mkl(); $b = $a; $b[0] = 99; return fmts($a[0]); }


try { echo "F-l7.a|" . fmts(f_Fl7_a()) . "\n"; } catch (Throwable $_e) { echo "F-l7.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/php/assign"; mkdir -p "$D"; cd "$D"
  cat > main.php <<'PROBE_SRC_EOF'
<?php
// runner template - php.
function identb($x) { return $x; }
function notb($x) { return (!$x); }
function d($x) { return (float)$x; }
function z() { return 0.0; }
function one() { return 1.0; }
function negone() { return -1.0; }
function nan() { return NAN; }
function inf() { return INF; }
function ninf() { return -INF; }
function fmtb($v) { return $v ? "true" : "false"; }
function fmtf($v) {
  if (is_nan($v)) return "nan";
  if ($v === INF) return "inf";
  if ($v === -INF) return "-inf";
  return (string)$v;
}
function fmts($v) { return (string)$v; }

// list probe bodies - php. php's "list" IS its ordered hash -- the impostor the
// census flags -- so probe F-l2.b asks it for its keys.
function mkl() { return [1, 2, 3]; }
function f_listU1_a() { $a = [1, 2]; $a[] = 3; return implode(",", $a); }
function f_listU2_a() { $a = mkl(); return fmts($a[1]); }
function f_listU3_a() { $o = []; foreach (mkl() as $x) { $o[] = $x; } return implode(",", $o); }
function f_listU4_a() { return fmts(count(mkl())); }
function f_Fl1_a() { return implode(",", [1, "a"]); }
function f_Fl2_b() { $a = [1, 2, 3]; unset($a[1]); return implode(",", array_keys($a)); }
function f_Fl3_a() { $a = [1, 2]; $a[] = 3; $a[] = 4; return fmts(count($a)); }
function f_Fl4_a() { $a = mkl(); $v = @$a[10]; return $v === null ? "null" : fmts($v); }
function f_Fl5_a() { $a = mkl(); $v = @$a[-1]; return $v === null ? "null" : fmts($v); }
function f_Fl6_a() { $a = mkl(); $s = array_slice($a, 0, 2); $s[0] = 99; return fmts($a[0]); }
function f_Fl7_a() { return fmtb([1, 2, 3] == mkl()); }
function f_Fl8_a() { $a = [10, 9, 1]; sort($a); return implode(",", $a); }
function f_Fl9_a() { $a = mkl(); $b = $a; $b[0] = 99; return fmts($a[0]); }


try { echo "F-l9.a|" . fmts(f_Fl9_a()) . "\n"; } catch (Throwable $_e) { echo "F-l9.a|<raise:" . get_class($_e) . ">\n"; }
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (php main.php) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "php assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "php assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "php: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- typescript
echo ""; echo "--- typescript ---"
OUT=/out/lst_typescript.txt
: > "$OUT"
if command -v tsc >/dev/null 2>&1; then
  D="$ROOT/typescript/main"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }


try { console.log("list-U1.a|" + fmts(f_listU1_a())); } catch (e: any) { console.log("list-U1.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("list-U2.a|" + fmts(f_listU2_a())); } catch (e: any) { console.log("list-U2.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("list-U3.a|" + fmts(f_listU3_a())); } catch (e: any) { console.log("list-U3.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("list-U4.a|" + fmts(f_listU4_a())); } catch (e: any) { console.log("list-U4.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-l1.c|" + fmts(f_Fl1_c())); } catch (e: any) { console.log("F-l1.c|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-l2.a|" + fmts(f_Fl2_a())); } catch (e: any) { console.log("F-l2.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-l3.a|" + fmts(f_Fl3_a())); } catch (e: any) { console.log("F-l3.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-l6.a|" + fmts(f_Fl6_a())); } catch (e: any) { console.log("F-l6.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
try { console.log("F-l8.a|" + fmts(f_Fl8_a())); } catch (e: any) { console.log("F-l8.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l1.c|$VERDICT" >> "$OUT"
    echo "F-l2.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }
// The checker's face: a declared number[] refuses a string.
function f_Fl1_b(): string { const a: number[] = [1, 2]; a.push("x"); return fmts(a.length); }


try { console.log("F-l1.b|" + fmts(f_Fl1_b())); } catch (e: any) { console.log("F-l1.b|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/oob"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }


try { console.log("F-l4.a|" + fmts(f_Fl4_a())); } catch (e: any) { console.log("F-l4.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/neg"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }


try { console.log("F-l5.a|" + fmts(f_Fl5_a())); } catch (e: any) { console.log("F-l5.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/eq"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }


try { console.log("F-l7.a|" + fmts(f_Fl7_a())); } catch (e: any) { console.log("F-l7.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/typescript/assign"; mkdir -p "$D"; cd "$D"
  cat > main.ts <<'PROBE_SRC_EOF'
// runner template - typescript (tsc -> node).
function identb(x: boolean): boolean { return x; }
function notb(x: boolean): boolean { return (!x); }
function d(x: number): number { return x; }
function z(): number { return 0.0; }
function one(): number { return 1.0; }
function negone(): number { return -1.0; }
function nan(): number { return NaN; }
function inf(): number { return Infinity; }
function ninf(): number { return -Infinity; }
function fmtb(v: boolean): string { return v ? "true" : "false"; }
function fmtf(v: number): string {
  if (v !== v) return "nan";
  if (v === Infinity) return "inf";
  if (v === -Infinity) return "-inf";
  return String(v);
}
function fmts(v: any): string { return String(v); }

// list probe bodies - typescript. Two faces, both recorded: the CHECKER's
// declared-element-type discipline (F-l1.b, its own program) and the RUNTIME's
// complete indifference to it (F-l1.c).
function mkl(): number[] { return [1, 2, 3]; }
function f_listU1_a(): string { const a = [1, 2]; a.push(3); return a.join(","); }
function f_listU2_a(): string { return fmts(mkl()[1]); }
function f_listU3_a(): string { const o: number[] = []; for (const x of mkl()) { o.push(x); } return o.join(","); }
function f_listU4_a(): string { return fmts(mkl().length); }
function f_Fl1_c(): string {
  const a: number[] = [1, 2]; (a as any).push("x");
  return fmts(a.length + "/" + typeof a[2]);
}
function f_Fl2_a(): string { const a = [1, , 3]; return fmts(a.length + "/" + (1 in a)); }
function f_Fl3_a(): string { const a = [1, 2]; a.push(3); a.push(4); return fmts(a.length); }
function f_Fl4_a(): string { return fmts(mkl()[10]); }
function f_Fl5_a(): string { return fmts((mkl() as any)[-1]); }
function f_Fl6_a(): string { const a = mkl(); const s = a.slice(0, 2); s[0] = 99; return fmts(a[0]); }
function f_Fl7_a(): string { return fmtb((mkl() as any) == (mkl() as any)); }
function f_Fl8_a(): string { const a = [10, 9, 1]; a.sort(); return a.join(","); }
function f_Fl9_a(): string { const a = mkl(); const b = a; b[0] = 99; return fmts(a[0]); }


try { console.log("F-l9.a|" + fmts(f_Fl9_a())); } catch (e: any) { console.log("F-l9.a|<raise:" + ((e && e.constructor) ? e.constructor.name : "unknown") + ">"); }
PROBE_SRC_EOF
  VERDICT=ok
  if ! (tsc --target es2020 --module commonjs main.ts) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "typescript assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "typescript assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "typescript assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "typescript: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- go
echo ""; echo "--- go ---"
OUT=/out/lst_go.txt
: > "$OUT"
if command -v go >/dev/null 2>&1; then
  D="$ROOT/go/main"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("list-U1.a|<raise:%v>\n", r) } }(); fmt.Println("list-U1.a|" + fmts(f_listU1_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("list-U2.a|<raise:%v>\n", r) } }(); fmt.Println("list-U2.a|" + fmts(f_listU2_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("list-U3.a|<raise:%v>\n", r) } }(); fmt.Println("list-U3.a|" + fmts(f_listU3_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("list-U4.a|<raise:%v>\n", r) } }(); fmt.Println("list-U4.a|" + fmts(f_listU4_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l3.a|<raise:%v>\n", r) } }(); fmt.Println("F-l3.a|" + fmts(f_Fl3_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l6.a|<raise:%v>\n", r) } }(); fmt.Println("F-l6.a|" + fmts(f_Fl6_a())) }()
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l8.a|<raise:%v>\n", r) } }(); fmt.Println("F-l8.a|" + fmts(f_Fl8_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }
// A declared []int refuses a string element, at build time.
func f_Fl1_b() string { a := []int{1, 2}; a = append(a, "x"); return strconv.Itoa(len(a)) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l1.b|<raise:%v>\n", r) } }(); fmt.Println("F-l1.b|" + fmts(f_Fl1_b())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/oob"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l4.a|<raise:%v>\n", r) } }(); fmt.Println("F-l4.a|" + fmts(f_Fl4_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/neg"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }
// go refuses a constant negative index at build time.
func f_Fl5_a() string { a := mkl(); return strconv.Itoa(a[-1]) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l5.a|<raise:%v>\n", r) } }(); fmt.Println("F-l5.a|" + fmts(f_Fl5_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/eq"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }
// go slices are NOT comparable with ==; only a nil check is allowed.
func f_Fl7_a() string { return fmtb(mkl() == mkl()) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l7.a|<raise:%v>\n", r) } }(); fmt.Println("F-l7.a|" + fmts(f_Fl7_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/go/assign"; mkdir -p "$D"; cd "$D"
  cat > main.go <<'PROBE_SRC_EOF'
// runner template - go. d() exists so every float literal becomes a TYPED
// float64 before any arithmetic; go's untyped constants are arbitrary
// precision and would answer the IEEE questions at the wrong precision.
package main

import (
	"fmt"
	"math"
	"strconv"
)

func identb(x bool) bool  { return x }
func notb(x bool) bool    { return (!x) }
func d(x float64) float64 { return x }
func z() float64          { return 0.0 }
func one() float64        { return 1.0 }
func negone() float64     { return -1.0 }
func nan() float64        { return math.NaN() }
func inf() float64        { return math.Inf(1) }
func ninf() float64       { return math.Inf(-1) }
func fmtb(v bool) string {
	if v {
		return "true"
	}
	return "false"
}
func fmtf(v float64) string {
	if math.IsNaN(v) {
		return "nan"
	}
	if math.IsInf(v, 1) {
		return "inf"
	}
	if math.IsInf(v, -1) {
		return "-inf"
	}
	return strconv.FormatFloat(v, 'g', -1, 64)
}

func fmts(v string) string { return v }

// list probe bodies - go (slices: a WINDOW onto a shared backing array).
// A small insertion sort lives here rather than an import of "sort", so the
// runner template's import list stays exactly as the calibration run left it.
func mkl() []int { return []int{1, 2, 3} }
func joinInts(v []int) string {
	out := ""
	for i, x := range v {
		if i > 0 {
			out += ","
		}
		out += strconv.Itoa(x)
	}
	return out
}
func sortInts(v []int) {
	for i := 1; i < len(v); i++ {
		for j := i; j > 0 && v[j-1] > v[j]; j-- {
			v[j-1], v[j] = v[j], v[j-1]
		}
	}
}
func f_listU1_a() string { a := []int{1, 2}; a = append(a, 3); return joinInts(a) }
func f_listU2_a() string { return strconv.Itoa(mkl()[1]) }
func f_listU3_a() string { o := []int{}; for _, x := range mkl() { o = append(o, x) }; return joinInts(o) }
func f_listU4_a() string { return strconv.Itoa(len(mkl())) }
func f_Fl3_a() string    { a := []int{1, 2}; a = append(a, 3); a = append(a, 4); return strconv.Itoa(len(a)) }
func f_Fl4_a() string    { return strconv.Itoa(mkl()[10]) }
func f_Fl6_a() string    { a := mkl(); s := a[0:2]; s[0] = 99; return strconv.Itoa(a[0]) }
func f_Fl8_a() string    { a := []int{10, 9, 1}; sortInts(a); return joinInts(a) }
func f_Fl9_a() string    { a := mkl(); b := a; b[0] = 99; return strconv.Itoa(a[0]) }


func main() {
	func() { defer func() { if r := recover(); r != nil { fmt.Printf("F-l9.a|<raise:%v>\n", r) } }(); fmt.Println("F-l9.a|" + fmts(f_Fl9_a())) }()
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (go mod init probe >/dev/null 2>&1; go build -o prog main.go) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "go assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "go assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "go assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "go: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- cpp
echo ""; echo "--- cpp ---"
OUT=/out/lst_cpp.txt
: > "$OUT"
if command -v g++ >/dev/null 2>&1; then
  D="$ROOT/cpp/main"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }


int main() {
  { try { std::string _v = fmts(f_listU1_a()); std::cout << "list-U1.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "list-U1.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_listU2_a()); std::cout << "list-U2.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "list-U2.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_listU3_a()); std::cout << "list-U3.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "list-U3.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_listU4_a()); std::cout << "list-U4.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "list-U4.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fl3_a()); std::cout << "F-l3.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l3.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fl4_b()); std::cout << "F-l4.b|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l4.b|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fl6_a()); std::cout << "F-l6.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l6.a|<raise:" << _e.what() << ">" << std::endl; } }
  { try { std::string _v = fmts(f_Fl8_a()); std::cout << "F-l8.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l8.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l4.b|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }
// A declared vector<int> refuses a string element, at build time.
static std::string f_Fl1_b() { std::vector<int> a{1, 2}; a.push_back(std::string("x")); return std::to_string(a.size()); }


int main() {
  { try { std::string _v = fmts(f_Fl1_b()); std::cout << "F-l1.b|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l1.b|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/oob"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }


int main() {
  { try { std::string _v = fmts(f_Fl4_a()); std::cout << "F-l4.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l4.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/neg"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }


int main() {
  { try { std::string _v = fmts(f_Fl5_a()); std::cout << "F-l5.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l5.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/eq"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }


int main() {
  { try { std::string _v = fmts(f_Fl7_a()); std::cout << "F-l7.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l7.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/cpp/assign"; mkdir -p "$D"; cd "$D"
  cat > main.cpp <<'PROBE_SRC_EOF'
// runner template - c++.
#include <cmath>
#include <iomanip>
#include <iostream>
#include <limits>
#include <sstream>
#include <string>
static bool identb(bool x) { return x; }
static bool notb(bool x) { return (!x); }
static double d(double x) { return x; }
static double z() { return 0.0; }
static double one() { return 1.0; }
static double negone() { return -1.0; }
static double nan_() { return std::numeric_limits<double>::quiet_NaN(); }
static double inf() { return std::numeric_limits<double>::infinity(); }
static double ninf() { return -std::numeric_limits<double>::infinity(); }
static std::string fmtb(bool v) { return v ? "true" : "false"; }
static std::string fmtf(double v) {
  if (std::isnan(v)) return "nan";
  if (std::isinf(v)) return v > 0 ? "inf" : "-inf";
  std::ostringstream o;
  o << std::setprecision(17) << v;
  return o.str();
}
static std::string fmts(std::string v) { return v; }

// list probe bodies - c++ (std::vector). Out-of-range operator[] is UNDEFINED,
// per the owner's ruling probed anyway and recorded, but no answer can be wrong;
// .at() throws, and both spellings are separate guarantees.
#include <algorithm>
#include <vector>
static std::vector<int> mkl() { return std::vector<int>{1, 2, 3}; }
static std::string joinInts(const std::vector<int>& v) {
  std::string out;
  for (size_t i = 0; i < v.size(); i++) { if (i) out += ","; out += std::to_string(v[i]); }
  return out;
}
static std::string f_listU1_a() { std::vector<int> a{1, 2}; a.push_back(3); return joinInts(a); }
static std::string f_listU2_a() { return std::to_string(mkl()[1]); }
static std::string f_listU3_a() { std::vector<int> o; for (int x : mkl()) o.push_back(x); return joinInts(o); }
static std::string f_listU4_a() { return std::to_string(mkl().size()); }
static std::string f_Fl3_a() { std::vector<int> a{1, 2}; a.push_back(3); a.push_back(4); return std::to_string(a.size()); }
static std::string f_Fl4_a() { std::vector<int> a = mkl(); return std::to_string(a[10]); }
static std::string f_Fl4_b() { std::vector<int> a = mkl(); return std::to_string(a.at(10)); }
static std::string f_Fl5_a() { std::vector<int> a = mkl(); return std::to_string(a[-1]); }
static std::string f_Fl6_a() { std::vector<int> a = mkl(); std::vector<int> s(a.begin(), a.begin() + 2); s[0] = 99; return std::to_string(a[0]); }
static std::string f_Fl7_a() { return fmtb(mkl() == mkl()); }
static std::string f_Fl8_a() { std::vector<int> a{10, 9, 1}; std::sort(a.begin(), a.end()); return joinInts(a); }
static std::string f_Fl9_a() { std::vector<int> a = mkl(); std::vector<int> b = a; b[0] = 99; return std::to_string(a[0]); }


int main() {
  { try { std::string _v = fmts(f_Fl9_a()); std::cout << "F-l9.a|" << _v << std::endl; } catch (const std::exception& _e) { std::cout << "F-l9.a|<raise:" << _e.what() << ">" << std::endl; } }
  return 0;
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (g++ -O2 -std=c++17 -o prog main.cpp) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "cpp assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "cpp assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "cpp assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "cpp: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- java
echo ""; echo "--- java ---"
OUT=/out/lst_java.txt
: > "$OUT"
if command -v javac >/dev/null 2>&1; then
  D="$ROOT/java/main"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }


  public static void main(String[] args) {
    try { System.out.println("list-U1.a|" + fmts(f_listU1_a())); } catch (Throwable _e) { System.out.println("list-U1.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("list-U2.a|" + fmts(f_listU2_a())); } catch (Throwable _e) { System.out.println("list-U2.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("list-U3.a|" + fmts(f_listU3_a())); } catch (Throwable _e) { System.out.println("list-U3.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("list-U4.a|" + fmts(f_listU4_a())); } catch (Throwable _e) { System.out.println("list-U4.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-l3.a|" + fmts(f_Fl3_a())); } catch (Throwable _e) { System.out.println("F-l3.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-l6.a|" + fmts(f_Fl6_a())); } catch (Throwable _e) { System.out.println("F-l6.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
    try { System.out.println("F-l8.a|" + fmts(f_Fl8_a())); } catch (Throwable _e) { System.out.println("F-l8.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/l1typed"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }
  // A declared List<Integer> refuses a String element, at build time.
  static String f_Fl1_b() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add("x"); return Integer.toString(a.size());
  }


  public static void main(String[] args) {
    try { System.out.println("F-l1.b|" + fmts(f_Fl1_b())); } catch (Throwable _e) { System.out.println("F-l1.b|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/oob"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }


  public static void main(String[] args) {
    try { System.out.println("F-l4.a|" + fmts(f_Fl4_a())); } catch (Throwable _e) { System.out.println("F-l4.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/neg"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }


  public static void main(String[] args) {
    try { System.out.println("F-l5.a|" + fmts(f_Fl5_a())); } catch (Throwable _e) { System.out.println("F-l5.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/eq"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }


  public static void main(String[] args) {
    try { System.out.println("F-l7.a|" + fmts(f_Fl7_a())); } catch (Throwable _e) { System.out.println("F-l7.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/java/assign"; mkdir -p "$D"; cd "$D"
  cat > Main.java <<'PROBE_SRC_EOF'
// runner template - java.
public class Main {
  static boolean identb(boolean x) { return x; }
  static boolean notb(boolean x) { return (!x); }
  static double d(double x) { return x; }
  static double z() { return 0.0; }
  static double one() { return 1.0; }
  static double negone() { return -1.0; }
  static double nan() { return Double.NaN; }
  static double inf() { return Double.POSITIVE_INFINITY; }
  static double ninf() { return Double.NEGATIVE_INFINITY; }
  static String fmtb(boolean v) { return v ? "true" : "false"; }
  static String fmtf(double v) {
    if (Double.isNaN(v)) return "nan";
    if (v == Double.POSITIVE_INFINITY) return "inf";
    if (v == Double.NEGATIVE_INFINITY) return "-inf";
    return Double.toString(v);
  }
  static String fmts(String v) { return v; }

  // list probe bodies - java (ArrayList; == is identity, .equals is content).
  static java.util.List<Integer> mkl() {
    return new java.util.ArrayList<Integer>(java.util.Arrays.asList(1, 2, 3));
  }
  static String joinInts(java.util.List<Integer> v) {
    StringBuilder b = new StringBuilder();
    for (int i = 0; i < v.size(); i++) { if (i > 0) b.append(","); b.append(v.get(i)); }
    return b.toString();
  }
  static String f_listU1_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); return joinInts(a);
  }
  static String f_listU2_a() { return Integer.toString(mkl().get(1)); }
  static String f_listU3_a() {
    java.util.List<Integer> o = new java.util.ArrayList<Integer>();
    for (int x : mkl()) o.add(x); return joinInts(o);
  }
  static String f_listU4_a() { return Integer.toString(mkl().size()); }
  static String f_Fl3_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>();
    a.add(1); a.add(2); a.add(3); a.add(4); return Integer.toString(a.size());
  }
  static String f_Fl4_a() { return Integer.toString(mkl().get(10)); }
  static String f_Fl5_a() { return Integer.toString(mkl().get(-1)); }
  static String f_Fl6_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> s = a.subList(0, 2);
    s.set(0, 99); return Integer.toString(a.get(0));
  }
  static String f_Fl7_a() { return fmtb(mkl() == mkl()); }
  static String f_Fl8_a() {
    java.util.List<Integer> a = new java.util.ArrayList<Integer>(java.util.Arrays.asList(10, 9, 1));
    java.util.Collections.sort(a); return joinInts(a);
  }
  static String f_Fl9_a() {
    java.util.List<Integer> a = mkl(); java.util.List<Integer> b = a;
    b.set(0, 99); return Integer.toString(a.get(0));
  }


  public static void main(String[] args) {
    try { System.out.println("F-l9.a|" + fmts(f_Fl9_a())); } catch (Throwable _e) { System.out.println("F-l9.a|<raise:" + _e.getClass().getSimpleName() + ">"); }
  }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (javac Main.java) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "java assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java Main) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "java assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "java assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "java: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- rust
echo ""; echo "--- rust ---"
OUT=/out/lst_rust.txt
: > "$OUT"
if command -v rustc >/dev/null 2>&1; then
  D="$ROOT/rust/main"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU1_a()) }); match _r { Ok(_v) => println!("list-U1.a|{}", _v), Err(_) => println!("list-U1.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU2_a()) }); match _r { Ok(_v) => println!("list-U2.a|{}", _v), Err(_) => println!("list-U2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU3_a()) }); match _r { Ok(_v) => println!("list-U3.a|{}", _v), Err(_) => println!("list-U3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU4_a()) }); match _r { Ok(_v) => println!("list-U4.a|{}", _v), Err(_) => println!("list-U4.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl3_a()) }); match _r { Ok(_v) => println!("F-l3.a|{}", _v), Err(_) => println!("F-l3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl4_c()) }); match _r { Ok(_v) => println!("F-l4.c|{}", _v), Err(_) => println!("F-l4.c|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl6_a()) }); match _r { Ok(_v) => println!("F-l6.a|{}", _v), Err(_) => println!("F-l6.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl8_a()) }); match _r { Ok(_v) => println!("F-l8.a|{}", _v), Err(_) => println!("F-l8.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l4.c|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// A declared Vec<i32> refuses a &str element, at build time.
fn f_Fl1_b() -> String { let mut a: Vec<i32> = vec![1, 2]; a.push("x"); format!("{}", a.len()) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl1_b()) }); match _r { Ok(_v) => println!("F-l1.b|{}", _v), Err(_) => println!("F-l1.b|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/oob"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl4_a()) }); match _r { Ok(_v) => println!("F-l4.a|{}", _v), Err(_) => println!("F-l4.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/neg"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// rust indexes by usize; a negative index is not expressible.
fn f_Fl5_a() -> String { let a = mkl(); format!("{}", a[-1]) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl5_a()) }); match _r { Ok(_v) => println!("F-l5.a|{}", _v), Err(_) => println!("F-l5.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/eq"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl7_a()) }); match _r { Ok(_v) => println!("F-l7.a|{}", _v), Err(_) => println!("F-l7.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust/assign"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// `let b = a` MOVES the vector and ENDS a's usability. The compile refusal IS
// rust's assignment guarantee -- the third camp of F-l9, all by itself.
fn f_Fl9_a() -> String { let a = mkl(); let mut b = a; b[0] = 99; format!("{}", a[0]) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl9_a()) }); match _r { Ok(_v) => println!("F-l9.a|{}", _v), Err(_) => println!("F-l9.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "rust: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- rust-release
echo ""; echo "--- rust-release ---"
OUT=/out/lst_rust-release.txt
: > "$OUT"
if command -v rustc >/dev/null 2>&1; then
  D="$ROOT/rust-release/main"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU1_a()) }); match _r { Ok(_v) => println!("list-U1.a|{}", _v), Err(_) => println!("list-U1.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU2_a()) }); match _r { Ok(_v) => println!("list-U2.a|{}", _v), Err(_) => println!("list-U2.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU3_a()) }); match _r { Ok(_v) => println!("list-U3.a|{}", _v), Err(_) => println!("list-U3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_listU4_a()) }); match _r { Ok(_v) => println!("list-U4.a|{}", _v), Err(_) => println!("list-U4.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl3_a()) }); match _r { Ok(_v) => println!("F-l3.a|{}", _v), Err(_) => println!("F-l3.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl4_c()) }); match _r { Ok(_v) => println!("F-l4.c|{}", _v), Err(_) => println!("F-l4.c|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl6_a()) }); match _r { Ok(_v) => println!("F-l6.a|{}", _v), Err(_) => println!("F-l6.a|<raise:panic>") } }
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl8_a()) }); match _r { Ok(_v) => println!("F-l8.a|{}", _v), Err(_) => println!("F-l8.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l4.c|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// A declared Vec<i32> refuses a &str element, at build time.
fn f_Fl1_b() -> String { let mut a: Vec<i32> = vec![1, 2]; a.push("x"); format!("{}", a.len()) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl1_b()) }); match _r { Ok(_v) => println!("F-l1.b|{}", _v), Err(_) => println!("F-l1.b|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/oob"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl4_a()) }); match _r { Ok(_v) => println!("F-l4.a|{}", _v), Err(_) => println!("F-l4.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/neg"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// rust indexes by usize; a negative index is not expressible.
fn f_Fl5_a() -> String { let a = mkl(); format!("{}", a[-1]) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl5_a()) }); match _r { Ok(_v) => println!("F-l5.a|{}", _v), Err(_) => println!("F-l5.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/eq"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl7_a()) }); match _r { Ok(_v) => println!("F-l7.a|{}", _v), Err(_) => println!("F-l7.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/rust-release/assign"; mkdir -p "$D"; cd "$D"
  cat > main.rs <<'PROBE_SRC_EOF'
// runner template - rust.
#![allow(unused)]
fn identb(x: bool) -> bool { x }
fn notb(x: bool) -> bool { (!x) }
fn d(x: f64) -> f64 { x }
fn z() -> f64 { 0.0 }
fn one() -> f64 { 1.0 }
fn negone() -> f64 { -1.0 }
fn nan() -> f64 { f64::NAN }
fn inf() -> f64 { f64::INFINITY }
fn ninf() -> f64 { f64::NEG_INFINITY }
fn fmtb(v: bool) -> String { if v { "true".to_string() } else { "false".to_string() } }
fn fmtf(v: f64) -> String {
    if v.is_nan() { return "nan".to_string(); }
    if v == f64::INFINITY { return "inf".to_string(); }
    if v == f64::NEG_INFINITY { return "-inf".to_string(); }
    format!("{:?}", v)
}

fn fmts(v: String) -> String { v }

// list probe bodies - rust (Vec; slices are BORROWS; assignment MOVES).
fn mkl() -> Vec<i32> { vec![1, 2, 3] }
fn joinInts(v: &Vec<i32>) -> String {
    v.iter().map(|x| x.to_string()).collect::<Vec<String>>().join(",")
}
fn f_listU1_a() -> String { let mut a = vec![1, 2]; a.push(3); joinInts(&a) }
fn f_listU2_a() -> String { format!("{}", mkl()[1]) }
fn f_listU3_a() -> String { let mut o: Vec<i32> = Vec::new(); for x in mkl() { o.push(x); } joinInts(&o) }
fn f_listU4_a() -> String { format!("{}", mkl().len()) }
fn f_Fl3_a() -> String { let mut a = vec![1, 2]; a.push(3); a.push(4); format!("{}", a.len()) }
fn f_Fl4_a() -> String { let a = mkl(); format!("{}", a[10]) }
fn f_Fl4_c() -> String { let a = mkl(); format!("{:?}", a.get(10)) }
fn f_Fl6_a() -> String {
    let mut a = mkl();
    { let s = &mut a[0..2]; s[0] = 99; }
    format!("{}", a[0])
}
fn f_Fl7_a() -> String { fmtb(mkl() == mkl()) }
fn f_Fl8_a() -> String { let mut a = vec![10, 9, 1]; a.sort(); joinInts(&a) }
// `let b = a` MOVES the vector and ENDS a's usability. The compile refusal IS
// rust's assignment guarantee -- the third camp of F-l9, all by itself.
fn f_Fl9_a() -> String { let a = mkl(); let mut b = a; b[0] = 99; format!("{}", a[0]) }


fn main() {
    std::panic::set_hook(Box::new(|_| {}));
    { let _r = std::panic::catch_unwind(|| { fmts(f_Fl9_a()) }); match _r { Ok(_v) => println!("F-l9.a|{}", _v), Err(_) => println!("F-l9.a|<raise:panic>") } }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (rustc -O main.rs -o prog) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "rust-release assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (./prog) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "rust-release assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "rust-release assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "rust-release: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- kotlin
echo ""; echo "--- kotlin ---"
OUT=/out/lst_kotlin.txt
: > "$OUT"
if test -x /persist/kotlinc/bin/kotlinc >/dev/null 2>&1; then
  D="$ROOT/kotlin/main"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }


fun main() {
    try { println("list-U1.a|" + fmts(f_listU1_a())) } catch (_e: Throwable) { println("list-U1.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("list-U2.a|" + fmts(f_listU2_a())) } catch (_e: Throwable) { println("list-U2.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("list-U3.a|" + fmts(f_listU3_a())) } catch (_e: Throwable) { println("list-U3.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("list-U4.a|" + fmts(f_listU4_a())) } catch (_e: Throwable) { println("list-U4.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("F-l3.a|" + fmts(f_Fl3_a())) } catch (_e: Throwable) { println("F-l3.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("F-l6.a|" + fmts(f_Fl6_a())) } catch (_e: Throwable) { println("F-l6.a|<raise:" + _e.javaClass.simpleName + ">") }
    try { println("F-l8.a|" + fmts(f_Fl8_a())) } catch (_e: Throwable) { println("F-l8.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }
// A declared MutableList<Int> refuses a String element, at build time.
fun f_Fl1_b(): String { val a = mutableListOf(1, 2); a.add("x"); return a.size.toString() }


fun main() {
    try { println("F-l1.b|" + fmts(f_Fl1_b())) } catch (_e: Throwable) { println("F-l1.b|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/oob"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }


fun main() {
    try { println("F-l4.a|" + fmts(f_Fl4_a())) } catch (_e: Throwable) { println("F-l4.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/neg"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }


fun main() {
    try { println("F-l5.a|" + fmts(f_Fl5_a())) } catch (_e: Throwable) { println("F-l5.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/eq"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }


fun main() {
    try { println("F-l7.a|" + fmts(f_Fl7_a())) } catch (_e: Throwable) { println("F-l7.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/kotlin/assign"; mkdir -p "$D"; cd "$D"
  cat > main.kt <<'PROBE_SRC_EOF'
// runner template - kotlin.
fun identb(x: Boolean): Boolean = x
fun notb(x: Boolean): Boolean = (!x)
fun d(x: Double): Double = x
fun z(): Double = 0.0
fun one(): Double = 1.0
fun negone(): Double = -1.0
fun nan(): Double = Double.NaN
fun inf(): Double = Double.POSITIVE_INFINITY
fun ninf(): Double = Double.NEGATIVE_INFINITY
fun fmtb(v: Boolean): String = if (v) "true" else "false"
fun fmtf(v: Double): String {
    if (v.isNaN()) return "nan"
    if (v == Double.POSITIVE_INFINITY) return "inf"
    if (v == Double.NEGATIVE_INFINITY) return "-inf"
    return v.toString()
}

fun fmts(v: String): String = v

// list probe bodies - kotlin (MutableList; == is content; slice() COPIES).
fun mkl(): MutableList<Int> = mutableListOf(1, 2, 3)
fun f_listU1_a(): String { val a = mutableListOf(1, 2); a.add(3); return a.joinToString(",") }
fun f_listU2_a(): String = mkl()[1].toString()
fun f_listU3_a(): String { val o = mutableListOf<Int>(); for (x in mkl()) o.add(x); return o.joinToString(",") }
fun f_listU4_a(): String = mkl().size.toString()
fun f_Fl3_a(): String { val a = mutableListOf(1, 2); a.add(3); a.add(4); return a.size.toString() }
fun f_Fl4_a(): String = mkl()[10].toString()
fun f_Fl5_a(): String = mkl()[-1].toString()
fun f_Fl6_a(): String { val a = mkl(); val s = a.slice(0..1).toMutableList(); s[0] = 99; return a[0].toString() }
fun f_Fl7_a(): String = fmtb(mkl() == mkl())
fun f_Fl8_a(): String { val a = mutableListOf(10, 9, 1); a.sort(); return a.joinToString(",") }
fun f_Fl9_a(): String { val a = mkl(); val b = a; b[0] = 99; return a[0].toString() }


fun main() {
    try { println("F-l9.a|" + fmts(f_Fl9_a())) } catch (_e: Throwable) { println("F-l9.a|<raise:" + _e.javaClass.simpleName + ">") }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/kotlinc/bin/kotlinc main.kt -d classes) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "kotlin assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (java -cp classes:/persist/kotlinc/lib/kotlin-stdlib.jar MainKt) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "kotlin assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "kotlin assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "kotlin: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- dart
echo ""; echo "--- dart ---"
OUT=/out/lst_dart.txt
: > "$OUT"
if test -x /persist/dart-sdk/bin/dart >/dev/null 2>&1; then
  D="$ROOT/dart/main"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("list-U1.a|" + fmts(f_listU1_a())); } catch (_e) { print("list-U1.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U2.a|" + fmts(f_listU2_a())); } catch (_e) { print("list-U2.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U3.a|" + fmts(f_listU3_a())); } catch (_e) { print("list-U3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U4.a|" + fmts(f_listU4_a())); } catch (_e) { print("list-U4.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l3.a|" + fmts(f_Fl3_a())); } catch (_e) { print("F-l3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l6.a|" + fmts(f_Fl6_a())); } catch (_e) { print("F-l6.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l8.a|" + fmts(f_Fl8_a())); } catch (_e) { print("F-l8.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }
// A declared List<int> refuses a String element, at build time.
String f_Fl1_b() { final a = <int>[1, 2]; a.add("x"); return a.length.toString(); }


void main() {
  try { print("F-l1.b|" + fmts(f_Fl1_b())); } catch (_e) { print("F-l1.b|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/oob"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l4.a|" + fmts(f_Fl4_a())); } catch (_e) { print("F-l4.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/neg"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l5.a|" + fmts(f_Fl5_a())); } catch (_e) { print("F-l5.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/eq"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l7.a|" + fmts(f_Fl7_a())); } catch (_e) { print("F-l7.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart/assign"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l9.a|" + fmts(f_Fl9_a())); } catch (_e) { print("F-l9.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if [ "$VERDICT" = ok ]; then
    if ! (/persist/dart-sdk/bin/dart run main.dart) > run.out 2> run.err; then
      if [ "$(grep -c "|" run.out)" = 0 ]; then VERDICT="<compile-error>"; else VERDICT="<aborted>"; fi
      echo "dart assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "dart: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

# ---------------------------------------------------------------- dart-web
echo ""; echo "--- dart-web ---"
OUT=/out/lst_dart-web.txt
: > "$OUT"
if test -x /persist/dart-sdk/bin/dart >/dev/null 2>&1; then
  D="$ROOT/dart-web/main"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("list-U1.a|" + fmts(f_listU1_a())); } catch (_e) { print("list-U1.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U2.a|" + fmts(f_listU2_a())); } catch (_e) { print("list-U2.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U3.a|" + fmts(f_listU3_a())); } catch (_e) { print("list-U3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("list-U4.a|" + fmts(f_listU4_a())); } catch (_e) { print("list-U4.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l3.a|" + fmts(f_Fl3_a())); } catch (_e) { print("F-l3.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l6.a|" + fmts(f_Fl6_a())); } catch (_e) { print("F-l6.a|<raise:" + _e.runtimeType.toString() + ">"); }
  try { print("F-l8.a|" + fmts(f_Fl8_a())); } catch (_e) { print("F-l8.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web main: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web main: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web main: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "list-U1.a|$VERDICT" >> "$OUT"
    echo "list-U2.a|$VERDICT" >> "$OUT"
    echo "list-U3.a|$VERDICT" >> "$OUT"
    echo "list-U4.a|$VERDICT" >> "$OUT"
    echo "F-l3.a|$VERDICT" >> "$OUT"
    echo "F-l6.a|$VERDICT" >> "$OUT"
    echo "F-l8.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/l1typed"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }
// A declared List<int> refuses a String element, at build time.
String f_Fl1_b() { final a = <int>[1, 2]; a.add("x"); return a.length.toString(); }


void main() {
  try { print("F-l1.b|" + fmts(f_Fl1_b())); } catch (_e) { print("F-l1.b|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web l1typed: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web l1typed: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web l1typed: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l1.b|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/oob"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l4.a|" + fmts(f_Fl4_a())); } catch (_e) { print("F-l4.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web oob: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web oob: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web oob: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l4.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/neg"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l5.a|" + fmts(f_Fl5_a())); } catch (_e) { print("F-l5.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web neg: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web neg: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web neg: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l5.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/eq"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l7.a|" + fmts(f_Fl7_a())); } catch (_e) { print("F-l7.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web eq: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web eq: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web eq: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l7.a|$VERDICT" >> "$OUT"
  fi
  D="$ROOT/dart-web/assign"; mkdir -p "$D"; cd "$D"
  cat > main.dart <<'PROBE_SRC_EOF'
import 'dart:convert';
// runner template - dart. ONE source, TWO modes (the record-both-modes rule):
//   dart-native  = `dart run main.dart`
//   dart-web     = `dart compile js` then node
// The two modes disagree about integers, which is exactly why both run.
bool identb(bool x) => x;
bool notb(bool x) => (!x);
double d(double x) => x;
double z() => 0.0;
double one() => 1.0;
double negone() => -1.0;
double nan() => double.nan;
double inf() => double.infinity;
double ninf() => double.negativeInfinity;
String fmtb(bool v) => v ? "true" : "false";
String fmtf(double v) {
  if (v.isNaN) return "nan";
  if (v == double.infinity) return "inf";
  if (v == double.negativeInfinity) return "-inf";
  return v.toString();
}
String fmts(String v) => v;

// list probe bodies - dart (List; sublist COPIES; == is identity).
List<int> mkl() => <int>[1, 2, 3];
String f_listU1_a() { final a = <int>[1, 2]; a.add(3); return a.join(","); }
String f_listU2_a() => mkl()[1].toString();
String f_listU3_a() { final o = <int>[]; for (final x in mkl()) { o.add(x); } return o.join(","); }
String f_listU4_a() => mkl().length.toString();
String f_Fl3_a() { final a = <int>[1, 2]; a.add(3); a.add(4); return a.length.toString(); }
String f_Fl4_a() => mkl()[10].toString();
String f_Fl5_a() => mkl()[-1].toString();
String f_Fl6_a() { final a = mkl(); final s = a.sublist(0, 2); s[0] = 99; return a[0].toString(); }
String f_Fl7_a() => fmtb(mkl() == mkl());
String f_Fl8_a() { final a = <int>[10, 9, 1]; a.sort(); return a.join(","); }
String f_Fl9_a() { final a = mkl(); final b = a; b[0] = 99; return a[0].toString(); }


void main() {
  try { print("F-l9.a|" + fmts(f_Fl9_a())); } catch (_e) { print("F-l9.a|<raise:" + _e.runtimeType.toString() + ">"); }
}
PROBE_SRC_EOF
  VERDICT=ok
  if ! (/persist/dart-sdk/bin/dart compile js -O0 -o main.js main.dart) > build.out 2> build.err; then
    VERDICT="<compile-error>"
    echo "dart-web assign: BUILD REFUSED (may itself be the finding)"
    sed -n 1,15p build.err; sed -n 1,15p build.out
  fi
  if [ "$VERDICT" = ok ]; then
    if ! (node main.js) > run.out 2> run.err; then
      VERDICT="<aborted>"
      echo "dart-web assign: RUN ENDED $VERDICT (may itself be the finding)"
      sed -n 1,15p run.err
    fi
    grep "|" run.out >> "$OUT"
    echo "dart-web assign: $(grep -c "|" run.out) lines, verdict $VERDICT"
  fi
  if [ "$VERDICT" != ok ]; then
    echo "F-l9.a|$VERDICT" >> "$OUT"
  fi
else
  echo "dart-web: ABSENT -- skipped"
  echo "__ABSENT__|<absent>" >> "$OUT"
fi

echo ""
echo "=== run complete ==="
ls -l /out/lst_*.txt

