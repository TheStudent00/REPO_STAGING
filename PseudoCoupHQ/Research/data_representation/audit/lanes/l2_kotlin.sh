#!/bin/sh
# layer-2 load audit -- kotlin (batched) -- generated by
# Research/data_representation/audit_generate.py . Do not hand-edit; regenerate.
# Every line of /out/l2_kotlin.txt is FACT_ID|RESULT, FACT_ID being
# <form>.<representation>.<probe>.
set -u
ROOT=/work/l2_kotlin
rm -rf "$ROOT"; mkdir -p "$ROOT"
export HOME=/work
export GOFLAGS=-mod=mod
export GO111MODULE=off
export GOCACHE=/work/.gocache
export GOPATH=/work/.gopath
export PATH=/persist/dart-sdk/bin:$PATH

# swift's binaries want an ncurses name the image does not ship under; the fix
# is per-container, so every script that may touch swift re-applies it.
ncurses_fix() {
  if [ -f /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 ]; then
    ln -sf /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 \
       /usr/lib/x86_64-linux-gnu/libncurses.so.6 2>/dev/null
    ldconfig 2>/dev/null
  fi
}
ncurses_fix

OUT=/out/l2_kotlin.txt
: > "$OUT"
echo "=== layer-2 load audit -- kotlin (batched) ==="
date -u +%Y-%m-%dT%H:%M:%SZ
if ! test -x /persist/kotlinc/bin/kotlinc >/dev/null 2>&1; then
  echo "kotlin: TOOLCHAIN ABSENT -- nothing run"
  echo "__ABSENT__|<absent>" >> "$OUT"
  exit 0
fi

SRCD=$ROOT/src; mkdir -p "$SRCD"; cd "$SRCD"

# ---- nothing.null.null
cat > P1.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int? = null;
    println("nothing.null.null" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nothing.Unit.null
cat > P2.kt <<'L2_SRC_EOF'

fun main() {
    val v: Unit = Unit;
    println("nothing.Unit.null" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nothing.Nothing?.null
cat > P3.kt <<'L2_SRC_EOF'

fun main() {
    val v: Nothing? = null;
    println("nothing.Nothing?.null" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- truth.Boolean.true
cat > P4.kt <<'L2_SRC_EOF'

fun main() {
    val v: Boolean = true;
    println("truth.Boolean.true" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- truth.Boolean.false
cat > P5.kt <<'L2_SRC_EOF'

fun main() {
    val v: Boolean = false;
    println("truth.Boolean.false" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.base_zero
cat > P6.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 0;
    println("whole.Int.base_zero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.base_42
cat > P7.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 42;
    println("whole.Int.base_42" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.i64max
cat > P8.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 9223372036854775807;
    println("whole.Int.i64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.i64max_plus1
cat > P9.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 9223372036854775808;
    println("whole.Int.i64max_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.p53_plus1
cat > P10.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 9007199254740993;
    println("whole.Int.p53_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Int.u64max
cat > P11.kt <<'L2_SRC_EOF'

fun main() {
    val v: Int = 18446744073709551615;
    println("whole.Int.u64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.base_zero
cat > P12.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 0L;
    println("whole.Long.base_zero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.base_42
cat > P13.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 42L;
    println("whole.Long.base_42" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.i64max
cat > P14.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 9223372036854775807L;
    println("whole.Long.i64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.i64max_plus1
cat > P15.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 9223372036854775808L;
    println("whole.Long.i64max_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.p53_plus1
cat > P16.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 9007199254740993L;
    println("whole.Long.p53_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.Long.u64max
cat > P17.kt <<'L2_SRC_EOF'

fun main() {
    val v: Long = 18446744073709551615L;
    println("whole.Long.u64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.base_zero
cat > P18.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 0uL;
    println("whole.ULong.base_zero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.base_42
cat > P19.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 42uL;
    println("whole.ULong.base_42" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.i64max
cat > P20.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 9223372036854775807uL;
    println("whole.ULong.i64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.i64max_plus1
cat > P21.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 9223372036854775808uL;
    println("whole.ULong.i64max_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.p53_plus1
cat > P22.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 9007199254740993uL;
    println("whole.ULong.p53_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.ULong.u64max
cat > P23.kt <<'L2_SRC_EOF'

fun main() {
    val v: ULong = 18446744073709551615uL;
    println("whole.ULong.u64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.base_zero
cat > P24.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("0");
    println("whole.BigInteger.base_zero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.base_42
cat > P25.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("42");
    println("whole.BigInteger.base_42" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.i64max
cat > P26.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("9223372036854775807");
    println("whole.BigInteger.i64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.i64max_plus1
cat > P27.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("9223372036854775808");
    println("whole.BigInteger.i64max_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.p53_plus1
cat > P28.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("9007199254740993");
    println("whole.BigInteger.p53_plus1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- whole.BigInteger.u64max
cat > P29.kt <<'L2_SRC_EOF'
import java.math.BigInteger

fun main() {
    val v: BigInteger = BigInteger("18446744073709551615");
    println("whole.BigInteger.u64max" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.base_1_5
cat > P30.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = 1.5;
    println("fractional.Double.base_1_5" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.base_pi
cat > P31.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = 3.141592653589793;
    println("fractional.Double.base_pi" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.negzero
cat > P32.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = -0.0;
    println("fractional.Double.negzero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.nan
cat > P33.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = Double.NaN;
    println("fractional.Double.nan" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.inf
cat > P34.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = Double.POSITIVE_INFINITY;
    println("fractional.Double.inf" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Double.point1
cat > P35.kt <<'L2_SRC_EOF'

fun main() {
    val v: Double = 0.1;
    println("fractional.Double.point1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.base_1_5
cat > P36.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = 1.5f;
    println("fractional.Float.base_1_5" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.base_pi
cat > P37.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = 3.141592653589793f;
    println("fractional.Float.base_pi" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.negzero
cat > P38.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = -0.0f;
    println("fractional.Float.negzero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.nan
cat > P39.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = Float.NaN;
    println("fractional.Float.nan" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.inf
cat > P40.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = Float.POSITIVE_INFINITY;
    println("fractional.Float.inf" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.Float.point1
cat > P41.kt <<'L2_SRC_EOF'

fun main() {
    val v: Float = 0.1f;
    println("fractional.Float.point1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.base_1_5
cat > P42.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("1.5");
    println("fractional.BigDecimal.base_1_5" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.base_pi
cat > P43.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("3.141592653589793");
    println("fractional.BigDecimal.base_pi" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.negzero
cat > P44.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("-0.0");
    println("fractional.BigDecimal.negzero" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.nan
cat > P45.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("Double.NaN");
    println("fractional.BigDecimal.nan" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.inf
cat > P46.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("Double.POSITIVE_INFINITY");
    println("fractional.BigDecimal.inf" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- fractional.BigDecimal.point1
cat > P47.kt <<'L2_SRC_EOF'
import java.math.BigDecimal

fun main() {
    val v: BigDecimal = BigDecimal("0.1");
    println("fractional.BigDecimal.point1" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.String.base_empty
cat > P48.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "";
    println("text.String.base_empty" + "|" + (v))
}
L2_SRC_EOF

# ---- text.String.base_hello
cat > P49.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "hello";
    println("text.String.base_hello" + "|" + (v))
}
L2_SRC_EOF

# ---- text.String.base_lines
cat > P50.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "line one\nline two";
    println("text.String.base_lines" + "|" + (v))
}
L2_SRC_EOF

# ---- text.String.eacute
cat > P51.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "\u00e9";
    println("text.String.eacute" + "|" + (v))
}
L2_SRC_EOF

# ---- text.String.clef
cat > P52.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "\uD834\uDD1E";
    println("text.String.clef" + "|" + (v))
}
L2_SRC_EOF

# ---- text.String.escapes
cat > P53.kt <<'L2_SRC_EOF'

fun main() {
    val v: String = "quote \" backslash \\ tab\there";
    println("text.String.escapes" + "|" + (v))
}
L2_SRC_EOF

# ---- text.CharArray.base_empty
cat > P54.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("").toCharArray();
    println("text.CharArray.base_empty" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.CharArray.base_hello
cat > P55.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("hello").toCharArray();
    println("text.CharArray.base_hello" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.CharArray.base_lines
cat > P56.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("line one\nline two").toCharArray();
    println("text.CharArray.base_lines" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.CharArray.eacute
cat > P57.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("\u00e9").toCharArray();
    println("text.CharArray.eacute" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.CharArray.clef
cat > P58.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("\uD834\uDD1E").toCharArray();
    println("text.CharArray.clef" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.CharArray.escapes
cat > P59.kt <<'L2_SRC_EOF'

fun main() {
    val v: CharArray = ("quote \" backslash \\ tab\there").toCharArray();
    println("text.CharArray.escapes" + "|" + (String(v)))
}
L2_SRC_EOF

# ---- text.StringBuilder.base_empty
cat > P60.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("");
    println("text.StringBuilder.base_empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.StringBuilder.base_hello
cat > P61.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("hello");
    println("text.StringBuilder.base_hello" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.StringBuilder.base_lines
cat > P62.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("line one\nline two");
    println("text.StringBuilder.base_lines" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.StringBuilder.eacute
cat > P63.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("\u00e9");
    println("text.StringBuilder.eacute" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.StringBuilder.clef
cat > P64.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("\uD834\uDD1E");
    println("text.StringBuilder.clef" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.StringBuilder.escapes
cat > P65.kt <<'L2_SRC_EOF'

fun main() {
    val v: StringBuilder = StringBuilder("quote \" backslash \\ tab\there");
    println("text.StringBuilder.escapes" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- text.ByteArray.base_empty
cat > P66.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.base_empty" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- text.ByteArray.base_hello
cat > P67.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("hello").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.base_hello" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- text.ByteArray.base_lines
cat > P68.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("line one\nline two").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.base_lines" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- text.ByteArray.eacute
cat > P69.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("\u00e9").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.eacute" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- text.ByteArray.clef
cat > P70.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("\uD834\uDD1E").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.clef" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- text.ByteArray.escapes
cat > P71.kt <<'L2_SRC_EOF'

fun main() {
    val v: ByteArray = ("quote \" backslash \\ tab\there").toByteArray(Charsets.UTF_8);
    println("text.ByteArray.escapes" + "|" + (String(v, Charsets.UTF_8)))
}
L2_SRC_EOF

# ---- sequence.List.empty
cat > P72.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<Long> = listOf()
    println("sequence.List.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.List.base
cat > P73.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<Long> = listOf(1, 2, 3)
    println("sequence.List.base" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.List.strs
cat > P74.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<String> = listOf("a", "b")
    println("sequence.List.strs" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.List.mixed
cat > P75.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<Any?> = listOf(1, "two", 3.0, true, null)
    println("sequence.List.mixed" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.List.bigint
cat > P76.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<Long> = listOf(9223372036854775808)
    println("sequence.List.bigint" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.MutableList.empty
cat > P77.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<Long> = mutableListOf()
    println("sequence.MutableList.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.MutableList.base
cat > P78.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<Long> = mutableListOf(1, 2, 3)
    println("sequence.MutableList.base" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.MutableList.strs
cat > P79.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<String> = mutableListOf("a", "b")
    println("sequence.MutableList.strs" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.MutableList.mixed
cat > P80.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<Any?> = mutableListOf(1, "two", 3.0, true, null)
    println("sequence.MutableList.mixed" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.MutableList.bigint
cat > P81.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<Long> = mutableListOf(9223372036854775808)
    println("sequence.MutableList.bigint" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.Array<T>.empty
cat > P82.kt <<'L2_SRC_EOF'

fun main() {
    val v: Array<Long> = arrayOf()
    println("sequence.Array<T>.empty" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.Array<T>.base
cat > P83.kt <<'L2_SRC_EOF'

fun main() {
    val v: Array<Long> = arrayOf(1, 2, 3)
    println("sequence.Array<T>.base" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.Array<T>.strs
cat > P84.kt <<'L2_SRC_EOF'

fun main() {
    val v: Array<String> = arrayOf("a", "b")
    println("sequence.Array<T>.strs" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.Array<T>.mixed
cat > P85.kt <<'L2_SRC_EOF'

fun main() {
    val v: Array<Any?> = arrayOf(1, "two", 3.0, true, null)
    println("sequence.Array<T>.mixed" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.Array<T>.bigint
cat > P86.kt <<'L2_SRC_EOF'

fun main() {
    val v: Array<Long> = arrayOf(9223372036854775808)
    println("sequence.Array<T>.bigint" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.IntArray.empty
cat > P87.kt <<'L2_SRC_EOF'

fun main() {
    val v: IntArray = intArrayOf()
    println("sequence.IntArray.empty" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.IntArray.base
cat > P88.kt <<'L2_SRC_EOF'

fun main() {
    val v: IntArray = intArrayOf(1, 2, 3)
    println("sequence.IntArray.base" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.IntArray.strs
cat > P89.kt <<'L2_SRC_EOF'

fun main() {
    val v: IntArray = intArrayOf("a", "b")
    println("sequence.IntArray.strs" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.IntArray.mixed
cat > P90.kt <<'L2_SRC_EOF'

fun main() {
    val v: IntArray = intArrayOf(1, "two", 3.0, true, null)
    println("sequence.IntArray.mixed" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.IntArray.bigint
cat > P91.kt <<'L2_SRC_EOF'

fun main() {
    val v: IntArray = intArrayOf(9223372036854775808)
    println("sequence.IntArray.bigint" + "|" + (v.contentToString()))
}
L2_SRC_EOF

# ---- sequence.ArrayDeque.empty
cat > P92.kt <<'L2_SRC_EOF'

fun main() {
    val v: ArrayDeque<Long> = ArrayDeque()
    println("sequence.ArrayDeque.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.ArrayDeque.base
cat > P93.kt <<'L2_SRC_EOF'

fun main() {
    val v: ArrayDeque<Long> = ArrayDeque(listOf(1, 2, 3))
    println("sequence.ArrayDeque.base" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.ArrayDeque.strs
cat > P94.kt <<'L2_SRC_EOF'

fun main() {
    val v: ArrayDeque<String> = ArrayDeque(listOf("a", "b"))
    println("sequence.ArrayDeque.strs" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.ArrayDeque.mixed
cat > P95.kt <<'L2_SRC_EOF'

fun main() {
    val v: ArrayDeque<Any?> = ArrayDeque(listOf(1, "two", 3.0, true, null))
    println("sequence.ArrayDeque.mixed" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- sequence.ArrayDeque.bigint
cat > P96.kt <<'L2_SRC_EOF'

fun main() {
    val v: ArrayDeque<Long> = ArrayDeque(listOf(9223372036854775808))
    println("sequence.ArrayDeque.bigint" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.Map.empty
cat > P97.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, Long> = mapOf()
    println("keyed.Map.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.Map.flat
cat > P98.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, Long> = mapOf("a" to 1, "b" to 2, "c" to 3)
    println("keyed.Map.flat" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.Map.intlike
cat > P99.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, String> = mapOf("1" to "string-one-key", "b" to "2")
    println("keyed.Map.intlike" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.Map.emptykey
cat > P100.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, String> = mapOf("" to "empty-string key")
    println("keyed.Map.emptykey" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.Map.nested
cat > P101.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, Map<String, List<Long>>> = mapOf("outer" to mapOf("inner" to listOf(1, 2)))
    println("keyed.Map.nested" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.MutableMap.empty
cat > P102.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableMap<String, Long> = mutableMapOf()
    println("keyed.MutableMap.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.MutableMap.flat
cat > P103.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableMap<String, Long> = mutableMapOf("a" to 1, "b" to 2, "c" to 3)
    println("keyed.MutableMap.flat" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.MutableMap.intlike
cat > P104.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableMap<String, String> = mutableMapOf("1" to "string-one-key", "b" to "2")
    println("keyed.MutableMap.intlike" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.MutableMap.emptykey
cat > P105.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableMap<String, String> = mutableMapOf("" to "empty-string key")
    println("keyed.MutableMap.emptykey" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.MutableMap.nested
cat > P106.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableMap<String, MutableMap<String, MutableList<Long>>> = mutableMapOf("outer" to mutableMapOf("inner" to mutableListOf(1, 2)))
    println("keyed.MutableMap.nested" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.LinkedHashMap.empty
cat > P107.kt <<'L2_SRC_EOF'

fun main() {
    val v = LinkedHashMap<String, Long>()
    println("keyed.LinkedHashMap.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.LinkedHashMap.flat
cat > P108.kt <<'L2_SRC_EOF'

fun main() {
    val v = LinkedHashMap<String, Long>(); v["a"] = 1; v["b"] = 2; v["c"] = 3
    println("keyed.LinkedHashMap.flat" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.LinkedHashMap.intlike
cat > P109.kt <<'L2_SRC_EOF'

fun main() {
    val v = LinkedHashMap<String, String>(); v["1"] = "string-one-key"; v["b"] = "2"
    println("keyed.LinkedHashMap.intlike" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.LinkedHashMap.emptykey
cat > P110.kt <<'L2_SRC_EOF'

fun main() {
    val v = LinkedHashMap<String, String>(); v[""] = "empty-string key"
    println("keyed.LinkedHashMap.emptykey" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.LinkedHashMap.nested
cat > P111.kt <<'L2_SRC_EOF'

fun main() {
    val v = LinkedHashMap<String, LinkedHashMap<String, List<Long>>>(); val inner = LinkedHashMap<String, List<Long>>(); inner["inner"] = listOf(1, 2); v["outer"] = inner
    println("keyed.LinkedHashMap.nested" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.data class.empty
cat > P112.kt <<'L2_SRC_EOF'

fun main() {
    class R0
    val v = R0()
    println("keyed.data class.empty" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.data class.flat
cat > P113.kt <<'L2_SRC_EOF'

fun main() {
    data class R3(val a: Long, val b: Long, val c: Long)
    val v = R3(1, 2, 3)
    println("keyed.data class.flat" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.data class.intlike
cat > P114.kt <<'L2_SRC_EOF'

fun main() {
    data class RI(val `1`: Long, val b: String)
    val v = RI(1, "2")
    println("keyed.data class.intlike" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.data class.emptykey
cat > P115.kt <<'L2_SRC_EOF'

fun main() {
    data class RE(val `` : String)
    val v = RE("empty-string key")
    println("keyed.data class.emptykey" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- keyed.data class.nested
cat > P116.kt <<'L2_SRC_EOF'

fun main() {
    data class RN(val outer: List<Long>)
    val v = RN(listOf(1, 2))
    println("keyed.data class.nested" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.List<List<Any?>>.seq_in_seq
cat > P117.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<List<Long>> = listOf(listOf(1, 2), listOf(3, 4))
    println("nesting.List<List<Any?>>.seq_in_seq" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.List<List<Any?>>.seq_in_map
cat > P118.kt <<'L2_SRC_EOF'

fun main() {
    val v: Map<String, Map<String, List<Long>>> = mapOf("outer" to mapOf("inner" to listOf(1, 2)))
    println("nesting.List<List<Any?>>.seq_in_map" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.List<List<Any?>>.mixed_depth
cat > P119.kt <<'L2_SRC_EOF'

fun main() {
    val v: List<Any?> = listOf(1, listOf(2, mapOf("k" to "t")), true, null)
    println("nesting.List<List<Any?>>.mixed_depth" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.sealed class Node.seq_in_seq
cat > P120.kt <<'L2_SRC_EOF'
sealed class Node
data class NWhole(val n: Long): Node()
data class NSeq(val items: List<Node>): Node()

fun main() {
    val v: Node = NSeq(listOf(NSeq(listOf(NWhole(1), NWhole(2))), NSeq(listOf(NWhole(3), NWhole(4)))))
    println("nesting.sealed class Node.seq_in_seq" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.sealed class Node.seq_in_map
cat > P121.kt <<'L2_SRC_EOF'
sealed class Node
data class NWhole(val n: Long): Node()
data class NSeq(val items: List<Node>): Node()
data class NMap(val entries: Map<String, Node>): Node()

fun main() {
    val v: Node = NMap(mapOf("outer" to NMap(mapOf("inner" to NSeq(listOf(NWhole(1), NWhole(2)))))))
    println("nesting.sealed class Node.seq_in_map" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- nesting.sealed class Node.mixed_depth
cat > P122.kt <<'L2_SRC_EOF'
sealed class Node
data class NNull(val u: Unit = Unit): Node()
data class NTruth(val b: Boolean): Node()
data class NWhole(val n: Long): Node()
data class NText(val s: String): Node()
data class NSeq(val items: List<Node>): Node()
data class NMap(val entries: Map<String, Node>): Node()

fun main() {
    val v: Node = NSeq(listOf(NWhole(1), NSeq(listOf(NWhole(2), NMap(mapOf("k" to NText("t"))))), NTruth(true), NNull()))
    println("nesting.sealed class Node.mixed_depth" + "|" + (v.toString()))
}
L2_SRC_EOF

# ---- identity.MutableList<MutableList<T>> (shared reference).shared
cat > P123.kt <<'L2_SRC_EOF'

fun main() {
    val s: MutableList<Long> = mutableListOf(1, 2, 3)
    val v: MutableList<MutableList<Long>> = mutableListOf(s, s)
    v[0].add(99)
    val r: String = if (v[1].last() == 99L) "shared" else "copied"
    println("identity.MutableList<MutableList<T>> (shared reference).shared" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.MutableList<MutableList<T>> (shared reference).diamond
cat > P124.kt <<'L2_SRC_EOF'

fun main() {
    val t: MutableList<Long> = mutableListOf(7)
    val v: List<MutableList<Long>> = listOf(t, t)
    val r: String = if (v[0] === v[1]) "same" else "distinct"
    println("identity.MutableList<MutableList<T>> (shared reference).diamond" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.MutableList<MutableList<T>> (shared reference).cycle
cat > P125.kt <<'L2_SRC_EOF'

fun main() {
    val v: MutableList<Any?> = mutableListOf()
    v.add(v)
    val r: String = if (v[0] === v) "cycle" else "no-cycle"
    println("identity.MutableList<MutableList<T>> (shared reference).cycle" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class holding a reference.shared
cat > P126.kt <<'L2_SRC_EOF'

fun main() {
    data class Box(val items: MutableList<Long>)
    val s: MutableList<Long> = mutableListOf(1, 2, 3)
    val left = Box(s)
    val right = Box(s)
    left.items.add(99)
    val r: String = if (right.items.last() == 99L) "shared" else "copied"
    println("identity.data class holding a reference.shared" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class holding a reference.diamond
cat > P127.kt <<'L2_SRC_EOF'

fun main() {
    data class Box(val items: MutableList<Long>)
    val t: MutableList<Long> = mutableListOf(7)
    val viaA = Box(t)
    val viaB = Box(t)
    val r: String = if (viaA.items === viaB.items) "same" else "distinct"
    println("identity.data class holding a reference.diamond" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class holding a reference.cycle
cat > P128.kt <<'L2_SRC_EOF'

fun main() {
    class Node(var name: String) { var selfref: Node? = null }
    val n = Node("loop")
    n.selfref = n
    val r: String = if (n.selfref === n) "cycle" else "no-cycle"
    println("identity.data class holding a reference.cycle" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class of primitives (value holder).shared
cat > P129.kt <<'L2_SRC_EOF'

fun main() {
    data class Point(val x: Long, val y: Long)
    val s = Point(1, 2)
    val left = s.copy()
    val right = s.copy()
    val r: String = if (left == right && left !== right) "copied" else "shared"
    println("identity.data class of primitives (value holder).shared" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class of primitives (value holder).diamond
cat > P130.kt <<'L2_SRC_EOF'

fun main() {
    data class Point(val x: Long, val y: Long)
    val t = Point(7, 7)
    val viaA = t.copy()
    val viaB = t.copy()
    val r: String = if (viaA === viaB) "same" else "distinct"
    println("identity.data class of primitives (value holder).diamond" + "|" + (r))
}
L2_SRC_EOF

# ---- identity.data class of primitives (value holder).cycle
cat > P131.kt <<'L2_SRC_EOF'

fun main() {
    data class Point(val x: Long, val self: Point)
    val n = Point(1, Point(2, Point(3, Point(4, null!!))))
    val r: String = if (n.self.self === n) "cycle" else "no-cycle"
    println("identity.data class of primitives (value holder).cycle" + "|" + (r))
}
L2_SRC_EOF

cat > $ROOT/manifest.txt <<'L2_MAN_EOF'
P1 nothing.null.null
P2 nothing.Unit.null
P3 nothing.Nothing?.null
P4 truth.Boolean.true
P5 truth.Boolean.false
P6 whole.Int.base_zero
P7 whole.Int.base_42
P8 whole.Int.i64max
P9 whole.Int.i64max_plus1
P10 whole.Int.p53_plus1
P11 whole.Int.u64max
P12 whole.Long.base_zero
P13 whole.Long.base_42
P14 whole.Long.i64max
P15 whole.Long.i64max_plus1
P16 whole.Long.p53_plus1
P17 whole.Long.u64max
P18 whole.ULong.base_zero
P19 whole.ULong.base_42
P20 whole.ULong.i64max
P21 whole.ULong.i64max_plus1
P22 whole.ULong.p53_plus1
P23 whole.ULong.u64max
P24 whole.BigInteger.base_zero
P25 whole.BigInteger.base_42
P26 whole.BigInteger.i64max
P27 whole.BigInteger.i64max_plus1
P28 whole.BigInteger.p53_plus1
P29 whole.BigInteger.u64max
P30 fractional.Double.base_1_5
P31 fractional.Double.base_pi
P32 fractional.Double.negzero
P33 fractional.Double.nan
P34 fractional.Double.inf
P35 fractional.Double.point1
P36 fractional.Float.base_1_5
P37 fractional.Float.base_pi
P38 fractional.Float.negzero
P39 fractional.Float.nan
P40 fractional.Float.inf
P41 fractional.Float.point1
P42 fractional.BigDecimal.base_1_5
P43 fractional.BigDecimal.base_pi
P44 fractional.BigDecimal.negzero
P45 fractional.BigDecimal.nan
P46 fractional.BigDecimal.inf
P47 fractional.BigDecimal.point1
P48 text.String.base_empty
P49 text.String.base_hello
P50 text.String.base_lines
P51 text.String.eacute
P52 text.String.clef
P53 text.String.escapes
P54 text.CharArray.base_empty
P55 text.CharArray.base_hello
P56 text.CharArray.base_lines
P57 text.CharArray.eacute
P58 text.CharArray.clef
P59 text.CharArray.escapes
P60 text.StringBuilder.base_empty
P61 text.StringBuilder.base_hello
P62 text.StringBuilder.base_lines
P63 text.StringBuilder.eacute
P64 text.StringBuilder.clef
P65 text.StringBuilder.escapes
P66 text.ByteArray.base_empty
P67 text.ByteArray.base_hello
P68 text.ByteArray.base_lines
P69 text.ByteArray.eacute
P70 text.ByteArray.clef
P71 text.ByteArray.escapes
P72 sequence.List.empty
P73 sequence.List.base
P74 sequence.List.strs
P75 sequence.List.mixed
P76 sequence.List.bigint
P77 sequence.MutableList.empty
P78 sequence.MutableList.base
P79 sequence.MutableList.strs
P80 sequence.MutableList.mixed
P81 sequence.MutableList.bigint
P82 sequence.Array<T>.empty
P83 sequence.Array<T>.base
P84 sequence.Array<T>.strs
P85 sequence.Array<T>.mixed
P86 sequence.Array<T>.bigint
P87 sequence.IntArray.empty
P88 sequence.IntArray.base
P89 sequence.IntArray.strs
P90 sequence.IntArray.mixed
P91 sequence.IntArray.bigint
P92 sequence.ArrayDeque.empty
P93 sequence.ArrayDeque.base
P94 sequence.ArrayDeque.strs
P95 sequence.ArrayDeque.mixed
P96 sequence.ArrayDeque.bigint
P97 keyed.Map.empty
P98 keyed.Map.flat
P99 keyed.Map.intlike
P100 keyed.Map.emptykey
P101 keyed.Map.nested
P102 keyed.MutableMap.empty
P103 keyed.MutableMap.flat
P104 keyed.MutableMap.intlike
P105 keyed.MutableMap.emptykey
P106 keyed.MutableMap.nested
P107 keyed.LinkedHashMap.empty
P108 keyed.LinkedHashMap.flat
P109 keyed.LinkedHashMap.intlike
P110 keyed.LinkedHashMap.emptykey
P111 keyed.LinkedHashMap.nested
P112 keyed.data class.empty
P113 keyed.data class.flat
P114 keyed.data class.intlike
P115 keyed.data class.emptykey
P116 keyed.data class.nested
P117 nesting.List<List<Any?>>.seq_in_seq
P118 nesting.List<List<Any?>>.seq_in_map
P119 nesting.List<List<Any?>>.mixed_depth
P120 nesting.sealed class Node.seq_in_seq
P121 nesting.sealed class Node.seq_in_map
P122 nesting.sealed class Node.mixed_depth
P123 identity.MutableList<MutableList<T>> (shared reference).shared
P124 identity.MutableList<MutableList<T>> (shared reference).diamond
P125 identity.MutableList<MutableList<T>> (shared reference).cycle
P126 identity.data class holding a reference.shared
P127 identity.data class holding a reference.diamond
P128 identity.data class holding a reference.cycle
P129 identity.data class of primitives (value holder).shared
P130 identity.data class of primitives (value holder).diamond
P131 identity.data class of primitives (value holder).cycle
L2_MAN_EOF

KC=/persist/kotlinc/bin/kotlinc
STDLIB=/persist/kotlinc/lib/kotlin-stdlib.jar
mkdir -p $ROOT/classes
# chunked batch compile, then per-file retry for any chunk the compiler refused
i=0
CHUNK=""
compile_chunk() {
  [ -z "$1" ] && return 0
  if timeout 900 $KC $1 -nowarn -d $ROOT/classes > $ROOT/kc.out 2>&1; then
    return 0
  fi
  echo "chunk refused, retrying file by file: $1"
  for f in $1; do
    b=$(basename "$f" .kt)
    if ! timeout 300 $KC "$f" -nowarn -d $ROOT/classes > $ROOT/kc_$b.out 2>&1; then
      echo "$b COMPILE-REFUSED"
      echo "$b" >> $ROOT/refused.txt
    fi
  done
}
: > $ROOT/refused.txt
for f in $SRCD/P*.kt; do
  CHUNK="$CHUNK $f"; i=$((i+1))
  if [ $((i % 12)) -eq 0 ]; then compile_chunk "$CHUNK"; CHUNK=""; fi
done
compile_chunk "$CHUNK"

cd $ROOT
while read name pid; do
  if grep -qx "$name" $ROOT/refused.txt 2>/dev/null; then
    echo "$pid|<compile-error>" >> "$OUT"; continue
  fi
  if [ ! -f $ROOT/classes/${name}Kt.class ]; then
    echo "$pid|<compile-error>" >> "$OUT"; continue
  fi
  if timeout 30 java -cp $ROOT/classes:$STDLIB ${name}Kt > r.out 2> r.err; then
    grep -F "$pid|" r.out >> "$OUT" || echo "$pid|<no-output>" >> "$OUT"
  else
    grep -F "$pid|" r.out >> "$OUT" || echo "$pid|<aborted>" >> "$OUT"
  fi
done < $ROOT/manifest.txt


echo ""
echo "=== l2_kotlin complete: $(wc -l < "$OUT") result lines ==="

