---
id: hq.research.compiler_graph.arch_unit.runtime_callee.progress
status: living
---

# PROGRESS — runtime_callee

- 2026-09-03: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_1_arch_unit.md`.
- 2026-09-03: the 308 affected units listed — **done**
  (`out_of_scope_library_calls.json`). Evidence:
  [arch_unit realization](../CORE_0_3_1_1_arch_unit.md).
- 2026-09-03: the cause carried as a census row, 300 units, no
  destination rule invented — **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §3.4.
- 2026-09-03: the "out of scope" verdict superseded by the owner's ruling —
  **done**. Evidence:
  [arch_unit settled rules](../CORE_0_3_1_1_arch_unit.md).
- 2026-09-03: extract the runtime routine's body from the archive and
  attach it — **planned**. Evidence:
  [arch_unit realization](../CORE_0_3_1_1_arch_unit.md).
- 2026-09-03: `call` into the runtime writes the accumulator, as a
  destination rule — **planned**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §9.2.
- 2026-09-03: `runtime_callee.py` written — `class RuntimeCallee` with
  the attribute `archive_paths` and the methods `is_runtime_routine`,
  `extract_callee`, `attach`, exactly the CORE's `## design`. Task 59,
  round 12 (log_158 binding rule 1). Evidence:
  `Research/op_pipeline/runtime_callee.py`.
- 2026-09-03: the archives LOCATED by asking each toolchain, never by
  typing a path — **done**. `gcc -print-libgcc-file-name`;
  `clang -print-file-name=libclang_rt.builtins-x86_64.a`; the same for
  `clang++`; `rustc --print sysroot` and the one `compiler_builtins`
  rlib under it. `swiftc` is NOT on this machine and is recorded as
  not located. Evidence:
  `Research/op_pipeline/runtime_callee_printed.txt` and
  `runtime_callee_units.json` `meta.location_record`.
- 2026-09-03: `is_runtime_routine` answers from MACHINE-FORM EVIDENCE
  — each archive's own symbol index (`nm --print-armap`), never from a
  list of names. All four routines are defined in gcc, clang, clang++
  and rustc's archives. Evidence: the same transcript.
- 2026-09-03: `extract_callee` — **done**. `ar x` the member the index
  names, `objdump -d --disassemble=<routine>`, address and byte
  columns dropped: 12 callee arch units (4 routines x 3 toolchains
  that answered), each with its own bytes, text and arrival contract
  (the families its text reads before it writes them). One body,
  `clang/__divti3`, 27 instructions, is printed literally in the
  transcript. Evidence: `Research/op_pipeline/runtime_callee_units.json`.
- 2026-09-03: `attach` — **done** for 304 of the 308 recorded callers
  (c 136, cpp 164, rust 4); the 4 swift callers are NOT attached,
  because attaching another compiler's archive is the "two toolchains
  stitched together" this CORE forbids. Evidence:
  `Research/op_pipeline/runtime_callee_attachments.json`.
- 2026-09-03: the supersession recorded WITHOUT editing the recorded
  file: `out_of_scope_library_calls.json` is read and left exactly as
  it is, and `out_of_scope_library_calls_superseded.json` states that
  its verdict no longer holds while its list of 308 stands, citing
  log_158. Evidence:
  `Research/op_pipeline/out_of_scope_library_calls_superseded.json`.
- 2026-09-03 (audit, log_165): attachment generalized from the four
  division names to EVERY callee the archive index defines (3,419
  call-bearing units; `__extendhfsf2` alone 2,308 sightings) —
  **planned**, round 13. swift's 4 callers: run the extraction on the
  `trickle` instance where `/persist/swift` lives — **planned**.
- 2026-09-03 (task 63): the attachment GENERALIZED — **done**. The
  census is computed off canon39 itself, not typed: 4,119 of the
  30,432 proved units carry a transfer, over 36 distinct callee names
  (`__extendhfsf2` 1,398 units, `__truncsfbf2` 920, `__truncsfhf2`
  911, `__netf2` 530, …). 3,923 are attached (c 1,579, cpp 2,327,
  rust 4, swift 13); the 196 not attached are go's 170 panic-path
  transfers and rust's 26 `panic_const` transfers, neither of which
  any builtins archive defines. Evidence:
  `Research/op_pipeline/canon39_callee_attachments.json`,
  `canon39_callee_census.json`, `canon39_callee_printed.txt`.
- 2026-09-03 (task 63): `extract_callee` locates a symbol by its
  ADDRESS AND SIZE (`nm -S --defined-only`), not by its name —
  **done**. clang's `comparetf2.c.o` defines `__cmptf2`, `__eqtf2`,
  `__netf2`, `__letf2` and `__lttf2` at one address, and
  `objdump --disassemble=__netf2` prints an empty body for it.
  Evidence: `canon39_callee_printed.txt`; the rule is in this CORE.
- 2026-09-03 (task 63): `extract_closure` — **done**. Nested callees
  are followed relocation-aware with a cycle guard: 76 callee arch
  units, of which 6 were reached ONLY as a nested callee
  (`__clzti2` and `__udivmodti4` in clang's and clang++'s archives,
  `__udivmodti4` in swift's, and rust's `specialized_div_rem`).
  `__stack_chk_fail` is refused by name, because no builtins archive
  index defines it. Evidence:
  `Research/op_pipeline/canon39_callee_units.json`.
- 2026-09-03 (task 63): swift's callers attached, from SWIFT'S OWN
  archive — **done**. The extraction ran as a lane on the `trickle`
  Airlock instance; `swiftc --version` answered `Swift version 6.0.3`
  from inside it, and `/persist/swift/usr/bin/clang
  -print-file-name=libclang_rt.builtins-x86_64.a` named
  `/persist/swift/usr/lib/clang/17/lib/linux/libclang_rt.builtins-x86_64.a`.
  13 swift call-bearing units attached; the 4 swift callers of the
  recorded 308-unit list now REFERENCE their callee body, and their
  answer row is not repointed because those four units are REFUSED in
  canon39 and carry no ledger. Evidence:
  `Research/op_pipeline/canon39_callee_swift_lane.json` and
  `canon39_callee_swift_lane_printed.txt`.
- 2026-09-03 (task 63): guard PASS over all six artifacts, one
  process, `grep -c exempt` = 0. Evidence:
  `Research/op_pipeline/guard63_transcript.txt`.
- 2026-09-03 (task 78): THE 196 CALLERS NO ARCHIVE INDEX DEFINES ARE
  NAMED, in this CORE, before any code. They are two shapes and the
  caller's own text separates them: 176 units whose transfer is the
  last instruction of its block (the routine does not come back) are
  **guard exits**, and 20 go units whose transfer is followed by more
  instructions of the same block (the routine does come back, and the
  caller reads `%rax`) are **unread runtime routines**. Neither makes a
  ledger row; each is recorded as a typed note. THE CHOICE IS FLAGGED
  FOR DEE -- three options are written out in the CORE and only the
  first is implemented. Evidence: this node's CORE, the settled rule
  "WHAT A GO OR RUST PANIC PATH IS IN THE LEDGER'S VOCABULARY", with
  the per-name counts and `go/op_30` printed.
- 2026-09-03 (task 78): THE TWO SHAPES REALIZED and carried on every
  unit as `transfer_shapes` -- **done**, and the naming FLAGGED for
  the owner. Census over canon40's 31,078: guard exit 180 units (go 150,
  rust 30), unread runtime routine 108 units (c 40, cpp 48, go 20).
  The first criterion was WRONG and is recorded as such in the CORE:
  an unlinked `call` disassembles as a transfer to its own next
  address, so "a label follows" is what a RETURNING call looks like
  here; `c/regen_1075` was misread as a guard exit until the
  return-point clause was added. Also done: the four swift callers task
  59 could not attach (`swift/regen_315`, `321`, `327`, `333`) are
  wrapped and proved in canon40. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/audit78_printed.txt`,
  `PRIVATE/PseudoCoupHQ/DevComms/log_185_task78_runtime_answer_register.md`.
