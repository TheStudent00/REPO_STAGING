---
id: hq.research.compiler_graph.term.transcribe.progress
status: living
---

# PROGRESS — transcribe

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_6_term.md`.
- 2026-09-03: walk from OUT-0 through producers and operands to the
  IN rows — **done** (`layer4.py`). Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_147_task48_layer4_ledger_census.md`
  §2.
- 2026-09-03: widths read off the producing line, operand slots
  replayed against the arch text — **done**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_147_task48_layer4_ledger_census.md`
  §2.3, §2.4.
- 2026-09-03: run over canon38, 29,149 terms of 30,436 units —
  **done** (`layer4c.py`). Evidence:
  [term realization](../CORE_0_3_1_6_term.md).
- 2026-09-03: `Term.transcribe` — the walk from OUT-0 over the canon39
  ledger, with each row's line recovered by re-running
  `ledger.Ledger.walk_dataflow` and CHECKED row for row against the
  stored ledger — **done**. Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: five defects found by the gate and fixed, each a unit
  task 58 had proved coming back DISPROVED: the constant-pool counter
  restarting per row (74 units); both halves of one instruction
  reading the state after its first half (`c/op_246`); the x87 stack
  rebuilt by push order instead of by the row's own operand wiring
  (`c/regen_36772`); a literal row published as a register's holder
  (`go/op_206`, 57 units); a value-writing flag setter leaving no
  flags for its reader (`c/op_282`, 1,364 units) — **done**.
  Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`, `task61_resume.md`.
- 2026-09-03: runtime-callee rows: 304 units, 608 rows, all attached
  bodies BRANCH (`call`/`jmp`/`jcc`) and one spells `endbr64`, so
  none yields a term against a text-order reference — a census row
  with its own written reason, not a silent gap. Evidence:
  `name_census5.json`, `~/Programming/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: the walk re-run over every canon39-proved unit with the
  branch-following, callee-stepping `reference.py` of task 64:
  30,013 units build a term, 419 do not, and the term/no-term split
  agrees with task 64's own transcription on **all 30,432** units (0
  disagreements) — **done**. Evidence: `term65_run.log`,
  `audit65_printed.txt`, `~/Programming/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-04: the canon40 transcription's memory bound MEASURED and the
  cause named. `term66_run.check_memory` aborts by name at 6 GB but is
  called BETWEEN shards, and the excursion is inside one: resumed in
  the t83 sandbox the process reached 8,391,436 kB and was ABORTed by
  the container's 8 GB cap in 11 s with 0 further shards walked. The
  runaway is neither a unit nor a callee body — all 76 attached bodies
  walk alone in under 0.21 s and 56,676 kB; the unit the run ABORTed in
  (`c/regen_1859`, 16 body lines, 21 runtime-callee rows) transcribes
  in 50,352 kB and proves. The appetite is the solver's allocator:
  given ceilings of 512 / 1024 / 2048 / 3072 / 5120 MB the peak follows
  the ceiling (464,828 / 971,144 / 2,031,888 / 3,068,796 / 5,178,796 kB)
  and the record is identical at all five — **done**. Evidence:
  `probe83b_memory.py`, `probe83e_callee_bodies.json`,
  `probe83f_one_unit_trace.json`, `probe83g_ceiling.json`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §2.
- 2026-09-04: the ceiling proved ZERO REGRESSION in the ruled sense
  against this corpus's own records: stored shards re-transcribed under
  it and compared record for record — 99 of 99 identical at each of
  4096, 1024 and 512 MB on `op_units2_c_c0002.json`; 237 of 241
  identical over three shards, with the same four go records differing
  under the bound TWICE and unbounded ONCE, so the difference is the
  machine and not the bound — **done**. Evidence: `term66_bounded.py`,
  `~/AirlockRuns/t83/agent/logs/20260904T184356Z__t83_l9_zero_regression.sh.log`,
  `...T184519Z__t83_l11_unbounded_control.sh.log`,
  `...T185115Z__t83_l14_ceiling_throughput.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §2.7–§2.10.
- 2026-09-04: FLAGGED, not decided — `gate.SOLVER_MILLISECONDS = 3000`
  is a wall clock, so a verdict near it is a function of the machine as
  well as of the unit: over 241 records re-transcribed with unchanged
  code, 4 differ (1.66%), 3 only in which counterexample z3 returned
  and 1 (`go/op_103`) moving DISPROVED -> UNDECIDED (0.41%) — **flagged
  for the owner**. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §2.9, §7.1.
- 2026-09-04: THE CORRECTION to the entry above, and the round's
  blocker. The ceiling is neutral on the EASY shards only. On
  `op_units2_c_c0004` (24 records) it changes 12 of 24 records at
  512 MB, 12 at 1024, 9 at 2048, and at 6144 MB — the largest this
  instance's 8 GB cap allows — the peak is 6,002,644 kB, again pinned
  against its own ceiling, and 2 of 24 still differ from what 4096 MB
  wrote. Over 512/1024/2048/4096/6144 MB the hard shard's records
  NEVER CONVERGE. Under 4096 MB the bound FIRED and became a written
  reason: 2 records of 2,701 carried `MemoryError`, because
  `Term.transcribe` records an exception raised while building a row as
  a hole. **term66 over canon40 is NOT COMPLETE and does not fit the
  round's stated 6 GB cap** — **blocked, flagged for the owner**. Evidence:
  `~/AirlockRuns/t83/agent/logs/20260904T193541Z__t83_l18_hard_shard_ceilings.sh.log`,
  `...T194034Z__t83_l19_hard_shard_safety.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §3, §7.2.
- 2026-09-04: the cost located and priced with its population: **3,927
  of the 30,324 units canon40 proves (12.95%) carry a runtime-callee
  row**, 49,362 rows over 30 distinct callees, against 608 rows in
  canon39 — a factor of 81 from task 78's corrected destination rule.
  Those units are the whole cost: `op_units2_c_c0004` walks 24 records
  in 1112 s, `c0005` walks 94 in 2 s; six shards took 2234 s, which
  projects the remaining 316 at about 32 hours. The 2026-09-03 part-run
  finished 2,132 records in 147 s because they held only **32 of the
  3,927** — the other 3,895 are in the 322 inputs still to walk —
  **done** (the measurement; the consequence is the blocker above).
  Evidence: `probe83d_callee_population.json`,
  `~/AirlockRuns/t83/agent/logs/20260904T185651Z__t83_l15_term66_full.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §3.1, §3.4.
- 2026-09-04: the store rolled back to the handoff state. The eight
  shards this session wrote (569 records) are preserved byte for byte
  in `term66_store_bound_fired_records/` with a `README_why.json` and
  removed from `term66_store` and `term66_state.json`;
  `term66_store` is again 10 shards / 2,132 records / 0 records naming
  a memory failure — **done**. Evidence:
  `~/AirlockRuns/t83/agent/logs/20260904T195958Z__t83_l20_rollback.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §3.5, §9.
- 2026-09-04: the unmodified `check_no_spelling_keys.py` over every
  JSON artifact task 83 wrote, ONE process: **27 paths, 27 PASS, 0
  FAIL, exit 0, `grep -c exempt` over the guard's own output 0** —
  **done**. Evidence: `guard83_term_pool_transcript.txt`,
  `~/AirlockRuns/t83/agent/logs/20260904T200124Z__t83_l22_guard.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_189_task83_term_pool_canon40.md` §5.
- 2026-09-05: the cost task 83 priced RE-ATTRIBUTED, and the walk
  finished. Task 83 measured `op_units2_c_c0004` at **1,112 s for 24
  records** and projected the remaining 316 inputs at about 32 hours.
  The cost is the LIFETIME of the z3 context, not the unit:
  `term66_run.run` pushes every unit of every shard through ONE
  `Reference`, ONE `Term` and ONE `Gate`, so each unit pays for
  everything the process built before it. `term97_walk.py` calls
  **`term66_run.one_unit` unedited** from a pristine parent that forks
  one sub-process per unit; the same 24 records took **13.4 s**, a
  factor of 83 — **done**. Evidence: `probe97a_unit_cost.json`,
  `~/AirlockRuns/t97/agent/logs/20260905T070502Z__t97_l1_sample.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §2.
- 2026-09-05: PASS 1 over the 322 inputs task 83 left, at the ordinary
  budget (per-unit ceiling 1,536 MB, per-unit wall clock 120 s enforced
  from outside the sub-process, six slices): **27,964 records walked
  and 228 flagged of the 28,192 units canon40 proves in those inputs,
  in 581.7 s**; the six slice parents peaked at 69,520 to 75,764 kB
  against a stated 6 GB bound, and `ABORT_MEMORY_T97` never fired —
  **done**. Evidence: `term97_flagged_slice0..5.json`,
  `~/AirlockRuns/t97/agent/logs/20260905T071105Z__t97_l3_pass1.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §3.
- 2026-09-05: PASS 2 over everything pass 1 flagged, per the owner's rule of
  2026-09-04. Leg a at 4,096 MB / 1,800 s, leg b at 18,432 MB / 3,600
  s: **of 228 flagged, 184 changed answer and are stored, 44 did not**.
  Direction: every one of the 184 landed on the SAME term state and
  verdict pass 1 had already reached, `TERM / PROVED_ON_SHIP` — the
  larger budget bought the layer-5 key, not a different answer —
  **done**. Evidence: `term97_pass2_a_slice0..3.json`,
  `term97_pass2_b_slice0.json`,
  `~/AirlockRuns/t97/agent/logs/20260905T072358Z__t97_l4_pass2.sh.log`,
  `~/AirlockRuns/t97/agent/logs/20260905T094242Z__t97_l6_pass2_residue.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §4.
- 2026-09-05: THE CONTROL — does the larger budget change an answer the
  ordinary budget already produced? Every 100th stored record re-run at
  4,096 MB / 1,800 s and compared field for field: **302 of 302
  identical, 0 differ**. The sample crosses the 10 inputs walked on the
  host and the 322 walked here, so task 83's flag that the store was
  not homogeneous (4 of 241 differing, 1 of 241 changing state) does
  not reproduce — **done**. Evidence: `term97_control.json`,
  `~/AirlockRuns/t97/agent/logs/20260905T094744Z__t97_l7_finalize_and_control.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §4.4, §9.3.
- 2026-09-05: the store as this round leaves it: **332 shards, 30,280
  records of the 30,324 units canon40 proves (99.855%)**, and
  `term66_state.json` lists **297 of 332** inputs, because an input is
  listed only when every unit canon40 proves in it has a record. The
  44 short are named unit by unit in `term97_finalize.json`. **0
  records in the store name a memory failure as a written reason**
  (audit66's layer-5 line: 27,866 proved terms, 27,866 with a
  normalized text, 0 refused) — **done, bounded**. Evidence:
  `term97_finalize.json`, `audit66_printed.txt`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §5.1, §7.2.
- 2026-09-05: THE FOUR TERM STATES over canon40, over 30,280 records:
  **27,866 proved / 1,676 disproved / 253 undecided / 485 no term**,
  against the round-14 line of 26,594 / 3,134 / 285 / 419 over 30,432,
  which `audit66.py` reproduces off `term65_store` rather than carrying
  from a log. Ten movement rows, each with a computed cause; the
  largest is **1,272 disproved to proved, 1,272 of 1,272 carrying a
  runtime-callee row in canon40** — task 78's corrected destination
  rule landing. The consistency line is **0** — **done**. Evidence:
  `audit66.json`,
  `~/AirlockRuns/t97/agent/logs/20260905T095120Z__t97_l10_downstream_partial.sh.log`;
  `~/Programming/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §5.
