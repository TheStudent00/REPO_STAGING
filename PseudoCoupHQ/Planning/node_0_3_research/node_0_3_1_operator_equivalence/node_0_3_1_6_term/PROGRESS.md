---
id: hq.research.compiler_graph.term.progress
status: living
---

# PROGRESS — term

- 2026-09-03: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: transcribe, normalize, census over canon38: 29,149 terms, 23,132 proved, census4 54 producers / 1,719 rows — **done**. Evidence: log_153 (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: render_back: term -> arch text, the return path — **planned**. Evidence: log_147 §8.1 (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: meanings table unified with reference.opcode_table — **planned**. Evidence: reference CORE (`PseudoCoupHQ/DevComms/`).
- 2026-09-03: `term.py` written — class `Term` with `transcribe`,
  `normalize`, `census`; meanings read from
  `reference.Reference.opcode_table` and from no second table —
  **done**. Evidence: `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: run over the 30,432 units canon39 proved: **26,040
  proved / 0 withdrawn / 3,865 undecided / 527 no term**, against task
  58's 25,179 / 0 / 3,970 / 1,287 over canon38's 30,436. Zero proofs
  lost; 861 gained; consistency line 0 — **done**. Evidence:
  `audit61_printed.txt`, `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: render_back remains **planned** and was NOT attempted;
  `Term.render_back` refuses by name. Evidence: task 61 brief; `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: `render` and `wrap_rendered` BUILT — `term.RenderBack`
  in `term.py`, reached through `Term.render_back`. The inverse map is
  `Reference.opcode_table`'s own builders read backwards (a template
  exists only where that table holds the mnemonic WITH a builder, and
  the constructor refuses otherwise); free symbols are the IN rows the
  prelude fills; the root reaches OUT-0 through the epilogue;
  intermediates become TEMP rows when the ledger walks the rendered
  body — **done**. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/term.py` section 5;
  `PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: `verify` BUILT — the rendered text is wrapped by the same
  `CanonicalForm.wrap` and gated by the same `Gate.prove_wrapped`
  against the unit's OWN ship body; only `PROVED_ON_SHIP` counts —
  **done**. Evidence: `render_back_run.py`;
  `PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: POPULATION ONE, the 158 members of pool entry `E00029`
  (one layer-5 text `v0 + v1`, seven languages): **158 rendered, 158
  assembled by `as`, 158 round-tripped through `objdump -d`, 158
  PROVED_ON_SHIP, 0 refused**. The collapse: 12 distinct layer-3
  wrapped texts to 5 distinct rendered texts; 0 are
  character-identical to their layer-3 text — **done**. Evidence:
  `render_back_E00029_printed.txt`, `render_back_E00029.json`.
- 2026-09-03: POPULATION TWO, all 26,040 proved terms of the 30,432
  canon39-proved units: **5,909 rendered, 5,909 assembled by `as`,
  5,909 round-tripped through `objdump -d`, 5,873 PROVED_ON_SHIP,
  20,131 refused across 17 named causes**. The collapse over the same
  26,040 units: 105 distinct rendered texts against 1,905 distinct
  layer-3 wrapped texts; 0 rendered texts are character-identical to
  their layer-3 text, because the rule always routes through the
  fixed temp pool where the compiler wrote in place — **done**.
  Evidence: `render_back_tally_printed.txt`,
  `render_back_tally.json`, `render_back_store/` (332 shards);
  `PseudoCoupHQ/DevComms/log_170_task66_render_back.md`.
- 2026-09-03: guard: `guard66.py` runs the UNMODIFIED
  `check_no_spelling_keys.py` as one process over all 335 JSON
  artifacts — 335 PASS, 0 FAIL, `grep -c exempt` 0 — **done**.
  Evidence: `guard66_transcript.txt`, `guard66.json`.
- 2026-09-03: the largest remaining cause is `if` (13,027 units): a
  term carrying a conditional has no template, because inverting one
  means rendering the CONDITION as a comparison and the choice as a
  conditional move — `build_set_condition` / `build_move_condition`
  read a flag triple this rule does not yet produce. Named, not worked
  around — **planned**. Evidence: `render_back_tally_printed.txt`.
- 2026-09-03: `term65_store` written over the 30,432 units canon39
  proved, with the round-13 `reference.py` and TASK 64's verdicts as
  the gate of record: **26,594 proved / 3,134 disproved / 285
  undecided / 419 no term**, reproducing task 64's four states
  exactly, per population, with 0 states differing; consistency line
  **0** — **done**. Evidence: `term65_run.log`, `audit65_printed.txt`, `PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03: the 3,134 disproved carried WITH THEIR CAUSE rather
  than dropped: 2,860 + 2 are log_168 section 5.3's one cause (a
  transfer into a routine the toolchain's archive defines with no
  ledger row for it; fix planned round 14), 13 are a transfer to a
  named panic path, 259 have another cause — **done**. Evidence:
  `audit65.json`, `PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03: the normalize rule CORRECTED in its own CORE before any
  code — the step list gains an ORDERING step (the arguments of a
  commutative operator put in a fixed order keyed on the arguments
  themselves: operator name WITH ITS PARAMETERS, sort, and the
  arguments' own keys, free symbols written as their sort) applied
  both after the first simplification and after the renaming. It is
  the repair the CORE's own correction of 2026-09-03 named as planned,
  and it is what makes the layer-5 text a function of the unit rather
  than of what the process built earlier — **done**. Evidence:
  `CORE_0_3_1_6_3_normalize.md` (definition, design, first settled
  rule); `PseudoCoupHQ/DevComms/log_186_task79_normalizer_function_of_unit.md`.
- 2026-09-03: the corrected rule ACCEPTED — three walks in three
  separate processes, two in name order and one with the shards and the
  units inside them shuffled (seed 79), print the SAME layer-5 text for
  **26,594 of 26,594** units whose term the round-13 gate proved; 0
  refusals in each walk; 0 units of the population unprinted. Those
  three walks read the transcription layer pinned to its state of
  23:02, the one that produced `term65_store`'s 26,594; against the
  tree as it now stands, with task 78's corrected destination rule in
  `ledger.py`, the same three walks print **26,152 of 26,152**
  identical and 442 units build no OUT-0 term at all — a change of the
  ledger node's, flagged in log_186 and not decided here. The
  distinct texts over that population fall from **1,267** (one walk of
  the uncorrected rule, `term65_store`) to **850**, the same 850 in all
  three walks; the correction changes the printed text of 8,091 units —
  **done**. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/acceptance79_printed.txt`,
  `normalize79_walk1.log` / `walk2` / `walk3`;
  `PseudoCoupHQ/DevComms/log_186_task79_normalizer_function_of_unit.md`.
- 2026-09-03: what each piece of the repair is worth, measured on the
  `c` shard's 582 proved units walked forward and reversed in one
  process: the rule as it stood 77 of 582 unstable; the ordering step
  with the operator's parameters left out of the key 11; with the float
  operators left out of the table 43; the corrected rule **0** —
  **done**. Evidence: `normalize79_cause_printed.txt`.
- 2026-09-03: THE PREDICTION task 83 checks, computed without
  rebuilding the pool: pool5's **1,831 entries become 1,813** — 50
  entries merge in 21 joins, 11 entries split, 30,432 members
  unchanged. The control (the same three grounds over the same members
  with pool5's own texts) reproduces pool5 member for member. Of the 11
  splits, five separate members z3 proves answer DIFFERENTLY for some
  register values (false merges the unstable numbering had made) and
  six are proved-equal pairs whose terms are structurally different,
  which a text ground could only ever have merged by accident — **done**.
  Evidence: `normalize79_pool_prediction_printed.txt`,
  `normalize79_split_causes_printed.txt`.
- 2026-09-03: guard: `guard79.py` runs the UNMODIFIED
  `check_no_spelling_keys.py` as ONE process over all seven JSON
  artifacts task 79 writes — 7 PASS, 0 FAIL, `grep -c exempt` 0 —
  **done**. Evidence: `guard79_transcript.txt`, `guard79.json`.
- 2026-09-03: TASK 83, the relink's inputs — **a defect found and
  fixed, one cause in a shared layer**. Task 78 gave `ledger.Ledger`
  two new arguments (`runtime_answers`, `toolchain`) and a refusal by
  name when a transfer names an archive-defined routine whose reading
  was not handed in; `term.relink` built its ledger with neither. Over
  the 30,324 units canon40 proved, 3,927 of the 3,962 that built no
  term carried that ONE refusal sentence. The rule
  **THE RELINK IS HANDED EVERY INPUT THE RENDER WAS HANDED** was
  written into `CORE_0_3_1_6_2_transcribe.md` before the code —
  **done**. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/probe83_relink_readings_printed.txt`
  (3,927 of 3,927 re-walk, 0 refuse, 0 disagree),
  `audit66_printed.txt` (the pre-fix measurement that found it).
