---
id: hq.research.compiler_graph.ledger.destination_rules.progress
status: living
---

# PROGRESS — destination_rules

- 2026-09-03: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_3_ledger.md`.
- 2026-09-03: the nine-opcode table written and applied — **done**
  (`ledger48.DESTINATION_RULES`); 3,807 rows. Evidence:
  `PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §2.1.
- 2026-09-03: both halves of a division proved by the gate — **done**.
  Evidence:
  `PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §4.1.
- 2026-09-03: `call` into the compiler's runtime as a destination rule
  — **blocked** on the owner's ruling; every other entry is unaffected.
  Evidence:
  `PseudoCoupHQ/DevComms/log_152_task52_ledger_repaired_canon38.md`
  §9.2.
- 2026-09-03: rule 4 REWRITTEN before any code, under round 12's
  binding rule 2: a transfer into the compiler's own runtime writes
  the accumulator, and the row is produced by
  `{"kind": "runtime_callee", "callee": ...}`. It is NOT keyed by the
  transfer's mnemonic — it fires only when the callee is a routine one
  of this machine's toolchain archives DEFINES. Provenance: the owner,
  2026-09-03; log_158 TASK 59 (b); it answers log_152 §9.2. Evidence:
  this node's CORE, `## design` rule 4.
- 2026-09-03: the rule REALIZED — **done**.
  `ledger.RUNTIME_TRANSFER_RULE`, `ledger.transfer_callee` (which
  reads the callee from the RELOCATION as well as from the assembler
  identifier, because an unlinked transfer disassembles as one going
  inside the unit), and the runtime branch of
  `Ledger.walk_dataflow`. Evidence:
  `Research/op_pipeline/ledger_sample_walk_printed.txt`
  (`c/regen_10427`: `TEMP-4  runtime __divti3 [the runtime routine's
  answer]`, and `OUT-0` produced by it).
- 2026-09-03: the table now sits under this node's own name,
  `ledger.DESTINATION_RULES`, importing nothing from `ledger47`/
  `ledger48`. Evidence: `Research/op_pipeline/ledger.py`.
- 2026-09-03 (task 64): the table's rows QUALIFIED BY OPERAND WIDTH
  and the two narrow widths added, written into the CORE before any
  code -- `AX` dividend and `AL`/`AH` answers at width 8, `DX:AX`
  dividend and `AX`/`DX` answers at width 16, and a one-operand
  widening multiply at width 8 whose whole product IS `AX`. The table
  was silent on width and named only the 64-bit spellings, which is
  why 20 units stopped (log_160 §1.7: 10 at division width 16, 5 at
  width 8, 4 at multiply width 8, 1 at width 16). Evidence: this
  node's CORE, `## design` table and its new settled rule.
- 2026-09-03 (task 64): the width-qualified rows REALIZED in the
  reference's own builders — **done** (`reference.build_narrow_division`
  and `reference.build_narrow_wide_multiply`), printed on lines the
  corpus spells (`idiv %sil`, `idiv %si`, `div %sil`, `mul %sil`,
  `mul %si`) with concrete values through them. `ledger.py`'s
  `DESTINATION_RULES` table itself is unchanged in this lap: its rows
  already fire at any operand count and name the accumulator and the
  data register, and WHICH BITS of those the two narrow widths use is
  the reference's rule, not a second table. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/acceptance64_printed.txt`
  part 4.
- 2026-09-03 (task 64): A DEFECT IN RULE 4, FOUND BY MEASUREMENT AND
  NAMED HERE. Rule 4 says a transfer into the compiler's own runtime
  "writes the accumulator". That is wrong for a routine that answers
  in `%xmm0` -- `__extendhfsf2`, `__truncsfbf2`, `__floatsitf` and
  every other float lowering -- and it is wrong in a second way for
  every one of them: the rule makes ONE row, where the callee's body
  writes several places. MEASURED: 2,862 of canon39's 30,432 units
  carry a transfer into a routine one of this machine's archives
  defines and NO ledger row naming it, so their stored term asserts
  the transfer changed nothing; with the reference now walking that
  callee's body, z3 disproves all 2,862. The general fix is one thing
  in one place -- the runtime set the ledger is handed must be the
  archive index's own answer (`runtime_callee.is_runtime_routine`,
  36 names) rather than the four-name set canon39 was built with, and
  the row must be the callee's ANSWER REGISTER computed off its body.
  Realizing it means REBUILDING canon39, which is node 0_3_5_1 /
  0_3_5_2 / 0_3_5_3's work and not this task's: `term.relink` checks
  its re-run against the stored ledger row for row, so a wider runtime
  set would refuse every one of those units instead. **planned**,
  round 14. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/audit64_printed.txt`
  ("EVERY MOVEMENT, BY COMPUTED CAUSE"), and
  `acceptance64_printed.txt` part 3, where `cpp/regen_12920` proves
  because its answer home is the caller-saved `%rax` and
  `c/regen_1056` is disproved because its answer home IS `%xmm0`.
- 2026-09-03 (task 78): RULE 4 REWRITTEN IN THIS CORE BEFORE ANY CODE,
  under round 12's binding rule 2. A transfer into the compiler's own
  runtime now writes EVERY register family the attached callee's own
  body changes -- one row per family, each produced by
  `{"kind": "runtime_callee", "callee": ...}` -- and the families are
  READ OFF THE CALLEE'S BODY by the six-step procedure the new settled
  rule states (token per family, move-like opcodes propagate, the
  machine stack tracked so a save-and-restore ends unchanged, a tail
  transfer followed with a cycle guard, an x87 answer named, and an
  unreadable callee refused BY NAME rather than guessed). The old
  wording ("writes the accumulator", one row) is kept beside it as
  superseded so the change is visible. Provenance: log_168 §5.3 and its
  2,862 measured units. Evidence: this node's CORE, `## design` rule 4
  and the settled rule "THE ANSWER REGISTERS ARE READ OFF THE ATTACHED
  CALLEE'S BODY".
- 2026-09-03 (task 78): THE CORRECTED RULE 4 REALIZED, AND CANON40
  RENDERED WITH IT -- **done**. `ledger.answer_registers_of_body` reads
  each attached callee's own body; `runtime_answers78.json` holds the
  76 readings (0 refused, 6 answering on the x87 stack, and NOT ONE
  naming a callee-saved family, a cross-check the reading was never
  told about). MEASURED over 31,078: canon39 carried 3,619 units with
  a transfer into an archive-defined routine and no ledger row naming
  it; canon40 carries **0**, and `runtime_callee` rows go 608 ->
  49,362. The wrapped text is byte-identical for all 30,432 units that
  had one, so nothing about layer 3 moved. Evidence:
  `PseudoCoupHQ/Research/op_pipeline/audit78_printed.txt`,
  `acceptance78_printed.txt`,
  `PseudoCoupHQ/DevComms/log_185_task78_runtime_answer_register.md`.
