---
id: hq.research.arch_unit_oracle.compiler_units.lowering_route_cut.progress
status: living
---

# PROGRESS — lowering_route_cut

- 2026-09-06: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_0_compiler_units/CORE_0_3_2_0_compiler_units.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-06: task o5 closed. Deliverable on disk:
  `Research/oracle/compiler_units/lowering_route_cut.{py,json,md}`;
  report `DevComms/log_215_task_o5_lowering_route_cut.md` (501
  lines). Final verifier (`check_conventions_log_claims.py --verify
  --timeout 20`, run from instance `o5`) tallies **claims 13 |
  MATCHES 5 | DIFFERS 0 | UNVERIFIABLE 3 | REFUSED 4 | NOT_RERUNNABLE
  1** after one paste fix (section 1.2's two per-compiler blocks were
  pasted in the wrong order; corrected to match the literal re-run,
  no value changed). Instance brought down.

  Checked log_215 section-by-section against `task_o5_brief.md` §2's
  five items; four gaps found, recorded here per the standing
  instruction to say so rather than write new analysis to close them:
  1. **Item 1 (three populations)** — the brief asks for (a)
     emitters, (b) emitters + one static hop callers, (c)
     diary-visited, with sizes and overlaps. `lowering_route_cut.py`'s
     own docstring (section "WHAT ON THE LOWERING ROUTE MEANS")
     redefines the route as the union of only TWO sets — (a) emitter
     definitions and (b) diary-visited functions (its own labels) —
     and never builds a "one static hop caller" population at all.
     `population_at_each_filter` in the json has no field for it.
  2. **Item 2 (per-population site cuts)** — the brief asks for one
     row per (compiler, population) — i.e. three population rows per
     compiler plus o4's whole-source row repeated for contrast. The
     json/md carry only ONE cut per compiler, over the UNION of (a)
     and (c) (`route_population_union`/`sites_on_route_*`); there is
     no site cut restricted to population (a) alone or (c) alone, and
     no explicit contrast row (the whole-source figure appears only
     as the first line of the same table, not a repeated o4 row).
  3. **Item 3 (variant tables per population)** — the brief asks for
     the variant tables (operator/lhs/rhs/sites) and the unresolved
     histogram separately for population (a) and population (c). Only
     one variant table exists per compiler (`route_variants`, over
     the union); no split by population, no separate unresolved
     histograms.
  4. **Item 4 (three emitter-population sites with their emitted
     instruction)** — not present. `route_examples_sample` carries no
     `instruction`/opcode field at all, and none of the 40 sampled go
     examples has `route_reason: emitter_def` alone (14 are
     `emitter_def+diary`, the rest `diary`) — no example is
     identifiable as coming from the emitter population specifically,
     let alone paired with the instruction it emits from the
     arch-opcode record.
  - Also: the brief's §2 preamble asks that rust/swift ("rust and
    swift have emitter files") get a stated note on what their
    emitter files contain and whether a cut is possible even without
    diaries. `arch_opcode_nodes_{rust,swift}.json` exist on disk
    (210 KB / 420 KB) but log_215 and the deliverable are silent on
    them — they are named only inside a `rows_out_of_scope` list with
    the reason "never ran" (true for the diary side, not addressed
    for the emitter-file side the brief specifically flagged).
  - **Not flagged as a gap**: item 5 (one plain-words paragraph on
    routing use vs whole-source use) — sections 1.2 and 1.4 of
    log_215 carry that comparison in gloss form for both compilers.
  - Awaiting the owner: whether to re-scope o5 to add population (b) and
    the per-population breakdown, or accept the union-only cut as the
    task's answer and close the gap list as known-and-accepted.
