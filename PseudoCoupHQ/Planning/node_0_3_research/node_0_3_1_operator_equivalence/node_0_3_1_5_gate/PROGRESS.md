---
id: hq.research.compiler_graph.gate.progress
status: living
---

# PROGRESS — gate

- 2026-09-03: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: prove_wrapped with structural checks C1–C6; two term routes; zero-regression audits per round — **done**. Evidence: log_152 §1.2, log_153 §6 (`PRIVATE/PseudoCoupHQ/DevComms/`).
- 2026-09-03: gate.py importing reference.py; re-gate the 415 (remainder) and the 5,602 (stack / x87) — **planned**. Evidence: log_153 §12.2 (`PRIVATE/PseudoCoupHQ/DevComms/`).
- 2026-09-03: `gate.py` written -- one class `Gate` with the CORE's
  five methods (`prove_wrapped`, `prove_term_against_ship` importing
  `reference.Reference`, `prove_term_against_text`,
  `structural_checks`, `zero_regression`) and one class `Verdict`;
  `canon37_gate.py`, `canon38_gate.py`, `gate48.py` and
  `textwalk48.py` become superseded records and are not edited --
  **done**. Evidence: log_160 §2, §7 (`PRIVATE/PseudoCoupHQ/DevComms/log_160_task58_gate.md`).
- 2026-09-03: ALL 30,436 canon38 terms re-gated on both routes.
  **23,132 proved / 415 withdrawn / 5,602 undecided / 1,287 no term**
  becomes **25,179 proved / 0 withdrawn / 3,970 undecided / 1,287 no
  term**; the 415 remainder withdrawals all prove; 1,632 of the 5,602
  undecided decide; zero proofs lost -- **done**. Evidence: log_160
  §1, `Research/op_pipeline/audit58_printed.txt`.
- 2026-09-03: `prove_wrapped` re-written over `reference.py` (no
  second simulator; the input rows bound through the prelude's own
  two-step load) and shown proving `c/op_246` -- **done**. Evidence:
  log_160 §4.3, `Research/op_pipeline/acceptance58_printed.txt`.
- 2026-09-03 (task 64): ALL 30,432 canon39 terms re-gated on both
  routes over the branch-following reference — **done**.
  **26,040 proved / 0 disproved / 3,865 undecided / 527 no term**
  becomes **26,594 proved / 3,134 disproved / 285 undecided / 419 no
  term**, per population: original 1,671 -> 1,665 proved, interpreter
  9 -> 9, regenerated 24,360 -> 24,920. Every movement carries a cause
  computed off the unit's own artifacts. 247 units lost a proof: 233
  to a z3 COUNTEREXAMPLE against the unit's own ship body (a proved
  cause, the strongest kind), and 14 to the solver's own 3,000 ms
  limit. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/audit64_printed.txt`,
  `audit64.json`, `regate64_store/` (332 shards).
- 2026-09-03 (task 64): A SILENT GAP IN `prove_term_against_text`
  FOUND AND NAMED — **done**. Route two walked a body down the page and
  quietly ignored a conditional transfer, so where a body holds two
  ALTERNATIVE computations it was reading them as one after the other.
  MEASURED before the fix: 230 units route two called PROVED while
  route one -- now following the branches -- DISPROVED with a
  counterexample, which is exactly the contradiction the gate CORE
  names as "a defect to be found". `c/op_117` is the worked case: `js`
  chooses between converting a value directly and halving it first,
  and route two was adding both answers together. Route two now
  REFUSES BY NAME when a transfer's target is a label the same body
  defines, and continues as before when the target leaves the unit
  (where the fall-through is the only reachable path). After the fix
  the consistency line is **0**. Evidence: `audit64_printed.txt` ("THE
  CONSISTENCY LINE"), `gate.py` (`transfers_inside_the_unit`).
- 2026-09-03 (task 64): the 14 proofs lost to the SOLVER'S OWN TIME
  LIMIT re-read at a longer limit, reported beside the run and not
  folded into it — the run keeps its 3,000 ms verdicts, which is what
  the CORE records the limit on every verdict for. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/timeout64_printed.txt`.
- 2026-09-03 (task 64): the 14 re-read — **done**, and the answer
  settles the zero-regression line. At 30,000 ms ten of the fourteen
  are DISPROVED; at 120,000 ms **all fourteen** are. The 3,000 ms limit
  was not hiding a proof, so every one of the 247 units that lost a
  proof this round lost it to a z3 COUNTEREXAMPLE against the unit's
  own ship body: **zero regressions in the ruled sense**. The run keeps
  its 3,000 ms verdicts and the default is unchanged. Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/timeout64_printed.txt`.
- 2026-09-03 (task 64): the guard — **done**. 334 paths in ONE process
  through the unmodified `check_no_spelling_keys.py`: `audit64.json`,
  `regression64.json` and every one of the 332 shards of
  `regate64_store`. 334 PASS, 0 FAIL, `grep -c exempt` = 0, exit 0.
  Evidence:
  `PRIVATE/PseudoCoupHQ/Research/op_pipeline/guard64_transcript.txt`,
  `guard64.json`.
