population: the units task 61 proved and this round's re-gate left UNDECIDED because the solver ran out of time -- 14 of the 30,432.
   c/op_153         the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/op_225         the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/op_230         the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/regen_13067    the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/regen_13235    the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/regen_6795     the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/regen_6963     the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   c/regen_8834     the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/op_153       the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/op_225       the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/regen_11958  the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/regen_11961  the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/regen_13058  the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 
   cpp/regen_13226  the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never 

at a 30000 ms limit (170 s of wall clock over the 14): {'DISPROVED': 10, 'UNDECIDED': 4}
   c/op_153         DISPROVED
   c/op_225         UNDECIDED
   c/op_230         DISPROVED
   c/regen_13067    UNDECIDED
   c/regen_13235    UNDECIDED
   c/regen_6795     DISPROVED
   c/regen_6963     DISPROVED
   c/regen_8834     DISPROVED
   cpp/op_153       DISPROVED
   cpp/op_225       UNDECIDED
   cpp/regen_11958  DISPROVED
   cpp/regen_11961  DISPROVED
   cpp/regen_13058  DISPROVED
   cpp/regen_13226  DISPROVED

at a 120000 ms limit (198 s of wall clock over the 14): {'DISPROVED': 14}
   c/op_153         DISPROVED
   c/op_225         DISPROVED
   c/op_230         DISPROVED
   c/regen_13067    DISPROVED
   c/regen_13235    DISPROVED
   c/regen_6795     DISPROVED
   c/regen_6963     DISPROVED
   c/regen_8834     DISPROVED
   cpp/op_153       DISPROVED
   cpp/op_225       DISPROVED
   cpp/regen_11958  DISPROVED
   cpp/regen_11961  DISPROVED
   cpp/regen_13058  DISPROVED
   cpp/regen_13226  DISPROVED

