{
 "meta": {
  "generator": "prove_interp_computation.py",
  "task": "TASK 27 -- computation-part proofs for the ruby and php carves",
  "prover": "cross_unit_prover.prove_pair, imported unmodified (z3 over the canon8/canon20 simulators)",
  "candidate_rule": "every compiled 0-branch unit whose class key (type pair, machine-fact result family) is a 64-bit integer pair with a 64-bit integer result: [('u64,u64', 'u64'), ('i64,i64', 'i64')].  No token participates.",
  "candidates_in_that_class": 88,
  "typed_keys": "dwarf_typed_key_t27.json -- read from the same binary each slice came from",
  "value_type_resolution": "ruby's VALUE resolves through DWARF to base type 'long unsigned int', DW_ATE_unsigned (encoding 7), byte size 8 -- measured in both ruby binaries this session, not assumed",
  "provenance_is_weaker": true
 },
 "records": [
  {
   "unit": "ruby/rb_fix_plus",
   "build": "ship",
   "language": "ruby",
   "symbol": "rb_fix_plus",
   "boundary": "add %rdi,%rax",
   "boundary_address": "0x104834",
   "computation_core": [
    "add %rdi,%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": "VALUE,VALUE",
   "dwarf_read_outcome": "READ",
   "dwarf_byte_sizes": [
    8,
    8
   ],
   "canonical_text": "mov %rsi,%rax; add %rdi,%rax; ret",
   "canonicalization_note": "traced values renamed to the designated registers (%rdi -> %rdi, %rax -> %rsi), the answer to %rax, with the entry contract's adapter move on its own line",
   "candidates_considered": 88,
   "proved_equal_to": [
    {
     "compiled_unit": "c/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "c/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_319",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_326",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_541",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_548",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    }
   ],
   "counts": {
    "proved": 8,
    "disproved": 70,
    "undecided": 10,
    "refused": 0
   },
   "verdict": "PROVED"
  },
  {
   "unit": "ruby/rb_int_plus",
   "build": "ship",
   "language": "ruby",
   "symbol": "rb_int_plus",
   "boundary": "add %rbx,%rax",
   "boundary_address": "0xfac1f",
   "computation_core": [
    "add %rbx,%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": "VALUE,VALUE",
   "dwarf_read_outcome": "READ",
   "dwarf_byte_sizes": [
    8,
    8
   ],
   "canonical_text": "mov %rdi,%rax; add %rsi,%rax; ret",
   "canonicalization_note": "traced values renamed to the designated registers (%rbx -> %rsi, %rax -> %rdi), the answer to %rax, with the entry contract's adapter move on its own line",
   "candidates_considered": 88,
   "proved_equal_to": [
    {
     "compiled_unit": "c/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "c/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_319",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_326",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_541",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_548",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    }
   ],
   "counts": {
    "proved": 8,
    "disproved": 70,
    "undecided": 10,
    "refused": 0
   },
   "verdict": "PROVED"
  },
  {
   "unit": "ruby/rb_fix_plus_fix",
   "build": "anchor",
   "language": "ruby",
   "symbol": "rb_fix_plus_fix",
   "boundary": "add %rdx,%rax",
   "boundary_address": "0x30",
   "computation_core": [
    "add %rdx,%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": "VALUE,VALUE",
   "dwarf_read_outcome": "READ",
   "dwarf_byte_sizes": [
    8,
    8
   ],
   "canonical_text": "mov %rsi,%rax; add %rdi,%rax; ret",
   "canonicalization_note": "traced values renamed to the designated registers (%rdx -> %rdi, %rax -> %rsi), the answer to %rax, with the entry contract's adapter move on its own line",
   "candidates_considered": 88,
   "proved_equal_to": [
    {
     "compiled_unit": "c/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "c/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_109",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "cpp/op_116",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_319",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "go/op_326",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_541",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    },
    {
     "compiled_unit": "rust/op_548",
     "compiled_text": "mov %rdi,%rax; mov %rsi,%r10; add %r10,%rax; ret",
     "detail": "z3 proved bit-level equality for every value of every register either text reads before writing (fpToIEEEBV/BitVec comparison, 12000ms timeout)",
     "label_deliberately_absent": "see this file's note on the guard refusal"
    }
   ],
   "counts": {
    "proved": 8,
    "disproved": 70,
    "undecided": 10,
    "refused": 0
   },
   "verdict": "PROVED"
  },
  {
   "unit": "php/add_function",
   "build": "anchor",
   "language": "php",
   "symbol": "add_function",
   "boundary": "or %edx,%eax",
   "boundary_address": "0x5e3e01",
   "computation_core": [
    "or %edx,%eax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": "zval*,zval*",
   "dwarf_read_outcome": "READ",
   "dwarf_byte_sizes": [
    8,
    8
   ],
   "verdict": "TYPE_INCOMPARABLE",
   "why": "the declared operand type is a POINTER pair (zval*,zval*).  The compiled classes in the comparable set carry integer operands; matching a pointer pair to them would be inventing a type, so no proof is attempted."
  },
  {
   "unit": "php/add_function",
   "build": "ship",
   "language": "php",
   "symbol": "add_function",
   "boundary": "or 0x8(%rdx),%al",
   "boundary_address": "0x40c05b",
   "computation_core": [
    "or 0x8(%rdx),%al",
    "cmp $0x44,%al"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": "zval*,zval*",
   "dwarf_read_outcome": "READ",
   "dwarf_byte_sizes": [
    8,
    8
   ],
   "verdict": "TYPE_INCOMPARABLE",
   "why": "the declared operand type is a POINTER pair (zval*,zval*).  The compiled classes in the comparable set carry integer operands; matching a pointer pair to them would be inventing a type, so no proof is attempted."
  },
  {
   "unit": "php/ZEND_ADD_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "anchor",
   "language": "php",
   "symbol": "ZEND_ADD_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add (%rsi),%rax",
   "boundary_address": "0x6db4b2",
   "computation_core": [
    "add (%rsi),%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  },
  {
   "unit": "php/ZEND_ADD_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "ship",
   "language": "php",
   "symbol": "ZEND_ADD_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add (%rsi),%rax",
   "boundary_address": "0x228ca1",
   "computation_core": [
    "add (%rsi),%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  },
  {
   "unit": "php/ZEND_ADD_LONG_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "anchor",
   "language": "php",
   "symbol": "ZEND_ADD_LONG_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add (%rsi),%rax",
   "boundary_address": "0x6dcb82",
   "computation_core": [
    "add (%rsi),%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  },
  {
   "unit": "php/ZEND_ADD_LONG_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "ship",
   "language": "php",
   "symbol": "ZEND_ADD_LONG_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add (%rcx),%rax",
   "boundary_address": "0x2257fc",
   "computation_core": [
    "add (%rcx),%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  },
  {
   "unit": "php/ZEND_ADD_LONG_NO_OVERFLOW_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "anchor",
   "language": "php",
   "symbol": "ZEND_ADD_LONG_NO_OVERFLOW_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add %rax,%rdx",
   "boundary_address": "0x6dcae9",
   "computation_core": [
    "add %rax,%rdx"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  },
  {
   "unit": "php/ZEND_ADD_LONG_NO_OVERFLOW_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "build": "ship",
   "language": "php",
   "symbol": "ZEND_ADD_LONG_NO_OVERFLOW_SPEC_TMPVARCV_TMPVARCV_HANDLER",
   "boundary": "add (%r14,%rcx,1),%rax",
   "boundary_address": "0x2257ce",
   "computation_core": [
    "add (%r14,%rcx,1),%rax"
   ],
   "provenance_is_weaker": true,
   "dwarf_typed_key": null,
   "dwarf_read_outcome": "REFUSED",
   "verdict": "TYPE_INCOMPARABLE",
   "why": "no typed key could be read for this unit at this build, so there is no measured operand type to match against a compiled class key.  The DWARF refusal, verbatim: the DIE was found and carries 0 formal parameter(s); a two-operand typed key is not expressible from parameter types for this handler -- the operands reach it another way (php's specialized executor handlers read them through the execute_data frame)"
  }
 ],
 "summary": {
  "carved_units_considered": 11,
  "proved": 3,
  "type_incomparable": 8,
  "not_renderable": 0,
  "undecided": 0,
  "no_match": 0
 }
}