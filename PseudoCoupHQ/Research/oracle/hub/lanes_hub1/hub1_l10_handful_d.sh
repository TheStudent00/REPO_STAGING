#!/usr/bin/env bash
# hub1_l10_handful_d.sh -- task hub1, lane 10: the handful, fourth pass.
# Since lane hub1_l9: a hole at a node whose cell the dictionary has no
# proved entry for now carries the DICTIONARY's own cause; the cause for a
# construct the narrow rule does not hold says what the corpus unit's body
# is rather than mis-stating its row count; and go's identity check is
# RE-POSED against the same composed file with `//go:noinline` dropped
# from each emulation, because the directive forbids exactly the
# cross-operator lowering source composition exists to obtain. Both go
# verdicts are recorded. Memory bound 6 GB, abort ABORT_MEMORY_HUB1.
set -uo pipefail
echo "[1/2] the handful"
cd PseudoCoupHQ/Research/oracle/hub
python3 hub.py handful
echo "[2/2] the spelling guard over what this lane wrote"
python3 PseudoCoupHQ/Research/op_pipeline/check_no_spelling_keys.py \
    PseudoCoupHQ/Research/oracle/hub/oracle_test.json
