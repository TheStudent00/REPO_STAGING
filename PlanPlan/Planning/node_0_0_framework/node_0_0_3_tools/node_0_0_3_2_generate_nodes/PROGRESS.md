---
id: pp.framework.tools.generate_nodes.progress
status: living
---

# PROGRESS — generate_nodes

- 2026-08-01: node folder generated by
  `<WORKSPACE_DIR>/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `<WORKSPACE_DIR>/PlanPlan/Planning/node_0_0_framework/node_0_0_3_tools/CORE_0_0_3_tools.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-08-05: **the skeletons now arrive conformant.** A generated
  CORE gets `node`, `super_node` and `sub_nodes: []`; a generated
  CHECK gets `id: <node id>.check`. Before this, every newly created
  node was born failing two rules the framework had already adopted,
  and the next check reported it as if someone had written it wrong.
  - The CHECK half was found the same day, by the generator itself:
    creating the `explorer` node put PlanPlan's tree back to 1
    warning, and the warning was the CHECK file the generator had just
    written. A tool that emits non-conformant output is worse than one
    that emits none — the defect looks like an author's.
- 2026-08-05: **`--adopt-edges` and `--adopt-checks` added**, the
  wholesale counterpart to `--adopt`. Same contract as everything else
  here: dry run by default, never overwrite an existing field, write
  only what is already true on disk. Detail and the reasoning for a
  tree-wide pass are in the framework node's PROGRESS.
- 2026-08-05: `chain_of` returns `"0"` for a planning root, not `"1"`,
  after the address renumbering. The docstring carries the reason.
