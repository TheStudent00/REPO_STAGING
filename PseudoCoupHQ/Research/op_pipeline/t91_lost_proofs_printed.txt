units log 153 records as proved that do not prove now: 245
bodies found in canon38:                              245
[1/245] c/op_117
[26/245] c/regen_2559
[51/245] c/regen_45894
[76/245] cpp/op_590
[101/245] cpp/regen_36477
[126/245] cpp/regen_69474
[151/245] swift/regen_1899
[176/245] swift/regen_1982
[201/245] swift/regen_2033
[226/245] swift/regen_2114

THE 245 LOST PROOFS, BY THE SHAPE OF THEIR OWN TEXT
    135 | carries a positional label and carries a conditional transfer
    110 | carries a positional label and carries a conditional transfer and carries an unconditional transfer or a trap

WHICH OF log 153's TWO ROUTES CARRIED THE PROOF THAT FELL
    245 | route two alone proved it in log 153

HOW IT FALLS IN THIS ROUND, in the two routes' own words
    129 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     38 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jae L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     16 | route one UNDECIDED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     14 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jae L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     12 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jge L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     11 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jb L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
     10 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jg L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
      6 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'ja L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
      4 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jbe L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
      3 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jl L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
      1 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'jb L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
      1 | route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L1', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach

THE TWO REFERENCES ON THE SAME BODY
    245 | the SUPERSEDED simulator refuses this body; the ONE reference answers it
          sightings: c/op_117, c/op_122, c/op_153

ONE INSTANCE OF EACH, WITH VALUES

  c/op_117 -- the SUPERSEDED simulator refuses this body; the ONE reference answers it
    LITERAL -- body: test %rdi,%rdi; js L0; cvtsi2ss %rdi,%xmm1; addss %xmm1,%xmm0; ret; L0:; mov %rdi,%rax; shr $1,%rax; and $0x1,%edi; or %rax,%rdi; cvtsi2ss %rdi,%xmm1; addss %xmm1,%xmm1; addss %xmm1,%xmm0; ret
    LITERAL -- the SUPERSEDED simulator's answer: None
    LITERAL -- the ONE reference's answer:        If(0 <= seed_rdi,
   fp.to_ieee_bv(fpToFP(Extract(31, 0, seed_xmm0)) + fpToFP(fp.to_ieee_bv(fpToFP(RNE(), seed_rdi)))),
   fp.to_ieee_bv(fpToFP(Extract(31, 0, seed_xmm0)) + fpToFP(fp.to_ieee_bv(fpToFP(fp.to_ieee_bv(fpToFP(RNE(), Concat(0,
                                        Extract(63,
                                        1,
                                        seed_rdi) |
                                        Concat(0,
                                        Extract(0,
                                        0,
                                        seed_rdi)))))) + fpToFP(fp.to_
    LITERAL -- they differ at: None
    log 153's proving route: route two alone proved it in log 153
    how it falls now:        route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
    this round, attached_callees: withdrawn -- z3 found a starting state under which the two sides differ
    this round, no_attached_callees: withdrawn -- z3 found a starting state under which the two sides differ

  c/op_122 -- the SUPERSEDED simulator refuses this body; the ONE reference answers it
    LITERAL -- body: test %rdi,%rdi; js L0; cvtsi2ss %rdi,%xmm1; addss %xmm1,%xmm0; ret; L0:; mov %rdi,%rax; shr $1,%rax; and $0x1,%edi; or %rax,%rdi; cvtsi2ss %rdi,%xmm1; addss %xmm1,%xmm1; addss %xmm1,%xmm0; ret
    LITERAL -- the SUPERSEDED simulator's answer: None
    LITERAL -- the ONE reference's answer:        If(0 <= seed_rdi,
   fp.to_ieee_bv(fpToFP(Extract(31, 0, seed_xmm0)) + fpToFP(fp.to_ieee_bv(fpToFP(RNE(), seed_rdi)))),
   fp.to_ieee_bv(fpToFP(Extract(31, 0, seed_xmm0)) + fpToFP(fp.to_ieee_bv(fpToFP(fp.to_ieee_bv(fpToFP(RNE(), Concat(0,
                                        Extract(63,
                                        1,
                                        seed_rdi) |
                                        Concat(0,
                                        Extract(0,
                                        0,
                                        seed_rdi)))))) + fpToFP(fp.to_
    LITERAL -- they differ at: None
    log 153's proving route: route two alone proved it in log 153
    how it falls now:        route one DISPROVED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
    this round, attached_callees: withdrawn -- z3 found a starting state under which the two sides differ
    this round, no_attached_callees: withdrawn -- z3 found a starting state under which the two sides differ

  c/op_153 -- the SUPERSEDED simulator refuses this body; the ONE reference answers it
    LITERAL -- body: test %rdi,%rdi; js L0; cvtsi2ss %rdi,%xmm1; jmp L1; L0:; mov %rdi,%rax; shr $1,%rax; and $0x1,%edi; or %rax,%rdi; cvtsi2ss %rdi,%xmm1; addss %xmm1,%xmm1; L1:; subss %xmm0,%xmm1; movaps %xmm1,%xmm0; ret
    LITERAL -- the SUPERSEDED simulator's answer: None
    LITERAL -- the ONE reference's answer:        fp.to_ieee_bv(fpToFP(If(0 <= seed_rdi,
                 fp.to_ieee_bv(fpToFP(RNE(), seed_rdi)),
                 fp.to_ieee_bv(fpToFP(fp.to_ieee_bv(fpToFP(RNE(), Concat(0,
                                        Extract(63,
                                        1,
                                        seed_rdi) |
                                        Concat(0,
                                        Extract(0,
                                        0,
                                        seed_rdi)))))) + fpToFP(fp.to_ieee_bv(fpToFP(RNE(), Concat(0,
                                   
    LITERAL -- they differ at: None
    log 153's proving route: route two alone proved it in log 153
    how it falls now:        route one UNDECIDED / route two UNDECIDED -- route two's own words: the text-order walk: this body transfers to 'js L0', a label it defines itself, so the page order is not the run order and a text-order walk has no one answer to reach
    this round, attached_callees: undecided -- the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never disproved
    this round, no_attached_callees: undecided -- the solver did not answer inside its 3000 ms limit, so the verdict is undecided and never disproved

wrote PseudoCoupHQ/Research/op_pipeline/t91_lost_proofs.json
PEAK RESIDENT SIZE: 98.8 MB, cap 6000 MB
