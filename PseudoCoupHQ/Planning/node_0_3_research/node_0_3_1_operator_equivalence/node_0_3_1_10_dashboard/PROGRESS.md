---
id: hq.research.compiler_graph.dashboard.progress
status: living
---

# PROGRESS — dashboard

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: node written at the owner's request for the dashboard and its
  compiler-graph pane (log_172 briefs). Every item in the CORE's
  realization table marked **planned** is round-14 work.
- 2026-09-03: `chronology` added at the owner's request (step through the
  vcs history; numbers recomputed from the artifacts git holds at
  each banking commit, testimony where untracked) — **planned**,
  task 76 in log_172.
- 2026-09-03: **task 68 landed — the live loader.** `dashboard.html` now
  opens by double-click, asks once for the folder through the File System
  Access API, and reads the artifacts itself in the browser. No server, no
  terminal, no build step. Measured, all 31,078 units and not a sample:
  **first paint 604 ms** in an interactive Chrome pane and **82–99 ms** in
  headless Chrome (both after the folder was granted, both well under the
  two seconds asked for), from 336 summary reads; the full unit index —
  31,078 units, 162 arch opcodes — completes in the background 6.7 s
  (headless) to 15.3 s (interactive pane) later, with each pane's
  population line ticking as it lands. The trick is that every artifact of
  this line writes `meta` and `tally` first and every graph file writes
  `pins` and `counts` first, so the first paint reads prefix slices —
  4 KB of each of 326 shards, 256 KB of each 331 MB graph — and one tail
  slice for `the_pool5.json`'s summary. Files:
  `Research/op_pipeline/dashboard.html`, `dashboard_loader.js`,
  `dashboard_join.js`. Evidence: `DevComms/log_176_task68_dashboard_live_loader.md`,
  screenshots under `DevComms/screens/log_176/`.
- 2026-09-03: **one join, proved shared.** The join and every pane are
  written once, in `dashboard_join.js`. The live page loads it with a
  script tag; `viewer_build.py` embeds the same file verbatim into
  `dashboard_snapshot.html`. `diff` between the JS extracted from the
  snapshot and `dashboard_join.js` reports nothing and both sha256 to
  `f045bb4b…`. `viewer_build.py` no longer holds a join of its own.
- 2026-09-03: **the spelling ban, mechanically checked on both sides.**
  Everything the page groups, selects or compares by is `opGroup`, an
  opaque id minted per language (`c#g07`); the token rides beside it as a
  display label. `?labels=glyph` replaces every label with ◆ and every pane
  still works (screenshots `glyph_test_units.png`, `glyph_test_opcodes.png`).
  The unmodified `check_no_spelling_keys.py` walks the json the builder
  emits and the builder refuses its own output on failure — PASS. A new
  `check_dashboard_js_no_spelling.py` runs the same operator inventory over
  the page's javascript — PASS on all four files, with the six coincidences
  (`&`/`<` html escaping, `/` the unit-id separator) named rather than
  waved away.
- 2026-09-03: **two constraints measured, not assumed.** A page opened by
  double-click has origin `null` and `indexedDB.open` there fires no event
  at all in Chrome, so the remembered folder is impossible from a
  double-clicked file; the loader refuses storage on an opaque origin,
  wires the folder button first, and the gate says so. Firefox 154.0.1 has
  no `showDirectoryPicker`; the page detects it, says so, and points at the
  snapshot, which was then opened in Firefox and works.
- 2026-09-03: **task 76 landed — pane 6, the chronology.** The line's
  progress is now stated from the repository's own history instead of from
  memory. `Research/op_pipeline/chronology_build.py` walks two scales —
  the commits whose message says a round was banked, and the last commit of
  every day since 2026-07-31 — and at each one asks git for the stat
  artifacts that commit's tree names (`git cat-file` on the blob) and
  RECOMPUTES the stats pane's numbers from them. **38 steps, 12 of them
  carrying at least one recomputed number, 1 carrying testimony, 25 with no
  stat artifact tracked at all.** Output `chronology.json` (56,904 bytes,
  tracked). Pane 6 is `Research/op_pipeline/dashboard_pane6.js`: a slider
  over the rounds with a day strip under it, the stats table as it stood,
  the change from the previous step, that round's logs and the bank message,
  and a population line naming the commit hash and date. Evidence:
  `DevComms/log_180_task76_pane6_chronology.md`, screenshots under
  `DevComms/screens/log_180/`.
- 2026-09-03: **the recomputation rule agrees with the testimony where both
  exist, which is what makes it checkable.** Round 11's bank message says
  23,132 proved / 415 withdrawn / 5,602 undecided / 1,287 no-term; walking
  `layer4c_terms_*.json` + `layer4c_interp.json` + `layer4c_regen_store/`
  at that commit and partitioning 30,436 rows by the two gate routes gives
  23,132 / 415 / 5,602 / 1,287 — the same four numbers, derived rather than
  copied. Round 13's 26,594 proved terms likewise agrees with
  `audit65.json`'s `layer5.proved_terms`.
- 2026-09-03: **what the history actually says, and it is not what a bank
  message says on its own.** Round 12 (`term61_store`) has **zero**
  withdrawn terms and 3,865 undecided; round 13 (`term65_store`) has 3,134
  withdrawn and 285 undecided. The re-gate of task 64 is what turned
  undecided into disproved — visible only because both generations were
  recomputed side by side.
- 2026-09-03: **the material is thinner than the brief assumed, stated
  rather than papered over.** `git log --grep=banked -i` matches 12 commits,
  not 13: two are the daemon committing the same round-4 message twice, two
  the same round-10 message twice, one is the round-4 RETRACTION OF RECORD
  (no round banked), and one is the 2026-08-24 lap bank. Eight distinct
  rounds are named — 4, 7, 8, 9, 10, 11, 12, 13; **rounds 5 and 6 have no
  banking commit at all**. And the research artifacts were only committed
  from 2026-08-25 on, so the 27 day steps before 2026-08-31 have nothing to
  recompute and the pane says so on the step rather than showing a gap.
- 2026-09-03: **`chronology_build.py --append` is a step of every bank task
  from now on** and is idempotent: run three times in a row the `steps`
  array is byte-identical (`c4fff900…` all three times). A day step is the
  LAST commit of its day, and while the day is still running that commit
  moves, so the day already carried is replaced rather than added beside.
- 2026-09-03: **guards, unmodified, one process each.** `check_no_spelling_keys.py`
  PASSes `chronology.json` (the program refuses its own output on failure and
  deletes the file); `check_dashboard_js_no_spelling.py` PASSes
  `dashboard_pane6.js` with 0 named coincidences. `grep -c exempt` over both
  transcripts is 0.
- 2026-09-03: **pane 1 completed (task 69): four more modes on the unit
  viewer, in a new module `Research/op_pipeline/dashboard_pane1.js`.**
  `context` shows the bytes of every constant the body reaches
  rip-relative, with the population line "329 of 31,078 units reach a
  constant at an offset from the instruction pointer; 300 of those have
  their bytes, 29 do not; 524 sites in all"; `rendered back, beside the
  wrapped` shows the two texts side by side with the verdict, over
  render_back_store's 5,909 rendered of 26,040 units with a proved term;
  `verdicts` shows the state, BOTH term routes with their outcomes,
  reasons and the counterexample where there is one, over term65_store's
  30,432 records (26,594 proved / 3,134 disproved / 285 undecided / 419
  no term); `interpreter handler source` shows the handler's C source for
  the 1 of 11 interpreter units whose record carries it and says "no
  source recorded" with the cause for the other 10. Every mode reads its
  file at the moment the unit is opened. Evidence:
  `DevComms/log_178_task69_pane1_context_verdicts.md`, screenshots under
  `DevComms/screens/log_178/`.
- 2026-09-03: **the added modes touch nothing they share.** `dashboard.html`
  gained one script line; `dashboard_join.js` gained three hooks (18 lines,
  additive, `git diff --stat`); `viewer_build.py` embeds the new module at
  the same seam panes 2 and 3 use. Two new sidecars,
  `canon39_context.json` and `canon39_interp_source.json`, are written by
  `context.py` and `unit_viewer_interp_source.py`.
- 2026-09-03: **the spelling ban was run, not asserted.** `?labels=glyph`
  replaces every operator label with a glyph and all four new modes still
  draw: `DevComms/screens/log_178/pane1_glyph_test_context.png`. Guards,
  unmodified, one process each: `check_no_spelling_keys.py` PASSes
  `canon39_context.json` and `canon39_interp_source.json`;
  `check_dashboard_js_no_spelling.py` PASSes `dashboard_pane1.js`.
- 2026-09-03: **task 70 landed — the selector and the arch opcode index
  over the whole population.** Both panes now run on all **31,078**
  arch-units rather than the 2,456-unit sample, and the hole task 68
  named is closed: **31,067 of 31,078** index rows carry a type
  signature, leaving **11** that cannot have one — the interpreter
  handlers, which have no probe (cpython 1, java 2, php 4, ruby 4). The
  selector's third menu therefore lists **3,390 distinct signatures**
  beside **131 operator groups** over **9 languages**, and the arch
  opcode index groups its **162 opcodes** by (language, operator group,
  signature) — **151,279** such groups in total. The signature is the
  probe's DECLARED types with the result read off the OUT row's own
  width when the compiler stated none, which is `signatureOf`, called
  rather than restated. Its inputs are **133,993 probes read from 10
  probe manifests, 94.2 MB of text**, taken by one regular expression
  instead of a json parse; `pane23_manifest_regex_check.py` proves that
  expression identical to `json.load` on every one of those probes.
  File: `Research/op_pipeline/dashboard_pane23.js`. Evidence:
  `DevComms/log_179_task70_panes23_full_population.md`, screenshots under
  `DevComms/screens/log_179/`.
- 2026-09-03: **the numbers on the page were checked against python, not
  trusted.** `pane23_ground_truth.py` recomputes the whole index off the
  same files on disk; the browser's own console line and python's agree
  on every figure — 31,078 rows, 31,067 with a signature, 11 without,
  3,390 signatures, 131 operator groups, 9 languages, 162 arch opcodes.
- 2026-09-03: **the spelling ban was RUN over both panes.** With
  `?labels=glyph` replacing every operator label, `pane23_clickthrough.py`
  drove a real browser through **5 panes, 10 language entries, 272
  operator-group entries, 3,391 type-signature entries, 162 arch opcode
  entries, 37,483 opcode narrowing-menu entries, 9 unit rows and 63
  viewer modes — 0 errors**. Guards, unmodified, one process each:
  `check_no_spelling_keys.py` PASSes `pane23_ground_truth.json`;
  `check_dashboard_js_no_spelling.py` PASSes `dashboard_pane23.js`.
- 2026-09-03: **two defects were found by running it rather than reasoning
  about it.** (1) The live page's LANGUAGE menu was empty: the shell fills
  it once at mount, and on the live page the index is empty at that
  moment. It is refilled on every repaint now. (2) The first cut of this
  module replaced the source's arch opcode map with a fresh object, but
  `mount` had already kept the old one, so the click-through reported "arch
  opcode entries in the index: 0"; the map's prototype is stripped in
  place now and its identity is kept.
- 2026-09-03: **the seeded sampler is reproducible across a reload.** The
  address carries both halves of a sample — the seed and the scope the
  seed indexes into (the four selector controls) — so reopening the same
  address restores the controls and selects the same unit. Evidence: the
  two loads in log_179's transcript.
- 2026-09-03: **task 73 landed — pane 4, the coverage view.** The pane the
  CORE names `coverage_view` now draws all three parts, per compiler, from
  the artifacts on disk. (a) THE STATIC STRUCTURE: the lowering region as a
  wiring diagram — one box per source file, sized by the graph nodes it
  holds, grouped by directory (a force layout beside it), wired by the
  inter-file edges — COLLAPSED TO FILE LEVEL, one file expanded on click
  into its definitions with the definition-to-definition edges. (b) THE
  DYNAMIC STRUCTURE: a probe picked by unit id, its diary path lit IN ORDER
  with a step slider and a play button, the path drawn as direction and not
  only as colour. (c) COVERAGE: every box shaded by how many of the 590
  probes entered it, and the never-visited set LISTED — the 15 files no
  probe ever entered first, then the 810 never-visited bodies by file with
  their names and lines. For cpp, rust and swift the pane draws (a) and
  says plainly that (b) and (c) are NOT MEASURED, with task 72's cost page
  named. Paint measured in an interactive Chrome pane: go 0.6–13.8 ms
  (81 boxes over a 10,393-node graph), rust 1.1–3.8 ms (134 boxes,
  13,446 nodes), swift 2.1–3.5 ms (230 boxes, 71,106 nodes), cpp
  1.1–3.5 ms (330 boxes, 112,364 nodes). Files:
  `Research/op_pipeline/dashboard_pane4.js` (added to the live page by one
  script tag and to the snapshot by `viewer_build.py`),
  `dashboard_pane4_harness.html`. Evidence:
  `DevComms/log_181_task73_pane4_coverage_view.md`, screenshots under
  `DevComms/screens/log_181/`.
- 2026-09-03: **the page never opens a whole graph.** Pane 4 reads
  `graph_<lang>_files.json` (41 KB–184 KB), `coverage_go_files.json`
  (2.6 MB), `graph_<lang>_defs.json` (0.6–3.3 MB, only when a file is first
  expanded), and for a chosen probe ONE `Blob.slice` of
  `coverage_go2.json` between the byte offsets the summary carries —
  measured at 929,634 bytes read out of 515,160,866 for probe `op_103`,
  18,270 diary events parsed in 8–10 ms. The 331 MB and 515 MB files are
  never opened whole, by the page or by the builders.
- 2026-09-03: **the spelling-ban test RUN over pane 4, not asserted.** With
  `?labels=glyph` on, pane 4's rendered text is character-for-character
  what it is with the flag off — 45,277 characters, the same rolling hash
  22110881, with `DashboardJoin.glyphMode()` reporting `true`. The pane
  keys nothing by an operator token, so replacing every token with a glyph
  cannot change it. `check_dashboard_js_no_spelling.py` PASSES on
  `dashboard_pane4.js` with four named coincidences (`/`, the unit-id
  separator).
- 2026-09-03: **pane 5, the stats, and the census-generation fix made
  general.** `Research/op_pipeline/dashboard_pane5.js` (new module,
  additive: one `<script>` line in `dashboard.html`, one embed block in
  `viewer_build.py`) draws the four term states over the full population
  — proved 26,594 (87.4%), disproved/withdrawn 3,134 (10.3%), undecided
  285 (0.9%), no term 419 (1.4%), 30,432 records total, read from
  `audit65.json`'s `term65` tally — beside the census's top causes, read
  from the HIGHEST-numbered `name_census*.json` beside the page (picked
  BY NUMBER, not by a hard-coded name, so the fix survives the next
  generation). This round that file is `name_census6.json`, closing the
  task-76 finding that pane 4/stats had been reading `name_census5.json`
  with a higher generation already on disk. The population line also
  carries the pool's own count, 1,831 entries / 30,432 members
  (`the_pool5.json` summary), unchanged this round. Both spelling-ban
  guards pass: `check_no_spelling_keys.py` over `dashboard_snapshot_data.json`
  and `check_dashboard_js_no_spelling.py` over `dashboard_pane5.js` (0
  findings, 0 named coincidences). Verified by screenshot,
  `DevComms/screens/log_182/pane5_stats.png`. Evidence:
  `DevComms/log_182_task74_pane5_bank_round14.md`.

- 2026-09-03: **the dashboard now has a second route, rendered by python**
  — `Research/op_pipeline/dashboard_ouro.html` + `dashboard_ouro.py`, run
  inside `~/Programming/Ourobrowser`. All six panes render; every pane
  carries its population line. `dashboard.html` and the five pane modules
  are NOT edited and NOT retired — `git diff` over them is empty. The
  settled rule that permits a second route, and the four realization rows,
  were written into this node's CORE first. Status: **done**.
  Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_184_task77_dashboard_in_ourobrowser.md`;
  screenshots `~/Programming/PseudoCoupHQ/DevComms/screens/log_184/`.
  Provenance: log_183 task 77, quoting the owner.

- 2026-09-04: **the chronology is the OUTER CONTROLLER, not pane 6** —
  a shape correction from the owner, written into this node's CORE before any
  code, per the standing rule. Verbatim: "the chronology is the outer
  controller. regardless of what tab im in, i can select a moment in the
  chronology (that is always visible at the top), and it shows what that
  moment in the chronology looked like in that tab but it also updates
  all the tabs for that time." Three CORE changes: the definition now
  enumerates FIVE panes with the chronology above them; the design's
  `chronology` block holds ONE moment for the whole page and every pane
  renders as of it; and two settled rules are added — "One moment, held
  outside the panes, applied to all of them" and "A pane that cannot be
  recomputed at a moment says so at that moment". This also resolves
  log_188's finding from the other end: the tab bar carried more entries
  than the CORE numbered because the chronology had been counted into it.
  What exists on disk today is still the pane-6 form
  (`dashboard_pane6.js`, `dashboard_ouro.py`), which now stands as the
  superseded realization. Status: **planned** — no code written this
  round. Provenance: the owner, 2026-09-04, in session.

- 2026-09-04: **priority set by the owner, and pane 4's defect named.** Order:
  (1) the chronology as the outer controller, (2) pane 4 as a visual
  compiler graph; and the chronology applies to the coverage pane like
  every other. The defect, measured not asserted: `dashboard_ouro.py:821`
  `pane_coverage()` emits four `<table>`s and one `<pre>` list of
  never-entered files — no boxes, no edges, no layout. The wiring diagram
  the log_172 task-73 brief specified (files as boxes, functions as nodes,
  call/read/write edges, collapsed to file level, expanded on click) was
  built ONLY in `dashboard_pane4.js`, the JavaScript route the owner does not
  open. log_184 raised "whether panes 4 and 6 should get the wiring
  diagram" as an open question and the coordinator never carried it to
  the owner, so he has asked for the same thing several times. Status of both
  items: **planned**. Provenance: the owner, 2026-09-04, in session.

- 2026-09-04: **task 85 landed — the chronology IS the outer controller,
  on the page the owner opens.** `Research/op_pipeline/dashboard_ouro.py` +
  `dashboard_ouro.html`, the python route rendered in Ourobrowser. The
  chronology is no longer tab 6: it is drawn ABOVE the tab bar by
  `frame()` itself, so **no pane can be drawn without it** — measured on
  **200 of 200 renders** (5 panes × 40 moments), above the tab bar on
  200/200, with the tab bar carrying **5 entries on 200/200**, which
  closes log_188's finding that it carried more entries than the CORE
  numbers. ONE moment is held in `STATE["moment_key"]`, outside every
  pane; `moment_change` sets it and redraws the tab in view, and every
  other pane is as of it because none holds a time of its own. Proved
  directly: one moment change, **two different panes** (2 and 5) both
  different, `t85_one_moment_proof2.json`.
- 2026-09-04: **history is RECOMPUTED at a past moment, and the numbers
  are different ones.** A moment other than now reads through
  `git ls-tree -r -l <commit>` (once per moment) and
  `git cat-file --batch` (one process per moment); nothing is checked
  out and no number comes from `chronology.json`, which supplies only
  which commits are moments. At 2026-09-03 `f6856895` (round 12) the
  index rebuilds **31,078 units in 1.8 s** from that commit's own blobs,
  and pane 5 reads **`the_pool4.json` — 1,961 entries / 30,432
  members**, against today's `the_pool5.json` at 1,831 / 30,432. That
  difference is the evidence that the past is being read rather than the
  present relabelled.
- 2026-09-04: **which panes can be recomputed at a moment, and which
  refuse — measured over 5 panes × 40 moments (39 chronology steps and
  the present moment).** Panes 1, 2, 3 recompute at **5 of 40** moments;
  pane 4 at **3 of 40**; pane 5 at **14 of 40**. Every other case draws
  the refusal AT THAT MOMENT and names the files version control does
  not hold at that commit — the settled rule, working. The cause is
  measured, not guessed: `t85_gitfacts.json` walks all 39 commits and
  shows the corpus entered version control only on 2026-09-03, the pool
  on 2026-09-02, the census on 2026-08-31, and that `graph_go.json`
  (49 MB), `graph_cpp.json` (331 MB), `graph_swift.json` (165 MB) and
  `coverage_go2.json` (515 MB) were **never tracked at any of them**.
- 2026-09-04: **the artifact GENERATION is now picked by number at every
  moment, not only at the present one.** Task 74 fixed the census;
  the pool was still the hard-coded `the_pool5.json`. A past moment
  makes the general form necessary — at round 12 the highest pool is
  `the_pool4.json` — so `highest_generation` now serves both families.
  `viewer_build.read_pool` gained ONE optional argument (the pool
  document) so one implementation serves both moments; with no argument
  its behaviour is byte-for-byte what it was.
- 2026-09-04: **two defects were found by RUNNING it, not by reading.**
  (1) Three of 200 renders raised `TypeError: unsupported format string
  passed to dict.__format__` — an older pool generation carries a nested
  object on its summary where the present one carries a count. A past
  moment is where an artifact's SHAPE changes, not only its numbers, so
  the fix (`count_cell`) draws a value as what it is; 0 errors of 200
  after it. (2) `check_dashboard_py_no_spelling.py` FAILED on
  `if "/" in rest` — the path separator is also an operator token in the
  91-token inventory, and the guard is right to refuse a token in a
  membership test. Fixed by naming `TREE_SEPARATOR`, not by exempting
  anything.
- 2026-09-04: **the memory bound holds, with the moment added to it.**
  `MEMORY_CAP_MB` 1500 and `OURO_MEMORY_ABORT` unchanged; the moment
  adds a second bound — only one moment's work is held, every cache
  dropped in `set_moment`. Sample first (10 renders, 223.6 MB), then the
  full pass: **200 renders over 40 moments peak at 286.1 MB** in one
  process. The real Ourobrowser, driven through 12 screenshots and 4
  moment changes, peaks at **577.8 MB** against the task-77 page's
  402.3 MB and the stated cap of 1,500 MB.
- 2026-09-04: **the JavaScript route was not touched.** `git diff` over
  `dashboard.html` and every `dashboard_pane*.js` is **0 lines**, and
  `check_dashboard_js_no_spelling.py` still PASSES all six modules.
  Both other guards pass: `check_no_spelling_keys.py`, UNMODIFIED
  (sha256 `a377462b…`), in ONE process over all five artifacts task 85
  produced, and `check_dashboard_py_no_spelling.py` over
  `dashboard_ouro.py` and `viewer_build.py`. `grep -c exempt` = 0 on
  every artifact. Status: **done**.
  Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_191_task85_chronology_outer_controller.md`;
  screenshots `~/Programming/PseudoCoupHQ/DevComms/screens/log_191/`
  (13 of them). Provenance: the owner, 2026-09-04, the CORE's priority order.

- 2026-09-04: **three corrections from the owner, written into the CORE
  before any code.** (1) THE CHRONOLOGY IS VERSION CONTROL, and nothing
  curates it — a moment is a commit, the scale is `git log` unfiltered
  and ungrouped, and no project vocabulary appears in the mechanism.
  The defect this retires, named exactly: `chronology_build.py:163` ran
  `git log --grep=banked -i` and `:155` matched `round\s+(\d+)\s+bank`,
  so the chronology's steps existed because the coordinator had written
  "banked" into a commit message — the rounds were the mechanism and
  version control only its substrate. Repository: 1,309 commits, 0 tags.
  the owner: "i said vcs chronology. i dont want you to have any say in how it
  updates... because i cant trust you." (2) THIS DASHBOARD IS BECOMING
  GENERAL-PURPOSE (the owner: "plans are now in place to convert this to a
  general purpose PlanPlan dashboard design. so think abstractly"), so
  anything that can only work for PseudoCoup is a defect in this node.
  (3) PANE 4 CANNOT EXIST BEFORE THE OBJECT IT DRAWS DOES; the
  coordinator's "bank a small per-commit summary" is WITHDRAWN as
  manufacturing something to display in place of a missing deliverable.
  Priority 2 is completing the graph at node 0_3_5_9, not rendering a
  pane. Status of all three: **planned**. What is on disk stays as the
  record: task 85's outer-controller frame is right and its round scale
  is wrong. Provenance: the owner, 2026-09-04, in session.

- 2026-09-04: **task 86 landed — the chronology IS version control, and
  nothing curates it.** `Research/op_pipeline/dashboard_ouro.py` no
  longer reads `chronology.json`: it reads `git log` itself, at render
  time, unfiltered and ungrouped. A MOMENT IS A COMMIT, and the moment
  list is proved equal to `git rev-list --reverse HEAD` — **1,324
  moments, 1,324 commits, identical order** — by the lane itself, which
  is the mechanical form of "no commit is selected by its message". The
  retired defect, named exactly: `chronology_build.py:163`'s
  `git log --grep=banked -i` and `:155`'s `round\s+(\d+)\s+bank`. Both
  that program and `chronology.json` stay on disk as records, and the
  program now carries a SUPERSEDED banner saying why. No project
  vocabulary survives in the mechanism: the grep for `round|bank|lap`
  over the changed page returns only the CSS property `background` and
  the one docstring sentence that names the retired defect.
- 2026-09-04: **what the scale degrades to, measured rather than
  assumed.** The repository carries **1,311 commits, 0 tags, 0 merge
  commits, 1 root commit, 2 refs and 31 distinct committer days**
  (`t86_vcs_scale.json`, each figure with the command that produced it).
  Tags and merges therefore offer nothing, so the scale degrades to
  exactly two rows — the commits, and the days their own timestamps fall
  on — and the page prints those marks at every moment so the
  degradation is visible. THE DAY ROW SELECTS NOTHING: it moves the
  window of the commit row and leaves `STATE["moment_key"]` alone, so no
  commit is ever elevated to stand for its day. Verified on the running
  page: after a day click the page still reads back `now — the working
  tree on disk`. The commit row draws a printed ceiling of 60 beside the
  population it was cut from, with two shift controls so every commit is
  reachable. Commits per day: min 1, median 3, **max 521** — which is why
  the shifts exist.
- 2026-09-04: **everything task 85 got right was kept, and measured
  again.** 5 panes × 41 moments = **205 renders, 0 errors**; the
  chronology drawn on 205/205, above the tab bar on 205/205, tab bar of
  five on 205/205. One moment change altered **all five panes**. Panes
  1/2/3 recompute at 18 of 41 moments, pane 4 at 12, pane 5 at 37 — and
  those are not an improvement on task 85's 5/40, 3/40 and 14/40 but a
  different POPULATION: 946 of the repository's 1,311 commits were made
  on 2026-09-02, -03 and -04, so an evenly spaced sample of commits
  lands mostly in the well-tracked days. Evidence:
  `Research/op_pipeline/t86_all_panes2.json`.
- 2026-09-04: **the memory bound was a habit and is now a mechanism —
  found by running, at 1,970.2 MB.** The commit's tree listing and its
  `git cat-file --batch` process were cached ON each `Moment`, so "only
  one moment's work is held" held only while the caller went through
  `set_moment`; a pass over the whole population held 1,321 tree
  listings and peaked **over this page's own 1,500 MB cap**. `open_at`
  now keeps ONE slot and evicts on any other moment: the same pass over
  1,325 moments peaks at **30.7 MB**. Full pass **276.4 MB** over 205
  renders (task 85: 286.1 MB over 200); the real Ourobrowser, driven
  through 14 screenshots, 4 moment changes and 4 window moves, peaks at
  **566.4 MB** (task 85: 577.8 MB). Both beat task 85; the cap and the
  abort `OURO_MEMORY_ABORT` are unchanged.
- 2026-09-04: **the testimony fallback is retired with the mechanism
  that fed it.** `testimony_for` drew a count line parsed out of a
  commit message where an artifact was untracked — a number taken from
  text a person wrote, which is what "nothing curates it" retires. A
  moment that cannot be recomputed now draws its refusal and names the
  files. The CORE's `chronology` design block still carries the older
  sentence about testimony; that sentence is the owner's to cut.
- 2026-09-04: **the JavaScript route was not touched.** `git diff` over
  `dashboard.html` and every `dashboard_pane*.js` is **0 lines** both in
  the working tree and across `b86366cd..HEAD` — the span of task 86 —
  which is the diff that proves it, because the repository's daemon
  commits every 30 s. All three guards pass:
  `check_no_spelling_keys.py`, UNMODIFIED (sha256 `a377462b…`), in ONE
  process over all five artifacts task 86 produced, with `grep -c
  exempt` = 0 on each; `check_dashboard_py_no_spelling.py` over
  `dashboard_ouro.py` and `viewer_build.py`;
  `check_dashboard_js_no_spelling.py` over all seven JavaScript modules.
  Status: **done**.
  Evidence: `~/Programming/PseudoCoupHQ/DevComms/log_192_task86_vcs_chronology.md`;
  screenshots `~/Programming/PseudoCoupHQ/DevComms/screens/log_192/`
  (fourteen of them). Provenance: the owner, 2026-09-04, "i said vcs
  chronology... because i cant trust you".

- 2026-09-04: **PANE 4 IS A DRAWING.** Task 93, log_198. The defect this
  node named on 2026-09-04 — `dashboard_ouro.py`'s `pane_coverage`
  emitting four tables and a `<pre>` list while the drawing existed only
  in `dashboard_pane4.js`, the route the owner does not use — is closed.
  the owner: "i want to be able to see the graph in a visualization. i dont
  want a messy massive un-organized graph like graphiz or whatever. i
  want it well structure since the compiler/interpreters are well
  structured", and "i want to be able to see a basic structure and
  dynamic connections and the traced directional connections made by our
  probes."

  **THREE FIGURES, IN DEE'S THREE WORDS, OVER ONE LAYOUT.**
  `Research/op_pipeline/dashboard_graph_draw.py` (new) draws;
  `pane_coverage` chooses the compiler and reads the summaries at the
  moment. (1) BASIC STRUCTURE — a band is a directory of the compiler's
  source in path order, a box is one file in path order inside its
  directory, shaded by its definition count, and **a box's place is a
  function of its path**: no physics, no randomness, the same coordinate
  at every moment and in every figure, so the picture is the compiler's
  shape rather than a picture of a layout algorithm. Inter-file `calls`
  connections are directed arcs, the heaviest 100 with the population and
  the ceiling printed; clicking a file draws that file's connections
  alone. (2) DYNAMIC CONNECTIONS — the same boxes in the same places,
  filled by the share of instrumented bodies at least one probe entered
  and carrying `visited/instrumented`; a file no probe entered is HOLLOW
  and dashed, a file with no instrumented body is HATCHED and named a
  frontier of the join, never a never-visited file. (3) THE TRACED
  DIRECTIONAL CONNECTIONS — in aggregate (every ordered file-to-file step
  weighted by how many probes take it, heaviest 80, ceiling printed), for
  ONE PROBE (its ordered walk with the step number on each box; `op_0`,
  599 steps), and for ONE OPERATOR TRACED VARIANT (the transitions that
  variant walks and no other walks — the third connection kind;
  `var_53d50fbe9bd4c8a2`, 4 arrows). The variant menu is ranked by how
  many transitions belong to a variant ALONE, which is machine-form
  evidence; the operator tokens appear once, as display labels on the
  member list, and are read back by nothing.

  **NO JAVASCRIPT**, measured on what the pane emits: `<script` 0,
  `javascript:` 0, `onload=` 0, `onerror=` 0, and 336
  `onclick="python:"` attributes the engine rewrites. The live page in
  Ourobrowser still carries exactly 2 script tags — the engine's own
  handshake and the page's one `text/python` block.

  **THE MEMORY RULE KEPT.** `graph_cpp.json` is read for its first
  256 KB and no more; every figure is drawn from
  `graph_<lang>_files.json` (41–184 KB), `coverage_<lang>_files.json`
  (2.6 MB go, 6.8 MB clang) and `variant_connections_<lang>.json`
  (0.6–3.2 MB). `coverage_go2.json` (515 MB) is never opened at all.
  Whole render pass over all four compilers: **114.1 MB peak** against
  `MEMORY_CAP_MB` 1500; the real Ourobrowser driven through 16
  screenshots peaks at **465 MB**.

  **WHAT EACH COMPILER DRAWS, measured off the rendered page.** go 3
  figures / 83 file boxes / 100 of 331 structural arcs; c and cpp (clang)
  3 figures / 330 boxes / 100 of 2,095; rust 1 figure / 134 boxes; swift
  1 figure / 230 boxes. rust and swift draw their structure and then
  REFUSE figures 2 and 3 by name — no probe of this corpus has ever been
  diaried through them, quoting the cost page (rust BLOCKED on this
  machine, swift unstartable here). java, cpython, php and ruby are drawn
  as `no graph` chips. Nothing is approximated.

  **THE CHRONOLOGY APPLIES TO THE DRAWING, and the graphs are now in a
  SECOND REPOSITORY.** They moved to `~/Programming/PseudoCoupGraphs`
  (task 93, node 0_3_5_9), so a moment has to reach across: `GraphFolder`
  resolves the graph folder's OWN commit at or before the selected
  moment's committer timestamp — `git rev-list -1 --before`, version
  control answering, no message read, no commit promoted. Where its
  history does not reach that far the figure draws its refusal and names
  the file. Measured on the page: at commit `51ad534fa0` (2026-09-04
  23:47) go renders all three figures and clang renders only figure 1,
  because `coverage_cpp_files.json` did not exist at that commit; at
  `e3547c87ba` (2026-09-03 00:01) the pane refuses entirely and names
  `Research/compiler_graph/graph_go_files.json` as what it would need.
  History is recomputed, never remembered.

  `dashboard_ouro.html` gained the drawing's CSS and ONE click function,
  `ouro_cov(lang, focus, pick)` — three machine identities: which
  compiler, which source file is held, and which probe or which variant.
  **`git diff` over `dashboard.html` and every `dashboard_pane*.js` is 0
  lines, and over `~/Programming/Ourobrowser` is 0 lines.**

  The page's own code guard `check_dashboard_py_no_spelling.py` passes
  `dashboard_ouro.py` and `dashboard_graph_draw.py`, exit 0; every
  mention it lists is a token as a value or as prose (`/` in a path,
  `<` and `&` in the html escape) in no position that keys anything.

  Evidence: `DevComms/log_198_task93_graphs_moved_shrunk_drawn.md`,
  16 screenshots in `DevComms/screens/log_198/`, the rig
  `Research/op_pipeline/t93_ouro_shots.py` (on the host, importing the
  engine and not editing it), lane
  `Research/compiler_graph/lanes_t93/t93_l6_readers_and_summaries.sh`.
- 2026-09-05: **task 98 landed — pane 5 explains every row, and pane 5
  runs its own analysis.** Two requests from the owner in one message: "add a
  question mark button for each row to describe what it means", and
  "i want analysis run to produce stats you havent curated."

  **THE PANE IS NOW TWO LABELLED HALVES.** `read from a stored summary`
  is what it was — the pool's `summary` block, the term audit's tally,
  the census. `counted at render time` is new: the artifacts walked while
  the page is drawn. Measured off the rendered page at its own half
  marker: **136 rows, 36 stored and 100 computed, in 17 tables, with 136
  `?` controls and 0 rows unexplained.**

  **THE EXPLANATIONS ARE DATA, so they cannot drift from the rows.**
  `Research/op_pipeline/dashboard_stats.py` holds `MEANINGS`, 46 records
  keyed per row, each carrying four fields — what is counted, out of
  what, which file and whether it was READ FROM A STORED SUMMARY or
  COUNTED AT RENDER TIME, and what the row does NOT mean. A row whose key
  has no record is drawn as UNEXPLAINED **on the page**, naming the
  missing key. That marker FIRED and was right: at the two 2026-09-03
  commits `9b9e7e674c` and `56755e32ca` the pool generation is
  `the_pool3`, which spells the same measurements under nine different
  summary names; nine records were added and no key goes unexplained at
  any of the 15 moments walked. **No JavaScript**: the `?` is a
  `<details>` element, and `<script` in what pane 5 emits is 0.

  **WHAT THE PANE NOW COUNTS ITSELF**, in two bounded walks — the corpus
  per language and arrival population; every gate outcome value that
  occurs and the unproved units per language; the arch-opcode count
  distribution, its shape and then one row per count value 0 to 57; how
  far machine code repeats; empty bodies; bytes per body; ledger rows per
  unit; the term store's two states per language; the store against the
  population it was built over; runtime-callee rows, holes and cascades.

  **THREE FIGURES IN THE BRIEF WERE RECOUNTED AND EXPLAINED.** (1) "3,547
  distinct `body_bytes`" counts a body once PER LANGUAGE; counted once
  for the corpus it is **2,744**, and **642 bodies appear in more than one
  language**, which is the whole of the difference — the pane draws both.
  (2) "624 empty regenerated bodies" is canon40; canon39 has 628, and
  **2 interpreter units** the brief omits also have empty bodies. (3) The
  brief's distribution is instruction lines counted WITH repeats, while
  its "93/162 distinct opcodes" is the population's vocabulary — measured,
  **0 of 31,078 units carry a body line that does not read as an
  instruction**. Everything else agreed exactly.

  **ARTIFACT GENERATIONS ARE NOW PICKED BY NUMBER for the canonical form
  and the term store too**, joining the pool and the census.
  `Moment` gained `dirs_under`, the twin of `names_under`, because a
  family can be a folder. `audit65.json` stays a written name and the page
  says why: its number is a TASK number, not a generation.

  **MEMORY.** Both walks drop each parsed document before opening the
  next. One render of pane 5: **1.0–1.3 s, 41.7–44.2 MB**; 15 moments
  peak at 75.8 MB. The real Ourobrowser over the comparable pass — every
  pane, four moment changes, twelve screenshots — peaks at **599.2 MB**
  against task 86's 566.4 MB for the same shape, a rise of 32.8 MB and
  40% of the 1,500 MB cap. The rise is at panes 2 and 3, which build the
  whole-population index; pane 5 alone never took it past 412.7 MB.

  **THE CHRONOLOGY APPLIES.** Over 15 moments of the repository's 1,681:
  10 drew numbers, 5 drew the refusal, 0 errored. At 2026-09-04
  `c63e1c8e90` the pane reports `term66_store, 10 shards` — the store as
  version control held it then, not as it is now.

  **GATES.** `check_no_spelling_keys.py`, unmodified, ONE process, over
  667 artifacts: 667 PASS, 0 FAIL, `grep -c exempt` 0.
  `check_dashboard_py_no_spelling.py` PASSES `dashboard_ouro.py` and
  `dashboard_stats.py`. **`git diff` over `dashboard.html` and every
  `dashboard_pane*.js` is 0 lines, and over `~/Programming/Ourobrowser`
  is 0 lines.** `check_conventions_log_claims.py --verify` over log_203:
  **14 of 14 claims reproduce, 0% carrying nothing to re-run** (task 96
  scored 28 of 35 with 20%).

  Files: `Research/op_pipeline/dashboard_stats.py` (new),
  `dashboard_ouro.py`, `dashboard_ouro.html` (CSS only),
  `t98_probe_shapes.py`, `t98_settle.py`, `t98_render_check.py`,
  `t98_ouro_shots.py`, and 14 lanes in `lanes_t98/`.
  Evidence: `DevComms/log_203_task98_stats_explained_and_computed.md`,
  25 screenshots in `DevComms/screens/log_203/`.
