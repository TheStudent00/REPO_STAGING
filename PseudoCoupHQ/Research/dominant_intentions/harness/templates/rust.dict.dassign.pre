// `let b = a` MOVES the map; a is no longer usable. The refusal IS the
// guarantee -- F-l9's third camp, inherited by dict unchanged.
fn f_Fl9d_a() -> String { let a = mkd(); let mut b = a; b.insert("k".to_string(), "99".to_string()); a["k"].clone() }
