---
id: hq.research.compiler_graph.pool.representative.progress
status: living
---

# PROGRESS — representative

- 2026-09-03: node folder generated by
  `PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_7_pool.md`.
- 2026-09-03: 1,144 distinct texts assembled and their bytes counted
  — **done** (`the_pool3_bytes.json`). Evidence:
  `PseudoCoupHQ/DevComms/log_154_task54_the_pool3.md` §2.4.
- 2026-09-03: representative chosen per entry, `go/op_319` at 35
  bytes for `E00029` — **done**. Evidence:
  `PseudoCoupHQ/DevComms/log_154_task54_the_pool3.md` §4.2.
- 2026-09-03: 8 texts that would not assemble handled by a stated
  substitution on 2 entries — **done**. Evidence:
  `PseudoCoupHQ/DevComms/log_154_task54_the_pool3.md` §2.4.
- 2026-09-03: the rule re-run over pool4: 480 entries carry more than
  one wrapped text, 1,512 distinct texts assembled with `as --64` and
  counted from `objdump -d`, 10 would not assemble and fall back to
  character length with the substitution stated on the entry —
  **done**. Evidence: `the_pool4_bytes.json`, `pool61_run.log`, `PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: the representative rule re-measured over pool5: **527
  entries carry more than one wrapped text** (was 480), 1,689 distinct
  texts to assemble, 288 newly assembled, 120 would not assemble and
  fall back to character length with the substitution stated on the
  entry — **done**. Evidence: `pool65_run.log`,
  `the_pool5_bytes.json`, `PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
