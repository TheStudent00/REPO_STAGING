---
id: hq.research.lean_proof_path_resistant_to_churn.sail_model.strip.progress
status: living
---

# PROGRESS — strip

- 2026-09-13: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `PRIVATE/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_2_arch_unit_oracle/node_0_3_2_3_architectures/node_0_3_2_3_1_riscv64/.archive/lean_proof_path_superseded_2026-09-14/node_0_3_2_3_1_3_0_sail_model/CORE_0_3_2_3_1_3_0_sail_model.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-14: carried into `Planning/node_0_3_research/node_0_3_3_lean_proof_path_resistant_to_churn` (the owner: start over in a different research node; copy the node, delete the wrong parts, re-plan them). This node was one of the right parts. It supersedes `hq.research.arch_unit_oracle.architectures.riscv64.lean_proof_path.sail_model.strip`.
- 2026-09-15, evening: THE EFFECTS RULE, for the float clauses, added to
  `Research/oracle/riscv/leanpath/leanpath/strip.py` (new functions
  `signature`, `propose_effects`, `effect_steps`, `effects_prop`,
  `lean_body_effects` and helpers; `run` falls back to it only where the
  one rule refuses; `lean_body` dispatches on the new shape). It reads
  the SIGNATURES of the helpers a clause calls, never their names: a
  read (one register-index parameter, returns `SailM (BitVec k)`), a
  selection (`match (← F p)` returning `SailM (Option U)`: the rounding
  mode read from the control register becomes an input), an effect with
  a value (`BitVec k → SailM Unit`: the accrued flags become an output),
  a register write (register index and bits); and a clause that matches
  on its parameter tuple is that shape per alternative. The pure form
  returns (the effect values, the written value) (status: done).
- 2026-09-15, evening: regression, no drift for what the one rule reads:
  over the cached emit, the 96 clauses the one rule proposes get
  byte-identical `propose` and `lean_body` output from the old and the
  new `strip.py` (0 differ). Of the 243 it refuses, the effects rule
  proposes 34 (status: done).
- 2026-09-15, evening: the 12 clauses whose bodies call one of the 27
  operations with a body (found by body: `F_BIN_RM_TYPE_{S,H,D}`,
  `F_BIN_X_TYPE_{H,D}`, `F_BIN_TYPE_X_S`, `FLTQ_{S,H,D}`,
  `FLEQ_{S,H,D}`) certified in the working copy `/work/proof_float`
  (`podman exec`, one Lean file each): 12 of 12, 1.1 to 1.2 s each. A
  negative control follows before the record run (status: done).
- 2026-09-15, evening: the negative control
  (`float_emit/negative_control.py`): two certificates as written prove,
  `#print axioms` shows no `sorryAx` (the float operations still appear
  as axioms there, because that copy's build was not yet redone); the
  same two files with the two register reads handed to the pure form in
  swapped order FAIL with unsolved goals. A certificate that passes can
  fail (status: done).
- 2026-09-15, evening: the record run of the 12 in the rebuilt working
  copy is step 1 of lane `lanes_lp1/lp3_l89_gate_c_float_arch_units.sh`
  (status: in-progress).
