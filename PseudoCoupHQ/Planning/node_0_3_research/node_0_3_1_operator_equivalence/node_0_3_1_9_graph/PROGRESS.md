---
id: hq.research.compiler_graph.graph.progress
status: living
---

# PROGRESS — graph

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: node written at the owner's request for the dashboard and its
  compiler-graph pane (log_172 briefs). Every item in the CORE's
  realization table marked **planned** is round-14 work.
- 2026-09-03 (log_173): CORE rewritten to carry the owner's ruling — the
  compiler analyzed as a graph, static + dynamic, IS the super-op
  detector; `super_ops` added as a method; `super_op_miner.py`
  recorded as an unruled substitution, superseded. Every row of the
  realization table except go's August build is **planned**; round
  14 (log_172, reordered) begins with this node.
- 2026-09-03 (task 72, log_175) — **`dynamic_structure`: the corpus now
  has diaries, go.** Every one of the 590 go units of canon39 (107
  original, `canon39_wrapped_go.json`; 483 regenerated,
  `canon39_regen_store/op_units2_go_c000{0,1}.json`) was recompiled
  through an instrumented `cmd/compile`, one ORDERED diary per probe at
  `Research/compiler_graph/diaries/go/<unit>.txt`. 590 of 590, 0 failed,
  8.7 M events, 1.2 GB, 111 s of compile time. The instrumented
  population is EVERY function body of the region — 1,549 of them, not
  August's 56 hand-picked spots. Toolchain pin, quoted from the
  toolchain: `go version go1.28-devel-pseudocoup linux/amd64`.
  Evidence: `Research/compiler_graph/t72/inject_report.json` (1,549
  targets read, 1,549 instrumented, 0 skipped),
  `Research/compiler_graph/t72/diary_state.json` (per-unit event
  counts), lanes `lanes_t72/t72_l1..l4`.
- 2026-09-03 (task 72, log_175) — **`coverage`: the join exists and has
  a finding.** `Research/compiler_graph/coverage.py` implements
  `Graph.diary_targets`, the reader for the produced diary format, and
  `Graph.coverage`, as `class Graph(graph.Graph)` over task 71's file;
  it folds into `graph.py` when that file settles. Result over the 590
  probes: **731 of 1,549 instrumented function bodies visited by at
  least one probe; 818 visited by none**, listed by file in
  `coverage_go.json`. The never-visited list is the finding — SIMD
  emission (`amd64/ssa.go` 53 bodies, `amd64/simdssa.go`), the
  magic-number division helpers (`ssa/magic.go` 40),
  `ssa/merge_conditional_branches.go` (30 of 30), the HTML debug writer
  (`ssa/html.go` 29) and 193 of `ssa/rewrite.go`'s helpers are compiler
  logic our scalar probes never exercise.
- 2026-09-03 (task 72, log_175) — **the join is against the August
  graph, and the re-join is one command.** `graph_go.json` from task
  71's `graph.py` did not exist when targets had to be selected, so the
  join uses `graph_go4.json` (`build_graph3.py`, 174,159 nodes). Each
  diary line carries BOTH graph spellings, so no recompile is needed:
  `coverage.py join --graph graph_go.json --graph-form task71
  --diaries diaries/go --out coverage_go.json`.
- 2026-09-03 (task 72, log_175) — **the cost page for clang / rustc /
  swiftc is measured, in log_175 §cost.** It is what round 15 schedules
  against. The blocking facts found by measuring rather than by
  assuming: `/sources/llvm-project` and `/sources/rust` have SPARSE
  WORKING TREES (llvm has no `clang/` and no `cmake/` on disk) with
  COMPLETE object stores, so a full tree needs no network; the rustc
  that compiled the corpus (1.96.1, `31fca3adb`) is installed as a
  binary but its source commit is ABSENT from the checkout; and a swift
  build cannot be started on this machine at all — `cmark`,
  `swift-syntax` and Apple's llvm fork are absent and the instance has
  no route out.
- 2026-09-03 (task 71, log_174) — **`static_structure`: four compilers
  are graphs, built by one class.** `Research/compiler_graph/graph.py`
  is `class Graph` with the CORE's own method names (`build`,
  `query_path`, `diary`, `coverage`, `super_ops`). Language dispatch is
  by file extension to a `LanguagePack` — per-language DATA (tree-sitter
  node type names), one universal mapper, the PCv5 ledgerer's design
  reused; adding a sixth compiler is a pack, not code. Node/edge
  vocabulary unchanged from the go lap: defined-in versus does; nodes
  are file / def / param / local / data_row; edges are contains / calls
  / reads / writes. Built from PINS with `git show`, never a working
  tree. Counts, each with its region:
  go `cmd/compile/internal/{ssagen,abi,amd64,ssa}` 10,393 nodes /
  63,797 edges / 77,530 frontier / 0 parse errors over 81 files;
  cpp (clang serves BOTH c and cpp) `clang/lib/CodeGen` +
  `llvm/lib/Target/X86` 112,364 / 386,065 / 523,694 / 2,082 named parse
  errors over 269 files including 61 TableGen data tables;
  rust `rustc_codegen_ssa` + `rustc_codegen_llvm` + `rustc_middle/src/mir`
  13,446 / 60,382 / 101,095 / 49 named parse errors over 134 files;
  swift `lib/SILGen` + `lib/IRGen` 71,106 / 192,655 / 278,245 / 311
  named parse errors over 230 files. Evidence:
  `Research/compiler_graph/graph_{go,cpp,rust,swift}.json`,
  `report_task71.json`, `DevComms/log_174_task71_compiler_graph_four.md`.
- 2026-09-03 (task 71, log_174) — **the go rebuild diffs clean against
  lap one.** `graph.py go` against `build_graph.py`'s own rebuilt output
  (`graph_go_lapone.json`): 1,500 named function definitions in both,
  0 only in the new graph, 14 only in lap one — all 14 in generated
  files (`ssa/opGen.go`, `amd64/simdssa.go`,
  `ssagen/simd*intrinsics.go`) that the stated region rule excludes.
  Node and edge TOTALS are not compared and the report says why: lap one
  mints a node per identifier occurrence, `graph.py` per file, def,
  param and local — two resolutions of one region, not two measurements
  of one quantity.
- 2026-09-03 (task 71, log_174) — **`query_path`: one answered, three
  named frontiers.** go reproduces the August acceptance answer through
  the new class: parameter `ft` of `ABIAnalyzeFuncType`
  (`abi/abiutils.go:353`) -> `assignParam` (603) -> `tryAllocRegs` (623)
  -> `allocateRegs` (514), 4 hops, 8 nodes visited. cpp, rust and swift
  stop, each with the unresolved reference that stops them named:
  cpp at `State.AllocateReg` (`X86CallingConv.cpp:282` and 7 more —
  the register allocator is reached through a callback state object);
  rust at `arg_abi.store` / `llvm::get_param` (`rustc_codegen_llvm/src/abi.rs`,
  111 records, 4 candidate defs carry the name); swift at
  `F.begin()->createFunctionArgument` (`SILGenBridging.cpp`, 56 records).
- 2026-09-03 (task 71, log_174) — **ambiguity is refused, not guessed.**
  A call whose name matches more than one def in the region now produces
  an `ambiguous_call` frontier record CARRYING ITS CANDIDATE SET instead
  of an edge. Measured effect: cpp calls fell 61,692 -> 35,773 with
  26,124 ambiguities recorded. The defect this fixes was observed, not
  anticipated: a cpp path query walked through a call to `size()` that
  had resolved to an unrelated def of the same name.
- 2026-09-03 (task 71, log_174) — **the guard passes, and it changed the
  id scheme.** `op_pipeline/check_no_spelling_keys.py`, unmodified, one
  process over all four graphs: 4 PASS, exit 0, `grep -c exempt` = 0, no
  role key. Its first run FAILED three graphs, and the cause is worth
  keeping: compilers contain functions whose own names ARE operator
  tokens (`new` in rustc, `consume` in swift and llvm, `as` in swift),
  so an id spelled `file:line:name` put a token into every edge's `src`
  and `dst` — a row structure. Node ids are now machine coordinates,
  `file#line#kind#ordinal`; the name lives only in the node's `label`,
  which is a per-unit display label and the one place the ban permits it.
  Evidence: `Research/compiler_graph/guard_task71.txt`.
- 2026-09-03 (task 71, log_174) — **`coverage` over task 72's diaries,
  through `graph.py` itself.** `Graph.diary` reads the produced format
  and `Graph.coverage` joins on the declaration coordinate `file:line`,
  the one coordinate both id schemes carry. Result, with its population:
  **724 of 1,859 go defs of this region visited by at least one of the
  590 probes; 1,135 visited by none**; 7 diary keys fall outside the
  region, all in the generated `ssa/opGen.go`, which is the region rule
  showing its own edge. This is a DIFFERENT population from task 72's
  own 731 of 1,549 — that denominator is instrumented function bodies,
  this one is defs of graph.py's region; neither replaces the other.
  cpp, rust and swift report 0 probes: their whole def population is
  never-visited BY ABSENCE OF MEASUREMENT, never stated as unreached.
- 2026-09-03 (task 75, log_177) — **the re-join to task 71's graph is
  done, and NOTHING CHANGED STATUS.** log_175 §5.3's command was run
  first, verbatim, and returned `population_body_nodes 0` /
  `bodies_visited 0`: `coverage.py`'s task-71 key is
  `<file>:<line>:<name>` over kinds `func`/`method`, while task 71's
  graph spells ids `<file>#<line>#<kind>#<ordinal>` with kind `def` —
  two mismatches, a total miss, and it would have been read as a
  coverage collapse. The honest join is on the DECLARATION COORDINATE
  `<file>:<line>`, the one coordinate both schemes carry:
  `graph.py join --graph graph_go.json --diaries diaries/go
  --instrumented t72/diary_targets_all.json --out coverage_go2.json`.
  Result: **724 of the 1,534 instrumented bodies visited, 810 by no
  probe**, 590 probes, over `coverage_go2.json` (the name is new;
  `coverage_go.json` is task 72's August-graph join and is not
  overwritten). Compared body by body on the coordinate, **0 of 1,534
  changed status in either direction**. The two remaining differences
  are named, not absorbed: 15 of task 72's 1,549 targets have no def at
  that coordinate in task 71's graph (12 in the generated
  `ssa/opGen.go`, 3 in the generated SIMD intrinsics files, all
  excluded by the stated region rule), of which 7 were visited and 8
  never — which is exactly 731−724 and 818−810.
- 2026-09-03 (task 75, log_177) — **`super_ops` exists and has
  candidates: the detector is the graph.**
  `Research/compiler_graph/graph.py` `Graph.super_ops` mines recurring
  CONTIGUOUS sub-paths of the dynamic structure across probes, ranked by
  recurrence, each joined to the arch-units its probes produced.
  Artifact `super_ops_go.json`: **9,809 closed candidates** over 590 go
  probes. PARAMETERS, on the artifact and not hidden: min_length 3,
  min_support 2 probes, subject `probe_own` (the events of the probe's
  own function — the diary interleaves several compiler entry points and
  order alone would read two chains as one), collapse_repeats true,
  max_length 12. Populations: 2,263,885 raw probe-own events (of 10,015,022 diary events; counted with awk straight over the diaries, not by the miner) collapse to
  **1,998,333 stream events over 626 distinct compiler-source
  coordinates**; stream length per probe 1,945 min / 5,182 max. THE CAP
  IS A FRONTIER, stated: frequent runs were still growing at length 12
  (2,495 at length 3 rising to 9,358 at length 12), so recurring paths
  longer than 12 nodes exist and were not mined this lap. 669 of the
  9,809 candidates are walked by all 590 probes. Evidence:
  `super_ops_go.json`, `super_ops_go_top20.txt` (each span quoted from
  the pinned compiler source with `git show`),
  `super_ops_go_discriminating10.txt`.
- 2026-09-03 (task 75, log_177) — **the miner's memory bound is now a
  settled rule of the CORE, and it was settled by an incident.** The
  first shape of `super_ops` held all 10,015,022 diary events as Python
  strings and a run dictionary for every length at once — both O(events)
  with no cap. It reached **13.2 GB resident, exhausted this machine's
  4 GB of swap** and had to be stopped from outside. The rebuilt shape
  streams diaries one file at a time into a compact integer cache on
  disk, keeps at most two length levels live, caps the width of a pass,
  and refuses by name (`MemoryCeilingReached`) at a stated ceiling. Same
  measurement, same 590 diaries: **21 s wall, 487 MB peak resident**,
  proved first on a 20-diary sample (1.1 s, 224 MB) before the full run.
- 2026-09-03 (task 75, log_177) — **what the substitution cost, measured
  both ways.** Against the superseded output-side miner
  (`op_pipeline/super_ops3.json`, 494 candidate records):
  **59 of its records name go units at all**, they name **17 distinct go
  units of the 590** (zero regenerated ones), all 59 sit in its
  `frontier_non_ambiguous` section, and **not one of them carries an
  emitting compiler function** — its own frontier text says why: that
  instrument's graph "reads only cpp/tablegen sources… go (named
  frontier)". So on go the output-side miner produced no compiler-source
  claim at all. Joined to the graph candidates: 59 of 59 have a graph
  candidate recurring in every unit they name (the LOOSE test), but
  **0 of 59 have a graph candidate confined to those units** (the STRICT
  test) — and both are reported, because 669 candidates are walked by
  all 590 probes and the loose test alone would report ubiquity as
  coincidence. In the other direction **5,330 of the 9,809 graph
  candidates have no output-side counterpart at all.** Artifact:
  `super_ops_comparison_go.json`.
- 2026-09-03 (task 75, log_177) — **`coverage.py` is folded into
  `graph.py` and marked a superseded record.** `load_august`,
  `diary_targets` and the four-population `coverage` are now methods of
  `class Graph`; the diary reader there keeps the subject column, which
  the miner needs. `coverage.py` is unedited below a header that says
  so and that names the id-scheme mismatch above. The guard,
  `op_pipeline/check_no_spelling_keys.py` UNMODIFIED (git reports no
  change; md5 `1d6aba67cbcdb021c3bdfd7f40fd2020`), one process over
  `super_ops_go.json`, `coverage_go2.json` and
  `super_ops_comparison_go.json`: **3 PASS, exit 0, `grep -c exempt`
  = 0**. Transcript: `Research/compiler_graph/guard_task75.txt`.
- 2026-09-03: **task 73 added two summary builders under this node**, both
  streaming and both stating their peak resident set, because the page that
  reads this graph may never hold one in memory.
  `Research/compiler_graph/graph_files_build.py` streams a
  `graph_<lang>.json` ONE RECORD AT A TIME and writes
  `graph_<lang>_files.json` (per file: nodes by kind, definitions, frontier
  by kind; plus the inter-file edge counts by relation) and
  `graph_<lang>_defs.json` (per file: the definitions with label and line
  span; plus the definition-to-definition edges). It builds no node table at
  all: task 71's id scheme `file#line#kind#ordinal` carries the file and the
  kind, so memory is the size of the aggregates. Measured, run smallest
  graph first: go 151,720 records → 17 MB peak, 0.5 s; rust 174,923 →
  20 MB, 0.5 s; swift 542,006 → 31 MB, 1.5 s; cpp 1,022,123 → 39 MB, 2.8 s.
  `coverage_files_build.py` streams `coverage_go2.json` (515,160,866 bytes,
  10,015,022 diary events) one LINE at a time and writes
  `coverage_go_files.json` (2,597,323 bytes): the populations, the per-file
  coverage rows, the 810 never-visited bodies with labels, the per-
  definition visitor counts, and per probe its file-level path plus THE BYTE
  RANGE of that probe's full path inside the join, so the page can read one
  probe without opening the file. Measured: 82 MB peak, 8.3 s. Both abort by
  name (`MemoryCeilingReached`) above a stated 6,144 MB ceiling rather than
  being stopped by the operating system, and both run the unmodified
  `check_no_spelling_keys.py` over their own output and delete it on
  failure — PASS on all nine emitted files, one process, exit 0. Evidence:
  `DevComms/log_181_task73_pane4_coverage_view.md`.
- 2026-09-03: **a difference between two of this node's own numbers, named
  rather than smoothed.** `t72/inject_report.json` says 1,549 targets read
  and 1,549 instrumented, 0 skipped, and `diary_targets_all.json` holds
  1,549 rows; `coverage_go2.json` reports `population_instrumented` 1,534.
  Fifteen bodies are counted as instrumented by the injector and not by the
  join. Pane 4 prints the join's 1,534 (it is the number the coverage set is
  measured against) and the builder carries the recount 1,549 beside it as
  `instrumented_bodies_recounted_here`. Which of the two is right is not
  settled here; it is a frontier for the next lap on this node.
- 2026-09-04 (task 81, log_190) — **the three planned rows for c and cpp
  are filled: `diary`, `coverage`, `super_ops`.** clang was instrumented
  at the pin `llvmorg-21.1.8` (`2078da43e25a4623cab2d0d60decddf709aaea28`,
  quoted from the binary that was built), 8,871 entry hooks placed in the
  region's 9,108 instrumentable bodies with 237 skipped under three named
  causes; the full build took 1,378 s and 801 MB against the cost page's
  1,316 s / 784 MB. **1,380 of 1,380 original c and cpp probes diaried,
  0 failed** — the population recounted inside the sandbox from
  `canon39_wrapped_c.json` (610) and `canon39_wrapped_cpp.json` (770) —
  in 33.6 s for 1,023 MB. **Coverage: 1,331 of 8,871 instrumented bodies
  visited by at least one of the 1,380 probes, 7,540 by none over 180
  files**, 1,918 defs a named frontier, 0 visited keys outside the
  region. The diary was then extended to 2,600 of the 27,080 regenerated
  c and cpp units (every 10th by unit id, sized by the join's own
  measured cost); over 3,980 probes the visited set is 1,482 and **151
  bodies are reached that no original probe reaches**, almost all in the
  X86 backend's floating-point and instruction-info files.
  **`super_ops`: 13,783 closed candidates** over the 770 cpp diaries with
  the go lap's parameters quoted from its own artifact; the both-ways
  comparison gives 347 of 347 output-side records with a loose
  counterpart and **207 with a strict one, where go had 0**. The
  unmodified `check_no_spelling_keys.py` passes all sixteen artifacts in
  ONE process, exit 0, `grep -c exempt` 0 — after it refused five of them
  for one real cause (a compiler function named `consume` is also one of
  the 91 operator tokens) and the ruled remedy was applied: the
  never-visited row and the injection target row are now TYPED UNIT
  OBJECTS carrying a language field beside their unit id, no role key and
  no field whitelist. Evidence:
  `DevComms/log_190_task81_clang_c_cpp_diaries.md`,
  `Research/compiler_graph/guard_task81.txt`,
  `Research/compiler_graph/coverage_cpp_summary.json`,
  `Research/compiler_graph/coverage_extended_summary.json`, lanes
  `lanes_t81/t81_l3..l16`, logs in `~/AirlockRuns/t81/agent/logs/`.
- 2026-09-04: **a bound this node states and did not hold.** The extended
  coverage join reached a peak resident set of 6,253.1 MB against the
  stated 6,144 MB ceiling, and nothing aborted, because `Graph.coverage`
  carries no in-process memory check — `Graph.super_ops` does, and the
  two were written in different laps. The overshoot is 1.8% and the
  machine was never at risk, but the rule is that a cap aborts BY NAME.
  Whether `coverage` gets `super_ops`'s `check_memory` call is a shape
  question for this CORE and is FLAGGED, not decided, by task 81.

- 2026-09-04: **the third connection kind named, and the graph's
  completion made priority 2.** the owner, correcting this node's definition:
  a completed compiler graph carries "connections that are structural,
  dynamic, and are part of each operator traced variant". The first two
  were in the definition since 2026-09-03; the third was not, and the
  node has never held it — the graph carries structural edges and a
  per-probe diary, but not, per operator variant, the connections that
  variant's own trace contributes. Written into the CORE's definition
  and as a **planned** realization row before any code, per the standing
  rule. The dashboard node's pane 4 depends on it: the owner, "until it
  exists, theres nothing to fucking see." Status: **planned**.
  Provenance: the owner, 2026-09-04, in session.

- 2026-09-04 (task 87) — **the third connection kind has a SHAPE in the
  tree, before any code.** `variant_structure` (attribute) and
  `variant_connections` (method) are added to this CORE's `sub_nodes`,
  its `## design` block and its call flow, and two settled rules are
  written with them: **a variant's identity is machine form, never a
  token** (a digest over `body_bytes`, `body_text`, `entry_contract`
  and the ledger's block / size / type / produced-by shape, all read
  off the unit's own ship code, with the operator token written onto
  each MEMBER as a display label afterwards and read back by nothing);
  and **the third kind speaks only about probes that have a machine
  form on disk**, every other diary being the named frontier
  `probe_without_a_machine_form`. That frontier's population was then
  MEASURED rather than assumed and is **0** on all three populations —
  go 590 of 590, c and cpp 1,380 of 1,380, the regenerated sample 2,600
  of 2,600. Provenance: the owner, 2026-09-04, naming all three connection
  kinds. Survey lanes:
  `Research/compiler_graph/lanes_t87/t87_l1_survey.sh`,
  `t87_l2_unit_sources.sh`, `t87_l3_go_machine_form_hunt.sh`,
  `t87_l4_go_unit_id_scan.sh`, `t87_l5_go_regen_units.sh`; logs in
  `~/AirlockRuns/t87/agent/logs/`.

- 2026-09-04 (task 87, log_193) — **the third connection kind EXISTS as
  data, over both compilers that have diaries.** `Graph.variant_
  connections` is in `graph.py` beside `build` / `query_path` / `diary`
  / `coverage` / `super_ops`, under the node's own name, and
  `variant_structure` is an attribute of the class. It groups every
  diaried probe into an OPERATOR TRACED VARIANT by machine form alone,
  attributes each visited-next transition to the variants that walk it,
  and names the nodes and transitions EXCLUSIVE to one variant.
  Measured, each number with its population: **go 590 of 590 probes →
  294 variants**, 626 nodes, 1,598 transitions, 14 of them walked by
  exactly one variant and 677 by every variant; **c 610 → 331**, **cpp
  770 → 353**, **c and cpp 1,380 of 1,380 → 512 variants**, 1,175 nodes,
  2,044 transitions, 48 walked by exactly one variant; **the extended
  3,980 → 1,361 variants**, 1,319 nodes, 2,474 transitions, 66 walked by
  exactly one variant. `probes_without_a_machine_form` is **0** on all
  five. rust and swift are written down as
  `UNMEASURED_BY_ABSENCE_OF_DIARIES` carrying the cost page's own reason
  and are not approximated. **What the third kind shows that the union
  does not**, in one worked example: `var_ac7b8914b059a2c0` is the four
  units whose emitted body is `0f 57 05 00 00 00 00 c3` (`xorps
  0x0(%rip),%xmm0; ret`), two c and two cpp; the transition
  `X86TargetLowering::PerformDAGCombine → combineFneg` is walked by that
  variant and by no other of the 512 — while `coverage` reports
  `PerformDAGCombine` as entered by **1,380 of 1,380** probes, which is
  indistinguishable from ubiquity. **THE BAN PROVED ON THE OUTPUT:** 133
  of the 512 variants carry more than one distinct display label among
  their members, and all 36 labels appear in more than one variant —
  both impossible if the token were the key. The unmodified guard (md5
  `1d6aba67cbcdb021c3bdfd7f40fd2020`) passes all seven artifacts in ONE
  process, exit 0, `grep -c exempt` = 0. Memory: 172.4 MB (go) and
  1,051.9 MB (c and cpp) peak resident against a 6,144 MB ceiling that
  refuses BY NAME, fitted from two measured points before the full pass;
  `coverage_c_and_cpp.json` (416 MB) was read one line at a time at
  12.7 MB peak. Evidence:
  `DevComms/log_193_task87_operator_variant_connections.md`,
  `Research/compiler_graph/guard_task87.txt`,
  `Research/compiler_graph/variant_connections_{go,c,cpp,c_and_cpp,
  extended,rust,swift}.json`, lanes
  `Research/compiler_graph/lanes_t87/t87_l1..l13` (all thirteen exit 0),
  logs in `~/AirlockRuns/t87/agent/logs/`. **Zero regressions proved:**
  the pre-task-87 `graph.py` (commit `e2cba0db`) and the current one were
  run side by side over the same 30-diary go sample — `Graph.coverage`
  byte-identical, `Graph.super_ops` identical on parameters, populations
  and candidates (lane `t87_l13`).

- 2026-09-04: **"completed" settled, and the coordinator's question
  withdrawn.** The graph is complete when it is an actual implementation
  of the object this node's definition specifies — all three connection
  kinds over the nine compilers and interpreters in scope. Two carry all
  three today. The graph is NOT complete and the remainder is work, not
  a definition to renegotiate. The round-16 coordinator had forwarded
  "does completed mean nine compilers or two" to the owner as a decision; it
  offered a reduced deliverable as a reading of the requirement, which is
  the same substitution failure log_173 records for this node. the owner:
  "completed compiler graph? when theres an actual graph that is an
  implementation of what ive asked. what the fuck is that question?"
  Recorded in the CORE under its own heading so it is not asked again.

- 2026-09-04: **the graphs moved out of this repository, and were made
  eight to ten times smaller with nothing lost.** Task 93, log_198.
  the owner: "we can have a separate companion folder for the graphs and we
  will use local git to track changes every 30 seconds."
  `~/Programming/PseudoCoupGraphs` is that folder and **has no remote,
  deliberately** — repo-daemon commits a remote-less repository locally
  and never pushes; no origin was added. Moved: the four graphs, the five
  coverage joins, the two super-op candidate files, the seven
  variant-connection files (each verified by size and md5 before its
  original was removed) and `diaries/` (4,484,020,997 bytes, moved
  through `tar` so its hardlinks survive; the six folders carry the same
  counts on both sides — c 610, c_and_cpp 1,380, cpp 770, extended
  3,980, go 590, regen 2,600). `Research/compiler_graph` went from
  8,576,760,358 bytes to 652,798,509. NOT moved, and it is deliberate:
  `graph_go2/go3/go4/go_lapone.json` (533,590,808 bytes between them),
  `graph_cpp2/cpp3.json` and `coverage_go.json` — earlier laps that stay
  as records.

  **THE COMPACT FORM** (`graph_compact.py`, new). Measured causes, not
  guessed: the frontier's `reason` sentence is 150–200 characters
  repeated on 77,530 go records and there are exactly FOUR distinct
  sentences in the whole go frontier; every edge writes two full source
  paths both of which are already in the node table. So one string table
  per document, ordered by frequency (machine-form evidence), every
  record an array whose first element is its schema, and each schema
  DISCOVERED from the file. `pins` and `counts` stay at the front so
  every existing 256 KB head read keeps working, checked on all four.

  | graph | before | after | share | factor |
  |---|---:|---:|---:|---:|
  | `graph_go.json` | 49,278,033 | 4,839,116 | 9.8% | 10.2× |
  | `graph_rust.json` | 61,987,708 | 6,297,219 | 10.2% | 9.8× |
  | `graph_swift.json` | 165,106,660 | 21,263,773 | 12.9% | 7.8× |
  | `graph_cpp.json` | 331,704,231 | 39,788,093 | 12.0% | 8.3× |
  | all four | 608,076,632 | 72,188,201 | 11.9% | 8.4× |

  **NOTHING IS LOST, and it is proved rather than asserted.**
  `graph_compact.py expand` rebuilds the old file and `cmp` reports
  **0 differing bytes on all four**, md5 against md5, and each compact
  document carries the original's byte count and md5 so the proof is
  re-runnable. The frontier is complete, counted both ways and by kind:
  go 77,530, rust 101,095, swift 278,245, cpp 523,694, every count
  identical; and record for record, every section, old form against
  compact, `every record equal: True`. Nothing was removed until it had
  been proved rebuildable in the same lane.

  **THE READERS ARE PROVED BY RUNNING, not by having been edited.**
  `graph_files_build.py` re-run over the moved compact graphs emits
  summaries **byte-identical to the eight already tracked here**
  (`graph_<lang>_files.json` and `graph_<lang>_defs.json`, all four
  compilers, 52 MB peak). `Graph.load` reads the compact
  `graph_go.json` in the companion folder and answers 10,393 nodes /
  63,797 edges / 77,530 frontier in 0.1 s at 91.9 MB.
  `Research/compiler_graph/graphs_home.py` is the ONE place that answers
  where the graphs are.

  **`coverage_files_build.py` now serves two compilers** and
  `coverage_cpp_files.json` exists for the first time: 187 files with a
  row, 1,380 probes, 1,331 visited definitions, 88 files entered by no
  probe, 185 MB peak over a 416 MB join. One measured correction rides
  with it: the `entered` flag had been read off `file_runs`, the
  summary's CAPPED first 600 runs of a probe's path, so a file reached
  only after run 600 was written down as never entered. Effect on go:
  **none** — 15 files never entered before and after, the same names.

  **THE GUARD CAUGHT A REAL VIOLATION AND IT WAS FIXED AT THE CAUSE.**
  The first cut of the string table wrote each distinct string as a BARE
  LIST ELEMENT and the unmodified guard refused go and rust, 7 places
  each: `new`, `and`, `not`, `delete`, `in`, `as`, `or`, `with`, `is`,
  `xor` are source identifiers in these compilers AND members of the
  91-token operator inventory. Every entry is now a row
  `{"text": "..."}` — the token as a VALUE beside an opaque position,
  which is the shape this line already ratified for the four x86
  mnemonics that are homographs, and the shape the ORIGINAL graph has at
  `"detail": "new"`. No exemption was added. The unmodified guard (md5
  `1d6aba67cbcdb021c3bdfd7f40fd2020`, `git status` clean) then passes all
  21 artifacts in ONE process, exit 0, `grep -c exempt` = 0. Transcript:
  `Research/compiler_graph/guard_task93.txt`.

  Memory: stated ceiling 6,144 MB, abort named `MemoryCeilingReached`;
  sampled first (holding `graph_go.json` whole and re-emitting it peaks
  at 258.9 MB, which is why the compactor streams). Measured peaks —
  compacting cpp 786.8 MB, the summary rebuild 52 MB, the coverage joins
  84 MB and 185 MB. No limit was hit and nothing was reduced to fit.

  Evidence: `DevComms/log_198_task93_graphs_moved_shrunk_drawn.md`,
  lanes `Research/compiler_graph/lanes_t93/t93_l1..l6` (l2 ABORTED on a
  named defect and kept as the record; the rest exit 0), logs in
  `~/AirlockRuns/t93/agent/logs/`.

  **FLAGGED, awaiting the owner:** 7.3 GB of what moved is in the companion
  folder but NOT in its git history. repo-daemon refuses it by its own
  stated cap — "NOT committing -- 9938 files totalling 8504 MB exceeds
  max_commit_mb (1500 MB). NEEDS A LOOK: commit it by hand once you have
  decided what belongs in history." The four compact graphs and the
  seven variant-connection files ARE committed (the latter by hand,
  because they were tracked here before the move). The folder's README
  says the diaries live there under local version control; the standing
  rule on what belongs in a repository gates by PROVENANCE and calls
  regenerable output out. Only the owner can say which governs.
- 2026-09-05 (task 95, log_200) — **THE ARCH-OPCODE-NODE: which parts of
  each compiler produce a machine instruction, detected from SOURCE
  ALONE.** the owner, 2026-09-05: "is there a way to know which parts of the
  compiler produces arch-opcodes (arch-units)? ... i mean just by
  analyzing the compiler source code." `Graph.arch_opcode_nodes` now sits
  in `graph.py` beside `coverage` and `variant_connections`, with the
  per-language rules as DATA in `ARCH_OPCODE_RULES` — the same
  language-agnostic shape `build` uses. It reads each region's source AT
  ITS PIN, finds every call site of that region's instruction emitter,
  grades the opcode argument, and marks every definition node with one of
  four states. **No compiler is run and no diary is read**, which is the
  whole point: it reaches the two compilers this machine cannot
  instrument.

  **THE FOUR STATES PARTITION EVERY DEFINITION POPULATION**, and the pass
  asserts that rather than hoping — names_its_opcode / one_static_hop /
  emits_opcode_dynamic / emits_nothing:
  **go 13 / 39 / 5 / 1,802 of 1,859**;
  **cpp 174 / 8 / 69 / 10,538 of 10,789**;
  **rust 0 / 0 / 1 / 3,312 of 3,313**;
  **swift 1 / 0 / 1 / 10,086 of 10,088**.
  Call sites graded: go 238, cpp 870, rust 2, swift 5.

  **EACH REGION'S EMITTER WAS LOCATED FROM ITS OWN SOURCE**, never
  carried over from go. In go a machine-form rule answers it: a function
  that TAKES the arch opcode type `obj.As` emits, one that RETURNS it is
  a table. That gives three emitters (`(*State).Prog` 206 call sites,
  `(*State).Br` 9, `opregreg` 23) and four tables — including
  `Op.Asm()`, the CORE's own hop into `ssa/opGen.go`'s `opcodeTable`.
  clang: `BuildMI` 724, `setDesc` 57, `MCInstBuilder` 53, `setOpcode` 36,
  the opcode taken from its POSITION in the call so `X86::EAX` never
  reaches an opcode index. **rust: NONE — 0 files of the WHOLE checkout
  name `BuildMI(`, `MCInst`, `MachineInstr` or an X86 namespace**, so the
  emitter is LLVM and LLVM is the cpp region. swift: the inline-assembly
  constructor, 5 sites, of which `lib/IRGen/IRGenSIL.cpp:5578` spells the
  fixed template `nop` and the source's own comment calls it "not
  architecture independent".

  **THE COORDINATOR'S GO MEASUREMENT REPRODUCES EXACTLY** from this
  task's own artifact: 206 `Prog` call sites, **50 pass an x86 constant,
  24 distinct** — `ADDQ CMOVLPC CMOVLPS CMOVQPC CMOVQPS CMOVWPC CMOVWPS
  DECL JNE LOCK MOVB MOVBLZX MOVL MOVQ MOVSQ MOVUPS RCRQ REP SETEQ SETPC
  SETPS STOSQ TESTB XCHGL`. Two counting differences are named in
  log_200 §6.1: the further 17 constant sites pass `obj.A*` PSEUDO-ops
  (counted separately here), and the "118 computed" is the count of three
  spellings, not of all 139 non-constant sites.

  **THE INVERSE INDEX** — per arch opcode, the definitions that emit it —
  holds **578** opcodes for go and **400** for cpp. go's is larger and
  BLUNTER, and the reason is stated as a limit: `s.Prog(v.Op.Asm())`
  resolves to the whole amd64 table, **564 opcodes at every such site**;
  the hop says which TABLE is read, never which ROW.

  **THE SHRINK.** Restricted to emitters and their callers each graph
  keeps **3.6%** of go's definitions (66 of 1,859), **3.3%** of cpp's
  (356 of 10,789), **0.2%** of rust's and **0.0%** of swift's. The
  caller closure is a LOWER BOUND, bounded by the graph's own resolved
  `calls` edges — go has 6,374 unresolved calls, cpp 94,656, already
  named frontiers.

  **AGAINST WHAT RUNNING THE COMPILER SHOWED, and entering is not
  emitting.** go: 57 static emitters against 724 entered definitions,
  **12 in both**; **712 of the 724 entered emit nothing**; 45 emitters
  were instrumented and entered by no probe, **39 of them the `simd*`
  helpers of `amd64/ssa.go` that 590 scalar probes never reach**; 0
  emitters outside the instrumented population. cpp: 251 against 1,331,
  **45 in both**, 1,286 entered-and-emitting-nothing, 203 instrumented
  and entered by none, 3 NEVER INSTRUMENTED — a named frontier, never a
  never-entered node, per this node's own settled rule. A first cut asked
  the wrong map (`per_def_visitors` holds only the ENTERED set) and lane
  `t95_l7` is kept as that record; `t95_l8` corrected it.

  **WHAT RUST AND SWIFT GAINED.** Both are blocked from instrumentation
  here and both now carry a positive, source-only statement instead of a
  blank: no node of the rust region is an arch-opcode-node, and the cause
  is structural rather than a limit of this machine — rustc emits LLVM
  IR and LLVM is already graphed, instrumented and covered by this line.
  The only assembly-looking text rustc spells is DIRECTIVES (`.globl`,
  `.align`, `.section`, `.att_syntax`), which are not instructions.
  swift gained one real arch-opcode-node and the same structural
  statement for its other 10,086 definitions.

  **THE GUARD CAUGHT A REAL VIOLATION AND IT WAS FIXED AT THE CAUSE.**
  The first go artifact was REFUSED at `$.call_sites[223].argument`: go's
  emitter is `func (s *State) Prog(as obj.As)` and `s.Prog(as)` puts the
  operator token `as` in a row-structure position. Remedy: the ratified
  typed shape `{"text": "..."}`, no exemption and no rename — and
  `Graph.arch_opcode_nodes` now runs the guard over its own output and
  raises `SpellingKeyRefused` rather than returning. The unmodified guard
  (md5 `1d6aba67cbcdb021c3bdfd7f40fd2020`, `git status` clean) then passes
  all 8 artifacts in ONE process, exit 0, `grep -c exempt` = 0.
  Transcript: `Research/compiler_graph/guard_task95.txt`.

  **NAMED FRONTIERS, not worked around.** The `.td` files could not serve
  as the instruction table the CORE expected: a plain `def <name>` scan
  answers 7,227 records and misses everything X86 defines through `defm`
  multiclasses (100 real instructions read as "not a record"), and the
  complete enumeration is in `X86GenInstrInfo.inc`, written by TableGen
  at build time and absent from this disk. Per-site non-resolutions are
  counted by reason, `token_pasting_suffix_not_readable_from_source`
  among them — `GET_EGPR_IF_ENABLED(OPC)` pastes a suffix that cannot be
  READ as a constant, so it is not recorded.

  Memory: stated ceiling 6,144 MB, refusal named `MemoryCeilingReached`,
  sampled first (all four compact graphs held one after another peak at
  324.1 MB). Measured peaks: go 120.9, cpp 566.9, rust 117.1, swift
  319.5 MB, each written onto its own artifact. The 515 MB and 416 MB
  coverage joins were never read; the comparison uses task 73's small
  `coverage_<lang>_files.json` summaries.

  Artifacts: `PseudoCoupGraphs/arch_opcode_nodes_{go,cpp,rust,swift}.json`
  and `Research/compiler_graph/arch_opcode_nodes_*_summary.json`.
  Evidence: `DevComms/log_200_task95_arch_opcode_nodes.md`, lanes
  `Research/compiler_graph/lanes_t95/t95_l1..l11` plus `t95_compare.py`,
  logs in `~/Programming/Airlock/agent/logs/20260905T05*__t95_*`.

  One operational fact: `mounts.conf` already named the
  `PseudoCoupGraphs` and read-write `PseudoCoupHQ` mounts, but the
  running `sandbox-runner` had neither — it is a systemd quadlet and the
  unit had been rendered before those lines were added. `./down.sh` and
  `./up.sh` cannot fix that; `./install_quadlet.sh` re-renders the unit
  from `mounts.conf`, which is Airlock's own way. No workaround written.

  **FLAGGED, awaiting the owner:** whether go's `one_static_hop` sites should
  carry all 564 amd64 opcodes in the inverse index, or carry only the
  constants and NAME the table instead. The marking stands either way;
  narrowing the hop needs a run-time fact, which is `coverage`'s
  question, not this one's.
