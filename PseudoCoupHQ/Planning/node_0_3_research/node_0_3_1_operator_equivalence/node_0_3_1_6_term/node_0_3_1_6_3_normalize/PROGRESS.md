---
id: hq.research.compiler_graph.term.normalize.progress
status: living
---

# PROGRESS — normalize

- 2026-09-03: node folder generated by
  `PRIVATE/PlanPlan/framework/generate_nodes.py` from the
  `sub_nodes` register of `../CORE_0_3_1_6_term.md`.
- 2026-09-03: simplify once, rename positionally, print on one line —
  **done** (`layer5.py`). Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_147_task48_layer4_ledger_census.md`
  §8.1.
- 2026-09-03: `v0 + v1` produced for `c/op_109` and `go/op_319` —
  **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_153_task53_layer4_canon38_census.md`
  §6.1.
- 2026-09-03: 1,104 distinct layer-5 texts across the eligible units —
  **done**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_154_task54_the_pool3.md` §2.2.
- 2026-09-03: `Term.normalize` re-expressed inside the node's own
  module, same fixed rule (simplify once, rename positionally, print
  on one line) — **done**; `go/op_319` still prints `v0 + v1`.
  Evidence: `show61_go_op_319.txt`, `PRIVATE/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: 1,286 distinct layer-5 texts among the eligible units of
  the canon39 pool, against 1,104 over canon38 — **done**. Evidence:
  `the_pool4.json` summary, `PRIVATE/PseudoCoupHQ/DevComms/log_163_task61_term_pool.md`.
- 2026-09-03: layer 5 computed for every PROVED term and no other:
  **26,594 of 26,594** proved terms produced a normalized text, 0
  refused, **1,267 distinct texts** (1,286 under the round-12 gate)
  — **done**. Evidence: `audit65_printed.txt`, `PRIVATE/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03: the collapse per arrival population, layer 5 beside
  layer 3 over the SAME units: ALL 26,594 units / 1,267 layer-5 texts
  (21.0x) / 2,023 layer-3 texts (13.1x); original 1,665 / 404 / 618;
  regenerated 24,920 / 1,219 / 2,010 — **done**. Evidence:
  `collapse65_printed.txt`, `PRIVATE/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-03: **FINDING — the ruled rule does not yet achieve its own
  stated purpose.** A second walk of the SAME rule over the SAME
  units printed a different layer-5 text for **1,479 of 26,594
  (5.6%)** — 525 c, 916 cpp, 13 go, 10 rust, 15 swift — because
  `z3.simplify` orders a commutative operator's arguments by the
  solver's internal node identity, which depends on what the
  process built earlier; the positional renaming then follows the
  moved order and `v0`/`v1` swap with it. A partial repair (sorting
  those arguments) was tried and MEASURED — it collapses 299 of
  1,292 texts and still leaves 39 of 582 `c` units unstable under a
  reversed walk — so the ruled three steps were NOT edited on it.
  Correction written into the CORE, fix **planned, round 14** —
  **flagged**. Evidence: `normalize_stability65_printed.txt`,
  `normalize_order_probe65_printed.txt`, `PRIVATE/PseudoCoupHQ/DevComms/log_169_task65_term_pool5.md`.
- 2026-09-05: WHERE THE MEMORY GOES, located to this node. Task 83
  traced `c/regen_1859` and concluded the memory was spent in the gate
  (log_189 §2.5). It is not the gate: the field that carries the
  failure is **`layer5_refusal`** — `term66_run.add_layer5` calling
  `Term.normalize` calling `z3.simplify`. The term is built and the
  gate PROVES it first: `term_state TERM`, `PROVED_ON_SHIP`, 0 holes,
  at every ceiling — **done** (the location; the consequence is the
  blocker below). Evidence:
  `<runs>/t97/agent/logs/20260905T070634Z__t97_l2_flag_reason.sh.log`;
  `PRIVATE/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §2.3.
- 2026-09-05: the population is BIMODAL, measured. Of the 228 units
  whose normalization refused at 1,536 MB, **184 converge given about
  2 GB** — `c/regen_1869` peaks at 1,993,856 kB under a 3,072 MB
  ceiling and at 1,993,756 kB under 6,144 MB, the same number with
  twice the room, and the two records are identical. **44 do not
  converge at any ceiling**: their peak IS the ceiling at 1,536, 4,096
  and 18,432 MB (1,469,000 to 4,082,072 to 17,014,600 kB) and the wall
  time grows with it (0.6 to 2.0 to 6.5 s) — **done** (the
  measurement). Evidence: `term97_pass2_b_slice0.json`,
  `<runs>/t97/agent/logs/20260905T094242Z__t97_l6_pass2_residue.sh.log`;
  `PRIVATE/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §2.4, §4.3.
- 2026-09-05: **BLOCKER, for the owner.** `Term.normalize` does not converge
  for **44 of the 30,324 units canon40 proves (0.145%)**. A peak that
  tracks its ceiling over a 12x range with time growing alongside is a
  non-terminating rewrite, not a working set, so a larger machine fails
  later rather than differently. Their terms are PROVED; the comparison
  key is missing. Nothing was composed in its place: no bounded
  normalizer, no solver resource limit returning a half-simplified
  text, and no record naming `MemoryError` written into the store —
  **flagged, not decided**. Evidence:
  `PRIVATE/PseudoCoupHQ/DevComms/log_202_task97_term_pool_canon40.md` §9.1.
- 2026-09-07: **the round-14 fix (flagged 2026-09-03) is applied.**
  `Term.normalize` now calls `order_commutative` a THIRD time, before
  the first `z3.simplify`, so the simplifier never sees an unordered
  term — one function changed. Measured over the 27,682 units one
  walk printed: a second term built from each unit's own term by
  permuting every commutative node's operands used to print a
  DIFFERENT text for **899 of them**; with the fix, the same
  measurement over 27,662 units reads **0**. Re-normalizing all
  30,280 `term66_store/` records into a new `term104_store/` (never
  overwriting `term66_store/`) changed **225 of 27,866** layer-5
  texts; the layer-4 term itself is untouched by printing (**0** of
  27,866 s-expression hash disagreements, checked not assumed) —
  **done**. The pool rebuilt over the new texts, same grounds as
  `pool66_run.py`: entries 1,652 → 1,650, distinct layer-5 texts
  972 → 970; of t100's 12 applied edges, the same 4 matched by text
  before and after (no change); of t100's 214 proved-but-unapplied
  edges, 0 are comparable by text at all — every one has a
  representative whose own body is not independently proved, which is
  why t100 declined to apply it, a proof-precondition gap this fix
  does not touch. The 44-unit BLOCKER above is UNCHANGED by this fix:
  asked again under the new rule, **0 of 44 converge**, same
  `MEMORY_REASON` outcome as before — **done** (confirms the blocker
  is independent of operand order). Evidence: `t104_audit.json`,
  `t104_the44.json`, `pool104_delta.json`;
  `PRIVATE/PseudoCoupHQ/DevComms/log_233_task_t104_normalize_commutative_order_close.md`.
