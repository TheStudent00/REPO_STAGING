---
id: pcv5.tools.progress
status: living
---

# PROGRESS — tools

- 2026-07-31: node founded, definition only. nothing built here yet.
- 2026-08-01 **done** (the owner's rulings, this session): four sub-nodes
  founded — ledgerer, transpiler, polyfill, tracer. the tracer is a
  SIBLING of the ledgerer, resolving the "belongs beside the
  ledgerer" ambiguity; its prose moved from this CORE into
  `node_0_0_3_tracer/CORE_0_0_3_tracer.md`. all four carry
  `designation: code (object)` — the coarse kind the owner had added to
  PROTOCOL §3a the same day (identifier settled, module-vs-class
  deferred; must refine before a node settles, enforced by
  `check_plans.py`).
- the runtime tracer recorded as wanted (the owner, 2026-07-31) — a harvest
  target, not only a design reference.
- ~~open: whether PseudoCoup_v6's existing `Tools/` move here as the
  composition seed~~ **NOT AN OPEN QUESTION** (the owner, 2026-07-31), and
  the wording was wrong twice over. Parts come from anywhere; if parts
  inside PCv6 are the best parts then they are used, as PARTS — not as
  "the" seed, which implied a privileged source that the
  parts-from-anywhere ruling had already denied. the owner: "idk why this is
  coming up as an open problem right now. when we dive into planning
  for PCv5, these will be resolved." Composition decisions belong to
  this node's deepening, not to a list of things blocking it.
