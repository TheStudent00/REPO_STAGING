#!/bin/sh
# layer-3 PROBE TIMING -- cpp -- generated by
# Research/kind_fuzz_clustering/timing_build.py . Do not hand-edit; regenerate.
# One probe per file, fresh process per probe, no batching: the WORST CASE.
# Every line of /out/tm_cpp.txt is
#   <probe id>|<predicted>|<compile rc>|<compile ns>|<run rc>|<run ns>
set -u
ROOT=/work/tm_cpp
rm -rf "$ROOT"; mkdir -p "$ROOT"
export HOME=/work
export GOFLAGS=-mod=mod
export GO111MODULE=off
export GOCACHE=/work/.gocache
export GOPATH=/work/.gopath
export PATH=/persist/dart-sdk/bin:$PATH

# swift's binaries want an ncurses name the image does not ship under; the fix
# is per-container, so every script that may touch swift re-applies it.
ncurses_fix() {
  if [ -f /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 ]; then
    ln -sf /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 \
       /usr/lib/x86_64-linux-gnu/libncurses.so.6 2>/dev/null
    ldconfig 2>/dev/null
  fi
}
ncurses_fix

OUT=/out/tm_cpp.txt
: > "$OUT"
echo "=== layer-3 probe timing -- cpp ==="
date -u +%Y-%m-%dT%H:%M:%SZ
if ! command -v g++ >/dev/null 2>&1; then
  echo "cpp: TOOLCHAIN ABSENT -- nothing run"
  echo "__ABSENT__|absent|-1|0|-1|0" >> "$OUT"
  exit 0
fi
mkdir -p "$ROOT/p1"
cat > "$ROOT/p1/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{1, 2, 3};
  auto b = std::vector<int>{1, 2, 3};
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p2"
cat > "$ROOT/p2/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = std::string("hello");
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p3"
cat > "$ROOT/p3/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 1000000;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p4"
cat > "$ROOT/p4/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto b = std::string("world");
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p5"
cat > "$ROOT/p5/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = 0;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p6"
cat > "$ROOT/p6/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 3.14;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p7"
cat > "$ROOT/p7/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = false;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p8"
cat > "$ROOT/p8/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = nullptr;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p9"
cat > "$ROOT/p9/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 1.5;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p10"
cat > "$ROOT/p10/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = 1000000;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p11"
cat > "$ROOT/p11/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}, {"b", 2}};
  auto b = 3.14;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p12"
cat > "$ROOT/p12/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("world");
  auto b = std::string("hello");
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p13"
cat > "$ROOT/p13/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = nullptr;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p14"
cat > "$ROOT/p14/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{1, 2, 3};
  auto b = std::vector<int>{1, 2, 3};
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p15"
cat > "$ROOT/p15/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("world");
  auto b = false;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p16"
cat > "$ROOT/p16/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = nullptr;
  auto b = 0;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p17"
cat > "$ROOT/p17/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("world");
  auto b = 3.14;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p18"
cat > "$ROOT/p18/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{7};
  auto b = std::map<std::string,int>{{"a", 1}, {"b", 2}};
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p19"
cat > "$ROOT/p19/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = false;
  auto b = 1000000;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p20"
cat > "$ROOT/p20/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = 42;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p21"
cat > "$ROOT/p21/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{7};
  auto b = std::vector<int>{7};
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p22"
cat > "$ROOT/p22/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = false;
  auto b = std::map<std::string,int>{{"a", 1}};
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p23"
cat > "$ROOT/p23/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 0.1;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p24"
cat > "$ROOT/p24/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("");
  auto r = (a)[0];
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p25"
cat > "$ROOT/p25/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{7};
  auto b = nullptr;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p26"
cat > "$ROOT/p26/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = false;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p27"
cat > "$ROOT/p27/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = std::vector<int>{1, 2, 3};
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p28"
cat > "$ROOT/p28/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 42;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p29"
cat > "$ROOT/p29/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 1.5;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p30"
cat > "$ROOT/p30/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = false;
  auto b = std::vector<int>{7};
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p31"
cat > "$ROOT/p31/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = false;
  auto b = 3.14;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p32"
cat > "$ROOT/p32/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = std::string("");
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p33"
cat > "$ROOT/p33/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = std::string("");
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p34"
cat > "$ROOT/p34/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 42;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p35"
cat > "$ROOT/p35/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{1, 2, 3};
  auto b = 1.5;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p36"
cat > "$ROOT/p36/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 1000000;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p37"
cat > "$ROOT/p37/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto b = std::string("");
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p38"
cat > "$ROOT/p38/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("");
  auto b = std::string("world");
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p39"
cat > "$ROOT/p39/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 0;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p40"
cat > "$ROOT/p40/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = nullptr;
  auto b = false;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p41"
cat > "$ROOT/p41/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 1000000;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p42"
cat > "$ROOT/p42/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto r = (a)[0];
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p43"
cat > "$ROOT/p43/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = false;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p44"
cat > "$ROOT/p44/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = std::vector<int>{7};
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p45"
cat > "$ROOT/p45/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto b = std::string("hello");
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p46"
cat > "$ROOT/p46/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = std::string("hello");
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p47"
cat > "$ROOT/p47/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto r = !(a);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p48"
cat > "$ROOT/p48/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 3.14;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p49"
cat > "$ROOT/p49/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 0;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p50"
cat > "$ROOT/p50/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 1000000;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p51"
cat > "$ROOT/p51/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = true;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p52"
cat > "$ROOT/p52/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = nullptr;
  auto b = nullptr;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p53"
cat > "$ROOT/p53/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 3.14;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p54"
cat > "$ROOT/p54/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}, {"b", 2}};
  auto b = 0.1;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p55"
cat > "$ROOT/p55/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{1, 2, 3};
  auto b = 1.5;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p56"
cat > "$ROOT/p56/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("");
  auto b = std::string("");
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p57"
cat > "$ROOT/p57/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 0.1;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p58"
cat > "$ROOT/p58/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto b = std::string("hello");
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p59"
cat > "$ROOT/p59/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = std::string("hello");
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p60"
cat > "$ROOT/p60/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 1000000;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p61"
cat > "$ROOT/p61/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = std::vector<int>{1, 2, 3};
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p62"
cat > "$ROOT/p62/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("world");
  auto b = 0;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p63"
cat > "$ROOT/p63/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = 0;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p64"
cat > "$ROOT/p64/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("world");
  auto b = std::string("hello");
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p65"
cat > "$ROOT/p65/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 3.14;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p66"
cat > "$ROOT/p66/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{7};
  auto b = std::vector<int>{1, 2, 3};
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p67"
cat > "$ROOT/p67/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = true;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p68"
cat > "$ROOT/p68/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = 0;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p69"
cat > "$ROOT/p69/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 42;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p70"
cat > "$ROOT/p70/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = 0.1;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p71"
cat > "$ROOT/p71/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = 1000000;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p72"
cat > "$ROOT/p72/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = 1000000;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p73"
cat > "$ROOT/p73/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = std::map<std::string,int>{{"a", 1}, {"b", 2}};
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p74"
cat > "$ROOT/p74/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = 1000000;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p75"
cat > "$ROOT/p75/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 3.14;
  auto b = 42;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p76"
cat > "$ROOT/p76/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{1, 2, 3};
  auto b = 42;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p77"
cat > "$ROOT/p77/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto b = 3.14;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p78"
cat > "$ROOT/p78/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = 0.1;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p79"
cat > "$ROOT/p79/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = true;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p80"
cat > "$ROOT/p80/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("hello");
  auto b = false;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p81"
cat > "$ROOT/p81/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = std::map<std::string,int>{{"a", 1}};
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p82"
cat > "$ROOT/p82/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 3.14;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p83"
cat > "$ROOT/p83/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::map<std::string,int>{{"a", 1}};
  auto b = nullptr;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p84"
cat > "$ROOT/p84/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = std::string("world");
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p85"
cat > "$ROOT/p85/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 0;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p86"
cat > "$ROOT/p86/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 1.5;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p87"
cat > "$ROOT/p87/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 42;
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p88"
cat > "$ROOT/p88/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 42;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p89"
cat > "$ROOT/p89/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = 42;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p90"
cat > "$ROOT/p90/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = false;
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p91"
cat > "$ROOT/p91/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0;
  auto b = 1.5;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p92"
cat > "$ROOT/p92/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::vector<int>{7};
  auto r = (a)[0];
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p93"
cat > "$ROOT/p93/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = true;
  auto b = 1.5;
  auto r = (a) * (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p94"
cat > "$ROOT/p94/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 0;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p95"
cat > "$ROOT/p95/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 0.1;
  auto r = -(a);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p96"
cat > "$ROOT/p96/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 42;
  auto b = 3.14;
  auto r = (a) - (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p97"
cat > "$ROOT/p97/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = std::string("");
  auto r = (a) < (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p98"
cat > "$ROOT/p98/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1000000;
  auto b = 1000000;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p99"
cat > "$ROOT/p99/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = 1.5;
  auto b = 1.5;
  auto r = (a) + (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
mkdir -p "$ROOT/p100"
cat > "$ROOT/p100/main.cpp" <<'TM_SRC_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  auto a = std::string("");
  auto b = std::string("world");
  auto r = (a) == (b);
  std::cout << "r=" << sizeof(r) << "\n";
  return 0;
}
TM_SRC_EOF
cat > "$ROOT/labels.txt" <<'TM_LABELS_EOF'
p1 refuse
p2 refuse
p3 accept
p4 accept
p5 accept
p6 accept
p7 refuse
p8 refuse
p9 accept
p10 accept
p11 refuse
p12 accept
p13 refuse
p14 accept
p15 refuse
p16 refuse
p17 refuse
p18 refuse
p19 refuse
p20 refuse
p21 accept
p22 refuse
p23 accept
p24 accept
p25 refuse
p26 refuse
p27 refuse
p28 accept
p29 accept
p30 refuse
p31 refuse
p32 refuse
p33 refuse
p34 accept
p35 refuse
p36 accept
p37 accept
p38 accept
p39 accept
p40 refuse
p41 accept
p42 accept
p43 refuse
p44 refuse
p45 accept
p46 refuse
p47 refuse
p48 accept
p49 accept
p50 accept
p51 refuse
p52 refuse
p53 accept
p54 refuse
p55 refuse
p56 accept
p57 refuse
p58 refuse
p59 refuse
p60 accept
p61 refuse
p62 refuse
p63 accept
p64 accept
p65 accept
p66 refuse
p67 refuse
p68 accept
p69 accept
p70 refuse
p71 refuse
p72 accept
p73 refuse
p74 refuse
p75 accept
p76 refuse
p77 accept
p78 accept
p79 accept
p80 refuse
p81 refuse
p82 accept
p83 refuse
p84 refuse
p85 accept
p86 accept
p87 accept
p88 accept
p89 refuse
p90 refuse
p91 accept
p92 accept
p93 refuse
p94 accept
p95 accept
p96 accept
p97 refuse
p98 accept
p99 accept
p100 accept
TM_LABELS_EOF
mkdir -p "$ROOT/batch"
cat > "$ROOT/batch/main.cpp" <<'TM_BATCH_EOF'
#include <iostream>
#include <map>
#include <string>
#include <vector>
int main() {
  {
      auto a0 = std::vector<int>{1, 2, 3};
      auto b0 = std::vector<int>{1, 2, 3};
      auto r = (a0) < (b0);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a1 = 1.5;
      auto b1 = 1000000;
      auto r = (a1) * (b1);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a2 = std::string("hello");
      auto b2 = std::string("world");
      auto r = (a2) == (b2);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a3 = 3.14;
      auto b3 = 0;
      auto r = (a3) * (b3);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a4 = 0.1;
      auto b4 = 3.14;
      auto r = (a4) - (b4);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a5 = 0;
      auto b5 = false;
      auto r = (a5) + (b5);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a6 = 0.1;
      auto b6 = 1.5;
      auto r = (a6) + (b6);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a7 = 42;
      auto b7 = 1000000;
      auto r = (a7) == (b7);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a8 = std::string("world");
      auto b8 = std::string("hello");
      auto r = (a8) == (b8);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a9 = std::vector<int>{1, 2, 3};
      auto b9 = std::vector<int>{1, 2, 3};
      auto r = (a9) == (b9);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a10 = false;
      auto b10 = 1000000;
      auto r = (a10) < (b10);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a11 = std::vector<int>{7};
      auto b11 = std::vector<int>{7};
      auto r = (a11) == (b11);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a12 = 1000000;
      auto b12 = 0.1;
      auto r = (a12) - (b12);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a13 = std::string("");
      auto r = (a13)[0];
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a14 = 0;
      auto b14 = false;
      auto r = (a14) - (b14);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a15 = 0.1;
      auto b15 = 42;
      auto r = (a15) + (b15);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a16 = 1.5;
      auto b16 = 1.5;
      auto r = (a16) - (b16);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a17 = false;
      auto b17 = 3.14;
      auto r = (a17) == (b17);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a18 = 1.5;
      auto b18 = 42;
      auto r = (a18) + (b18);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a19 = 0;
      auto b19 = 1000000;
      auto r = (a19) < (b19);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a20 = std::string("hello");
      auto b20 = std::string("");
      auto r = (a20) == (b20);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a21 = std::string("");
      auto b21 = std::string("world");
      auto r = (a21) + (b21);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a22 = 1.5;
      auto b22 = 0;
      auto r = (a22) < (b22);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a23 = 1000000;
      auto b23 = 1000000;
      auto r = (a23) - (b23);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a24 = std::string("hello");
      auto r = (a24)[0];
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a25 = 0.1;
      auto b25 = false;
      auto r = (a25) + (b25);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a26 = std::string("hello");
      auto b26 = std::string("hello");
      auto r = (a26) + (b26);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a27 = 1000000;
      auto b27 = 3.14;
      auto r = (a27) * (b27);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a28 = 1000000;
      auto b28 = 0;
      auto r = (a28) * (b28);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a29 = 0;
      auto b29 = 1000000;
      auto r = (a29) == (b29);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a30 = 1000000;
      auto b30 = true;
      auto r = (a30) < (b30);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a31 = 1.5;
      auto b31 = 3.14;
      auto r = (a31) - (b31);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a32 = std::string("");
      auto b32 = std::string("");
      auto r = (a32) + (b32);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a33 = 0;
      auto b33 = 0.1;
      auto r = (a33) == (b33);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a34 = 0.1;
      auto b34 = 1000000;
      auto r = (a34) - (b34);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a35 = 42;
      auto b35 = 0;
      auto r = (a35) + (b35);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a36 = std::string("world");
      auto b36 = std::string("hello");
      auto r = (a36) + (b36);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a37 = 0.1;
      auto b37 = 3.14;
      auto r = (a37) * (b37);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a38 = 42;
      auto b38 = true;
      auto r = (a38) * (b38);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a39 = 42;
      auto b39 = 0;
      auto r = (a39) == (b39);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a40 = 0.1;
      auto b40 = 42;
      auto r = (a40) * (b40);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a41 = 3.14;
      auto b41 = 1000000;
      auto r = (a41) - (b41);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a42 = 3.14;
      auto b42 = 42;
      auto r = (a42) < (b42);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a43 = 0.1;
      auto b43 = 3.14;
      auto r = (a43) == (b43);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a44 = 42;
      auto b44 = 0.1;
      auto r = (a44) - (b44);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a45 = true;
      auto b45 = true;
      auto r = (a45) == (b45);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a46 = 1.5;
      auto b46 = 3.14;
      auto r = (a46) == (b46);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a47 = 0;
      auto b47 = 0;
      auto r = (a47) == (b47);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a48 = 1000000;
      auto b48 = 1.5;
      auto r = (a48) + (b48);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a49 = 1000000;
      auto b49 = 42;
      auto r = (a49) == (b49);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a50 = 0;
      auto b50 = 42;
      auto r = (a50) < (b50);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a51 = true;
      auto b51 = 42;
      auto r = (a51) * (b51);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a52 = true;
      auto b52 = false;
      auto r = (a52) < (b52);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a53 = 0;
      auto b53 = 1.5;
      auto r = (a53) - (b53);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a54 = std::vector<int>{7};
      auto r = (a54)[0];
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a55 = true;
      auto b55 = 1.5;
      auto r = (a55) * (b55);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a56 = 1.5;
      auto b56 = 0;
      auto r = (a56) + (b56);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a57 = 0.1;
      auto r = -(a57);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a58 = 42;
      auto b58 = 3.14;
      auto r = (a58) - (b58);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a59 = 1000000;
      auto b59 = 1000000;
      auto r = (a59) + (b59);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a60 = 1.5;
      auto b60 = 1.5;
      auto r = (a60) + (b60);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a61 = std::string("");
      auto b61 = std::string("world");
      auto r = (a61) == (b61);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a62 = std::vector<int>{1, 2, 3};
      auto b62 = std::vector<int>{1, 2, 3};
      auto r = (a62) < (b62);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a63 = 1.5;
      auto b63 = 1000000;
      auto r = (a63) * (b63);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a64 = std::string("hello");
      auto b64 = std::string("world");
      auto r = (a64) == (b64);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a65 = 3.14;
      auto b65 = 0;
      auto r = (a65) * (b65);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a66 = 0.1;
      auto b66 = 3.14;
      auto r = (a66) - (b66);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a67 = 0;
      auto b67 = false;
      auto r = (a67) + (b67);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a68 = 0.1;
      auto b68 = 1.5;
      auto r = (a68) + (b68);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a69 = 42;
      auto b69 = 1000000;
      auto r = (a69) == (b69);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a70 = std::string("world");
      auto b70 = std::string("hello");
      auto r = (a70) == (b70);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a71 = std::vector<int>{1, 2, 3};
      auto b71 = std::vector<int>{1, 2, 3};
      auto r = (a71) == (b71);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a72 = false;
      auto b72 = 1000000;
      auto r = (a72) < (b72);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a73 = std::vector<int>{7};
      auto b73 = std::vector<int>{7};
      auto r = (a73) == (b73);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a74 = 1000000;
      auto b74 = 0.1;
      auto r = (a74) - (b74);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a75 = std::string("");
      auto r = (a75)[0];
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a76 = 0;
      auto b76 = false;
      auto r = (a76) - (b76);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a77 = 0.1;
      auto b77 = 42;
      auto r = (a77) + (b77);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a78 = 1.5;
      auto b78 = 1.5;
      auto r = (a78) - (b78);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a79 = false;
      auto b79 = 3.14;
      auto r = (a79) == (b79);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a80 = 1.5;
      auto b80 = 42;
      auto r = (a80) + (b80);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a81 = 0;
      auto b81 = 1000000;
      auto r = (a81) < (b81);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a82 = std::string("hello");
      auto b82 = std::string("");
      auto r = (a82) == (b82);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a83 = std::string("");
      auto b83 = std::string("world");
      auto r = (a83) + (b83);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a84 = 1.5;
      auto b84 = 0;
      auto r = (a84) < (b84);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a85 = 1000000;
      auto b85 = 1000000;
      auto r = (a85) - (b85);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a86 = std::string("hello");
      auto r = (a86)[0];
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a87 = 0.1;
      auto b87 = false;
      auto r = (a87) + (b87);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a88 = std::string("hello");
      auto b88 = std::string("hello");
      auto r = (a88) + (b88);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a89 = 1000000;
      auto b89 = 3.14;
      auto r = (a89) * (b89);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a90 = 1000000;
      auto b90 = 0;
      auto r = (a90) * (b90);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a91 = 0;
      auto b91 = 1000000;
      auto r = (a91) == (b91);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a92 = 1000000;
      auto b92 = true;
      auto r = (a92) < (b92);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a93 = 1.5;
      auto b93 = 3.14;
      auto r = (a93) - (b93);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a94 = std::string("");
      auto b94 = std::string("");
      auto r = (a94) + (b94);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a95 = 0;
      auto b95 = 0.1;
      auto r = (a95) == (b95);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a96 = 0.1;
      auto b96 = 1000000;
      auto r = (a96) - (b96);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a97 = 42;
      auto b97 = 0;
      auto r = (a97) + (b97);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a98 = std::string("world");
      auto b98 = std::string("hello");
      auto r = (a98) + (b98);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  {
      auto a99 = 0.1;
      auto b99 = 3.14;
      auto r = (a99) * (b99);
      std::cout << "r=" << sizeof(r) << "\n";
  }
  return 0;
}
TM_BATCH_EOF

CAP=400
TOTAL=100
# The container's `timeout` is uutils coreutils 0.8.0, which polls at 100 ms
# and so quantises every wrapped step to a 100 ms bucket (measured: see
# raw/tm_diag.txt). It is therefore NOT used around a timed step. The residual
# instrument cost is the two `date` forks, measured here and reported so it
# can be subtracted.
GAP=0
for k in 1 2 3 4 5 6 7; do
  G0=$(date +%s%N); G1=$(date +%s%N); GAP=$((GAP+G1-G0))
done
GAP=$((GAP/7))
echo "__GAP__|instrument|0|$GAP|0|0" >> "$OUT"
echo "instrument overhead per timed step: ${GAP} ns"
LANE0=$(date +%s)
i=0
SUM=0
while IFS=' ' read -r PID LABEL; do
  i=$((i+1))
  cd "$ROOT/$PID" || continue
  CRC=0; CNS=0
  C0=$(date +%s%N)
  g++ -w -std=c++17 main.cpp -o prog > b.out 2> b.err
  CRC=$?
  C1=$(date +%s%N)
  CNS=$((C1-C0))
  RRC=-1; RNS=0
  if [ "$CRC" -eq 0 ]; then
    R0=$(date +%s%N)
    ./prog > r.out 2> r.err
    RRC=$?
    R1=$(date +%s%N)
    RNS=$((R1-R0))
  fi
  echo "$PID|$LABEL|$CRC|$CNS|$RRC|$RNS" >> "$OUT"
  SUM=$((SUM+CNS+RNS))
  NOW=$(date +%s)
  EL=$((NOW-LANE0))
  MEANMS=$((SUM/i/1000000))
  ETA=$(( (TOTAL-i) * (SUM/i) / 1000000000 ))
  echo "[$i/$TOTAL] elapsed=${EL}s mean=${MEANMS}ms eta=${ETA}s  ($PID $LABEL c=$CRC r=$RRC)"
  if [ "$EL" -ge "$CAP" ]; then
    echo "CAP REACHED at $i/$TOTAL after ${EL}s -- stopping early"
    break
  fi
done < "$ROOT/labels.txt"
echo "PROBES_DONE=$i of $TOTAL"

# ---- one batched file: 100 accepting probes, compiled once, run once.
cd "$ROOT/batch" || exit 0
BC0=$(date +%s%N)
BCRC=0
g++ -w -std=c++17 main.cpp -o prog > b.out 2> b.err
BCRC=$?
BC1=$(date +%s%N)
BRRC=-1; BR1=0; BR0=0
if [ "$BCRC" -eq 0 ]; then
  BR0=$(date +%s%N)
  ./prog > r.out 2> r.err
  BRRC=$?
  BR1=$(date +%s%N)
fi
echo "__BATCH100__|accept|$BCRC|$((BC1-BC0))|$BRRC|$((BR1-BR0))" >> "$OUT"
echo "batch: compile rc=$BCRC ns=$((BC1-BC0))  run rc=$BRRC ns=$((BR1-BR0))"
sed -n 1,3p b.err 2>/dev/null
echo "=== tm_cpp complete: $(wc -l < "$OUT") result lines ==="
