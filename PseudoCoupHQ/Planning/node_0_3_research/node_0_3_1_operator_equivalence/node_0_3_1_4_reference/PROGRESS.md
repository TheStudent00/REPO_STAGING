---
id: hq.research.compiler_graph.reference.progress
status: living
---

# PROGRESS — reference

- 2026-09-03: node folder generated by
  `~/Programming/PlanPlan/framework/generate_nodes.py` from the
  `nodes` register of `~/mnt/Programming/PseudoCoupHQ/Planning/node_0_3_research/node_0_3_1_operator_equivalence/CORE_0_3_1_operator_equivalence.md`. Skeleton only — definition,
  designation, and content pending.
- 2026-09-03: this node written top-down from the round 10–11 artifacts (the owner: "plan out every step"); sub-nodes generated and filled the same day.
- 2026-09-03: one `reference.py`: SRem remainder, STACK and X87 state, IEEE float terms, one opcode_table shared with term — **planned**. Evidence: log_153 §5, log_157 §3 (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: canon9 / canon10 / canon12 behaviour checkers and gate48's Sim10 become superseded records — **planned (with the above)**. Evidence: this CORE (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03: one `reference.py` with class `Reference` (`simulate`,
  `answer_of`), class `MachineState`, and ONE `opcode_table` — **done**.
  Reach over the whole population, none sampled: **25,827 of 31,078
  units walk to an answer term** (interpreter 9 of 11, original 1,660
  of 1,779, regenerated 24,158 of 29,288), against the superseded
  `canon10` route's 20,892 on the same population; 642 units carry no
  body at all and are counted in their own column. **Zero regressions:
  0 units the superseded route answered are refused here**; 4,935
  gained. Evidence: `~/Programming/PseudoCoupHQ/Research/op_pipeline/coverage57_printed.txt`,
  `coverage57.json`, and the report
  `~/Programming/PseudoCoupHQ/DevComms/log_159_task57_reference.md`.
- 2026-09-03: the three acceptance instances printed with values —
  **done**. `c/op_246`'s answer term is `bvsrem_i(...)` and gives `1`
  on `7, -3` (z3's `%` gives `-2`); `c/regen_11491`'s machine stack
  round trips, z3 answering `unsat` to pushed and popped differing;
  `cpp/regen_36796`'s two `fldt` loads both reach the compare and the
  answer term reproduces log_153 §4.3's own printed GUARD-0 term.
  Evidence: `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`.
- 2026-09-03: `canon9_behaviour_check.py`,
  `canon10_behaviour_check.py` and `canon12_behaviour_check.py`
  marked superseded by `reference.py` — **done**, one header comment
  line each and nothing else changed. Evidence: commit `784551a`,
  `git show --stat 784551a` reading `1 +` against each of the three.
- 2026-09-03: WORDING CORRECTED the same day, after review. The first
  version of `reference.py` and of the acceptance text called a `call`
  into a compiler support routine "out of scope per AgentMemory". That
  framing is superseded: log_158 TASK 59(b) rules the callee's body is
  extracted from the toolchain's libgcc / compiler-rt archive and
  attached as an ArchUnit the caller references, producer
  `{"kind": "runtime_callee", "callee": "__divti3"}`, and says of
  `out_of_scope_library_calls.json` "its verdict superseded; its list
  stands". The 3,743 `call` refusals now read "runtime callee not yet
  attached (Task 59, node 0_3_5_1_8)"; `Entry` gained a `cause` field
  so every census row states its own reason. Re-run: the figures did
  not move (25,827 of 31,078 reach an answer, 0 regressions). This
  line is **blocked on task 59**, not out of scope. Evidence:
  `~/Programming/PseudoCoupHQ/DevComms/log_159_task57_reference.md` §10,
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance57_printed.txt`
  parts 0 and (b), and
  `~/Programming/PseudoCoupHQ/DevComms/log_158_claude_code_task_briefs_round12.md`
  TASK 59(b).
- 2026-09-03: `reference.py` corrected at one line by task 58's gate:
  the logic family's flag triple is `(mnemonic, result, 0)`, the
  shape this file already used for `test`. It is the only edit; the
  file is otherwise task 57's. Evidence: log_160 §3, and the
  `machine_state` CORE's new settled rule.
- 2026-09-03 (audit, log_165): branch following (fork on the condition,
  merge at the join as If-terms), stepping into attached runtime
  callees, 8/16-bit division and widening multiply — **planned**,
  round 13. Evidence: log_160 §1.7 (499 + 3,419 + 20 units).
- 2026-09-03 (task 64): FIVE settled rules written into this CORE
  BEFORE any code, under round 12/13's binding rule that a shape the
  tree lacks goes into the tree first (PROTOCOL §2) -- (1) a body is
  walked as its own control-flow graph, blocks cut at the positional
  labels, visited in reverse postorder, joins entered on the merge, a
  CYCLE refused by name; (2) a transfer whose target is not a label
  defined in this body leaves the unit, so that side is unreachable
  and its condition is a guard row; (3) what a merge does to each kind
  of state and where it refuses (the flag triple, the two stack
  depths); (4) a `call` with an attached callee is not a transfer --
  the callee's own arrival contract in, its answer register out,
  `xmm0` when its body writes `xmm0` and `rax` otherwise, computed off
  the body; (5) a rip-relative ADDRESS computation is the body's next
  `ripconst_<k>`, the same counter a rip-relative load reads. The
  `## design` block gained `blocks_of`, `walk_body`, `merge` and
  `guard_rows`. Evidence: this CORE's `## settled rules`, and log_166
  TASK 64 (`~/Programming/PseudoCoupHQ/DevComms/`).
- 2026-09-03 (task 64): branch following — **done**. `Reference.walk_body`
  cuts a body into blocks at its own positional labels and after every
  transfer (`Reference.blocks_of` is the class `Body`), visits them in
  reverse postorder, and enters a join on the merge of its incoming
  states, `If(path condition, this side, the rest)` per cell. A body
  whose control flow has a cycle is refused by name. MEASURED over
  canon39's 30,432 proved units: of the 474 units round 12 left
  undecided on a conditional transfer, 266 now prove, 177 are
  disproved with a counterexample and 31 stay undecided.
  `go/op_174`'s `jl` to go's panic path proves, and the unreachable
  side's condition is on its guard row. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance64_printed.txt`
  part 1, `audit64_printed.txt`.
- 2026-09-03 (task 64): stepping into an attached runtime callee —
  **done**. `Reference.enter_callee` reads the callee from the
  relocation through `ledger.transfer_callee`, walks its body with the
  same `walk_body`, and returns through the answer register computed
  off that body (`answer_register_of`). PUT TO NUMBERS rather than
  asserted: `clang/__extendhfsf2` walked over concrete halves returns
  the IEEE-correct float for 1.0, -2.0, +infinity, the nearest half to
  1/3, and the smallest subnormal (0x0001 -> 0x33800000 =
  5.960464477539063e-08), which is the case its `bsr` branch exists
  for; `clang/__truncsfhf2` inverts them. 442 units gain a proof this
  way. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/acceptance64_printed.txt`
  part 3 and part 3b.
- 2026-09-03 (task 64): the rip-relative ADDRESS computation modelled
  as the body's next `ripconst_<k>` — **done**. All 32 units round 12
  left undecided on it prove, with no other movement in that bucket.
  Evidence: `audit64_printed.txt`, "THE FOUR NAMED CAUSES OF log_160
  SECTION 1.7, TESTED".
- 2026-09-03 (task 64): the walk is UNCHANGED where it should be —
  **done**, and checked over the whole population rather than a
  sample. Of the 25,937 canon39 proved units whose own text has no
  label, no conditional transfer, no `jmp`, no `call` and no `ud2`,
  25,827 answer character-for-character as the module did before this
  task, 110 turn a refusal into a term, and **0 change an answer and 0
  turn a term into a refusal**. In the other column -- the 4,495 units
  whose text DOES carry one of the five -- 539 turn a refusal into a
  term and 3,956 refuse both times with the wording moved. Evidence:
  `~/Programming/PseudoCoupHQ/Research/op_pipeline/regression64_printed.txt`.
- 2026-09-05 (task 91): the node's realization table CORRECTED against
  what is on disk — **done**. The table still read "`canon9` … in use;
  **wrong remainder** (`%`), no stack, no x87" and "machine_state.stack
  / x87 | nothing | **planned**", which tasks 57, 58 and 64 had already
  answered the same week; the PROGRESS lines above record that work and
  the table had not been brought forward with them. Verified in one
  Airlock lane rather than asserted: each of `canon9`, `canon10` and
  `canon12` carries the one `# SUPERSEDED 2026-09-03 by reference.py`
  header line, and `grep -c "^import canon[0-9]*_behaviour_check"
  gate.py` reads **0** — the gate reaches exactly one simulator.
  Evidence: `~/Programming/PseudoCoupHQ/Research/op_pipeline/t91_reference_evidence.json`
  (sections A1, A2, A2b) and the lane log
  `~/AirlockRuns/t91/agent/logs/20260905T033051Z__t91_l6_reference_evidence.sh.log`.
- 2026-09-05 (task 91): the x87 CONTROL WORD ruled out by census rather
  than by silence — **done**. Over the 62,156 bodies canon38 and
  canon40 hold together, 2,810 spell an x87 arch opcode and **0** spell
  `fldcw` / `fnstcw` / `fstcw` / `fldenv` / `fnstenv` / `fstenv` /
  `fnsave` / `frstor` / `finit` / `fninit` / `fnclex` / `fclex`. Under
  the opcode_table rule "no entry invented for an opcode no body
  contains", no control word is modelled and none is needed. Evidence:
  `t91_reference_evidence.json` section B3.
