-- the population
   layer-4 records read 30432
-- the census over canon39
   producers 52
   rows blocked 1877
   units blocked 1283
   cascades (not census entries) 1136

   producer                                           rows    units  languages
   flag_pair:test,js                                   424      424  c,cpp,swift
   arch_opcode:lea                                     154      138  c,cpp,go,rust
   flag_pair:cmp,jae                                   153      115  swift
   flag_pair:cmp,jbe                                   126      126  go,swift
   flag_pair:test,jl                                   122      122  go
   non_opcode_phrase:an x87 stack position this unit did not itself load       84       84  c,cpp
   flag_pair:test,je                                    80       80  go,rust,swift
   runtime_callee:__udivti3                             78       78  c,cpp,rust
   runtime_callee:__umodti3                             78       78  c,cpp,rust
   runtime_callee:__divti3                              74       74  c,cpp,rust
   runtime_callee:__modti3                              74       74  c,cpp,rust
   arch_opcode:div                                      64       32  c,cpp,go,rust,swift
   non_opcode_phrase:the body's last write to %xmm0       32       32  c,cpp,go,rust,swift
   flag_pair:cmp,jle                                    29       29  swift
   flag_pair:cmp,jne                                    28       28  go,swift
   arch_opcode:idiv                                     24       12  go,rust,swift
   flag_pair:cmp,jl                                     23       23  swift
   flag_pair:cmp,jb                                     22       22  swift
   flag_pair:cmp,je                                     19       19  swift
   flag_pair:or,je                                      18       18  rust
   flag_pair:cmp,jg                                     15       15  swift
   flag_pair:cmp,jge                                    15       15  swift
   flag_pair:shr,je                                     12       12  swift
   flag_pair:sbb,jae                                    11       11  swift
   flag_pair:sbb,jl                                     10        9  swift
   arch_opcode:mul                                       8        4  rust,swift
   arch_opcode:movzbl                                    7        7  c,cpp,rust,swift
   flag_pair:add,jo                                      7        7  swift
   flag_pair:neg,jo                                      7        7  swift
   flag_pair:sub,jo                                      7        7  swift
   flag_pair:add,jb                                      6        6  swift
   flag_pair:cmp,ja                                      6        6  swift
   flag_pair:imul,jo                                     6        6  swift
   flag_pair:sub,jb                                      6        6  swift
   flag_pair:ucomiss,jb                                  6        6  swift
   flag_pair:sbb,jb                                      5        5  swift
   flag_pair:sbb,jge                                     5        5  swift
   flag_pair:mul,jo                                      4        4  swift
   flag_pair:ucomisd,jb                                  4        4  swift
   flag_pair:sar,je                                      3        3  swift
   non_opcode_phrase:the body's last write to %rax        3        3  go
   arch_opcode:imul                                      2        1  swift
   arch_opcode:movsbl                                    2        2  rust,swift
   arch_opcode:pcmpeqb                                   2        2  rust
   arch_opcode:pcmpeqd                                   2        2  rust
   flag_pair:mul,seto                                    2        1  swift
   flag_pair:sbb,jo                                      2        2  swift
   flag_pair:test,jne                                    2        1  swift
   flag_pair:adc,jb                                      1        1  swift
   flag_pair:adc,jo                                      1        1  swift
   flag_pair:test,jns                                    1        1  swift
   flag_pair:xor,js                                      1        1  swift

-- each producer's written reason sentence
   flag_pair:test,js
       arch opcode 'js' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   arch_opcode:lea
       the shared opcode table refused: an address computation over base register '%rip': this reference has no model for the address that register holds, only for the value a read through it returns
   flag_pair:cmp,jae
       arch opcode 'jae' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,jbe
       arch opcode 'jbe' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:test,jl
       arch opcode 'jl' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   non_opcode_phrase:an x87 stack position this unit did not itself load
       a row of block X87 carries a producer that is not an arch opcode, and this file has no rule for that pairing
   flag_pair:test,je
       arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   runtime_callee:__udivti3
       the attached body of the runtime callee '__udivti3' contains the transfer 'je 79 <__udivti3+0x79>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       the attached body of the runtime callee '__udivti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
   runtime_callee:__umodti3
       the attached body of the runtime callee '__umodti3' contains the transfer 'je 86 <__umodti3+0x86>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       the attached body of the runtime callee '__umodti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
   runtime_callee:__divti3
       the attached body of the runtime callee '__divti3' contains the transfer 'call 4c <__divti3+0x4c>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       the attached body of the runtime callee '__divti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
   runtime_callee:__modti3
       the attached body of the runtime callee '__modti3' contains the transfer 'call 4a <__modti3+0x4a>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       the attached body of the runtime callee '__modti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
   arch_opcode:div
       the shared opcode table refused: a division at width 16 is not modeled
       the shared opcode table refused: a division at width 8 is not modeled
   non_opcode_phrase:the body's last write to %xmm0
       no arch opcode in this body writes the answer register, so no row produces the answer
   flag_pair:cmp,jle
       arch opcode 'jle' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,jne
       arch opcode 'jne' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   arch_opcode:idiv
       the shared opcode table refused: a division at width 16 is not modeled
       the shared opcode table refused: a division at width 8 is not modeled
   flag_pair:cmp,jl
       arch opcode 'jl' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,je
       arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:or,je
       arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,jg
       arch opcode 'jg' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,jge
       arch opcode 'jge' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:shr,je
       arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sbb,jae
       arch opcode 'jae' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sbb,jl
       arch opcode 'jl' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   arch_opcode:mul
       the shared opcode table refused: a widening multiply at width 16 is not modeled: its destination pair is the accumulator's own two halves, which are not two families
       the shared opcode table refused: a widening multiply at width 8 is not modeled: its destination pair is the accumulator's own two halves, which are not two families
   arch_opcode:movzbl
       the shared opcode table refused: operand '%ah' is neither an immediate, a register nor a memory operand this file reads
   flag_pair:add,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:neg,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sub,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:add,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:cmp,ja
       arch opcode 'ja' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:imul,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sub,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:ucomiss,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sbb,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sbb,jge
       arch opcode 'jge' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:mul,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:ucomisd,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:sar,je
       arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   non_opcode_phrase:the body's last write to %rax
       no arch opcode in this body writes the answer register, so no row produces the answer
   arch_opcode:imul
       the shared opcode table refused: a widening multiply at width 8 is not modeled: its destination pair is the accumulator's own two halves, which are not two families
   arch_opcode:movsbl
       the shared opcode table refused: operand '%ah' is neither an immediate, a register nor a memory operand this file reads
   arch_opcode:pcmpeqb
       arch opcode 'pcmpeqb' is a census row, not a silent gap: no z3 term is written for this arch opcode yet
   arch_opcode:pcmpeqd
       arch opcode 'pcmpeqd' is a census row, not a silent gap: no z3 term is written for this arch opcode yet
   flag_pair:mul,seto
       the shared opcode table refused: this opcode reads the signed-overflow bit and no flag-setting arch opcode precedes it in this body
   flag_pair:sbb,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:test,jne
       arch opcode 'jne' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:adc,jb
       arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:adc,jo
       arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:test,jns
       arch opcode 'jns' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
   flag_pair:xor,js
       arch opcode 'js' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order

-- the delta against census4, matched on the REASON SENTENCE
   census4: 54 producers, 1719 rows
   census5: 52 producers, 1877 rows
   causes CLOSED 8
       -2 rows | no z3 term is written for this arch opcode
       -400 rows | no z3 term is written for this x87 arch opcode
       -9 rows | operand '%ah' names the SECOND byte of a register, which canon.py's register table does not carry as a family, so ledger47 made no row for it and there is no value to read
       -2 rows | the flag-setting arch opcode 'adc' left flags this file could not build -- either its own row is missing from the ledger (ledger47 makes no row when the destination is a register it never renames) or that row's own operands have no term
       -1 rows | the flag-setting arch opcode 'imul' left flags this file could not build -- either its own row is missing from the ledger (ledger47 makes no row when the destination is a register it never renames) or that row's own operands have no term
       -10 rows | the flag-setting arch opcode 'mul' left flags this file could not build -- either its own row is missing from the ledger (ledger47 makes no row when the destination is a register it never renames) or that row's own operands have no term
       -774 rows | the flag-setting arch opcode 'sbb' left flags this file could not build -- either its own row is missing from the ledger (ledger47 makes no row when the destination is a register it never renames) or that row's own operands have no term
       -186 rows | this suffix reads the carry bit, and the flag-setting arch opcode 'fucomip' has no carry model in this file
   causes APPEARED 31
       +84 rows | a row of block X87 carries a producer that is not an arch opcode, and this file has no rule for that pairing
       +6 rows | arch opcode 'ja' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +164 rows | arch opcode 'jae' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +50 rows | arch opcode 'jb' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +126 rows | arch opcode 'jbe' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +132 rows | arch opcode 'je' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +15 rows | arch opcode 'jg' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +20 rows | arch opcode 'jge' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +155 rows | arch opcode 'jl' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +29 rows | arch opcode 'jle' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +30 rows | arch opcode 'jne' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +1 rows | arch opcode 'jns' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +34 rows | arch opcode 'jo' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +425 rows | arch opcode 'js' is a census row, not a silent gap: a transfer or trap, and this reference walks a body in text order
       +2 rows | arch opcode 'pcmpeqb' is a census row, not a silent gap: no z3 term is written for this arch opcode yet
       +2 rows | arch opcode 'pcmpeqd' is a census row, not a silent gap: no z3 term is written for this arch opcode yet
       +74 rows | the attached body of the runtime callee '__divti3' contains the transfer 'call 4c <__divti3+0x4c>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       +74 rows | the attached body of the runtime callee '__divti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
       +74 rows | the attached body of the runtime callee '__modti3' contains the transfer 'call 4a <__modti3+0x4a>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       +74 rows | the attached body of the runtime callee '__modti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
       +78 rows | the attached body of the runtime callee '__udivti3' contains the transfer 'je 79 <__udivti3+0x79>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       +78 rows | the attached body of the runtime callee '__udivti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
       +78 rows | the attached body of the runtime callee '__umodti3' contains the transfer 'je 86 <__umodti3+0x86>', whose target the attachment did not record: an archive member's calls are unrelocated, so the nested callee has no name to follow
       +78 rows | the attached body of the runtime callee '__umodti3' spells 'endbr64', which the shared opcode table refused: arch opcode 'endbr64' has no entry in the opcode table -- no body in the corpus this table was built over spells it
       +88 rows | the shared opcode table refused: a division at width 16 is not modeled
       +88 rows | the shared opcode table refused: a division at width 8 is not modeled
       +8 rows | the shared opcode table refused: a widening multiply at width 16 is not modeled: its destination pair is the accumulator's own two halves, which are not two families
       +10 rows | the shared opcode table refused: a widening multiply at width 8 is not modeled: its destination pair is the accumulator's own two halves, which are not two families
       +154 rows | the shared opcode table refused: an address computation over base register '%rip': this reference has no model for the address that register holds, only for the value a read through it returns
       +9 rows | the shared opcode table refused: operand '%ah' is neither an immediate, a register nor a memory operand this file reads
       +2 rows | the shared opcode table refused: this opcode reads the signed-overflow bit and no flag-setting arch opcode precedes it in this body
   causes that MOVED 1
       335 -> 35 | no arch opcode in this body writes the answer register, so no row produces the answer

-- wrote name_census5.json
