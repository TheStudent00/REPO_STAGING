#!/bin/sh
# layer-2 load audit -- rust -- generated by
# Research/data_representation/audit_generate.py . Do not hand-edit; regenerate.
# Every line of /out/l2_rust.txt is FACT_ID|RESULT, FACT_ID being
# <form>.<representation>.<probe>.
set -u
ROOT=/work/l2_rust
rm -rf "$ROOT"; mkdir -p "$ROOT"
export HOME=/work
export GOFLAGS=-mod=mod
export GO111MODULE=off
export GOCACHE=/work/.gocache
export GOPATH=/work/.gopath
export PATH=/persist/dart-sdk/bin:$PATH

# swift's binaries want an ncurses name the image does not ship under; the fix
# is per-container, so every script that may touch swift re-applies it.
ncurses_fix() {
  if [ -f /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 ]; then
    ln -sf /usr/lib/x86_64-linux-gnu/libncursesw.so.6.6 \
       /usr/lib/x86_64-linux-gnu/libncurses.so.6 2>/dev/null
    ldconfig 2>/dev/null
  fi
}
ncurses_fix

OUT=/out/l2_rust.txt
: > "$OUT"
echo "=== layer-2 load audit -- rust ==="
date -u +%Y-%m-%dT%H:%M:%SZ
if ! command -v rustc >/dev/null 2>&1; then
  echo "rust: TOOLCHAIN ABSENT -- nothing run"
  echo "__ABSENT__|<absent>" >> "$OUT"
  exit 0
fi


# ---- nothing.unit.null
D=$ROOT/p1; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: () = ();
    println!("{}|{}", "nothing.unit.null", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nothing.unit.null|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nothing.unit.null|" "$OUT" || echo "nothing.unit.null|$V" >> "$OUT"

# ---- nothing.Option::None.null
D=$ROOT/p2; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Option<i64> = None;
    println!("{}|{}", "nothing.Option::None.null", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nothing.Option::None.null|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nothing.Option::None.null|" "$OUT" || echo "nothing.Option::None.null|$V" >> "$OUT"

# ---- truth.bool.true
D=$ROOT/p3; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: bool = true;
    println!("{}|{}", "truth.bool.true", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "truth.bool.true|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "truth.bool.true|" "$OUT" || echo "truth.bool.true|$V" >> "$OUT"

# ---- truth.bool.false
D=$ROOT/p4; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: bool = false;
    println!("{}|{}", "truth.bool.false", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "truth.bool.false|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "truth.bool.false|" "$OUT" || echo "truth.bool.false|$V" >> "$OUT"

# ---- whole.i32.base_zero
D=$ROOT/p5; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 0;
    println!("{}|{}", "whole.i32.base_zero", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.base_zero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.base_zero|" "$OUT" || echo "whole.i32.base_zero|$V" >> "$OUT"

# ---- whole.i32.base_42
D=$ROOT/p6; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 42;
    println!("{}|{}", "whole.i32.base_42", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.base_42|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.base_42|" "$OUT" || echo "whole.i32.base_42|$V" >> "$OUT"

# ---- whole.i32.i64max
D=$ROOT/p7; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 9223372036854775807;
    println!("{}|{}", "whole.i32.i64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.i64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.i64max|" "$OUT" || echo "whole.i32.i64max|$V" >> "$OUT"

# ---- whole.i32.i64max_plus1
D=$ROOT/p8; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 9223372036854775808;
    println!("{}|{}", "whole.i32.i64max_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.i64max_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.i64max_plus1|" "$OUT" || echo "whole.i32.i64max_plus1|$V" >> "$OUT"

# ---- whole.i32.p53_plus1
D=$ROOT/p9; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 9007199254740993;
    println!("{}|{}", "whole.i32.p53_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.p53_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.p53_plus1|" "$OUT" || echo "whole.i32.p53_plus1|$V" >> "$OUT"

# ---- whole.i32.u64max
D=$ROOT/p10; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i32 = 18446744073709551615;
    println!("{}|{}", "whole.i32.u64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i32.u64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i32.u64max|" "$OUT" || echo "whole.i32.u64max|$V" >> "$OUT"

# ---- whole.i64.base_zero
D=$ROOT/p11; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 0;
    println!("{}|{}", "whole.i64.base_zero", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.base_zero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.base_zero|" "$OUT" || echo "whole.i64.base_zero|$V" >> "$OUT"

# ---- whole.i64.base_42
D=$ROOT/p12; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 42;
    println!("{}|{}", "whole.i64.base_42", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.base_42|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.base_42|" "$OUT" || echo "whole.i64.base_42|$V" >> "$OUT"

# ---- whole.i64.i64max
D=$ROOT/p13; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 9223372036854775807;
    println!("{}|{}", "whole.i64.i64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.i64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.i64max|" "$OUT" || echo "whole.i64.i64max|$V" >> "$OUT"

# ---- whole.i64.i64max_plus1
D=$ROOT/p14; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 9223372036854775808;
    println!("{}|{}", "whole.i64.i64max_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.i64max_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.i64max_plus1|" "$OUT" || echo "whole.i64.i64max_plus1|$V" >> "$OUT"

# ---- whole.i64.p53_plus1
D=$ROOT/p15; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 9007199254740993;
    println!("{}|{}", "whole.i64.p53_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.p53_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.p53_plus1|" "$OUT" || echo "whole.i64.p53_plus1|$V" >> "$OUT"

# ---- whole.i64.u64max
D=$ROOT/p16; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i64 = 18446744073709551615;
    println!("{}|{}", "whole.i64.u64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i64.u64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i64.u64max|" "$OUT" || echo "whole.i64.u64max|$V" >> "$OUT"

# ---- whole.u64.base_zero
D=$ROOT/p17; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 0;
    println!("{}|{}", "whole.u64.base_zero", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.base_zero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.base_zero|" "$OUT" || echo "whole.u64.base_zero|$V" >> "$OUT"

# ---- whole.u64.base_42
D=$ROOT/p18; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 42;
    println!("{}|{}", "whole.u64.base_42", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.base_42|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.base_42|" "$OUT" || echo "whole.u64.base_42|$V" >> "$OUT"

# ---- whole.u64.i64max
D=$ROOT/p19; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 9223372036854775807;
    println!("{}|{}", "whole.u64.i64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.i64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.i64max|" "$OUT" || echo "whole.u64.i64max|$V" >> "$OUT"

# ---- whole.u64.i64max_plus1
D=$ROOT/p20; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 9223372036854775808;
    println!("{}|{}", "whole.u64.i64max_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.i64max_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.i64max_plus1|" "$OUT" || echo "whole.u64.i64max_plus1|$V" >> "$OUT"

# ---- whole.u64.p53_plus1
D=$ROOT/p21; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 9007199254740993;
    println!("{}|{}", "whole.u64.p53_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.p53_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.p53_plus1|" "$OUT" || echo "whole.u64.p53_plus1|$V" >> "$OUT"

# ---- whole.u64.u64max
D=$ROOT/p22; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: u64 = 18446744073709551615;
    println!("{}|{}", "whole.u64.u64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.u64.u64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.u64.u64max|" "$OUT" || echo "whole.u64.u64max|$V" >> "$OUT"

# ---- whole.i128.base_zero
D=$ROOT/p23; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 0;
    println!("{}|{}", "whole.i128.base_zero", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.base_zero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.base_zero|" "$OUT" || echo "whole.i128.base_zero|$V" >> "$OUT"

# ---- whole.i128.base_42
D=$ROOT/p24; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 42;
    println!("{}|{}", "whole.i128.base_42", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.base_42|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.base_42|" "$OUT" || echo "whole.i128.base_42|$V" >> "$OUT"

# ---- whole.i128.i64max
D=$ROOT/p25; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 9223372036854775807;
    println!("{}|{}", "whole.i128.i64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.i64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.i64max|" "$OUT" || echo "whole.i128.i64max|$V" >> "$OUT"

# ---- whole.i128.i64max_plus1
D=$ROOT/p26; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 9223372036854775808;
    println!("{}|{}", "whole.i128.i64max_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.i64max_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.i64max_plus1|" "$OUT" || echo "whole.i128.i64max_plus1|$V" >> "$OUT"

# ---- whole.i128.p53_plus1
D=$ROOT/p27; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 9007199254740993;
    println!("{}|{}", "whole.i128.p53_plus1", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.p53_plus1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.p53_plus1|" "$OUT" || echo "whole.i128.p53_plus1|$V" >> "$OUT"

# ---- whole.i128.u64max
D=$ROOT/p28; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: i128 = 18446744073709551615;
    println!("{}|{}", "whole.i128.u64max", format!("{}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "whole.i128.u64max|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "whole.i128.u64max|" "$OUT" || echo "whole.i128.u64max|$V" >> "$OUT"

# ---- fractional.f64.base_1_5
D=$ROOT/p29; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = 1.5;
    println!("{}|{}", "fractional.f64.base_1_5", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.base_1_5|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.base_1_5|" "$OUT" || echo "fractional.f64.base_1_5|$V" >> "$OUT"

# ---- fractional.f64.base_pi
D=$ROOT/p30; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = 3.141592653589793;
    println!("{}|{}", "fractional.f64.base_pi", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.base_pi|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.base_pi|" "$OUT" || echo "fractional.f64.base_pi|$V" >> "$OUT"

# ---- fractional.f64.negzero
D=$ROOT/p31; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = -0.0;
    println!("{}|{}", "fractional.f64.negzero", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.negzero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.negzero|" "$OUT" || echo "fractional.f64.negzero|$V" >> "$OUT"

# ---- fractional.f64.nan
D=$ROOT/p32; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = f64::NAN;
    println!("{}|{}", "fractional.f64.nan", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.nan|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.nan|" "$OUT" || echo "fractional.f64.nan|$V" >> "$OUT"

# ---- fractional.f64.inf
D=$ROOT/p33; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = f64::INFINITY;
    println!("{}|{}", "fractional.f64.inf", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.inf|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.inf|" "$OUT" || echo "fractional.f64.inf|$V" >> "$OUT"

# ---- fractional.f64.point1
D=$ROOT/p34; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f64 = 0.1;
    println!("{}|{}", "fractional.f64.point1", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f64.point1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f64.point1|" "$OUT" || echo "fractional.f64.point1|$V" >> "$OUT"

# ---- fractional.f32.base_1_5
D=$ROOT/p35; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = 1.5;
    println!("{}|{}", "fractional.f32.base_1_5", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.base_1_5|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.base_1_5|" "$OUT" || echo "fractional.f32.base_1_5|$V" >> "$OUT"

# ---- fractional.f32.base_pi
D=$ROOT/p36; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = 3.141592653589793;
    println!("{}|{}", "fractional.f32.base_pi", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.base_pi|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.base_pi|" "$OUT" || echo "fractional.f32.base_pi|$V" >> "$OUT"

# ---- fractional.f32.negzero
D=$ROOT/p37; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = -0.0;
    println!("{}|{}", "fractional.f32.negzero", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.negzero|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.negzero|" "$OUT" || echo "fractional.f32.negzero|$V" >> "$OUT"

# ---- fractional.f32.nan
D=$ROOT/p38; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = f32::NAN;
    println!("{}|{}", "fractional.f32.nan", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.nan|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.nan|" "$OUT" || echo "fractional.f32.nan|$V" >> "$OUT"

# ---- fractional.f32.inf
D=$ROOT/p39; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = f32::INFINITY;
    println!("{}|{}", "fractional.f32.inf", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.inf|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.inf|" "$OUT" || echo "fractional.f32.inf|$V" >> "$OUT"

# ---- fractional.f32.point1
D=$ROOT/p40; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: f32 = 0.1;
    println!("{}|{}", "fractional.f32.point1", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "fractional.f32.point1|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "fractional.f32.point1|" "$OUT" || echo "fractional.f32.point1|$V" >> "$OUT"

# ---- text.String.base_empty
D=$ROOT/p41; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("").to_string();
    println!("{}|{}", "text.String.base_empty", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.base_empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.base_empty|" "$OUT" || echo "text.String.base_empty|$V" >> "$OUT"

# ---- text.String.base_hello
D=$ROOT/p42; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("hello").to_string();
    println!("{}|{}", "text.String.base_hello", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.base_hello|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.base_hello|" "$OUT" || echo "text.String.base_hello|$V" >> "$OUT"

# ---- text.String.base_lines
D=$ROOT/p43; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("line one\nline two").to_string();
    println!("{}|{}", "text.String.base_lines", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.base_lines|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.base_lines|" "$OUT" || echo "text.String.base_lines|$V" >> "$OUT"

# ---- text.String.eacute
D=$ROOT/p44; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("\u{00e9}").to_string();
    println!("{}|{}", "text.String.eacute", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.eacute|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.eacute|" "$OUT" || echo "text.String.eacute|$V" >> "$OUT"

# ---- text.String.clef
D=$ROOT/p45; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("\u{1D11E}").to_string();
    println!("{}|{}", "text.String.clef", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.clef|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.clef|" "$OUT" || echo "text.String.clef|$V" >> "$OUT"

# ---- text.String.escapes
D=$ROOT/p46; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: String = ("quote \" backslash \\ tab\there").to_string();
    println!("{}|{}", "text.String.escapes", v.clone());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.String.escapes|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.String.escapes|" "$OUT" || echo "text.String.escapes|$V" >> "$OUT"

# ---- text.&str.base_empty
D=$ROOT/p47; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "";
    println!("{}|{}", "text.&str.base_empty", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.base_empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.base_empty|" "$OUT" || echo "text.&str.base_empty|$V" >> "$OUT"

# ---- text.&str.base_hello
D=$ROOT/p48; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "hello";
    println!("{}|{}", "text.&str.base_hello", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.base_hello|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.base_hello|" "$OUT" || echo "text.&str.base_hello|$V" >> "$OUT"

# ---- text.&str.base_lines
D=$ROOT/p49; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "line one\nline two";
    println!("{}|{}", "text.&str.base_lines", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.base_lines|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.base_lines|" "$OUT" || echo "text.&str.base_lines|$V" >> "$OUT"

# ---- text.&str.eacute
D=$ROOT/p50; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "\u{00e9}";
    println!("{}|{}", "text.&str.eacute", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.eacute|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.eacute|" "$OUT" || echo "text.&str.eacute|$V" >> "$OUT"

# ---- text.&str.clef
D=$ROOT/p51; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "\u{1D11E}";
    println!("{}|{}", "text.&str.clef", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.clef|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.clef|" "$OUT" || echo "text.&str.clef|$V" >> "$OUT"

# ---- text.&str.escapes
D=$ROOT/p52; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &str = "quote \" backslash \\ tab\there";
    println!("{}|{}", "text.&str.escapes", v.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.&str.escapes|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.&str.escapes|" "$OUT" || echo "text.&str.escapes|$V" >> "$OUT"

# ---- text.Vec<u8>.base_empty
D=$ROOT/p53; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.base_empty", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.base_empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.base_empty|" "$OUT" || echo "text.Vec<u8>.base_empty|$V" >> "$OUT"

# ---- text.Vec<u8>.base_hello
D=$ROOT/p54; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("hello").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.base_hello", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.base_hello|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.base_hello|" "$OUT" || echo "text.Vec<u8>.base_hello|$V" >> "$OUT"

# ---- text.Vec<u8>.base_lines
D=$ROOT/p55; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("line one\nline two").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.base_lines", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.base_lines|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.base_lines|" "$OUT" || echo "text.Vec<u8>.base_lines|$V" >> "$OUT"

# ---- text.Vec<u8>.eacute
D=$ROOT/p56; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("\u{00e9}").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.eacute", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.eacute|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.eacute|" "$OUT" || echo "text.Vec<u8>.eacute|$V" >> "$OUT"

# ---- text.Vec<u8>.clef
D=$ROOT/p57; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("\u{1D11E}").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.clef", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.clef|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.clef|" "$OUT" || echo "text.Vec<u8>.clef|$V" >> "$OUT"

# ---- text.Vec<u8>.escapes
D=$ROOT/p58; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<u8> = ("quote \" backslash \\ tab\there").as_bytes().to_vec();
    println!("{}|{}", "text.Vec<u8>.escapes", String::from_utf8(v).unwrap());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "text.Vec<u8>.escapes|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "text.Vec<u8>.escapes|" "$OUT" || echo "text.Vec<u8>.escapes|$V" >> "$OUT"

# ---- sequence.Vec<T>.empty
D=$ROOT/p59; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<i64> = vec![];
    println!("{}|{}", "sequence.Vec<T>.empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.Vec<T>.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.Vec<T>.empty|" "$OUT" || echo "sequence.Vec<T>.empty|$V" >> "$OUT"

# ---- sequence.Vec<T>.base
D=$ROOT/p60; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<i64> = vec![1, 2, 3];
    println!("{}|{}", "sequence.Vec<T>.base", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.Vec<T>.base|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.Vec<T>.base|" "$OUT" || echo "sequence.Vec<T>.base|$V" >> "$OUT"

# ---- sequence.Vec<T>.strs
D=$ROOT/p61; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<&str> = vec!["a", "b"];
    println!("{}|{}", "sequence.Vec<T>.strs", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.Vec<T>.strs|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.Vec<T>.strs|" "$OUT" || echo "sequence.Vec<T>.strs|$V" >> "$OUT"

# ---- sequence.Vec<T>.mixed
D=$ROOT/p62; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<i64> = vec![1, "two", 3.0, true, ()];
    println!("{}|{}", "sequence.Vec<T>.mixed", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.Vec<T>.mixed|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.Vec<T>.mixed|" "$OUT" || echo "sequence.Vec<T>.mixed|$V" >> "$OUT"

# ---- sequence.Vec<T>.bigint
D=$ROOT/p63; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<i64> = vec![9223372036854775808];
    println!("{}|{}", "sequence.Vec<T>.bigint", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.Vec<T>.bigint|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.Vec<T>.bigint|" "$OUT" || echo "sequence.Vec<T>.bigint|$V" >> "$OUT"

# ---- sequence.[T; N].empty
D=$ROOT/p64; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: [i64; 0] = [];
    println!("{}|{}", "sequence.[T; N].empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.[T; N].empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.[T; N].empty|" "$OUT" || echo "sequence.[T; N].empty|$V" >> "$OUT"

# ---- sequence.[T; N].base
D=$ROOT/p65; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: [i64; 3] = [1, 2, 3];
    println!("{}|{}", "sequence.[T; N].base", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.[T; N].base|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.[T; N].base|" "$OUT" || echo "sequence.[T; N].base|$V" >> "$OUT"

# ---- sequence.[T; N].strs
D=$ROOT/p66; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: [&str; 2] = ["a", "b"];
    println!("{}|{}", "sequence.[T; N].strs", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.[T; N].strs|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.[T; N].strs|" "$OUT" || echo "sequence.[T; N].strs|$V" >> "$OUT"

# ---- sequence.[T; N].mixed
D=$ROOT/p67; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: [i64; 5] = [1, "two", 3.0, true, ()];
    println!("{}|{}", "sequence.[T; N].mixed", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.[T; N].mixed|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.[T; N].mixed|" "$OUT" || echo "sequence.[T; N].mixed|$V" >> "$OUT"

# ---- sequence.[T; N].bigint
D=$ROOT/p68; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: [i64; 1] = [9223372036854775808];
    println!("{}|{}", "sequence.[T; N].bigint", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.[T; N].bigint|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.[T; N].bigint|" "$OUT" || echo "sequence.[T; N].bigint|$V" >> "$OUT"

# ---- sequence.&[T].empty
D=$ROOT/p69; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &[i64] = &[];
    println!("{}|{}", "sequence.&[T].empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.&[T].empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.&[T].empty|" "$OUT" || echo "sequence.&[T].empty|$V" >> "$OUT"

# ---- sequence.&[T].base
D=$ROOT/p70; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &[i64] = &[1, 2, 3];
    println!("{}|{}", "sequence.&[T].base", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.&[T].base|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.&[T].base|" "$OUT" || echo "sequence.&[T].base|$V" >> "$OUT"

# ---- sequence.&[T].strs
D=$ROOT/p71; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &[&str] = &["a", "b"];
    println!("{}|{}", "sequence.&[T].strs", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.&[T].strs|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.&[T].strs|" "$OUT" || echo "sequence.&[T].strs|$V" >> "$OUT"

# ---- sequence.&[T].mixed
D=$ROOT/p72; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &[i64] = &[1, "two", 3.0, true, ()];
    println!("{}|{}", "sequence.&[T].mixed", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.&[T].mixed|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.&[T].mixed|" "$OUT" || echo "sequence.&[T].mixed|$V" >> "$OUT"

# ---- sequence.&[T].bigint
D=$ROOT/p73; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: &[i64] = &[9223372036854775808];
    println!("{}|{}", "sequence.&[T].bigint", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.&[T].bigint|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.&[T].bigint|" "$OUT" || echo "sequence.&[T].bigint|$V" >> "$OUT"

# ---- sequence.VecDeque<T>.empty
D=$ROOT/p74; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::VecDeque;

fn main() {
    let v: VecDeque<i64> = VecDeque::from(vec![]);
    println!("{}|{}", "sequence.VecDeque<T>.empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.VecDeque<T>.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.VecDeque<T>.empty|" "$OUT" || echo "sequence.VecDeque<T>.empty|$V" >> "$OUT"

# ---- sequence.VecDeque<T>.base
D=$ROOT/p75; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::VecDeque;

fn main() {
    let v: VecDeque<i64> = VecDeque::from(vec![1, 2, 3]);
    println!("{}|{}", "sequence.VecDeque<T>.base", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.VecDeque<T>.base|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.VecDeque<T>.base|" "$OUT" || echo "sequence.VecDeque<T>.base|$V" >> "$OUT"

# ---- sequence.VecDeque<T>.strs
D=$ROOT/p76; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::VecDeque;

fn main() {
    let v: VecDeque<&str> = VecDeque::from(vec!["a", "b"]);
    println!("{}|{}", "sequence.VecDeque<T>.strs", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.VecDeque<T>.strs|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.VecDeque<T>.strs|" "$OUT" || echo "sequence.VecDeque<T>.strs|$V" >> "$OUT"

# ---- sequence.VecDeque<T>.mixed
D=$ROOT/p77; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::VecDeque;

fn main() {
    let v: VecDeque<i64> = VecDeque::from(vec![1, "two", 3.0, true, ()]);
    println!("{}|{}", "sequence.VecDeque<T>.mixed", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.VecDeque<T>.mixed|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.VecDeque<T>.mixed|" "$OUT" || echo "sequence.VecDeque<T>.mixed|$V" >> "$OUT"

# ---- sequence.VecDeque<T>.bigint
D=$ROOT/p78; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::VecDeque;

fn main() {
    let v: VecDeque<i64> = VecDeque::from(vec![9223372036854775808]);
    println!("{}|{}", "sequence.VecDeque<T>.bigint", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.VecDeque<T>.bigint|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.VecDeque<T>.bigint|" "$OUT" || echo "sequence.VecDeque<T>.bigint|$V" >> "$OUT"

# ---- sequence.tuple.empty
D=$ROOT/p79; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: () = ();
    println!("{}|{}", "sequence.tuple.empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.tuple.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.tuple.empty|" "$OUT" || echo "sequence.tuple.empty|$V" >> "$OUT"

# ---- sequence.tuple.base
D=$ROOT/p80; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v = (1i64, 2i64, 3i64);
    println!("{}|{}", "sequence.tuple.base", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.tuple.base|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.tuple.base|" "$OUT" || echo "sequence.tuple.base|$V" >> "$OUT"

# ---- sequence.tuple.strs
D=$ROOT/p81; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v = ("a", "b");
    println!("{}|{}", "sequence.tuple.strs", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.tuple.strs|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.tuple.strs|" "$OUT" || echo "sequence.tuple.strs|$V" >> "$OUT"

# ---- sequence.tuple.mixed
D=$ROOT/p82; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v = (1i64, "two", 3.0f64, true, ());
    println!("{}|{}", "sequence.tuple.mixed", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.tuple.mixed|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.tuple.mixed|" "$OUT" || echo "sequence.tuple.mixed|$V" >> "$OUT"

# ---- sequence.tuple.bigint
D=$ROOT/p83; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v = (9223372036854775808u64,);
    println!("{}|{}", "sequence.tuple.bigint", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "sequence.tuple.bigint|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "sequence.tuple.bigint|" "$OUT" || echo "sequence.tuple.bigint|$V" >> "$OUT"

# ---- keyed.HashMap<String,V>.empty
D=$ROOT/p84; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::HashMap;

fn main() {
    let v: HashMap<String, i64> = HashMap::new();
    println!("{}|{}", "keyed.HashMap<String,V>.empty", { let mut ks: Vec<String> = v.keys().cloned().collect(); ks.sort(); format!("{:?} {:?}", ks, v.len()) });
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.HashMap<String,V>.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.HashMap<String,V>.empty|" "$OUT" || echo "keyed.HashMap<String,V>.empty|$V" >> "$OUT"

# ---- keyed.HashMap<String,V>.flat
D=$ROOT/p85; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::HashMap;

fn main() {
    let v: HashMap<String, i64> = HashMap::from([("a".to_string(), 1), ("b".to_string(), 2), ("c".to_string(), 3)]);
    println!("{}|{}", "keyed.HashMap<String,V>.flat", { let mut ks: Vec<String> = v.keys().cloned().collect(); ks.sort(); format!("{:?} {:?}", ks, v.len()) });
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.HashMap<String,V>.flat|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.HashMap<String,V>.flat|" "$OUT" || echo "keyed.HashMap<String,V>.flat|$V" >> "$OUT"

# ---- keyed.HashMap<String,V>.intlike
D=$ROOT/p86; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::HashMap;

fn main() {
    let v: HashMap<String, String> = HashMap::from([("1".to_string(), "string-one-key".to_string()), ("b".to_string(), "2".to_string())]);
    println!("{}|{}", "keyed.HashMap<String,V>.intlike", { let mut ks: Vec<String> = v.keys().cloned().collect(); ks.sort(); format!("{:?} {:?}", ks, v.len()) });
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.HashMap<String,V>.intlike|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.HashMap<String,V>.intlike|" "$OUT" || echo "keyed.HashMap<String,V>.intlike|$V" >> "$OUT"

# ---- keyed.HashMap<String,V>.emptykey
D=$ROOT/p87; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::HashMap;

fn main() {
    let v: HashMap<String, String> = HashMap::from([("".to_string(), "empty-string key".to_string())]);
    println!("{}|{}", "keyed.HashMap<String,V>.emptykey", { let mut ks: Vec<String> = v.keys().cloned().collect(); ks.sort(); format!("{:?} {:?}", ks, v.len()) });
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.HashMap<String,V>.emptykey|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.HashMap<String,V>.emptykey|" "$OUT" || echo "keyed.HashMap<String,V>.emptykey|$V" >> "$OUT"

# ---- keyed.HashMap<String,V>.nested
D=$ROOT/p88; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::HashMap;

fn main() {
    let v: HashMap<String, HashMap<String, Vec<i64>>> = HashMap::from([("outer".to_string(), HashMap::from([("inner".to_string(), vec![1, 2])]))]);
    println!("{}|{}", "keyed.HashMap<String,V>.nested", { let mut ks: Vec<String> = v.keys().cloned().collect(); ks.sort(); format!("{:?} {:?}", ks, v.len()) });
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.HashMap<String,V>.nested|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.HashMap<String,V>.nested|" "$OUT" || echo "keyed.HashMap<String,V>.nested|$V" >> "$OUT"

# ---- keyed.BTreeMap<String,V>.empty
D=$ROOT/p89; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::BTreeMap;

fn main() {
    let v: BTreeMap<String, i64> = BTreeMap::new();
    println!("{}|{}", "keyed.BTreeMap<String,V>.empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.BTreeMap<String,V>.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.BTreeMap<String,V>.empty|" "$OUT" || echo "keyed.BTreeMap<String,V>.empty|$V" >> "$OUT"

# ---- keyed.BTreeMap<String,V>.flat
D=$ROOT/p90; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::BTreeMap;

fn main() {
    let v: BTreeMap<String, i64> = BTreeMap::from([("a".to_string(), 1), ("b".to_string(), 2), ("c".to_string(), 3)]);
    println!("{}|{}", "keyed.BTreeMap<String,V>.flat", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.BTreeMap<String,V>.flat|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.BTreeMap<String,V>.flat|" "$OUT" || echo "keyed.BTreeMap<String,V>.flat|$V" >> "$OUT"

# ---- keyed.BTreeMap<String,V>.intlike
D=$ROOT/p91; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::BTreeMap;

fn main() {
    let v: BTreeMap<String, String> = BTreeMap::from([("1".to_string(), "string-one-key".to_string()), ("b".to_string(), "2".to_string())]);
    println!("{}|{}", "keyed.BTreeMap<String,V>.intlike", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.BTreeMap<String,V>.intlike|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.BTreeMap<String,V>.intlike|" "$OUT" || echo "keyed.BTreeMap<String,V>.intlike|$V" >> "$OUT"

# ---- keyed.BTreeMap<String,V>.emptykey
D=$ROOT/p92; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::BTreeMap;

fn main() {
    let v: BTreeMap<String, String> = BTreeMap::from([("".to_string(), "empty-string key".to_string())]);
    println!("{}|{}", "keyed.BTreeMap<String,V>.emptykey", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.BTreeMap<String,V>.emptykey|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.BTreeMap<String,V>.emptykey|" "$OUT" || echo "keyed.BTreeMap<String,V>.emptykey|$V" >> "$OUT"

# ---- keyed.BTreeMap<String,V>.nested
D=$ROOT/p93; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::collections::BTreeMap;

fn main() {
    let v: BTreeMap<String, BTreeMap<String, Vec<i64>>> = BTreeMap::from([("outer".to_string(), BTreeMap::from([("inner".to_string(), vec![1, 2])]))]);
    println!("{}|{}", "keyed.BTreeMap<String,V>.nested", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.BTreeMap<String,V>.nested|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.BTreeMap<String,V>.nested|" "$OUT" || echo "keyed.BTreeMap<String,V>.nested|$V" >> "$OUT"

# ---- keyed.struct.empty
D=$ROOT/p94; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    #[derive(Debug)] struct R0; let v = R0;
    println!("{}|{}", "keyed.struct.empty", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.struct.empty|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.struct.empty|" "$OUT" || echo "keyed.struct.empty|$V" >> "$OUT"

# ---- keyed.struct.flat
D=$ROOT/p95; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    #[derive(Debug)] struct R3 { a: i64, b: i64, c: i64 } let v = R3 { a: 1, b: 2, c: 3 };
    println!("{}|{}", "keyed.struct.flat", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.struct.flat|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.struct.flat|" "$OUT" || echo "keyed.struct.flat|$V" >> "$OUT"

# ---- keyed.struct.intlike
D=$ROOT/p96; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    #[derive(Debug)] struct RI { 1: i64, b: i64 } let v = RI { 1: 1, b: 2 };
    println!("{}|{}", "keyed.struct.intlike", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.struct.intlike|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.struct.intlike|" "$OUT" || echo "keyed.struct.intlike|$V" >> "$OUT"

# ---- keyed.struct.emptykey
D=$ROOT/p97; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    #[derive(Debug)] struct RE { : String } let v = RE { : "empty-string key".to_string() };
    println!("{}|{}", "keyed.struct.emptykey", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.struct.emptykey|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.struct.emptykey|" "$OUT" || echo "keyed.struct.emptykey|$V" >> "$OUT"

# ---- keyed.struct.nested
D=$ROOT/p98; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    #[derive(Debug)] struct RN { outer: Vec<i64> } let v = RN { outer: vec![1, 2] };
    println!("{}|{}", "keyed.struct.nested", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "keyed.struct.nested|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "keyed.struct.nested|" "$OUT" || echo "keyed.struct.nested|$V" >> "$OUT"

# ---- nesting.Vec<Vec<T>>.seq_in_seq
D=$ROOT/p99; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<Vec<i64>> = vec![vec![1, 2], vec![3, 4]];
    println!("{}|{}", "nesting.Vec<Vec<T>>.seq_in_seq", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.Vec<Vec<T>>.seq_in_seq|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.Vec<Vec<T>>.seq_in_seq|" "$OUT" || echo "nesting.Vec<Vec<T>>.seq_in_seq|$V" >> "$OUT"

# ---- nesting.Vec<Vec<T>>.seq_in_map
D=$ROOT/p100; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    use std::collections::BTreeMap; let v: BTreeMap<String, Vec<i64>> = BTreeMap::from([("outer".to_string(), vec![1, 2])]);
    println!("{}|{}", "nesting.Vec<Vec<T>>.seq_in_map", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.Vec<Vec<T>>.seq_in_map|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.Vec<Vec<T>>.seq_in_map|" "$OUT" || echo "nesting.Vec<Vec<T>>.seq_in_map|$V" >> "$OUT"

# ---- nesting.Vec<Vec<T>>.mixed_depth
D=$ROOT/p101; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let v: Vec<Vec<i64>> = vec![vec![1], vec![2, 3], vec![]];
    println!("{}|{}", "nesting.Vec<Vec<T>>.mixed_depth", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.Vec<Vec<T>>.mixed_depth|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.Vec<Vec<T>>.mixed_depth|" "$OUT" || echo "nesting.Vec<Vec<T>>.mixed_depth|$V" >> "$OUT"

# ---- nesting.enum Node.seq_in_seq
D=$ROOT/p102; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    use std::collections::BTreeMap; #[derive(Debug)] enum Node { Null, Truth(bool), Whole(i64), Text(String), Seq(Vec<Node>), Map(BTreeMap<String, Node>) } let v = Node::Seq(vec![Node::Seq(vec![Node::Whole(1), Node::Whole(2)]), Node::Seq(vec![Node::Whole(3), Node::Whole(4)])]);
    println!("{}|{}", "nesting.enum Node.seq_in_seq", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.enum Node.seq_in_seq|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.enum Node.seq_in_seq|" "$OUT" || echo "nesting.enum Node.seq_in_seq|$V" >> "$OUT"

# ---- nesting.enum Node.seq_in_map
D=$ROOT/p103; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    use std::collections::BTreeMap; #[derive(Debug)] enum Node { Null, Truth(bool), Whole(i64), Text(String), Seq(Vec<Node>), Map(BTreeMap<String, Node>) } let v = Node::Map(BTreeMap::from([("outer".to_string(), Node::Map(BTreeMap::from([("inner".to_string(), Node::Seq(vec![Node::Whole(1), Node::Whole(2)]))])))]));
    println!("{}|{}", "nesting.enum Node.seq_in_map", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.enum Node.seq_in_map|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.enum Node.seq_in_map|" "$OUT" || echo "nesting.enum Node.seq_in_map|$V" >> "$OUT"

# ---- nesting.enum Node.mixed_depth
D=$ROOT/p104; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    use std::collections::BTreeMap; #[derive(Debug)] enum Node { Null, Truth(bool), Whole(i64), Text(String), Seq(Vec<Node>), Map(BTreeMap<String, Node>) } let v = Node::Seq(vec![Node::Whole(1), Node::Seq(vec![Node::Whole(2), Node::Map(BTreeMap::from([("k".to_string(), Node::Text("t".to_string()))]))]), Node::Truth(true), Node::Null]);
    println!("{}|{}", "nesting.enum Node.mixed_depth", format!("{:?}", v));
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "nesting.enum Node.mixed_depth|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "nesting.enum Node.mixed_depth|" "$OUT" || echo "nesting.enum Node.mixed_depth|$V" >> "$OUT"

# ---- identity.Vec<T> (owned elements).shared
D=$ROOT/p105; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let s: Vec<i64> = vec![1, 2, 3];
    let mut v: Vec<Vec<i64>> = vec![s.clone(), s.clone()];
    v[0].push(99);
    let r = if v[1].last() == Some(&99) { "shared" } else { "copied" };
    println!("{}|{}", "identity.Vec<T> (owned elements).shared", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Vec<T> (owned elements).shared|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Vec<T> (owned elements).shared|" "$OUT" || echo "identity.Vec<T> (owned elements).shared|$V" >> "$OUT"

# ---- identity.Vec<T> (owned elements).diamond
D=$ROOT/p106; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let t: Vec<i64> = vec![7];
    let v: Vec<Vec<i64>> = vec![t.clone(), t.clone()];
    let r = if v[0].as_ptr() == v[1].as_ptr() { "same" } else { "distinct" };
    println!("{}|{}", "identity.Vec<T> (owned elements).diamond", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Vec<T> (owned elements).diamond|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Vec<T> (owned elements).diamond|" "$OUT" || echo "identity.Vec<T> (owned elements).diamond|$V" >> "$OUT"

# ---- identity.Vec<T> (owned elements).cycle
D=$ROOT/p107; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]

fn main() {
    let mut v: Vec<Vec<i64>> = Vec::new();
    v.push(v);
    let r = "cycle";
    println!("{}|{}", "identity.Vec<T> (owned elements).cycle", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Vec<T> (owned elements).cycle|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Vec<T> (owned elements).cycle|" "$OUT" || echo "identity.Vec<T> (owned elements).cycle|$V" >> "$OUT"

# ---- identity.Rc<T>.shared
D=$ROOT/p108; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;

fn main() {
    let s: Rc<Vec<i64>> = Rc::new(vec![1, 2, 3]);
    let v = vec![Rc::clone(&s), Rc::clone(&s)];
    let r = if Rc::ptr_eq(&v[0], &v[1]) { "shared-read-only" } else { "copied" };
    println!("{}|{}", "identity.Rc<T>.shared", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<T>.shared|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<T>.shared|" "$OUT" || echo "identity.Rc<T>.shared|$V" >> "$OUT"

# ---- identity.Rc<T>.diamond
D=$ROOT/p109; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;

fn main() {
    let t: Rc<Vec<i64>> = Rc::new(vec![7]);
    let v = vec![Rc::clone(&t), Rc::clone(&t)];
    let r = if Rc::ptr_eq(&v[0], &v[1]) { "same" } else { "distinct" };
    println!("{}|{}", "identity.Rc<T>.diamond", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<T>.diamond|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<T>.diamond|" "$OUT" || echo "identity.Rc<T>.diamond|$V" >> "$OUT"

# ---- identity.Rc<T>.cycle
D=$ROOT/p110; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;

fn main() {
    struct N { name: String, selfref: Option<Rc<N>> }
    let n = Rc::new(N { name: "loop".to_string(), selfref: None });
    let mut m = N { name: "loop".to_string(), selfref: Some(Rc::clone(&n)) };
    m.selfref = Some(Rc::new(m));
    let r = "cycle";
    println!("{}|{}", "identity.Rc<T>.cycle", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<T>.cycle|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<T>.cycle|" "$OUT" || echo "identity.Rc<T>.cycle|$V" >> "$OUT"

# ---- identity.Rc<RefCell<T>>.shared
D=$ROOT/p111; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;
use std::cell::RefCell;

fn main() {
    let s = Rc::new(RefCell::new(vec![1i64, 2, 3]));
    let v = vec![Rc::clone(&s), Rc::clone(&s)];
    v[0].borrow_mut().push(99);
    let r = if v[1].borrow().last() == Some(&99) && Rc::ptr_eq(&v[0], &v[1]) { "shared" } else { "copied" };
    println!("{}|{}", "identity.Rc<RefCell<T>>.shared", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<RefCell<T>>.shared|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<RefCell<T>>.shared|" "$OUT" || echo "identity.Rc<RefCell<T>>.shared|$V" >> "$OUT"

# ---- identity.Rc<RefCell<T>>.diamond
D=$ROOT/p112; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;
use std::cell::RefCell;

fn main() {
    let t = Rc::new(RefCell::new(vec![7i64]));
    let v = vec![Rc::clone(&t), Rc::clone(&t)];
    let r = if Rc::ptr_eq(&v[0], &v[1]) { "same" } else { "distinct" };
    println!("{}|{}", "identity.Rc<RefCell<T>>.diamond", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<RefCell<T>>.diamond|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<RefCell<T>>.diamond|" "$OUT" || echo "identity.Rc<RefCell<T>>.diamond|$V" >> "$OUT"

# ---- identity.Rc<RefCell<T>>.cycle
D=$ROOT/p113; mkdir -p "$D"; cd "$D"
cat > main.rs <<'L2_SRC_EOF'
#![allow(unused)]
use std::rc::Rc;
use std::cell::RefCell;

fn main() {
    struct N { name: String, selfref: RefCell<Option<Rc<N>>> }
    let n = Rc::new(N { name: "loop".to_string(), selfref: RefCell::new(None) });
    *n.selfref.borrow_mut() = Some(Rc::clone(&n));
    let held = n.selfref.borrow();
    let r = if Rc::ptr_eq(held.as_ref().unwrap(), &n) { "cycle" } else { "no-cycle" };
    println!("{}|{}", "identity.Rc<RefCell<T>>.cycle", r.to_string());
}
L2_SRC_EOF
V=ok
if ! timeout 180 rustc -A warnings main.rs -o prog > build.out 2> build.err; then V="<compile-error>"; fi
if [ "$V" = ok ]; then
  if timeout 30 ./prog > run.out 2> run.err; then :; else
    V="<aborted>"
  fi
  grep -F "identity.Rc<RefCell<T>>.cycle|" run.out >> "$OUT" || V="${V}"
fi
grep -qF "identity.Rc<RefCell<T>>.cycle|" "$OUT" || echo "identity.Rc<RefCell<T>>.cycle|$V" >> "$OUT"

echo ""
echo "=== l2_rust complete: $(wc -l < "$OUT") result lines ==="

